# digital_human · 系统结构总览

> 2026-08-22 生成。**下次对话直接给这份文件即可开工**（浓缩系统结构 + 最新架构）。
> 详情入口：README.md（开发者导览）、CHANGELOG.md（时间线）、docs/design/蒸馏产物schema设计.md、docs/design/合规统一词库-全系统入口.md。

---

## 0. 这是什么

**数字人短视频生产流水线**（FastAPI 后端 + SQLite + 本地 GPU）。输入新闻/书/PPT → AI 洗稿/拆书 → TTS 配音 → 剪映草稿 → 人工精修导出。当前 4 条产线：**新闻线 / 拆书 / PPT 出片 / 导演(J线/HF)**。

## 1. 技术栈

- Python 3.11 + FastAPI + uvicorn（`run_web.py`，端口 54321）
- SQLite + SQLAlchemy（`data/pipeline.db`）
- LLM：DeepSeek（创作/洗稿，`config/app.yaml` deepseek）+ 本地 Gemma-31B（蒸馏/打标/合规，llama-server）+ Qwythos-9B（本地视觉）
- TTS：IndexTTS2（主，gpu_service_manager 托管 4090 自动拉起）
- 视频：ComfyUI LTX2.3（数字人）、HyperFrames（HF 视觉，CPU）
- 剪映草稿：pyJianYingDraft（J 线/PPT 出片）
- PPT 截图：Playwright + 系统 Chrome

## 2. 产线地图

```
新闻线: 文章 → 素材包(七层审计+智谱补搜) → 洗稿(laotan-tech/geo) → TTS → 导演 → 剪映草稿
拆书:   书 → L0蒸馏(两级) → facing(八面向) → 5步创作(自动填充+豆瓣真实反应) → 创作层+编辑层 → TTS → 剪映草稿
PPT:    上传pptx → 每页台词TTS → 元素级拆解(jy2) → 剪映多轨草稿
导演:   音频对齐 → slot规划 → broll/HF/host执行 → 合成 → 剪映草稿
动画:   音频先行 → 🧭两级化(🛑立意人闸: 模块切场+立意模块v2可编辑 → ▶️refs分镜) → K2首帧 → 人检 → H3视频 → 🎞剪映草稿   ← 0918 模块总线
```

### 动画线架构 (0918 模块总线, 案卷 docs/improvements/已完成-20260917-模块总线-*.md)

**六拍模块 = 全链唯一结构总线** (单独动一层其他层联动):
```
讲书稿(六拍标记) → Episode.module_json (module_map 解析, tail=模块末句锚)
  → TTS: 模块墙断包 + 气口档位(界1.5/问句0.8/段末1.0) + manifest module_id
  → 切场: 场=模块 系统定界 (module_windows, LLM 零时间输出, 受众锚=L1书定+账号两轴池)
  → 立意人闸: 立意模块v2 (六维度: core/mappings/承载句/母题/禁用史/人批注)
    页面逐场编辑+单场🎲重掷+保存回写骨架; visual_metaphor=系统投影
  → 分镜: refs 协议 — LLM 只引句号("c01-c03") 禁时间, expand_refs_groups 查表回填
    + 密度≥4s并镜 + H3≤10s切块; 镜界=音频句边界(零时间漂移, LLM 结构上不可能漂)
  → K2(品牌镜首帧直通) → 人检(重roll记账 reroll_ledger.jsonl) → H3(时长=槽铁律+孤儿回收)
  → 🎞装配: 时长核验逐镜 ffprobe 实测; 字幕=句级表直出(洗涤TTS指令/数字形态LLM源头定)
```
**硬保证**: 语音多长→镜多长→H3生成多长 (ep2 实测 39镜全对齐); 单镜重roll 零时间偏移(轴全局/视频局部); 断链重启产物由孤儿回收自动认领。
**对齐落盘(🎚)**: v1 归真工具, 仅"音频变了不想重规划"时用; v2 流程镜界生来对齐, 音频未变点了也是零动作(指纹守卫待落)。

## 3. 模块地图（app/）

- `routers/`：articles(洗稿/素材) / materials(素材包) / scripts / audio(TTS) / books(拆书) / ppt / director_routes(导演) / library(视频库) / personas / voices / roles / visual_render(HF) / tagging / tts_services
- `services/`：
  - `book_service/`：**reader**(书解析/L0章节) / **l0**(L0蒸馏管线) / **facing**(第二层八面向) / **distiller**(Gemma蒸馏+合规GateA) / **orchestrator**(拆书5步+创作/编辑两层) / **douban**(豆瓣搜索+高赞短评+书评爬虫) / **reference_library**(源头书库+选题池+引用榜)
  - `boost_service`(爆品改造P2-P5) / `material_service`(素材聚合) / `zhipu_search`(智谱补搜)
  - `ppt_service`+`ppt_frame_capture`(PPT出片) / `jy_draft_service`(剪映草稿) / `skin_pack_service`(系列皮肤)
  - `llm_service`(LLM三级链kimi→qwen→deepseek+熔断) / **`compliance`(全系统统一禁用词)** / `tts_service` / `director_service` / `gpu_service_manager`
  - **`anim_pipeline/` + `anim_service` + `anim_draft`**（动画产线 0918 模块总线）: **module_map**(六拍标记解析+模块表真相源, book_service 下) / director2(模块定场 module_windows+refs协议 expand_refs_groups+立意模块v2 reroll_concept/save_concept+受众锚) / k2(2ST配方+灭字) / h3(节拍壳+时长=槽铁律+孤儿回收) / shots(norm_for_match 比对口径/strip媒体失效/重roll账本) / planner(三入口统一 _audio_manifest) / anim_draft(句级表 sentence_table 洗涤+字幕+装配+微降速拉满) / 工坊页 web/anim.html(立意面板/草稿徽章) + /api/anim
- `models/`：content(文章/脚本/音频/素材) / book(拆书) / director / roles / assets

## 4. 核心流程

### 新闻线（5 步）
文章 → 素材包(七层审计) → 洗稿(注入素材+伪评论) → 爆品改造(P2→P5) → TTS → 导演 → 剪映

### 拆书（L0 两级蒸馏 + 5 步自动填充 + 创作/编辑两层）
```
L0 第一层: reader章节块(元信息/排版强调§B§/注释/引用源头) → 自适应打包 → Gemma逐章提取
          → data/l0/{书名}/l0-chapter-v1.json
facing 第二层(八面向): kernel(内核) / units(三段结构) / quotes(金句分级) / cases(当代桥接)
          / compliance(tier) / readers(痛点) / hooks(开篇钩子) / relations(人物图谱+论证位置+作者意图)
建书: 【新书】只列全流程蒸馏完成的书(蒸馏txt+L0+全facing) → 豆瓣高赞短评/书评自动抓取(input_json.douban)
     → L0未就绪时后台补跑(轮询 /books/{id}/prep) → auto_fill(补全/合规/评论层/总纲)
逐集(创作层): 质量标准8条 + ⟦⟧原话标记 + relations人物图谱 + 豆瓣真实反应(共鸣/槽点)注入
     → 编辑层 editorial_pass(身份=审核编辑, 专职口语化, ⟦⟧/术语保护) → 人设注入(自称"静")
```
总纲三段结构：**E1 导读(含系列预告) → 中间拆解 → 末集落地(实操清单)**，集数按 units 自适应（4-8 集）。
源头书库：reference_library 聚合 L0 references → 选题池(下一批拆书候选) + 引用榜 + 每本书单体系。

### 统一合规入口（全系统）
`config/compliance_common.json`（6 类禁用词）→ `app/services/compliance.py`（build_redline_prompt 源头注入 + check_text 检查）→ 拆书 facing/逐集 + 新闻线洗稿模板自动注入。

## 5. 关键入口

| 用途 | 入口 |
|---|---|
| 启动 | `./.venv/Scripts/python run_web.py` |
| Web UI | http://127.0.0.1:54321/web/（books/index/writing/audio/director 等）|
| L0 蒸馏 | `python -m app.services.book_service.l0 <书路径> [--skip-llm]` |
| facing 面向 | `python -m app.services.book_service.facing <L0目录> [--facing kernel,units,...]` |
| 重蒸馏 | UI books.html 🔄 重蒸馏 / `POST /distill/start-single?path=` |
| 拆书自动填充 | `POST /books/{id}/auto-fill`（创建书自动触发）|
| 源头书库/选题池 | `python -m app.services.book_service.reference_library [--pool]` |
| 豆瓣高赞/书评 | `python -m app.services.book_service.douban <书名>` |

## 6. 最近架构（09-15 动画产线；前史 08-23 见下）

- **口播稿创作/编辑两层**：创作层（质量标准 8 条 + ⟦⟧ 原话标记）→ 编辑层 `editorial_pass`（"抖音口播稿审核编辑"身份，专职口语化，⟦⟧/术语保护）→ 人设注入（自称"静"非"静姐"）
- **facing relations 理解面向**（第 8 面向）：跨章人物图谱 + 概念论证位置（核心框架 vs 支撑）+ 作者意图；L0 是提取层，理解在面向层
- **豆瓣爬虫**（douban.py）：建书自动抓高赞短评（情绪/共识）+ 书评正文（笔记/深度）→ 逐集注入真实共鸣 + 真人槽点（康复太快/投资房地产）
- **源头书库**：reference_library 聚合 L0 references → 选题池（下一批拆书候选，电子书线落地）+ 引用榜 + 每本书单体系
- **建书流程**：【新书】只列全流程蒸馏完成的书；L0 未就绪后台补跑（轮询 `/books/{id}/prep`）；auto_fill 评论层 LLM 回退
- **成本**：一本书全走 deepseek v4-pro 约 3~6.5 元（峰谷定价）；现状（L0/facing 本地 Gemma）约 1.5~3 元/本
- 08-22：L0 两级蒸馏 + 产线自动化 + 总纲三段结构 + 统一禁用词（详见 CHANGELOG.md）

## 7. 数据库表（核心）

articles/scripts/segments/audio_jobs/audio_files/material_packages/material_items / book_projects/book_episodes / director_jobs/director_slots / visual_render_jobs / video_assets/video_outputs / personas/hosts/voices/roles

## 8. 配置与密钥

`config/app.yaml`（端口/LLM/TTS/ComfyUI/HF/书源）+ `.env`（DEEPSEEK/PEXELS/ZHIPU）+ `config/compliance_rules_{book,geo,tech}.json`（赛道合规，book 已接代码）+ `config/compliance_common.json`（统一词库）
