# 剪映模板套路分析 — 效果配方库（J2 种子）

> 状态：**首次全量提取完成（2026-08-15）**
> 数据源：`data/jy_template_snapshot/`（29 个学习期模板快照）→ 聚合产物 **`data/jy_effect_catalog.json`**（J2 `jy_effect_library` 的机器可读种子）
> 提取脚本：`scripts/_scan_jy_effect_catalog.py`（可重跑，快照更新后刷新目录）
> 关联：[剪映草稿产线-设计方案](剪映草稿产线-设计方案.md) §5 J2、[剪映模板拆解-学习25期](剪映模板拆解-学习25期-抖音是小鼎呀.md)
> 战略定位（用户口径）：轨道搬运（J1）只是体力活；**画面剪辑效果才是模板的核心价值——复现模板级效果 = 大幅提升产线成片美化程度**

---

## 1. 通用工具箱（29 模板全局频次）

### 入场动画（anim in）
| 频次 | 动画 | 用途判断 |
|---|---|---|
| 73× | **渐显** | 万金油，字幕/贴纸/素材通用 |
| 46× | **向上滑动** | 字幕主流入场（下往上浮） |
| 34× | 放大 | 强调素材/数字卡 |
| 22× | 开幕 | 大标题/封面式入场 |
| 17× | 向上露出 | 配合蒙版的揭示 |
| 15× | 折叠 / 11× 弹入 / 11× 呐喊声波 | 花哨点缀 |
| 8× | 跃进 / 镜像翻转 / 滚入 / 生长 II | 备选 |

### 出场动画（anim out）
| 频次 | 动画 |
|---|---|
| 43× | **渐隐**（绝对主流） |
| 11× | 镜像翻转 / 4× 溶解 / 4× 向下滑动 |
| 1× 级 | 粒子消散 / 飘散 / 玻璃爆开 / 水墨（高级点缀，单模板定制） |

### ⏱️ 动画时长规范（重要）
**500ms 占绝对主导（345 次），300ms 次之（36 次）**。J2 默认值直接定：入场 500ms、出场 500ms、快节奏场景 300ms。

### 氛围特效（video_effect，挂画面）
| 频次 | 特效 | 用途 |
|---|---|---|
| 16× | **左右摇晃** | 手持感/冲击段 |
| 15× | **暗角** | 聚焦主体（口播人像常驻） |
| 10× | 星火 / 7× 光晕 / 7× 丁达尔光线 | 情绪渲染 |
| 9× | 模糊 / 5× 轻微抖动 | 背景层/晃动感 |
| 3× | 故障 II / 聚光灯 / 灵机一动 / 边缘发光 | 科技感强调 |

### 转场
| 频次 | 转场 |
|---|---|
| 13× | **叠化**（默认转场） |
| 3× | 炫光 II / 向左；单发：漫画撕纸/画笔擦除/故障/推近 II 等（风格化点睛） |

### 装饰贴纸（标注层美学）
白色光线(6×)、虚线分割线(4×)、聚光灯(3×)、箭头(2-3×)、星光(2×)、红圈/叉叉(2×)——**"光线+箭头+圈注"三件套 = 知识区标注风格**。

### 美颜组（挂口播人像，10× 齐刷刷出现）
祛黑眼圈/天鹅颈/祛法令纹/小翘鼻/直角肩/小脸/发际线/流畅脸——**整套人像精修是博主标配**，J2 给 host/DHV 素材段默认挂上。

### 画面特效
- 最大悬案：**id `7416258989467374091` ×180**（不在 pyJianYingDraft 元数据里，25期挂 video 轨的就是它——疑似新版新增的基础特效或"即梦AI"相关蒙版效果，需在剪映里对号后补录名字）
- 其余：透明渐变-白(26×)/简约透明渐变(14×)（渐隐边缘蒙版类）、滤色(13×)（混合模式做透明叠加）
- 花字：**"简约白色镂空花字，排版适用"(12×)** = 字幕主流花字

## 2. 套路配方（J2 直接可实现）

每条配方 = pyJianYingDraft 调用组合，`jy_effect_library` 按 ID 引用（catalog JSON 已含）：

| 配方名 | 组合 | 适用 slot |
|---|---|---|
| **R1 字幕浮现** | TextSegment + anim_in 向上滑动(500ms) + anim_out 渐隐(500ms)，花字"简约白色镂空" | 口播字幕逐句 |
| **R2 强调数字卡** | anim_in 放大(500ms) + video_effect 边缘发光/星火 | 数据/结论强调 |
| **R3 人像常驻** | 美颜组全套 + 暗角（15× 验证的口播标配） | host 出镜段 |
| **R4 冲击晃动** | 左右摇晃 + 轻微抖动 + 故障 II，转场闪黑/炫光 | 情绪高点/翻车段 |
| **R5 温暖讲述** | 光晕 + 丁达尔光线，转场叠化 | 背景/过渡段 |
| **R6 知识标注** | 贴纸三件套（白色光线+红色箭头+红圈），箭头 anim_in 渐显 | 指代/圈重点 |
| **R7 开场钩子** | 开幕动画大标题 + 星火/聚光灯（25期范式：复合片段+多层并行） | 首 8 秒 |
| **R8 错峰收尾** | 贴纸每 0.5s 递进入场（渐显 500ms）+ 字幕 5 层渐隐（25期结尾范式） | 结尾 20 秒 |

## 3. J2 落地路径

### 3.0 四点用户判定（2026-08-15 效果演示草稿《J线效果演示_全家桶》后）

> **总评（用户原话）**：基本在素材基础上加了一些效果，**有效但有限**，离商业化博主水平还相当稚嫩。
> **校准共识**：今日演示证明的是**供给能力**（一档效果全部可代码生成），不是成片水平。真正的差距 = ①**编排智力**（效果要出现在内容节拍上而非均匀轮换——我们有语义/情绪数据但未接入，最大也最有希望的差距）②素材设计感 ③音效未接 ④多层编排。
> **现实定位**：J 线价值主张 = 混合模式（自动 80 分草稿 + 人工 10 分钟精修到 90 分），贴合 BGM 手调习惯；纯自动天花板取决于编排知识沉淀（研究循环），无捷径。
> **量化方法**：挑一段 20s 手工精修到满意 = 满分参考；自动版 vs 手修版的逐节拍差异 = R 配方迭代清单。
> **已工具化（2026-08-15）**：`scripts/jy_draft_diff.py <基线草稿> <手修草稿>` — 你在剪映里正常精修并保存（零额外工作，编辑即标注），diff 自动读两版明文时间轴，输出新增/删除/效果与时长变化 = 结构化学习数据。分工契约：用户提供目标（手修），系统提供路径（diff→配方）。

| # | 判定 | 系统侧结论 |
|---|---|---|
| 1 | 纯字幕/标题类：复刻不难 | ✅ J1 已覆盖（动画/花字/划重点） |
| 2a | 截图二创标注（圈重点/标色/动画画重点）：难——"图上什么是重点"纯系统难判断 | **半优势项**：我们的流水线知道语义（口播稿说什么，LLM 就知道该圈截图上哪个数）。分两种情况：截图由我们生成（HF 渲染/浏览器自动截图）→ **坐标已知，HTML 层直接画标注**（HF 线增强）；外部截图 → 需 VLM grounding（Qwen-VL 标坐标），J2 后期验证 |
| 2b | 图片多蒙版进出场编排：实现难度大 | 降级处理：pyJYD 有 mask+keyframe 但编排繁琐；先用单蒙版+入场动画近似，多蒙版编排列入"模板研究"慢工 |
| 3 | 多图飞入飞出的"眼花缭乱"来自**原素材设计感**，不是动效 | 认同。素材质量 > 动效质量 → **加大设计感素材投入**（canva ID-052 / flaticon ID-051 / 爱给网），动效只做加分 |
| 4 | 特效配**固定音效**才成立，没音效打折非常厉害 | 🔥 **已解决原料**：模板音效=剪映导入时落地的本地 mp3（非服务器资源），已从草稿收割 **45 个语义命名音效** 到 `data/jy_sounds/`（嗖嗖/唰/叮/白闪/紧张转场/键盘打字/综艺咚/盖章/铅笔画...），索引 `_index.json`。挂音效 = AudioSegment 进草稿，与 J1 同机制，**零新技术风险** |

**修订后的配方形态**：R 配方 = 画面效果（动画/特效/蒙版）+ **音效（动效必配）** + 时机。例如 R2 强调数字卡 = 放大入场 + `叮` 音效；R4 冲击晃动 = 摇晃特效 + `白闪效果17`/`紧张转场音效`；打字机字幕 = `键盘打字长音效2`。

### 3.5 语义库实战验证 + 密度规则（2026-08-16，16-疗愈 全片文案×音效联读）

**验证方式（用户方法）**：忽略动画，逐句读文案层+配对音效。58.5s / 29 音效（每 2.0s 一个）。

**验证结论：语义库分类全部命中**——刀出鞘=标题入场（0s 钩子题）/ 钱到账×2=金额语境（"成交高端客户"即钱语境）/ 滴水+悬疑声+紧张噔噔=设问审视 / 紧张回响=金句前铺垫（"你内心的投射啊"）/ 回忆刷=结尾回环。

**新增语义三条**（16-疗愈 独有发现）：
- **格斗踢击 = 果断/决断**（"我没有那么多犹豫的"配踢击声）
- **综艺咚 = 困住/落定**（"把自己困住了"配咚）
- **弹簧长 = 决策瞬间的情绪**（"他们做决策的时候"）

**密度规则（用户口径，定为 J 线标准）**：短视频音效堆密（本条每 2.0s 一个，45期 2.4s）；**长篇只在前 30 秒用这个密度**（钩子段），之后降档至 4~6s/个或仅节拍点，再多就烦人。

```
R9 密度参数: 0-30s → 高密度(2~2.5s/个, 全配齐)
             30s后 → 低密度(4~6s/个, 只留 金额/金句/转场 三类核心)
```

### 3.1 开发顺序

1. `jy_draft_service` 加 `attach_sound(name, at_sec)` —— 音效轨 AudioSegment（半小时级）
2. R1~R8 配方补音效字段，`jy_effect_library` 读取 `jy_sounds/_index.json`
3. `jy_effect_library.py`：加载 `jy_effect_catalog.json`，提供 `apply_recipe(segment, recipe_id, **params)`，内部映射到 pyJianYingDraft 的 `add_animation / add_effect / add_transition / 贴纸` 调用（resource_id 硬编码自 catalog）
4. 导演规划：slot 输出新增 `effect_recipe` 字段（R1~R8），由 `_intent.py` 按段落情绪/功能自动建议（惊讶→R4、讲述→R5、数据→R2）
5. `jy_draft_service`（J1 的产物生成器）消费 recipe，产出带效果的完整草稿
6. 2a 的 HF 线标注增强（语义→截图坐标标注）立项验证
7. 待补：`7416258989467374091` 的效果名与视觉（剪映里对照）；单发高级转场（粒子消散/水墨等）做成可选风格包

### 3.2 音效语义库（2026-08-16 首批用户标注入库）

**数据源**：用户听音标注（学习17期）+ AI 融会贯通扩展 → `config/jy_sound_semantics.json`（9 类：悬疑设问/标题入场/金句点题/文字翻入/打字/金额收益/冲击/标注书写/轻过渡），每类带 `pipeline_signal` 指明与产线信号对接点（划重点词瞬间/hook 段/P2 回收句/打字机动画字幕…）。

**已挂载**：`jy_draft_service.attach_sound(script, "sfx", 名, at_sec)` + 导出草稿新增空 sfx 轨（J2 配方挂载点）。`sound_path(名)` 查库，缺文件自动跳过不阻塞。

**待补**：出鞘刀/悬疑梆/旌旗/横幅展开3/金币散落等 10 个文件——17期 草稿在剪映里打开一次（触发资源下载）后重跑收割脚本即齐。

## 4. 单模板模仿验证

### 4.1 学习16期_小羽毛（排版流，2026-08-15 模仿验证）

**拆解**：38.7s / 零特效零贴纸 / 两层图（格子.jpg 底 + 油画女入场）+ 15 条短文字逐词出现；重点词 20 号红 [0.72,0.11,0.11] / 普通 15 号白；9 种文字动画 200-500ms。

**结构性认知（拆解意外收获）**：逐词节奏 ≠ 单轨排队，是**多条文字轨并行叠现**（16期用了 7 条文字轨；pyJYD 单轨禁重叠，4 轨轮换可实现）。

**模仿版**：`scripts/_demo_jy_16_style.py` → 草稿「J线模仿_16期排版流」（同款底图 + 12 词组 + 同参数大小色 + 动画轮换 + 4 轨轮换）。

**用户判定：意思对，需要调教**——机械复刻已通，差距在编排手感：词组怎么断（现在是机械 4 字切）、哪个词做大做红（固定第 2 组，应按语义选——接 find_highlight_ranges 的语义选词即可复用）、时机密度。这些进 §3.0 研究循环（手修 diff 喂数据）。

**待对号**：16期 字体 id `7078971008769593887`（大字款）/ `7265611310132367931`（正文款）——剪映里点开 16期 文字看字体名后补录 pyJYD FontType 映射。


### 3.3 动画↔音效官方搭配表（2026-08-16，教学科 2-1 提取）

**数据源**：横版教学科「2-1-文字动画和音效常用搭配」——字幕名=动画名、音效同帧演示，即卖家的官方搭配教学。83 组配对 → 聚合为 **42 个动画的音效族表**：`config/jy_animation_sound_pairs.json`。

**用法**：`jy_effect_library` 应用任一 TextIntro/Outro/Loop 动画时，查表取音效族轮换（音效族规则再次实证：如 轻微放大 → 叮提示音4/叮3/提示音/卡通弹簧拉伸 4 个变体）。此表与 `_semantics.json`（语义类别）互补：语义库管"什么内容配什么类"，搭配表管"什么动画配什么音"——R9 的双查询表。

### 3.4 叙事功能→音效课程表（2026-08-16，教学科 2-2 提取）

**数据源**：教学科「2-2-音效分类」135s 全量时间轴，按画面文字语义+出场顺序标注（用户方法）→ `config/jy_sound_narrative_map.json`。前 30s 为**搜索关键词卡片**（嗖/呼/风→速度风声、高科技/机器/科幻→科技电流等 6 类），30s 后按**叙事功能**演示。

**杀手级内容——与脚本结构一一对应**：
- 开场三型（提问问题/反常规观点/观点表达）各有专属音效族
- 痛点三连：逐项"气泡弹出"轻音 + 合并"咚"重音
- **情绪音效表 12 条**（尴尬→挤压玩具鸭…惊讶→猛然一惊）→ **直接对接 P5 emotion_annotations**
- 数字细分：金额→收银机族 / 数量→胶片滚动 / 年龄→"正确"
- 序号卡音效族 14 个（轮换空间大）/ 转场族 / 结尾引导三连

至此音效知识三层闭环：**语义类别**（用户标注）→ **动画搭配**（2-1 官方表）→ **叙事功能**（2-2 课程表），R9 的选音逻辑 = 叙事功能定类 → 类内轮换 → 动画配对校验。

### 4.2 学习28期_字体排版（卡点排版教学，2026-08-16 拆解）

**结构**：20.5s / 1440×1920(3:4) / AI 自动踩点 / 41 文字元素按 **0.5s 卡点网格**逐点演示 20 种排版手法。

**新提炼手法（增量于 16期）**：
| 手法 | 实例 | 参数 |
|---|---|---|
| **错峰递次逐字** | 符Fu→He合→风Feng→Ge格 | **0.1s** 间隔递次（比 25期 贴纸 0.5s 更细一档；逐字节奏参数档：0.1s/0.5s） |
| **对开入场** | 效(向右露出)+音(向左露出) 同帧 | 成对概念词对开动画 |
| **三重叠加入场** | 排版预设：故障/向左露出/向右露出 0s 同帧三元素 | 标题区层叠入场 |
| 文字单独放大 | 星光闪闪(in) | 重点词 |
| 描边填充 / 粒子消散 / 生长II / 吸入 | 审美/高级感/字文 | 新动画入候选库 |

**字体体系（"字体重叠"的真相）**：不是单文本混字体，是**多字体分层**——主字体 7078971008769593887 ×12（**与 16期大字款同款 = 知识区标配**）+ 家族副款 7078972382651617805 ×4 + 9 款场景展示字体（每演示点一款）。

**红色第三证**：(0.72, 0.11, 0.11) 在 16/28/45期 三现——三色板定为 J 线标准（白/金 1,0.96,0.54/红 0.72,0.11,0.11）。

**卡点机制**：剪映 AI 踩点（.beat 文件，enable_ai_beats）；我们的对等物 = TTS 分段时长网格或 beats 数据（远期接 BGM 踩点时用）。

### 4.3 64-商业认知 5.4~15s「金句引用五件套」（2026-08-16 用户指认常用组）

**结构**（背景 = 视频全程挂"模糊"特效虚化垫底）：

| 拍 | 内容 | 效果 | 音效 |
|---|---|---|---|
| ① 人物引出 | 人物图(纳瓦尔.jpg) + 人名条"埃里克乔根森" | **折叠开幕(in, 1500ms)** | 报纸折叠或展开 |
| ② 引导句 | "在他的书里提到" | — | — |
| ③ 引文打字 | "赚钱跟工作的努力程度" | **打字光标(in)** | 打字声（同帧） |
| ④ 接续强调 | "即使每周在餐厅拼命工作80个小时" | **向右擦除(in)** | "呼"2（同帧） |
| ⑤ 金句收点 | "也不可能发财" + 署名"——《纳瓦尔宝典》" | 轻微放大(in) / 渐显(in) | 敲锣声（同帧） |

**定为 R10 引用金句组**：适用名人言/书摘/权威引用段——正好接我们**挂起中的 hf_quote 槽位**（ID-042：无名/纯文字引用卡随时可用）。引文打字（打字光标+打字声）与 2-1 搭配表互证。背景虚化垫底 = 引用段画面兜底手法（不用找素材，当前画面虚化即可）。

### 4.4 10-女性情感 0.9~2.2s「列举举证鱼贯组」（2026-08-16 用户指认）

**场景**：口播列举多个例子/人物/案例（潘金莲、田小娥、甄嬛）时，资料卡鱼贯入场：

| 拍 | 内容 | 效果 | 音效 |
|---|---|---|---|
| item N | 人物图(缩放 0.9) | **向下滑动(in, 300ms)** | 纸张抖动声（同帧） |
| item N | 名字条 | 渐显(in, 300ms) | — |
| 步进 | 每项间隔 **≈0.17s（5 帧）** | 图与名条错半拍 | 每项一响 |

**定为 R11 列举举证组**：口播"比如 A、B、C"式排比枚举的标准视觉化。列举完直接接引文打字（打字光标+打字声，本模板 2.23s 即转 R10 组件）——**R11→R10 串联 = "举证后引用"叙事链**。

**错峰步进参数族（三档收敛）**：逐字 0.1s（28期）/ 列举项 ≈0.17s（10期）/ 贴纸装饰 0.5s（25期）——密度由内容粒度决定。

### 4.5 39-知识付费 8.9~10.1s「引号夹观点组」（2026-08-16 用户指认：纯文字排版·双引号引入观点）

| 拍 | 内容 | 样式 | 效果 | 音效 |
|---|---|---|---|---|
| ① 序号引导 | "第一点" | 白 sz20 | 向下露出(300ms) | 叮咚提示音1 |
| ② 左引号 | `"` **独立文字元素** | **红 (0.72,0.11,0.11)** | 渐显(500ms) | — |
| ③ 观点句 | "能稳定输出选题" | 白 sz20 | 向下露出(300ms) | **开门声**（语义：打开观点之门） |
| ④ 右引号 | `"` 独立元素 | 红 | 渐显(500ms) | — |
| ⑤ 解释接续 | "并不是拍了5条视频后…" | 白 sz12（降一号） | — | — |

**定为 R12 引号观点组**：引入他人观点/核心论点的标准排版。要点：
- **引号是独立大字元素**（不是句内标点）——红引号先出后收，把观点"夹"起来，视觉上等于给观点加书名号框
- 字号阶梯：序号/观点 20 → 解释 12
- 红色 (0.72,0.11,0.11) 第四证（16/28/45/39期）
- 开门声 = 新音效语义（"观点开门"），入库待收割

### 4.6 14-读书 4.9~14.4s「名言连发组（文艺变体）」（2026-08-16 用户指认：引用名人名言另一方案）

| 拍 | 内容 | 样式 | 效果 | 音效 |
|---|---|---|---|---|
| ① 引导句 | "直到我读到莫言的这段话" | 白 sz12 | — | — |
| ② 转折 | "他说" | 白 | — | — |
| ③ 人物入场 | 莫言-2.jpg + 背景(复合片段)挂**模糊** | — | 渐显(500ms) | 纸张抖动声 |
| ④ 金句连发 | "爱,是克制不住的"→"是心疼"→"是惦记"→"是想见面" **7 句逐句** | **金色 (1,0.96,0.54)** sz12 | 各句渐显(500ms)，间隔 0.8~1.6s 跟旁白节奏 | — |
| ⑤ 回白解释 | "真心相爱的两个人" | 白 | — | — |

**定为 R13 名言连发组**。与 R10 成对——引用的两套方案按内容气质选型：

| | R10 金句引用五件套（64-商业认知） | R13 名言连发（14-读书） |
|---|---|---|
| 风格 | 打字机·商务干练 | 静谧·文艺抒情 |
| 人物引出 | 折叠开幕 1.5s + 人名条 + 报纸声 | 人物图渐显 + 纸张声 + 背景模糊 |
| 引文呈现 | 打字光标逐字 + 擦除强调 + 署名收点 | **金色逐句连发**（跟口播节奏） |
| 适用 | 商业/科技/认知类 | 情感/读书/疗愈类 |

**新语义**：金色在引用语境 = **引文专属色**（三色板扩展：白正文/金强调兼引文/红冲击）。R10/R13 由导演按 slot 的内容气质（财经 vs 情感赛道）自动选型。

### 4.7 17-二手车 3.5~4.4s「否定标记组」（2026-08-16 用户指认：红色线段压文字=观点错误）

| 拍 | 内容 | 样式/效果 | 音效 |
|---|---|---|---|
| ① 错误观点词 | "原生家庭/提供帮助" | 白字 随机上升/收拢(in) | 纸张抖动声 |
| ② **否定标记** | **贴纸「红色长方形背景色块」(id 6989977315748957471) 压上文字** | 3.87s 同帧 | **撕开拉扯开胶布声**（听觉上的"划掉"） |
| ③ 正确答案接力 | "千万"(sz36 大字) → "建议:"(黄 1,0.87,0) → 正确做法打字机 | 缩小(in)+综艺嘭 / 打字光标 | 嘭 / 叮3 / 打字声 |

**定为 R14 否定标记组**：纠错段（"你以为…其实…"）的否定拍 = **红色块/线段压字 + 撕胶布声**——视觉×听觉双重否定标记。备选音效：系统警告（用户标注：错误认知专用）。
**纠错链**：R14(否定) → 黄色"建议:"+打字机(正确) —— 完整纠错叙事链，正对接 R12 引号观点组的反向场景。
**新资产**：两个贴纸 resource_id 直接入库可用（红块 6989977315748957471 / 红箭头 7142870400358255905）。
**色彩注意**：出现第二个黄——"建议:"用划重点黄 (1, 0.87, 0)，与引文金 (1, 0.96, 0.54) 并存：划重点黄=指令/建议，引文金=引文，语义分工待更多样本。

### 4.8 56-律师 4.2~6.4s「证据展示组」（2026-08-16 用户指认：截图出现+画重点=展示证据）

| 拍 | 内容 | 效果 | 音效 |
|---|---|---|---|
| ① 截图入场 | **裁定书.png（文书/网页截图）** | **点开(in, 800ms)**——像鼠标点开一样 | **鼠标单击**（2-2 课程表"指认时刻"互证） |
| ② 标记层 | 复合片段挂**正片叠底**混合模式 ×2 | 压在截图上的荧光笔式画重点 | — |
| ③ 字幕同步 | "冻结600万资产"（关键结论） | 白字跟截图节奏 | — |

**定为 R15 证据展示组**：展示证据/引用文书/网页截图的标准组——"点开"入场 + 单击声 + **混合模式标记层**画重点。这正是 §3.0 四点判定里 2a"截图二创标注"的模板级答案：标记层用**正片叠底混合模式**实现（不是贴纸不是蒙版）——pyJYD 有 mix_mode 支持，且坐标在我们自己截图（HF/浏览器）时已知。

### 4.9 53-金融 20.4~23.6s「金句递进组」（2026-08-16 用户指认：文字适合制作金句，包括字体）

| 拍 | 内容 | 字号 | 颜色 | 效果 | 音效 |
|---|---|---|---|---|---|
| ① 引导 | "给大家的建议是" | 12 | 白 | — | — |
| ② 对句A | "可以先看" | 24 | 白 | 模糊缩小(in) | 风铃魔法转场 |
| ③ 对句B | "但别急着动" | 24 | 白 | 开幕(in) | — |
| ④ **核心金句** | **"捂好钱袋子"** | **27.6** | **深红 (1, 0.09, 0.22)** | 向上滑动(in) | 风效转场 |
| ⑤ 重复强调 | "捂好钱袋子" 再现 | 27.6 | 深红 | — | 综艺咚 |
| ⑥ 收尾词 | "走的更远" | **34.6（最大）** | **黄 (1, 0.75, 0.09)** | **辉光(in)** | — |

**定为 R16 金句递进组**：视频收尾金句的公式——
- **字号阶梯递进**：12→24→24→27.6→34.6，情绪随字号爬升
- **双色对撞**：核心词深红 (1,0.09,0.22)（新红值，玫红系，区别于冲击红 0.72,0.11,0.11）× 收尾词黄+辉光
- 每句换一种入场动画、每句配音效（密度=每拍一响）
- **重复强调**：核心金句出两遍（第二遍+综艺咚）
- 对句A/B 用 模糊缩小/开幕 交替入场（2-1 搭配表同族动画）

适用：结尾 CTA/金句收割段——正对接 boost P2 回收句与固定结尾。

### 4.10 50-法律 3.4~3.7s「评论区证据组」（2026-08-16 用户指认：展示证据之评论区）

| 拍 | 内容 | 效果 | 音效 |
|---|---|---|---|
| 背景 | 视频 1-素材.mp4 挂**模糊**（虚化垫底） | — | — |
| 评论卡 N | **评论素材 v1/v2/v3.jpg**（评论卡截图） | 向右滑动 / 向左滑动 / 向左滑动(in, 300ms) **交替方向** | **回忆-刷一下** ×3（每卡一响，刷屏的声音语义） |
| 步进 | 0.19s→0.11s（≈0.1~0.2s 档） | — | — |

**定为 R17 评论区证据组**。"用评论证明舆论"的标准视觉化。

**🔴 对我们系统的特殊价值——数据优势反超模板作者**：模板作者要**手工制作评论卡图片**（评论素材v1/v2/v3.jpg 是预制图）；而我们有 `deconstruct_json`（解构层：五种人设伪评论，8/15 已上线评论层面板）——**评论内容本来就是结构化数据**，评论卡可以用 HF 线 HTML 渲染成图（或 J 线文字轨直接排评论气泡样式），完全免手工。证据类三件套就位：R15 截图/文书 + R17 评论区 + R11 列举人物。

附带验证：本模板 1.63~1.93s 的 新闻.png+正片叠底标记 = R15 同款（第二次实证）；关键词黄 (1, 0.75, 0.09) 与 53期 同值（关键词黄跨模板确认）。

### 4.11 学习9期_超赞的小靓 6.6~12.2s「回忆跑马灯组」（2026-08-16 用户指认：素材和滤镜两层起作用，历史记忆快闪感）

| 层 | 做法 |
|---|---|
| **素材层** | 截图/录屏序列（PixPin 截图×4 + WeChat 录屏）**多轨叠放**，起始点 0.3~0.5s 步进交叠（非硬切，层层压上），每张挂 **模糊 / 放大(in,500ms) / 光晕** 之一 |
| **滤镜层** | 光晕（回忆光感）+ 模糊（年代虚化）交替——"素材运动 + 滤镜氛围"双层同时起作用 |
| **封装** | 整体封进复合片段 + 形状装饰层（形状1.png）压边 |

**定为 R18 回忆跑马灯组**：历史回顾段（"这些年/想当年/一路走来"）的快闪记忆流。要点：**多轨叠放交叠而非顺序快切**（与 R11 鱼贯同理但同屏共存），滤镜按张轮换制造"翻旧照片"感。对接：素材包历史素材/往期视频截帧；语义=历程回顾（与 BGM Dismantle/Veil of Darkness 同段适配，BGM 配方联动）。

### 4.12 49-财经(1) 0.6~5.7s「新闻回顾人物卡」（2026-08-16 用户指认：经典新闻回顾式卡片，比截图高级）

| 层 | 做法 | 效果/音效 |
|---|---|---|
| 引子快闪 | 旋转美元 0.6s + 红字"开战" | 聚光灯+动感缩小 / 呐喊声波+影视咚 / **单反快门声**（定格感） |
| **纸张底** | 纸张.jpg | 暗角 + **7416258989467374091（悬案特效=纸张做旧质感，本卡 11 次实证）** |
| **人物照×3 层** | 特朗普.jpg 原图 / **黑白漫画滤镜变体** / 重复层，三层同步 | 向左滑动(300ms) 齐动 |
| 时间戳 | "2025-04-02 16:00" 白 12 号 | 向上露出(500ms) |
| 新闻正文 | "特朗普签署行政令…征收至少10%关税" | **打字光标(in, 2517ms 长打字)** + 打字声同步 |
| 黑字标题 | "特朗普摁下关税战核按钮" **黑字 (0,0,0)** 压纸 | 向右模糊 II(500ms) |
| 印章收尾 | 复合片段 正片叠底 | "呼"转场 |
| 数字接力 | "34%" 70.6 号巨型字 | 圆柱体滚动 + 复古红花字 + 收银机叮 |

**定为 R19 新闻回顾人物卡**："某年某月某日某人说了什么"的标准呈现——比截图高级（用户口径）。要点：**人物照三层玩法**（原图+黑白漫画滤镜变体+复制层同步滑动）、黑字压纸的标题、长打字 2.5s 新闻正文。
**对接**：人物素材库（ID-042 待办——"路人皆知的脸"由用户备料）+ 新闻时间线；新闻类稿件的"事件锚点"段。
**悬案告破**：`7416258989467374091` 挂在纸张.jpg 与人物图上 ×11 → **纸张/做旧质感特效**（新闻卡片质感的核心）。§1 悬案更新。

### 4.13 R20 参考资料尾卡（2026-08-16 用户提议，大博主标准免责手法）

**场景**：点赞/升华收尾后，片尾列出本片引用的参考资料（媒体名+标题/日期）。

| 层 | 做法 |
|---|---|
| HF 线 | 片尾卡：参考资料列表（来源媒体名+标题，2-5 条，停留 3-5s）——模板可复用 hf_title 样式做列表布局 |
| J 线 | 文字轨尾卡（多行参考资料，渐显+停留片尾） |

**🔴 数据优势全自动**：素材包 MaterialItem 每条都有 `source_url`——参考资料卡由代码从 `script.material_package_id` 反查自动生成，零手工（模板作者要手抄，我们查库）。

**合规定位（与豆包规则对齐）**：
- ✅ 是 AI 溯源格式 B 的落地形态（"全部事实引用自：XXX报道"片尾停留≥3s）
- ⚠️ **不能替代正文就近标注**（网信要求每组数据就近带源）——尾卡是辅助免责层，正文带源是主层；两层都要
- 同时服务商誉防御（批评有出处=事实批评受保护）

**对接**：composition/J1 导出时，若 script 挂了素材包 → 自动追加尾卡段落；模板池新增 `hf_references_v1`（列表布局）。
