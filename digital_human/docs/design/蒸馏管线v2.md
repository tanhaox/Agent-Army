# 设计-蒸馏管线 v2 (0908 用户令: 先设计块和步骤, 定稿再跑)

> 状态: **✅ 已全实现 (0908)** — 五层管线/三道质检闸门/章名修复/料仓+三消费方全迁, 语法全绿, 待跑 HBO 首蒸验证。
> 定位 (用户令集合): 蒸馏 = 书 → **AI 外挂大脑** (结构化、可按需调用的知识库), 非静态摘要。
> 下游三个饥饿口: 总纲(元问题/一页核/路径) · 稿件(故事库/方法边界/反模式) · 尾卡(原句池)。
> 原则: 确定性优先 — 能代码验的不靠 LLM 自觉; 分层不叠加 — 每层一个职责。

## 五层管线

### Layer 0 解析层 (纯代码)
epub/txt → 章节列表 {name, text}
- ✅ 已修两个吞章坑 (head 关键词误杀 24/43 章 / "前言"兜底名二次误杀): HBO 2.1万→22.9万字
- ✅ 章名识别已修 (0908): ncx src 带锚点/缺目录前缀两头不匹配 → basename+全路径双键; HBO 27 章真名复活 («公共电视台，电视领域的西蓝花»«欲望都市黑道家族六尺之下…»)
- ✅ **覆盖率闸门已落** (distill_book epub 分支): 解析字数/包内文本总字数 <80% 抛 RuntimeError 拒跑; HBO 实测 98%

### Layer 1 分块层 (纯代码)
- ✅ **章级分块已落**: 每章一块 (≤40k); 仅超长章 (>40k) 才句界再切
- 依据 (0908 设计讨论): 12k 句界切块会把**跨块故事切断** — 半个故事入池=脏料; 章级天然零断裂
- ✅ 块带章名进提取 prompt (`【所在章节】{cname}`)
- HBO 实测: 27 章 = 27 块 (原 33 块/12k 方案废)

### Layer 2 提取层 (Gemma, 每章一次)
- 单提示词 9 类目 (不拆多提取器 — 拆 N 器 = N 倍时长, Gemma 31B 单提示词吃得下):
  - **重类目** (结构化六要素): 故事{title/背景/主体/决策与转折/结果/启示} · 方法{name/步骤/来源/适用/**边界**} · 反模式{坑名/情境/错误做法/后果/正解} · 路径{阶段/触发/动作/结果}
  - **轻类目**: 观点 · 概念{name/mech(决策规则 if-then)} · 案例 · 数据 · 原句(逐字)
- ✅ **提取质检已落** (_check_extract): JSON 结构 / 长章无故事无原句 / 六要素空洞率>50% → 该章重提 1 次

### Layer 3 合并+审计层
- 跨章去重 (seen 键)
- Gate A 合规审计 (池级, pro) → 高危物理删 + 合规映射.json (Gate 0.5 机器源) — ✅ 已有

### Layer 4 编纂层 (Gemma 128k 全池一次)
- **十一节产物** (✅ 已定稿): 元问题 / 核心哲学 / 概念与机制(反直觉圈点+决策规则) / 故事库 / 方法与清单(含边界) / 成功路径 / 反模式与坑 / 可引用原句 / 一页核(+自动合规备注)
- ✅ **编纂质检已落** (_check_compose): 节头齐备 / 故事条数 ≥池×0.7 (编纂合并容忍度, 弱信号退存在性判) / 一页核非空 → 重编 1 次
- ✅ sections/ 料仓拆分 (split_sections, 蒸馏尾部自动)

### Layer 5 料仓层 (产物结构)
```
data/l0/{书名}/
  {书名}.蒸馏.txt              整合稿 (人看 + 兼容旧消费方)
  sections/                    机器料仓 (按需调用, 0908 用户令)
    元问题.txt 核心哲学.txt 概念与机制.txt 故事库.txt 方法与清单.txt
    成功路径.txt 反模式与坑.txt 可引用原句.txt 一页核.txt 合规备注.txt
  合规映射.json                Gate 0.5 阀门源
  chapters/  facing/  overview.json ...
```
- ✅ **下游按需接口已全迁** (0908 用户令: 全迁) — `load_section(book_title, 节名)`, 各自保留 fallback:
  | 消费方 | 已改吃 | fallback |
  |---|---|---|
  | quotes.py (B-Q1) | sections/可引用原句.txt (行格式宽松兼容 * / - / 编号) | 旧整txt节解析 |
  | series_outline 材料 | 一页核+元问题+概念/故事/方法/路径 (截断限量) | input_json 字段 |
  | episode_gen 素材包 | 命中章节 + 故事库/方法与清单 (按本集概念过滤段块, Gate0.5 过滤) | 无料时静默 |
- 书籍档案 (作者/出版社) 不走料仓 — 已在 BookProject/input_json

## 参数表
| 参数 | 值 | 依据 |
|---|---|---|
| 块策略 | 章级一块, >40k 才句界细分 | 零故事断裂 |
| 提取 | Gemma temp 0.2, 单提示词 9 类目 | 注意力/时长平衡 |
| 提取重试 | 1 次/章 (质检不过) | 已落 |
| 编纂重试 | 1 次 (节不齐) | 已落 |
| HBO 预计 | 27 章 × 3~6min ≈ 1.5~2.5h | 章块 600字~3.5万字 不均 |

## 与既往令的闭环
- 分层思想 (用户令): 五层各一职责, 层间产物可验
- 三类料 (用户定谳): 故事/方法/路径 各有节+各有消费方
- 按需调用 (用户令 0908): sections 料仓替代整 txt 灌注
- cangjie B 元素: 方法边界 ✓; 五路提取器→9 类目单器 (权衡记录)
- reader 事故教训: 每层出口有确定性质检, 拒绝"跑完才算成功"
