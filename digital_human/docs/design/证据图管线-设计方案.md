# 证据图管线 (evidence_image) · 设计方案

> 定稿 2026-09-04。触发案例: Gemini 3.8 稿 (`projects/20260903_183552_f79dbb`) —— 稿中大量测试/对比数据段 (智能测试 59/62、SWE 基准 73.7、305词/s vs 154、单价对比), 最有说服力的画面是真实榜单/跑分/价格截图, 但成片全是 Pexels 通用空镜。

## 一、问题与根因

1. `url_fetcher.fetch_url` 只抽文本丢图 —— 素材包 31 条 + 原文, 图片 URL = 0
2. 导演层无"证据图" workflow
3. 素材层 2.0 只管官频视频切片, 不管网页图片

**用户裁决**: 全链一次到位, 硬条件 = **只有"有测试、有比较"的段落才配图**; 图片抓取起点 = index.html「🌐 抓取 URL」同行的「📷 搜图」开关 (全链总闸)。

## 二、数据流

```
index.html「🌐 抓取 URL」+「📷 搜图」开关(总闸, 默认开)
   ├─ 抓取时: fetch_url(with_images=True) 顺手提取本页 <img> URL (零额外请求)
   │    → 前端暂存 fetchedImages → 建稿存 articles.images_json (非空 = 总闸开)
   ▼
素材包建包 → 审计完成后 (仅当 article.images_json 非空) 自动扫图
   ├─ 原文条目复用 article.images_json (不重抓); 其余条目逐页 fetch+extract (条目≤20, 每页≤4, 整包≤24)
   ├─ 下载缓存 {materials_dir}/evidence/cache/{sha1(url)[:16]}.{ext} (Pillow 校验: 可解码+宽≥400)
   ├─ 9B VLM 打标 (llama-server 自拉起, 单图→JSON: is_chart/kind/desc_zh/numbers/quality/watermark)
   │    fail-closed: VLM 不可用/超预算(120s/包) → 存 vlm:null 不进池
   ▼ 写回 material_items.images_json
导演规划 (LLM 菜单加 evidence_image) → 代码闸门三道 (可用性/语义/数量≤10) → evidence slots
   ▼
slot 执行: collect_evidence_pool (is_chart ∧ quality≥4 ∧ watermark≠heavy)
   → pick_image_for_slot (数字重合×10 + 关键词×2 + kind 匹配×2, 单图中文读法匹配)
   → ffmpeg 单 pass: 等比+黑边 → zoompan Ken Burns → fade → drawtext 图源角标
   → render_scale_pad 归一化 (30fps+静音轨) → evidence_{idx:03d}.mp4
   ▼
合成 P 线 / 剪映草稿 J 线 自动接轨 (workflow 无关的 video 轨)
```

## 三、模块清单

| 层 | 文件 | 内容 |
|---|---|---|
| ① 抓图 | `app/services/url_fetcher.py` | `extract_images` (懒加载属性优先/装饰图黑名单/宽度过滤/去重/cap8); `fetch_html`; `fetch_url(with_images)` |
| ① 入口 | `web/index.html` + `web/news.js` | 「📷 搜图」checkbox (总闸); fetchUrl/createArticle 透传; 缩略图行; 扫图 SSE |
| ① 落库 | `app/models/content.py` `app/database.py` `app/schemas/` | articles/material_items 各加 `images_json` (幂等 ALTER) |
| ② 扫图/选图 | `app/services/evidence_service.py` | `scan_package_images` / `collect_evidence_pool` / `is_evidence_claim` / `pick_image_for_slot` / `_download_image` / VLM 打标 |
| ② 接线 | `app/routers/materials.py` | 建包后台第 4 步自动扫 (闸=article.images_json 非空); `POST /packages/{id}/scan-images` 手动补扫 (不受闸); `add_item` 顺手存图 |
| ③ 执行器 | `app/services/slot_workflows/evidence_image.py` | Ken Burns+角标成片; 缓存丢失误下; params_json 写回 image_url/source_media (同 job 去重); 失败 RuntimeError→fallback 链 |
| ④ 导演 | `config/visual_director_v2.txt` + `director_service/_postprocess.py` | 菜单行/params{claim含数字,keywords}/规则行/约束≤10/示例 slot; 三道闸门降级 broll_pexels + fallback_reason + trace |
| ⑤ 角标 | 同 ③ | drawtext `图源：{media}` 右下角 white@0.75 + box black@0.35, 避开底部字幕区 (y=h-th-8%H), 字体 msyh.ttc→simhei 兜底 |

### 12 处注册点 (漏一处即断链)

schemas/director.py Literal×2 → _rows.py 白名单+中英映射 → slot_workflows/__init__.py WORKFLOW_HANDLERS → slot_executor.py _EXECUTION_PHASES (phase1 "p") → director_routes/common.py _WORKFLOW_PIPELINE → director_prompt/_prompt.py _PIPELINE_MAP("P线 在线素材") → _fallback.py 降级链 (→broll_pexels) → _rules.py _chain_for → composition_service/common.py _WF_TIER=1 → _postprocess.py _MIN_DUR=3.0 → director.js WF_LABELS+'证据图' + director.html/audio.html P线标签 → models/director.py 注释。

## 四、关键设计决策

1. **数字中文读法匹配** (最强选图信号): 图上 OCR 出 "73.7", 稿件口播是 "七十三点七" —— 复用 `tts_verify._num_to_reading` 把池图数字转中文读法再子串匹配。单个数字 ("5"→"五") 太泛, 只认阿拉伯独立 token。
2. **语义闸门数字+词集双条件**: 数字字符 ∧ (测试/比较词集 ∨ "比X快/高/贵"句式)。"一样/一下/一般"等"一"字词素先剥离, 否则中文句句有"一"。"比如"不算比较句式。
3. **fail-safe 全链**: JS 渲染页抓不到图→无池→全降级; VLM 挂→vlm:null 不进池→退化现状; 池空/无匹配→RuntimeError→fallback 链 broll_pexels。扫图失败绝不置包 failed。
4. **截图不变形**: 先等比缩放+黑边 pad 再 zoompan (PPT 线直接 zoompan 会拉伸数字)。
5. **版权口径**: 角标+评述性短时使用; 片尾 hf-source-v1 来源卡 (_append_source_slot) 条目级来源双保险。
6. **幂等扫图**: `_has_scan_product` (带 local_path 的条目才跳过) —— add_item 抓取时存的 URL 级 images_json 恰是待扫对象, 不误跳。

## 五、验证

- 单测 `tests/test_evidence_service.py` 30 用例: extract_images 过滤 / 语义闸门正反例 (Gemini 稿真实段落) / pick 打分与 used 排除 / 池过滤 6 形态 / 三道闸门 (语义/空池/cap) / handler 集成 (monkeypatch, 产物命名+params 写回+空池 raise)
- 真机 ffmpeg: 1080×1920 Ken Burns 138帧 ✓; drawtext 角标像素方差 49.4 vs 黑边 8 ✓ (Windows 盘符冒号转义 `C\:` 验证过)
- 回归: tests/ 离线套件 48+30 全过; app 装配 + scan-images 路由注册 ✓

## 六、边界与后续

- 微信/APP JS 渲染页抓不到图 → fail-safe 落 Pexels (已知边界, 不阻塞)
- 30B 视觉模型 (8081) 实验位不依赖; 9B (8080) 自拉起同 asset_quality
- v2 候选: 图片本地缩略图走静态路由 (当前直接热链原图 onerror 隐藏); 池图跨包共享缓存已有 (sha1 key)
