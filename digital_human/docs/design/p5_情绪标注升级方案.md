# P5 情绪标注升级方案 (2026-08-13)

> ⚠️ **历史注记 (2026-08-14/15 更新)**: 本方案的"三层分工"中 **P1-P3 标意图已随七层适配被砍除** — 7 层洗稿稿已是完整爆款结构, boost 流水线现为 **P4(逐句精修)→P5(情绪标注)**, 详见 `docs/status/backlog.md` ID-046. 本方案仍有效的部分: **P5 情绪字典设计、P4/P5 分工原则、各链路消费方改造点** (已落地为 `app/services/emotion_dict.py`, 链路 8/14 全线打通, 见 ID-045). 文中涉及 P1-P3 的描述仅作历史参考.
>
> 目标: 全链路"有快慢有情绪有叙述". 从文稿 → 音频 → 导演 → 画面, 统一情绪语言.
> 状态: **已实施完成 (2026-08-13)**, 回测通过. ~~待真实 P1-P5 整链验证 + 音频听感确认~~ (**2026-08-14 链路已打通**: P5 落库 key bug / TTS 不消费情绪标注 / 段接缝破音 三处连环修复, IndexTTS 首次真正拿到情绪).

## 一、核心架构: 三层分工

```
P1(标意图) → P2(标意图) → P3(标意图)   ← 轻量情绪意图, 供 P4 参考
  ↓ 意图随稿传给 P4
P4(纯精修) ← 回归现状, 不标情绪, 保留全部铁律(信息守恒/数字锚定/开头锁定/多音字/主观句中立化)
  ↓ 定稿 (文字稳定)
P5(标情绪, 新增) ← 拿统一字典, 对定稿挨段标「段落 + 情绪 + 强度档(弱/中/强)」→ 唯一下游标注源
```

- **P1-P3 标意图**: 各层句式选择本身就是情绪决策, 轻量标注(一段一个意图标签), 供 P4 精修时有上下文
- **P4 纯精修**: 不承担情绪标注, 避免双任务过载; 现有铁律全部保留
- **P5 标情绪**: 新增工序, 输入 P4 定稿, 只读不写文字, 输出结构化标注; 可复跑(调情绪只重跑 P5)

## 二、统一情绪字典 (各链路共享)

| 情绪 | 8维向量 | 情绪基准α | 强度档(弱/中/强) | 视觉意图 | 画面搜索词 |
|---|---|---|---|---|---|
| calm | 平静主导 | 0.4 | ×0.7/×1.0/×1.3 | 沉稳/冷峻 | 城市/建筑/街道 |
| serious | 愤怒0.5 | 0.4 | ×0.7/×1.0/×1.3 | 紧张/严肃 | 会议/芯片/机房 |
| angry | 愤怒0.8 | 0.4 | ... | 张力/对立 | 冲突/抗议/数据 |
| surprised | 惊讶0.7 | 0.6 | ... | 爆点/冲击 | 特写/震撼画面 |
| happy | 高兴0.7 | 0.6 | ... | 升华/上扬 | 光/成果/前景 |
| ... | ... | ... | ... | ... | ... |

**关键设计**:
- P4/P5 只填「情绪标签 + 强度档(弱/中/强)」, **不填 TTS 参数**
- α = 情绪基准α × 强度档系数 × 音色系数 (音色只是参考坐标, 系统映射)
- 情绪集定死白名单 (基于实验: 平静/严肃/愤怒/惊讶/高兴 + 必要扩展)
- 各链路查字典: TTS 查向量+α, _intent.py 查视觉意图, 画面搜索查关键词

## 三、消费方与改造点

| 消费方 | 现状 | 改造 |
|---|---|---|
| TTS 层 text.py | 剥离 [xxx] + 按句号/||切段 | 尊重 P5 段落边界切段; 每段带情绪参数; 不再剥离情绪标记 |
| text.py _truncate_overlong | 超120字硬切(词断裂) | 回退标点切 (复刻 indexTTS 修复) |
| 导演 _intent.py | 白名单正则 3 情绪 + _EMOTION_VISUAL | 扩展情绪 + 改查统一字典 |
| 画面搜索 | 无情绪维度 | 接字典搜索词 |
| indexTTS split_segments | 已修词断裂 bug (2026-08-13) | ✓ 已完成 |

## 四、实验依据 (详见 tts_emotion_experiments.md)

- qwen 0.6B 自动识别不可靠(三连击): 转述情绪分不清 / 段落识别一半偏 / 平静科技稿误判惊讶 → **LLM 标情绪为主**
- 音色只作参考坐标: 不同音色 α 不同(大学教授0.4/用户声音0.6) 但不用过度工程化, 音色系数系统映射
- 情绪级自然 α 不同(实验): 反感0.3 / 高兴0.6 / 惊讶0.6 → 每情绪基准α 入字典
- 主体平静叙述 + 关键句情绪 → 自然; 全段强制情绪 = 神经病
- indexTTS split_segments 词断裂 bug 已修复 (front.py.bak_20260813)

## 五、实施步骤 (待排)

1. ✅ 建统一情绪字典模块 `app/services/emotion_dict.py`
2. ✅ P5_PROMPT + 调用逻辑 (boost_service.py)
3. ✅ P1-P3 标意图 (提示词各加一行 + P4 加"前工序意图理解")
4. ✅ P4 回归纯精修 (去情绪标记要求)
5. ✅ TTS 下游: 按段情绪合成 (lines/orchestrator/tts_service/audio)
6. ✅ _intent.py 扩展查字典
7. ✅ 端到端验证 (e2e 情绪合成 + import 回归)

## 六、实施回测记录 (2026-08-13)

| 步 | 回测 | 结果 |
|---|---|---|
| 字典 | 冒烟: vec_sum=0.8, α∈[0.1,0.65] | ✅ |
| P5 解析 | 4段正确 / 容错续行 / 空→None | ✅ |
| TTS 映射 | 行→段映射 (初版有bug → 修正 cur 段号) | ✅ 修正后全对 |
| TTS 分组 | 同段行合并, 每批带情绪参数 | ✅ |
| _intent | 情绪标注→视觉意图 + 回退旧稿 | ✅ |
| e2e | 3段真实合成, 每批 emo_vector/α 正确, wav 有效 | ✅ |
| subprocess 编码 | UnicodeDecodeError (Windows gbk) → 加 errors=replace (tts_lib 4处) | ✅ |
| import 回归 | 全部改动模块无循环依赖 | ✅ |

**备份**: boost_service/content/database/tts_service/_intent/_llm/orchestrator/lines/audio/silence_split 均 .bak_20260813

**待验证**: ① e2e 音频 (`E:\数字人计划\outputs\tts_experiment\e2e\` 3段) 听感 ② 真实 run_boost P1→P5 整链 (需 DeepSeek API)

## 七、α 坡度验证与修复 (2026-08-13, 实验定稿)

### 发现: 段落情绪突变 = 断层 (不可用)

- 切情绪类型(惊讶→平静)段级拼接 → 语气断层/挫裂感 (e2e_full / e2e_modules 验证失败)
- indexTTS 一次合成一个情绪参数, 段间直接切 = 突变, 无渐变

### 定稿: 同一情绪基调 + α 强度坡度

- **不切情绪类型**; 整篇/大段一个情绪基调
- **α 坡度**: 0.30~0.60, 0.05 步进, 平滑升降 (0.6→0.55→...→0.30→...→0.6)
- **去掉"平静"** (2026-08-13 用户验证): 平淡叙述会让人划走 → 低强度段(叙述/身份)用 **惊讶/1(0.3)** 替代. 惊讶 0.3 不拖慢语速(惊讶向量非低落).
- **P5 强度档 1-7**: `α = 0.30 + (档-1)×0.05` (1→0.30 ... 7→0.60), 相邻段差 ≤2 档 (坡度平滑), 主体惊讶低档, 关键段 4-7 档
- **语速维度: 不接入** (2026-08-13 用户决定) — atempo 变速段内部有爆破音 artifact("噗"), 且 α 坡度已够"有情绪"

### 修复: 句尾破音 (每段"噗")

- 根因: `_tts_text` 把**句号后的 || 清掉** (`句1。||句2。`→`句1。句2。`), 气口丢失 → 句尾破音
- 修复: `_tts_text` 加 `keep_breaks` 参数, indextts 路径**保留 ||** (indexTTS 识别为停顿, 实测 +0.6s)

### 修复: 段拼接破音 (段尾音+段首紧贴)

- 根因: 直接 concat 时段尾音与下段起音紧贴 → 拼接处"噗"
- 修复: `_concat_wavs_with_fade` — 段尾淡出(st=段尾, **afade out 必须指定 st**, 默认 st=0 整段静音!) + 段首淡入, **无静音 gap** (用户验证 gap=0 停顿最舒服)

### 备份

text.py / emotion_dict.py / boost_service.py / audio.py / tts_service.py 均 .bak_20260813

## 八、生产模型: 千问 flash (2026-08-13)

- DeepSeek 拥堵(访问量大崩, 长请求空响应/连接断), P1-P5 生产改用**千问 qwen3.6-flash** (token-plan 专属通道)
- config/app.yaml 加 qwen 节 + config.py `QwenConfig` + `boost_service._call` 用千问 flash
- 性能: flash 跑 P3-P5 约 2 分钟 (max 要 10 分钟)
- json_mode (P1 用) 千问 compatible-mode 支持 ✓
- 用户决定: 所有 P 序列用 flash (能力差不多, 不用最强模型, 关键太快)

## 九、完整链路 + 存储断链修复

```
run_boost: P1→P2→P3→P4→P5(紧随P4) → emotion_annotations
  ↓
scripts.py:  script.emotion_annotations = boost['emotion_annotations']   ← 修复(之前漏存, audio.py 读到 None)
  ↓
audio.py:    tts.generate(emotion_annotations=script.emotion_annotations)
  ↓
tts_service: emotion_segments → 每段 α 坡度合成 → fade 拼接
```

- P5 已在 run_boost 紧随 P4 (boost_service.py:803), 失败不阻塞(回退 emotion_annotations=None)
- `_parse_emotion_annotations` 支持中英文情绪名 (千问可能输出 surprised/serious)

## 十、语义重复追踪 (2026-08-13)

| 步 | 家族 | 博士 | 打工 | 标题重复 |
|---|---|---|---|---|
| 原稿 | 8 | 5 | 4 | 4 |
| P2 | 8 | 5 | 4 | 4 |
| P3 | 8 | 5 | 6 | 4 |
| **P4** | **3** | **4** | **3** | **0** |
| P5 | 3 | 4 | 3 | 0 |

- 语义重复**原稿就有** (解释稿/新闻稿特性, 同一比喻讲多遍)
- **P1-P3 只加不减**, **P4 删大部分** (删32%), P5 只标注不改
- 正式流程**必须走 P4** (跳过 P4 直接 P5 会保留重复 — deepseek-ok.txt 案例)
- 待优化: P4 删重复力度可加强 (同一比喻全文只留 1 次)

## 十一、真实整链验证 (2026-08-13)

- deepseek.txt (3793字) → 千问 qwen3.8-max P5 → **deepseek-ok.txt** (8段大模块, 全惊讶基调, α坡度, 无漏标无硬切)
- deepseek-ok.txt → TTS 合成 → deepseek_emotion.wav (546s, 8段 α 坡度, fade 拼接)
