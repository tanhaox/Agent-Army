# processed/

Inputs the skill has finished consuming get moved here from `input/`.

This keeps `input/` clean while preserving your originals — the skill never
deletes your files, it relocates them. If you need to re-run a task on a source
document, move it back into `input/`.

Files here are git-ignored (kept local). This README is the only tracked file.
