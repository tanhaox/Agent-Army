# HyperFrames XCYJ Layer

This folder is the HyperFrames user-owned layer for XCYJ rules, assets, templates, skills, scripts, and production notes.

- Ownership: user-owned layer; commit this layer.
- Git: this folder should be committed in the HyperFrames repository when its contents are ready.
- Agent routing: Codex and Claude should read this after `hyperframes/AGENTS.md` or `hyperframes/CLAUDE.md`, and after the official HyperFrames skills (installed via `npx skills add heygen-com/hyperframes`).
- Boundary: keep HyperFrames implementation here aligned with HTML + GSAP production; do not add Remotion API code.

