# HyperFrames Video Projects

This folder is the HyperFrames project layer for actual HTML + GSAP video projects and future project organization.

- Ownership: project layer; preserve nested `.git` directories.
- Git: project folders may have their own Git boundaries; do not remove or flatten them during migration.
- Agent routing: Codex and Claude should read the specific project instructions inside each project before working there.
- Boundary: keep rendered-video projects separate from the official skills (`.agents/skills/`, npx-installed) and the reusable `cyxj/` layer.

