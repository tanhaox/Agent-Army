# 数字人短视频项目 — 状态总览

**最后更新**: 2026-08-28（素材层 2.0 实体管线贯通 + 发力方向定稿：画面层主攻）
**本文件角色**: 反映"当前真实进度 + 真实技术栈"，与 [`执行计划清单.md`](执行计划清单.md) v10（**规划目标参考**）配合使用
**变更日志**: 见 [`CHANGELOG.md`](CHANGELOG.md)
**入口**: 仍以 [`README.md`](README.md) 为准；本文件与 CHANGELOG 在 README "项目入口" 表格中索引

---

## 零、最新动作（2026-08-27~28 素材层 2.0 贯通 + 方向裁决 + 视觉双层定稿）

**方向裁决（用户令，后续以此为准）**：双主线=拆书+新闻（财经/地缘）；**稿件层 80-90 分只做维护**；**画面层 40-50 分是唯一主攻**（剪辑+素材）；ComfyUI 停止投入；实验未成功不并入产线。设计见 [`../design/素材层2.0-实体驱动管线.md`](../design/素材层2.0-实体驱动管线.md)

- ✅ **素材层 2.0 首跑贯通（实验态）**：实体抽取（六类路由+显著性过滤）→ 油管官频批量（NVIDIA/台积电/小米）→ 切片 471 段 → llama 打标（含**烧录字幕检测**）→ 入库（2129→2600，无字幕 253 优先）→ **实体靶心命中实测通过**（口播提"小米/英伟达"→直取官片无字幕片段）。产线接线挂 `ENTITY_LAYER_ENABLED=False` 等验证后并入
- ✅ **视觉双层定稿**：9B 粗标（批量，87% 可信）+ **Qwen3-VL-30B-A3B-Q5**（21.7G 已就位，MoE 激活 3B；描述/OCR/品牌识别全面高一档）——20 片交叉验证数据支撑
- ✅ **GPU-VPN 互斥守卫系统级固化**：gpu_service_manager.session 入口拦截（虚拟网卡与显卡驱动冲突死机风险；admin 进程 Path 隐藏漏判坑已修）。mihomo(Clash Meta) 已装 E:/AI/mihomo 待订阅；变色龙弃用
- ✅ **J 线深化（同批）**：J2 效果目录 v2（悬案 7416258989467374091 对号=智能调色基线）+ slot 镜头契约层（Hell Grind 裁剪版+effect_recipe 接线）+ i2v 迭代纪律工具 + 意象生成收编 tools.i2v（托管拉起）——后三者属 ComfyUI 线，**随停止令封存待模型跃迁**
- ✅ **稿件层维护级三件**：口播适配端点（手改稿重跑数字禁令 50%→百分之五十）、情绪标注**全程惊讶打底**（推翻 8-25 geo=serious）、P4 画面感公式（参照物证据/物理行为二选一）两线提示词
- ✅ **知识资产**：全项目 md 归类+INDEX 总索引；Hell Grind 官方开源参考库入库（refs/ 34 子技+中文七层架构版）；参考片拆解 skill 产物入库
- 🎯 **待办**：素材层接产线（验证后开闸）→ semiengineering/ASE 动画源 + Intel 批量修复 → VPN 订阅（mihomo 贴链接即用）→ J 线素材内动效第一步（Ken Burns+入场+转场）
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-27/28 条目

## 零-0、上次动作（2026-08-22 拆书 L0 两级蒸馏 + 产线自动化）

- ✅ **两级蒸馏**：L0 章节（元信息/排版强调/注释/引用源头/概念/案例/金句，按真实章）+ 第二层 facing（kernel/units/quotes/cases/compliance/readers），产物落 `data/l0/{书名}/`；设计见 `docs/蒸馏产物schema设计.md`
- ✅ **总纲三段结构**：E1 导读（含系列预告 N 集）/ 中间拆解 / 末集落地；每集带卖点（挂车钩子）；集数按 units 自适应
- ✅ **产线自动化**：创建书 → 蒸馏 → auto-fill 自动填充（补全/合规/评论层/总纲）；分析类按钮（预评估/补全/确认1/生成总纲）去掉 → 🧠 L0 面板 + 🔄 同步蒸馏
- ✅ **评论层精修 + 逐集按单元消费**（痛点/素材/金句/评论均关联 unit_id）；E1 稿注入系列预告
- ✅ **UI 融合**：books_content L0 面板、books.html 书库 L0 标 + 🔄 重蒸馏（已蒸馏书可重跑）、books_story 总纲卖点列
- 🎯 **待办**：素材书外搜索接入（zhipu）、compliance 云端选项、拆书 P2 后半段（每集一键执行→剪映草稿）、书系视觉模板
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-22 条目

## 零-0、上次动作（2026-08-21 PPT 元素级剪映出片 jy2 + 拆书 6 集系列上线）

## 零、最新动作（2026-08-21 PPT 元素级剪映出片 jy2 + 拆书 6 集系列上线）

- ✅ **PPT 出片（/api/ppt，jy2 默认）**：每页 = 1 base 层 + 逐元素透明 PNG → 剪映多轨草稿逐级渐显（替代逐帧捕获 3h → 秒级截图）；`compute_page_timing` 纯确定性几何编排（宫格/块级/口播匹配）；字幕轨复用 R9 v3；TTS 缓存（台词哈希免重跑）
- ✅ **系列皮肤包**：母本集抽 color_tokens/背景 → 拆书 5-6 集视觉统一；**08-22 改：保留原 PPT 文字色/背景**（皮肤不再强改配色，避免"颜色与原 PPT 不一"）
- ✅ **剪映草稿样式定稿（08-21~22）**：孤月体/字号5/缩放70%；免责独立轨（右上）+ 系列角标（左上）；位置换算坑记录：**剪映面板读数 = transform × 画布全尺寸(1920×1080)**，transform 单位是半个画布宽/高
- ✅ **拆书系列（/api/books）**：6 集编排（输入补全→评论层→素材包→多集总纲→逐集生成，级联重跑）；蒸馏（本地 Gemma → 精华稿）；**安全评级预评估 Gate 0**（green/yellow/red + 雷区 + 安全操作，🟢/🟡/🔴 前端图标）
- ✅ **合规审查**：豆包规则榨取入库（财经科技/AI 专项/时政 CR-0/拆书 6 发）；灵性检测下沉代码扫描；R20 参考资料尾卡
- ✅ **建稿赛道勾选**（08-16）：科技/商业 vs 地缘/国际，驱动评论层 + 七层审计 + 洗稿模板分支
- ✅ **素材质检治理**（08-16）：本地碰撞策略开关 + VLM 内容质量分 + Pexels 质检三档
- ✅ **J 线 R9 自动编排**（08-17）：字幕块语义分类 → 内联划重点(金/红) + 同帧语义音效 + 动效 + 密度闸门
- 🎯 **下次开工**：拆书 P2 后半段（每集一键执行 → 剪映草稿）；书系视觉模板；来源卡书版；发布挂车清单
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-21 条目

## 零-0、上次动作（2026-08-16 J 线剪映草稿产线上线 + 效果知识库大丰收）

- ✅ **J1 上线**：导演台「🎬 导出剪映草稿」——slot→video 轨+TTS 分段→audio 轨+字幕（洗涤/断句/自动划重点），音画不预合成，草稿直落剪映文件夹，人工审+BGM+导出。四连实验定案：**新版剪映接受代码生成的明文草稿**，无需旧版
- ✅ **效果知识库（用户指认→AI 解码研究循环跑通）**：配方 **R1~R19**（核心=R9 协同三件套：字幕句+关键词大字+同帧音效）、音效三层知识（语义 13 类/动画搭配 42 族/叙事功能 12 类含情绪表对接 P5）、BGM 光谱 6 条、三色板定标、密度规则（前 30s 高密度后降档）
- ✅ **素材资产**：73 行业工程+教学科入快照（明文在 template.json.bak）；118 模板效果目录；音效库 49 个
- ✅ **P 线关键词双层防线**（ID-050）：提示词具象化映射表 + 代码兜底过滤
- 🎯 **下次开工**：J2/R9 编排实现（划重点词→强调轨大字→同帧语义音效，弹药已全部就位）；悬案：主字体 7078971008769593887 名字对号、17期 音效落盘
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-16 条目

## 零-0、上次动作（2026-08-15 素材聚合 + 流水线三页拆分 + 智谱补搜）

- ✅ **素材聚合层上线**：7 层模板字数塌根因 = 单篇原文喂不饱 L3~L6（占字数 75%）。新增「新闻线索」页素材包：多源抓取 → 七层覆盖审计（红绿图+缺口+补搜词）→ 智谱 web-search 定向补搜 → 整包按层注入洗稿。实测带包 **1843 字**（无包同链路 827 字）
- ✅ **流水线页拆三页**：`index.html`（新闻线索：文章+素材包）→ `writing.html`（文字加工：洗稿/修正/爆改/字数统计）→ `audio.html`（音频加工：段落/TTS/一键成片）。URL 参数交接；全站 nav 9 页改三链接；app.js v7 只留共享层
- ✅ **智谱补搜接入**：独立 `/api/paas/v4/web_search` 端点；**免费额度 2026-09-12 到期**——到期/耗尽/key 未配置都会 material_error 明确中文提示（新闻线索页面板常显到期日）
- ⚠️ **待用户**：`.env` 填 `ZHIPU_API_KEY`（open.bigmodel.cn/usercenter/apikeys）后重启，补搜即全通
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-15 条目

## 零-0、上次动作（2026-08-09~12 提示词体系扩展 + 导演台僵尸修复 + 合成归一化）

- ✅ **提示词体系从单套扩展为三套**：
  - `config/laotan.txt`（8/9 更新）— 老谭聊财经，原版深度分析人设
  - `config/laotan-geo.txt`（8/11 新建）— 地缘政治/国际博弈变体
  - `config/laotan-tech.txt`（8/11 新建）— 科技/开源项目科普变体
  - ⚠️ 待确认：geo/tech 变体对应哪些赛道/账号，是否已绑定 Persona
- ✅ **视觉导演提示词大版本更新**：`config/visual_director_v2.txt` 8/12 更新至 33KB
- ✅ **导演台 Slot 重试僵尸修复**（8/8）：`running` 状态的僵尸 slot 不再被 409 拒绝，新增 `_job_truly_executing()` 检测 + SSE retry 事件流打通
- ✅ **合成视频时长归一化**（8/8）：HF 卡 `duration_sec` 被模板 clamp 到 [5,30] 导致视频比音频长 +18.3s → 新增 `_normalize_clip_duration`，成片 drift 从 +18.30s 降到 +0.00s
- ✅ **合成后产出洗稿.txt**（8/8）：成片同目录生成 `洗稿.txt` 供对照复核
- ✅ **Persona 吸收品牌/开结尾/host_id**（8/8）：成片去「老陈」硬编码，人物即账号
- ⚠️ **ComfyUI 海报能力**（8/10 调研，搭建中断）：TextOverlay + FLUX 方案已调研完毕，`ComfyUI-Advanced-TextOverlay` 节点未安装，**搭建未完成**
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-08 条目

## 零-1、上次动作（2026-08-08 HF 模板三件套 — 圆饼图缺角修复 + 横屏模板 + 品牌泛化）

- ✅ **圆饼图缺角修复（模板层）**：竖屏/横屏新闻杂志模板 `buildDonut` 三处守恒重写
  - 根因三重叠：主段 GSAP `scale:1.1`/`rotation:14°` 分离动画 + 极小段 `Math.max` clamp 失真 + offset 不守恒（末段 wrap 落进首段内部）
  - 修复：`avail = C - n*SEG_GAP`（缺口按比例预留）、`stroke-dashoffset = -used` + `used += len + SEG_GAP`（offset 守恒）、扇区改纯 opacity 错峰淡入
  - **验证**：双模板各重渲染一条含 pie 成片（`items:[58,22,15,4.9,0.1]`，含 0.1% 极小段），PIL 沿环 0.5° 扫描 2.4s/7.8s 关键帧全部整环闭合（竖屏 719/720、横屏 720/720）；几何模拟段对重叠 2 次 → 0 次
- ✅ **HF 横屏模板**：新增 `news-magazine-v1-ls`（1920×1080，composition_id `news_main_ls`），`_pick_hf_template` 按 `job.video_format` 宽高比自动选横/竖模板（代码引入 commit `4997ca1c`，2026-08-07）
- ✅ **品牌泛化**：`_SAFE_KEYS` 27 个 key 新增 `brand_name`/`stamp_name`/`brand_tag`；`_merge_brand` 从 `slot.director_job.script.host` 注入账号名/印章/标语（lazy=selectin 零额外查询，缺省中性回退）
- ⚠️ **待重启后端**后 E2E 验证品牌注入（`reload=False` 不会自动加载新代码）；模板实体位于 `E:\AI\digital_human\hf_prep\`，**不在 git 仓库内、无版本控制**
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-08 条目 + [docs/backlog.md](docs/backlog.md) ID-035/036/037

## 零、上次动作（2026-08-07 素材生命周期策略升级 — 保留 slot 素材防返工）

- ✅ **核心矛盾解决**：删除合成成功后的 `shutil.rmtree(slots/)`，slot 素材默认保留 `slot_retention_days`（默认 7 天）
  - **问题**：原策略无条件删全部 slot 产出，而 retry 只重置部分 slot → 「重试同类 / 换素材」局部重制触发全量 REPAIR 返工，REPAIR 失败即合成崩溃
  - **效果**：retry 单条时其余 slot 复用磁盘素材（实测 mtime 不变），不再返工
- ✅ **删除 slots/ 唯二出口**：
  1. **用户删 job** → `_cleanup_job_files` rmtree 整个 job 目录（含 slots/）
  2. **保留扫描** → completed 且 `completed_at` 超 7 天 → 清 `slots/` + `hf_visual/<job_id>/` + 孤儿中间件，**保留成片** `director_<job_id>.mp4` + manifest
- ✅ **清理补充**：`_cleanup_intermediates` 追加 `silence_fallback.wav`（master 音频缺失兜底，此前遗漏）
- ✅ **purge 连带删**：`purge_replaced_slots` 删 DB 行时连带删对应 slot 目录
- ✅ **新配置项**：`slot_retention_days: 7`（0 = 关闭保留，回退旧行为）
- ✅ **验证 19/19 通过**：隔离脚本 `scripts/verify_retention.py`（临时 DB + 临时目录，不触碰真实数据）— 核心链路保留 / retry 单条 mtime 不变 / 幂等返回 / 保留扫描 / fallback 清理
- ⚠️ **待重启后端**后 E2E 验证（`reload=False` 不会自动加载新代码）
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-07 条目 + [docs/improvements/已完成-20260807-素材生命周期保留策略.md](docs/improvements/已完成-20260807-素材生命周期保留策略.md)

## 零、上次动作（2026-08-03 导演生产路径优化 v3 — 零拷贝 + 48kHz 统一 + 8 阶段完成）

- ✅ **8 阶段导演生产路径优化**：从 ComfyUI workflow + TTS 切片两个源头消除三重浪费
  1. **ComfyUI 双 workflow 模板**：新建竖版 1080×1920 + 横版 1920×1080，均 @30fps，+ImageScale node 440，无需后归一化
  2. **poll_comfyui 零搬运**：直接返回 ComfyUI 输出路径，不 copy2
  3. **音频链路统一 48kHz**：extract_audio_slice_wav → 48000；非 host workflow +48kHz 静音轨；mixed_host_broll +ar 48000
  4. **删除 `_prepare_inputs()`**：~80 行归一化函数 → 存在性校验 + 直接 `-c copy` concat
  5. **目录统一 + HF 归一化**：slot_root() → `composition_output_root/slots/`；HF 产物自动加静音轨 wrapper
  6. **copy2 → os.replace**：5 处原子 replace（跨盘 fallback copy2+unlink）
  7. **合成后自清理**：compose 成功删 `slots/` 目录；stale job 磁盘同步删除
  8. **config deprecation**：`director_output_root` + `hf_visual_root` 标注 `@deprecated`
- 📄 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-03 条目 + [docs/backlog.md](docs/backlog.md) ID-031
- ⚠️ **待重启后端**后 E2E 验证（`reload=False` 不会自动加载新代码）

## 零、上次动作（2026-08-02 项目盘符迁移 C: → F: + 全量硬编码路径清理 + 启动验证）

- ✅ **全量硬编码路径扫描 + 修复**：将 `F:\AI-Agent-Local\digital_human` 全部替换为脚本自定位或相对项目根目录的动态解析
  - 核心配置：`config/app.yaml` `data_dir` 改为 `"data"`，由 `app/config.py` 解析为项目根目录下 `data/`
  - 启动脚本：`launch_system*.bat`、`launch_news_system.bat`、`simple_launch.bat`、`run_4090_gpu.bat`、`start_dashboard_simple.bat`、`_run_fix.bat`、`_apply_vocab_fix.bat`、`_run_tokenizer_fix.bat` 全部改为 `cd /d "%~dp0"` 自定位；`_apply_vocab_fix.sh` 改为 `cd "$(dirname "$0")`
  - Python 辅助脚本：`scripts/audit_llm_slot_drift.py`、`scripts/verify_timeline.py`、`scripts/rewrite_setnode.py`、`_concat_audio.py`、`_list_scripts.py`、`_apply_vocab_fix.py`、`_trigger_fix.py`、`smoke_test.py` 全部改为 `Path(__file__).resolve().parent/parents[1]` 动态定位
- ✅ **盘符迁移验证**：从 `F:\AI-Agent-Local\digital_human` 启动，配置加载、数据库 URL、Uvicorn、静态页面、`/openapi.json`、`/api/articles/prompt-templates` 均正常
- ✅ **文档标记**：CHANGELOG 新增 2026-08-02 迁移条目；本文件新增“迁移记录 / 排坑优先级”章节

## 零-1、上次动作（2026-08-01 全项目审计修复收尾 P0×2 + P1×3 + P2×7 + docs 全面更新）
- ✅ **P0-2 video_outputs 脏路径清洗**：删 `5882f87b` 脏行（前缀错 `E:\机器人计划\`）+ 4 个 zombie visual job，残留目录整体回收站（经用户确认）
- ✅ **P1 系列前端修复**：app.js `btn-save-director` 空引用 5 处（helper 化）、`renderProjectDir` 崩溃（`script-text` 锚点 + 守卫）、library.js tags 判空
- ✅ **P2 系列收尾**：library HEAD 路由、失败任务全量清理（DB 删 66 行）、SSE 重连上限、JSON.parse try/catch、.test_videos 回收站、fetch r.ok 检查
- ✅ **library.py 删除走回收站**：`_recycle_file` helper（PowerShell `Microsoft.VisualBasic.FileSystem::DeleteFile`）端到端实测通过
- ✅ **docs 全面更新同步**：backlog ID-002 状态 done + 排期 #6 删除线、CHANGELOG 追加 2026-08-01、ID-002 计划文档重命名 `已完成-20260727-ID002-视觉导演Agent2.md` 标记全阶段完成
- 📄 完整报告见 [`docs/improvements/已完成-20260801-全项目排查.md`](docs/improvements/已完成-20260801-全项目排查.md)

## 零-1、上次动作（2026-07-31 合成异步化 + SSE 进度透传 + 日志面板 DOM 修复 + 合成去重质量优先级 + 按钮转圈恢复）

- ✅ **合成视频异步化 + SSE 进度透传**：compose 端点改为后台线程 + SSE 实时推送 6 步进度（标准化/拼接/字幕/响度/校验/完成）
- ✅ **执行日志面板 DOM 修复**：`innerHTML = ''` 改为 `pre.textContent = ''`，避免销毁 `<pre id="exec-log-pre">` 导致 SSE 事件全部静默丢弃
- ✅ **合成去重质量优先级**：dedup 按 workflow 质量 tier 优先（host=0 > broll/hf=1 > black_subtitle=9），解决 black_subtitle 重试时间戳更新覆盖所有 host slot 的 bug
- ✅ **按钮 spinner 转圈恢复**：exec_done/exec_cancelled/compose_done/compose_error/SSE 断连 5 条路径均恢复按钮；selectJob 选择 executing 任务时自动连接 SSE

## 零-2、上次动作（2026-07-31 导演台 Slot 重试去重 + 执行日志抽屉 + black_subtitle 字体路径修复）

- ✅ **Slot 重试/去重修复**：fallback 链产生多行 → 前端按 slot_index 去重 + 后端拒绝重试 replaced + 新增 purge 端点 + 「🧹 清理已替换」按钮
- ✅ **执行日志固定底部抽屉**：HTML 移到 body 末尾 + CSS `position: fixed` 260px + JS `_sseCollapseTimer` 定时器管理（仅 terminal 事件后 3s 自动收起）
- ✅ **black_subtitle ffmpeg fontfile 修复**：`r"C\\:"` 双反斜杠 → `"C\\:"` 单反斜杠，ffmpeg 正确解析驱动器号
- ✅ **HF 渲染管线验证**：hf_chart (29.1s/333KB) + hf_title (20.9s/415KB) 单 slot 测试均成功，HF 无 bug

## 零-3、上次动作（2026-07-31 视频库：素材库 + 成品库 全链路落地）

- ✅ **DB 模型**：`VideoAsset`（素材库 16 字段：来源/pexels_id/画幅/中英描述/标签/赞/使用计数）+ `VideoOutput`（成品库 8 字段：标题/路径/画幅/时长/关联导演任务）
- ✅ **素材库 API**：`app/routers/library.py` — CRUD + 关键词搜索（中英文描述+标签+大类）+ 画幅/大类/赞筛选 + 标签去重 + 大类列表 + 文件流
- ✅ **成品库 API**：同上文件 — list / file / delete
- ✅ **Pexels 自动入库**：`pexels_service._register_video_asset()` — 下载成功即按 pexels_id 去重写入 VideoAsset，带画幅/时长/摄影师/标签
- ✅ **Compose 自动登记**：`director.py` compose 端点 — 合成成功后自动创建 VideoOutput 行（标题取脚本前 30 字）
- ✅ **前端页面**：`web/library.html` + `library.js` + `library.css` — 双 Tab（📦 素材库 / 🎬 成品库）+ 搜索/筛选/赞/编辑/删除 + 视频预览 + 深色主题
- ✅ **全站导航**：9 个页面均已包含「视频库」链接

## 零-4、上次动作（2026-07-30 导演台执行引擎健壮性大修 + 前端交互完善）

- ✅ **大文件上传修复**：BaseHTTPMiddleware → 纯 ASGI 中间件，解决 4 张高分辨率 PNG 上传失败
- ✅ **HF 渲染死锁修复**：Popen + taskkill /F /T 杀整棵进程树，解决 Windows 管道继承死锁
- ✅ **GPU 服务失败优雅降级**：ComfyUI 启动失败不再炸掉整个任务，后续 Phase 继续执行
- ✅ **Host–Persona–Role 关联链修复**：通过 persona_key 前缀匹配获取 view_groups 机位图
- ✅ **replaced 状态不再被重复执行**：pending 只取 queued，replaced 为终态
- ✅ **Slot 重试不再创建重复 slot**：原 slot 重置重跑，不走 fallback 链
- ✅ **black_subtitle ffmpeg 转义修复**：去除情绪标签 + 完整特殊字符转义
- ✅ **导演台前端交互完善**：
  - Slot 详情面板不再一闪而过（记住展开状态）
  - Workflow 类型中文化（数字人/下载素材/HF图表/本地素材/黑底字幕）
  - 停止/取消按钮（执行中可见，phase 边界检查取消标志）
  - 重试按钮策略（执行中隐藏，非执行时仅 failed 可点）
  - 按 workflow 批量重试（「重试同类」按钮）
  - 执行日志始终可切换（「📡 日志」按钮）

## 零-5、上次动作（2026-07-29 导演台执行引擎重构 + 人物关联 + 全站导航统一）

- ✅ **PEXELS_API_KEY 加载修复**：`app/config.py` 新增 `_load_dotenv()` 轻量 .env 加载器
- ✅ **ComfyUI 纳入 GPU 托管**：`gpu_service_manager.py` 新增 comfyui 服务规格，按需拉起/空闲自动关停
- ✅ **执行模型重构为 Phase 调度**（`slot_executor.py` 核心重写）：
  - 按 workflow 类型分 4 阶段批量执行：ComfyUI(host/mixed) → Pexels → HF → 本地
  - 30 段音频中仅 4-5 段需 ComfyUI，只拉起一次 session 跑完
  - Fallback 集中兜底：非 GPU 步骤立即重试，GPU 步骤攒一批一个 session
- ✅ **相邻约束 + 参考来源段处理**：
  - host 不能挨 host、HF 不能挨 HF（交换策略）
  - 参考来源段自动检测 + 追加 `hf_title` slot（无配音，轻音乐带过）
- ✅ **人物关联（Persona）功能**：
  - 提示词模板 + 音色 + 形象 三维绑定为“一套人物”
  - 后端 CRUD + `GET /api/personas/by-template/{name}` 联动查询
  - `web/personas.html` 人物管理页面
  - 流水线选模板后自动锁定音色/形象（disabled + 锁定提示条）
- ✅ **全站导航 UI 统一**：8 个页面顶部导航与 director.html 看齐（sticky 毛玻璃 + 7 链接 + active 高亮）

## 零-6、上次动作（2026-07-29 音频质量三连修 + TTS GPU 托管 + 流水线→导演台衔接）

- ✅ 音频残音/极短句/alignment 500 三连修 + 服务器切换 .venv
- ✅ TTS GPU 服务托管（按需启动/排队/空闲自动关停）
- ✅ 流水线→导演台自动衔接（8s 倒计时横幅 + URL 参数透传）
- ✅ GPU 服务强制端口释放兜底 + 桌面启动 bat 升级

## 零-7、上次动作（2026-07-28 前端 UI 升级 + 模板管理）

- ✅ **前端设计系统全面升级**
  - 统一深色主题 + 玻璃拟态 + 径向渐变光晕 + 蓝紫渐变文字
  - 所有页面共享同一套 CSS 变量系统
  - 统一 8 页导航栏（流水线 / 导演控制台 / 模板管理 / 视觉渲染 / 音色管理 / 角色管理 / 人物）
- ✅ **新建 `web/director.html` 导演控制台**（ID-002 前端落地）
  - 4 步流程指示器 + 左右两栏布局 + Slot 时间轴可视化 + 自动轮询
- ✅ **重写 `web/index.html` 流水线页面**
  - 3 步流程（输入文章 → AI 洗稿 → 生成音频）
  - AI 洗稿步骤新增"数字人/提示词模板"下拉选择器（动态加载 `config/*.txt`）
  - 移除冗余的"分段/导演选择"步骤（已迁移到导演控制台）
- ✅ **新建 `web/templates.html` 提示词模板管理页面**
  - 上传/改名/删除模板，非内置模板可管理，`laochen_default` 受保护
  - 后端新增 4 个 API：`GET/POST/PUT/DELETE /api/articles/prompt-templates`
- ✅ **ID-002 阶段 6 前端 UI 完成**，待阶段 7 文档收尾

## 零-8、上次动作（2026-07-27 Web UI 导演选择/URL抓取/模型开关）

- ✅ **ID-007：分段导演选择 UI 优化完成**
  - `web/index.html` 步③ 增加全选/取消全选、拖拽提示、已选句数/预估总时长
  - `web/app.js` 支持批量勾选、HTML5 拖拽排序、显示 `segment_type` 中文标签与 `estimated_duration`
  - 保存时先 `POST /api/scripts/{id}/segments/reorder` 批量更新顺序，再 `PUT /api/segments/{id}` 更新选择状态
- ✅ **ID-001：URL 自动抓取标题和正文完成**
  - 新增 `app/services/url_fetcher.py`（Toutiao 站点规则 + JSON-LD + meta/paragraph 回退）
  - `app/routers/articles.py` 新增 `POST /api/articles/fetch-url`
  - `web/index.html` / `web/app.js` 增加 "🌐 抓取 URL" 按钮与自动回填
- ✅ **ID-008：洗稿模型选择开关完成**
  - `web/index.html` 步② 增加 flash/pro 下拉选择；`web/app.js` 透传 `model` 参数

## 零-9、上次动作（2026-07-26 HF 视觉渲染）

- ✅ **HF (HyperFrames) 视觉渲染模块接入 54321 Web 完成** — 独立管线,不占 GPU、不接 ComfyUI、与 LTX23 状态机解耦
  - 单一模板 `news-data-v1` v1.0.0（composition_id=`news_main`，来源 `E:/AI/digital_human/hf_prep/test_demo/`，时长 5–30s）
  - 后端：`app/routers/visual_render.py`（8 端点 templates/jobs/generate/download/frames/delete）+ 4 个服务模块
    - `app/services/hf_client.py` — `npx hyperframes render . -o visual_segment.mp4` subprocess 封装，300s 硬超时
    - `app/services/template_library.py` — `TEMPLATES` 字典 + jsonschema（title/subtitle/metrics[1-4]/chart/caption/source/duration_sec）
    - `app/services/template_filler.py` — 模板复制 + `input.json` + 26-key 白名单占位符替换
    - `app/services/visual_render_service.py` — preparing/rendering/validating 6 步编排 + ffprobe 校验 + 抽帧 + render_manifest.json
  - 数据模型：`app/models.py` 新增 `VisualRenderJob` 表（19 字段，状态机 `queued→preparing→rendering→validating→completed/failed/cancelled`）；`app/schemas.py` 加 `VisualRenderJobCreate/Out/TemplateInfo/GenerateVisualResponse`
  - 配置：`config/app.yaml` Defaults 加 4 个 key（`hyperframes_bin=npx` / `hf_render_timeout_sec=300` / `hf_visual_root=E:/数字人计划/hf_visual` / `hf_template_root=E:/AI/digital_human/hf_prep`）
  - 前端：`web/visual_render.html`（4 步 SPA：选模板 → 动态表单填入参 → 提交 + 2s 轮询 → 历史 + 视频预览 + 3 帧缩略图 + 下载）；`web/index.html` 顶部导航新增 "🎨 视觉渲染"
  - 持久化：产物落 `E:/数字人计划/hf_visual/<job_id>/{input.json, index.html, avatar.b64, render.log, render_manifest.json, rendered/visual_segment.mp4, rendered/frames/{first,middle,final}.png}`

## 零-10、上次动作（2026-07-26 LTX23）

- ✅ **LTX23 音频→视频 接入 54321 Web 数字人视频环节完成**
  - 工作流 SSOT：`workflows/digital_human_video_ltx23.json`（运行时副本自动同步）
  - 后端：`app/routers/digital_human_video.py`（8 端点）+ `app/services/audio_aggregator.py`（ffmpeg concat，5s 阈值）+ `app/services/lit_video_builder.py`（LTX23 workflow dict 构造）+ `app/services/video_validator.py`（ffprobe 7 项硬校验）
  - 数据模型：`app/models.py` 新增 `DigitalHumanVideo` 表；`app/schemas.py` 加系列 Pydantic
  - 前端：`web/digital_human_video.html`（4 步 SPA：选音频 → 上传分镜 → ComfyUI 提交 → 历史）；`web/index.html` 顶部导航加 "🎬 数字人视频"
  - 持久化：产物落 `E:/数字人计划/dhv/<video_id>/<video_id>.mp4`；校验包含 audio stream / fps / frame count (`8n+1`) / duration ≥ min
  - **不**覆盖原始工作流 `G:\下载\LTX23-...`；**不**把临时实验 payload 当 SSOT
  - 与 IndexTTS2 入口并列：复用 `article → audio → 数字人视频` 自动接续链路

## 零-11、上次动作（2026-07-25 当晚）

- ✅ **ComfyUI 多视图定型 → 54321 Web 角色管理接入完成**
  - 工作流 SSOT：`workflows/character_three_view.json` + `workflows/manifest.yaml`
  - 后端：`app/routers/comfyui.py`（HTTP 客户端 + 6 端点） + `app/routers/roles.py`（角色 CRUD + 生成视图 + apply）
  - 启动同步：`app/services/workflow_sync.py`（lifespan 钩子 + WorkflowSync 表 + E:\数字人计划\logs 日志）
  - 前端：`web/roles.html`（卡片网格 + 创建/编辑/生成/应用 modal）+ `web/index.html` 导航加 "🎭 角色管理"
  - **不**走 A 管线主流水线分镜（`apply` 端点仅产 `mainstream_input` JSON，留接口位给 ID-007）

---

## 一、技术栈（实际在用 · 2026-07-25 起）

| 层 | 工具 | 备注 |
|----|------|------|
| 图像 / 视频 / 后期 / 合成 | **ComfyUI**（唯一底座） | MuseTalk / SadTalker / LivePortrait / LatentSync 已废，不进入工作流 |
| TTS（中文主力） | **Fish Speech** 本地推理（`E:\AI\tts\fish-speech\`） | 详见 `TTS栈部署记录.md` |
| TTS（轻量备选） | **F5-TTS** 本地推理（`E:\AI\tts\F5-TTS\`） | 同上 |
| TTS（英文/高质感） | **ElevenLabs** API | 待用户注册 key（M0 期间未注册） |
| 角色一致性 / 多视图 | **多维度定型图生成器**（Krea2 + Klein 一致性 LoRA + prompt 工程） | 角色资产层，**M0+ 全阶段复用** |
| 唇同步 / 视频驱动 | 待定（ComfyUI 视频节点 / Wan / LTX / 其他方案） | 未跑通，backlog |
| LLM | **DeepSeek V4 Pro / Flash**（云端） | 见 [`docs/deepseek_v4_model_guide.md`](docs/deepseek_v4_model_guide.md) |
| 后期合成（演示） | 剪映（38m 实验田用过，不进生产工作流） | 仅历史实验工具 |

**已废弃（不写入 v10 之外的任何新工作流）**：
- ❌ MuseTalk（`E:\tools\MuseTalk-main\` + 隔离 venv + wrapper，整套清理）
- ❌ Edge TTS（已废，调研文档明文）
- ❌ SadTalker / LivePortrait / LatentSync（v10 明文不主推）

---

## 二、当前阶段：**M0+ 精进期（提示词迭代 + 产量积累）**

- ✅ M0 跑通（用户 2026-07-25 拍板：原"1 分钟 3 条 30 秒"门禁过期，跑通就过）
- 🟡 **当前重心**（2026-08-15）：
  - **七层洗稿喂饱**：素材聚合层上线（MaterialPackage 七层审计 + 智谱补搜，无包 827 字 → 带包 1843 字），待投稿数据验证
  - **爆品改造七层适配**：P1-P3 砍除，boost 只剩 P4(逐句精修)→P5(情绪标注)；情绪链路（P5→IndexTTS→导演视觉意图）已全线打通
  - **流水线前端三页拆分**：index(新闻线索+素材包+评论层) / writing(文字加工) / audio(音频加工)，persona 音色锁
  - ⚠️ **待确认**：geo/tech 人设赛道绑定；前 30 秒留存测试是否开始；`.env` 填 ZHIPU_API_KEY（智谱免费额度 2026-09-12 到期，有硬拦截）

| 方向 | 当前状态 | 关键文档 |
|------|----------|----------|
| 提示词体系（洗稿） | **七层定稿**：laotan-tech_7layer_v2.txt（总量约束+素材使用心法），v1/anchored 已归档 | `config/laotan-tech_7layer_v2.txt` |
| 素材聚合层 | **已上线**（2026-08-15）：素材包 8 端点 + 七层审计 + 智谱补搜 | `docs/backlog.md` ID-047 |
| 爆品改造 (boost) | **P4→P5 七层适配完成**（P1-P3 砍除，A/B 实证） | `docs/backlog.md` ID-046 |
| 情绪链路 (P5) | **全线打通**（8/14）：P5 标注→IndexTTS 情绪参数→导演视觉意图 | `docs/backlog.md` ID-045 |
| 视觉导演提示词 | v2（8/12）+ 维度分级重构（门槛维/加分维拆分，8/14） | `config/visual_director_v2.txt` |
| 端到端视频管线 | ComfyUI 主链已跑通；v3 零拷贝 + 合成归一化 + 时间轴满铺（8/14 修 8.7s 空洞） | [docs/backlog.md](docs/backlog.md) ID-005/031/049 |
| 视频库（素材+成品） | **已完成**：素材库 CRUD+搜索+标签+赞 / 成品库 CRUD / Pexels 自动入库 / Compose 自动登记 + 成片复用上限（8/15） | `app/routers/library.py` / `web/library.html` |
| 文案 + 视觉导演 | **ID-002 全部完成** + 僵尸修复 + 合成归一化（2026-08-08） | `docs/backlog.md` ID-002 |
| ComfyUI 海报 | **调研完成，搭建中断**（TextOverlay + FLUX 方案） | ⚠️ 待继续 |
| 爬虫 / 文章库 | 爬虫独立脚本，未与后端打通 | `docs/backlog.md` ID-001/006 |
| 平台账号矩阵 | M1 任务，未启动 | `执行计划清单.md` v10 §三 |

---

## 二·五、当前开发权重（**用户口径，每轮同步更新**）

> 用户原话："开发权重每次我会列出来看以便，以我为准，这个只是个备忘录而已。"
> 本节 = 用户口头同步的开发优先级，**不是 SSOT**，不替代 `docs/backlog.md`（backlog 是用户个人备忘录，本节是双方对齐点）。
> 我（Hermes）**只读不写**，由你每轮口头同步后我落字。

### 2026-07-25 当前权重（用户同步）

| 权重序 | 任务 | 备注 |
|--------|------|------|
| **1**（在做） | **音色选择 UI** | 音频生成的前置；当前 voice 硬编码，需前端能选 voice_id |
| **2**（紧随其后） | **音频跑通** | 等 #1 完才能稳定输出 |
| **3** | 视觉导演 Agent 接口（`backlog ID-002`） | #2 完才能接；当前最大链路断点 |
| **4** | 本地素材库索引（`backlog ID-003`） | #3 的前置 |
| 后续 | 爬虫 / 数据图表 / 合成 / 发布 | 暂不展开，待你逐轮同步 |

---

## 三、整合决策（2026-07-25 用户拍板）

| # | 决策 | 影响 |
|---|------|------|
| 1 | **MuseTalk 完全废**，技术栈移除 | `E:\tools\MuseTalk-main\` + 隔离 venv + wrapper 整套清理（用户授权） |
| 2 | **M0 门禁过期** | 不再卡"1 分钟 3 条 30 秒"；只关心后续精进 |
| 3 | **38m 项目** = M0 实验田，已完成 | `C:\Users\tanha\Desktop\38m\` 不维护，回头可清 |
| 4 | **多维度定型图** = M0+ 全阶段**角色资产层** | 不是独立阶段 |
| 5 | **v10 暂不改** | 通过本"总览" + CHANGELOG 反映真实差异；v10 作为规划目标保留 |
| 6 | **赛道矛盾**（高 CPM 数字人禁区 vs 国内主战场） | ✅ **已决**：用户选定两条安全赛道——① 财经新闻"解读 + 评论"（带明确新闻源、不推荐具体金融产品 / 个股 / 投资建议）；② GitHub 开源项目介绍（天然合规，AI 风口）。详见 v10 §0.3 T13 执行口径。 |
| 7 | **两条赛道人设口径** | ✅ **已决**：完全避免演播室感 / 央视味 / 氛围上的权威。人/稿可专业，但视觉像"个人 Up 主在自己桌前讲"——自然、大众。**不**做冷灰演播室底、**不**做西服领正装、**不**做央视主播腔。如要做央视味须用户单独说"用央视味"。 |
| 8 | **两管线 + 两账号并行** | ✅ **已决**：两赛道用完全独立的两套管线（角色/视觉/音色/workflow）+ 两套账号矩阵隔离内容，避免被平台判定关联降权。 |
| 9 | **GitHub 冻结 + A 管线音色锁定**（2026-07-25） | ✅ **已决**：B 管线（GitHub 解说）冻结不建设，等 A 跑通后回头复用。A 管线音色 = **Fish Speech s2-pro 4B 默认中文音色**，服务 `http://127.0.0.1:7860`（pid 仅为当时快照，不作长期配置）。验证样本 `C:\Users\tanha\tts_smoke\fish_default_02_short.wav`（用户听音通过）。**不要** Edge TTS / 其他音色 / 1-shot 克隆，除非用户明确说换。 |
| 10 | **Workflow SSOT**（2026-07-25） | ✅ **已决**：项目 `workflows/` 为唯一真源；ComfyUI `user/default/workflows/` 为运行时副本；同步过程必须可见、可核验。 |
| 11 | **角色管理接入 ComfyUI**（2026-07-25） | ✅ **已决**：通过 54321 Web `web/roles.html` 创建角色 + 上传参考图 + 调 ComfyUI 出 3 视图。**不**绕开 54321 直接调 ComfyUI API。ComfyUI 内部参数(checkpoint / LoRA / prompt 模板)对用户不可见。`apply` 端点只产主流水线入参 JSON，真正接分镜留 ID-007 backlog。 |
| 12 | **LTX23 音频→视频 接入数字人视频环节**（2026-07-26） | ✅ **已决**：通过 54321 Web `web/digital_human_video.html` 选音频(≥5s) + 上传分镜图(≥1) + 调 ComfyUI 出 mp4。**不**覆盖原始工作流 `G:\下载\LTX23-...`。**不**仅凭 HTTP 200 判定成功，必须 ffprobe 校验 audio stream / fps / frame count / duration。**不**为修单次失败阻塞主流程（SageAttention DLL 等启动警告 → WARN + 继续）。`target_duration_sec` ≥ 5s；帧数对齐 `8n+1`。 |
| 13 | **HF (HyperFrames) 视觉渲染模块接入 54321 Web**（2026-07-26） | ✅ **已决**：通过 `web/visual_render.html` 选模板（只 1 个 `news-data-v1`）+ 标准 JSON 入参 + 同步 `npx hyperframes render` 出 mp4 + 抽 3 张关键帧。**不**接 ComfyUI / **不**占 4090 / **不**复用 DigitalHumanVideo 状态机 — 必须独立表独立 status。**不**批量建 30~60 个模板，只 1 个 + 接口位预留（comparison-chart-v1 / title-card-v1 / quote-card-v1 / timeline-v1）。**不**让模板内部调 DeepSeek/Fish/ComfyUI。模板是项目侧固定文件 + 占位符替换，**不**每次从零生成 HTML。子进程硬超时 300s；占位符走 26-key 白名单防注入；成功必须 ffprobe 校验。**不**自动 git commit。 |
| 14 | **Web UI 导演选择/URL 抓取/模型开关**（2026-07-27） | ✅ **已决**：`web/index.html` + `web/app.js` 完成 ID-007（批量选择 + 拖拽排序 + segment_type/estimated_duration 展示）、ID-001（URL 自动抓标题正文）、ID-008（flash/pro 模型选择）。保存顺序走 `POST /api/scripts/{id}/segments/reorder` 批量排序 + `PUT /api/segments/{id}` 更新选择状态。 |
| 15 | **前端 UI 全面升级 + 提示词模板管理**（2026-07-28） | ✅ **已决**：统一深色主题设计系统 + 8 页导航栏互链。新建 `web/director.html`（导演控制台）+ `web/templates.html`（模板管理：上传/改名/删除）。重写 `web/index.html` 为 3 步流程，AI 洗稿新增数字人/提示词模板下拉选择器。后端新增 4 个模板 API。ID-002 阶段 6 完成。 |
| 16 | **导演台执行引擎 Phase 调度**（2026-07-29） | ✅ **已决**：执行模型从“逐段顺序”改为“按 workflow 类型分 Phase 批量”。ComfyUI 单次 session 跑完所有 host/mixed slot，不再逐段拉起。Fallback 集中兜底。 |
| 17 | **人物关联三维绑定**（2026-07-29） | ✅ **已决**：提示词模板 + 音色 + 形象 = 一套人物（Persona）。流水线选模板后音色/形象自动锁定不可更改。`prompt_template` 唯一约束。 |
| 18 | **全站导航 UI 统一**（2026-07-29） | ✅ **已决**：以 director.html 为基准，8 页统一 sticky 毛玻璃导航 + 蓝紫渐变 logo + 7 链接 + active 高亮 + API Docs。 |
| 19 | **视频库：素材库 + 成品库**（2026-07-31） | ✅ **已决**：`web/library.html` 双 Tab 管理页。`VideoAsset` 素材库（Pexels 下载自动入库，按 pexels_id 去重）+ `VideoOutput` 成品库（Compose 合成自动登记）。素材库支持 CRUD + 搜索 + 标签 + 赞 + 大类筛选。成品库支持 list + delete。全站 9 页导航统一加「视频库」链接。 |
| 20 | **导演台 Slot 重试去重 + black_subtitle 修复**（2026-07-31） | ✅ **已决**：前端按 slot_index 去重显示（不修改后端 fallback 链逻辑），后端新增 purge 端点供手动清理。`replaced` 为终态禁止重试。执行日志提升为 body 级固定抽屉。black_subtitle fontfile 路径修复（单反斜杠）。 |
| 21 | **合成异步化 + SSE 进度透传 + 合成去重质量优先级**（2026-07-31） | ✅ **已决**：compose 端点改为异步线程 + SSE 实时推送进度，与执行 Slots 同一套进度反馈模式。合成去重按 workflow 质量 tier 优先（host=0 > broll/hf=1 > black_subtitle=9），host 内容永远不被 black_subtitle 覆盖。SSE 断连时兜底恢复所有按钮防止永远转圈。 |
| 22 | **全项目审计修复收尾**（2026-08-01） | ✅ **已决**：P0-1 方案 1（失败不删盘 + 可续传，重试跳过已存在段）；P0-2/P2-2 脏数据清理经用户确认（删 66 行 + 磁盘 5 处回收站）；P2 系列收尾（HEAD 路由/SSE 重连/JSON.parse/fetch r.ok）；删除磁盘文件一律走 Windows 回收站（library.py `_recycle_file` 与 scripts.py 同源）。 |
---

## 四、已跑通资产（时间倒序）

| 日期 | 里程碑 | 关键产物 / 文档 |
|------|--------|------------------|
| 2026-08-11 | 提示词体系三套人设建立 | `config/laotan-geo.txt`（地缘政治）+ `config/laotan-tech.txt`（科技科普）新建 |
| 2026-08-10 | ComfyUI 海报方案调研（TextOverlay + FLUX） | 方案 A/B/C 对比报告 `C:\Users\tanhaox\comfyui_chinese_text_research.md`；搭建中断 |
| 2026-08-09 | 老谭洗稿提示词更新 + 视觉导演 v2 大版本 | `config/laotan.txt` 更新 / `config/visual_director_v2.txt` 33KB |
| 2026-08-08 | Slot 僵尸修复 + 合成归一化 + Persona 品牌吸收 + 洗稿.txt 产出 | `retry.py` 僵尸检测 / `_normalize_clip_duration` drift +18.3s→0 / 成片去「老陈」硬编码 / 详见 CHANGELOG |
| 2026-08-08 | HF 模板三件套（圆饼图缺角修复 + 横屏模板 + 品牌泛化） | `buildDonut` offset 守恒重写（`avail = C - n*SEG_GAP` + `offset = -used` + 纯 opacity 淡入）/ PIL 像素验证 4 帧无缺角 / 横屏模板 `news-magazine-v1-ls` 1920×1080 + `_pick_hf_template` 按画幅自动选模板 / `_SAFE_KEYS` 27 keys + `_merge_brand` 品牌注入 / 详见 [CHANGELOG.md](CHANGELOG.md) 2026-08-08 + [docs/backlog.md](docs/backlog.md) ID-035/036/037 |
| 2026-08-07 | 素材生命周期策略升级（保留 slot 素材防返工） | 删 Step 7 `rmtree(slots/)` / 保留扫描（completed 超 `slot_retention_days` 清 slots + hf_visual + 孤儿中间件，留成片+manifest）/ `_cleanup_intermediates` 追加 `silence_fallback.wav` / `purge_replaced_slots` 连带删目录 / `config/app.yaml` `slot_retention_days: 7` / 隔离验证 19/19 通过（`scripts/verify_retention.py`） |
| 2026-08-03 | 导演生产路径优化 v3（8 阶段零拷贝流水线） | 双 workflow 模板（竖版 1080×1920 + 横版 1920×1080 @30fps）/ poll 零搬运 / 48kHz 统一 / 删 _prepare_inputs() / slot_root 统一 / os.replace ×5 / slots 自清理 + stale job 磁盘删除 / config deprecation |
| 2026-08-02 | 项目盘符迁移 C: → F: + 全量硬编码路径清理 + 启动验证 | `config/app.yaml` `data_dir="data"` / 全部 `.bat` 改为 `cd /d "%~dp0"` / `_apply_vocab_fix.sh` 改为 `cd "$(dirname "$0")"` / Python 辅助脚本改为 `Path(__file__).resolve()` 动态定位 / CHANGELOG 2026-08-02 条目 / Uvicorn+静态页+`openapi.json`+`/api/articles/prompt-templates` 验证通过 |
| 2026-08-01 | 全项目审计修复收尾（P0×2 + P1×3 + P2×7）+ docs 全面更新 | `tts_client.py` 失败不删盘 + 续传跳过已存在段 / `audio.py` 保留 completed_segments / `app.js` `setSaveDirectorEnabled` + `renderProjectDir` 守卫 + JSON.parse try/catch / `library.js` tags 判空 / `library.py` HEAD 路由 + `_recycle_file` 回收站 + fetch r.ok / `director.js` SSE 重连上限 / `director.css` / `director.html` / ID-002 文档收尾重命名 / `docs/backlog.md`+`CHANGELOG.md`+`项目状态总览.md` 同步 |
| 2026-07-31 | 合成异步化 + SSE 进度透传 + 日志面板 DOM 修复 + 合成去重质量优先级 + 按钮转圈恢复 | `composition_service.py` 异步 evt 回调 + `director.py` 后台线程 compose + `director.js` SSE compose 事件 + DOM 修复 + dedup 质量 tier + `_resetSpinnerBtns` 兜底 |
| 2026-07-31 | 导演台 Slot 重试去重 + 执行日志抽屉 + black_subtitle 字体路径修复 | `director.js` 按 slot_index 去重 + purge 按钮 / `director.css` fixed bottom drawer / `director.py` purge-replaced 端点 + retry 拒绝 replaced / `slot_workflows.py` fontfile `C\:` 单反斜杠修复 |
| 2026-07-31 | 视频库：素材库 + 成品库全链路落地 | `app/routers/library.py`（素材 CRUD+搜索+标签+赞 / 成品 CRUD）+ `VideoAsset`/`VideoOutput` 模型 + `web/library.html`+`library.js`+`library.css`（双 Tab 前端）+ `pexels_service._register_video_asset()`（Pexels 自动入库）+ `director.py` compose 端点自动登记 VideoOutput |
| 2026-07-29 | 导演台执行引擎重构 + 人物关联 + 全站导航统一 | `slot_executor.py` Phase 调度重写 + `Persona` 表 + `app/routers/personas.py` + `web/personas.html` + 8 页 nav 统一 + ComfyUI GPU 托管 + 相邻约束 + 参考来源段处理 |
| 2026-07-29 | 音频质量三连修 + TTS GPU 托管 + 流水线→导演台衔接 | `gpu_service_manager.py` + `script_parser.py` 短句合并 + `tts_client.py` 残音修复 + .venv 切换 + 衔接横幅 |
| 2026-07-28 | 前端 UI 全面升级 + 提示词模板管理 | `web/director.html`（导演控制台）+ `web/templates.html`（模板管理）+ `web/index.html` 重写 + `app/routers/articles.py` 新增 4 个模板 API + 统一深色主题设计系统 |
| 2026-07-27 | ID-002 视觉导演 Agent 2.0 后端 E2E 跑通 | `app/services/{alignment_service,director_service,slot_executor,composition_service}.py` + `app/routers/director.py`（8 端点）+ `DirectorJob`/`DirectorSlot` 表 + E2E: 32 slots → 58 completed, 696s mp4 |
| 2026-07-26 | HF 视觉渲染模块接入 54321 Web | `app/routers/visual_render.py` (8 端点) + `app/services/{hf_client,template_library,template_filler,visual_render_service}.py` (4 层职责) + `web/visual_render.html` (4 步 SPA + 2s 轮询) + `VisualRenderJob` 表（19 字段 + 独立状态机）+ `E:/数字人计划/hf_visual/<job_id>/{input.json, render_manifest.json, visual_segment.mp4, frames/{first,middle,final}.png}` 产物目录布局 |
| 2026-07-26 | LTX23 音频→视频接入数字人视频环节 | `workflows/digital_human_video_ltx23.json` + `app/routers/digital_human_video.py` (8 端点) + `app/services/{audio_aggregator,lit_video_builder,video_validator}.py` + `web/digital_human_video.html` (4 步 SPA) + `DigitalHumanVideo` 表 + E2E 已通（聚合 8.7s 音频 → ComfyUI /prompt → 失败捕获 → DB 落盘 `failed`） |
| 2026-07-25 | Pose 库分类沉淀（1700+ 张） | [`pose问题规律.md`](pose问题规律.md) |
| 2026-07-23 | 多维度定型图女性古装 V4（纯 prompt 方案定型） | `E:\AI\ComfyUI_windows_portable\ComfyUI\output\krea2_gufeng_face_v4_00001_.png` + `数字人多维度定型图生成器跑通记录.md` |
| 2026-07-23 | 多维度定型图男性宗师（V1/V2/V3 三套） | `C:\Users\tanha\Desktop\38m\master_v{1,2,3}_face_v2_00001_.png`（38m 实验产物，但方法回流） |
| 2026-07-23 | 多维度定型图赛博朋克 2.5D 美少女 | `krea2_cyber_face_00001_.png` + `krea2_cyber_body_00001_.png` |
| 2026-07-22 | v10 执行计划发布（**规划目标**，与现实有 6 处差异） | `执行计划清单.md` v10 |
| 2026-07-22 | ComfyUI 出图全局约定（SSOT） | [`sop/ComfyUI出图规范.md`](sop/ComfyUI出图规范.md) |
| 2026-07-22 | Fish Speech + F5-TTS 本地部署 + 中文冒烟 | [`TTS栈部署记录.md`](TTS栈部署记录.md) |
| 2026-07-22 | GPU 自检 A 级（4090 eGPU 49GB + 4060 8GB） | [`M0-W1-D1-GPU自检报告.md`](M0-W1-D1-GPU自检报告.md) |
| 2026-07-21 | M0 端到端 demo（**MuseTalk 方案，已废**，仅作历史） | `E:\数字人计划\m0-day1\demo_yongen_eng_v15.mp4` + `W1-MuseTalk跑通.md` |

---

## 五、与 v10 的差异处置（已决项）

| # | 矛盾点 | 状态 | 当前口径 |
|---|--------|------|----------|
| 1 | 主线技术栈 MuseTalk | ✅ **已决**：废 | — |
| 2 | Edge TTS | ✅ **已决**：废 | — |
| 3 | M0 门禁 | ✅ **已决**：过期 | — |
| 4 | **Workflow 目录约定** | ✅ **已决** | 项目 `workflows/` 为唯一真源；ComfyUI `user/default/workflows/` 为运行时副本，后续由 Claude Code 实现可观测同步 |
| 5 | 多维度定型图定位 | ✅ **已决**：角色资产层 | — |
| 6 | **赛道矛盾**（高 CPM 数字人禁区 vs 国内主战场） | ✅ **已决** | 2026-07-25：用户选定**两条安全赛道**——① 财经新闻"解读 + 评论"（带明确新闻源、不推荐具体金融产品 / 个股 / 投资建议）；② GitHub 开源项目介绍（天然合规，AI 风口）。详见 v10 §0.3 T13 执行口径。 |

---

## 六、清理待办（待内容决策）

- [ ] 是否删除 `E:\tools\MuseTalk-main\` + `E:\tools\MuseTalk-venv\` + `E:\tools\musetalk.sh`
- [ ] 是否清理 `C:\Users\tanha\Desktop\38m\`（含实验产物和 workflows，删除前先确认是否已有回流/归档）
- [ ] 是否将 v10 + 旧调研移入 `archives/v10-snapshot/`，降级为历史快照
- [ ] 是否归档 M0 期间产物 `E:\数字人计划\m0-day1\`

> 这里等待的是“保留/归档/删除”的内容决策，不是权限确认；执行前按已选范围核对路径并保留备份。

---

## 七、迁移记录 / 排坑优先级（2026-08-02 C: → F:）

- **项目已整体从 `F:\AI-Agent-Local\digital_human` 迁移到 `F:\AI-Agent-Local\digital_human`**
- **此前已全链路跑通**；若后续突然出现「找不到文件 / 数据库 / 目录 / 启动脚本」类错误，**优先排查硬编码绝对路径残留**
- 修复范围：
  - `config/app.yaml` 中 `data_dir` 已改为 `"data"`（相对项目根），由 `app/config.py` 自动解析
  - 所有 `.bat` 启动脚本开头统一改为 `cd /d "%~dp0"`（脚本自定位）
  - `_apply_vocab_fix.sh` 改为 `cd "$(dirname "$0")"`
  - Python 辅助脚本统一使用 `Path(__file__).resolve().parent/parents[1]` 动态定位项目根，不再写死 `F:\AI-Agent-Local\digital_human`
- **再次迁移时只需复制整个文件夹**，无需再改 `.bat`/`.sh`/Python 辅助脚本
- **外部资源目录（`E:/数字人计划/*`、`E:/AI/ComfyUI_windows_portable/*`）不属于本次盘符变更范围**；若这些资源也迁移，需同步更新 `config/app.yaml` 中对应配置项
- **孤儿文件清理（2026-08-02）**：清除历史开发残留的无引用文件 33 个
  - 根目录临时脚本/配置/测试页 28 个、`data/pipeline.db.bak-*` 数据库备份 4 个、`tests/__init__.py` 1 个
  - 全部经 Windows 回收站安全移除，使用命令 `Microsoft.VisualBasic.FileIO.FileSystem::DeleteFile(path, 'OnlyErrorDialogs', 'SendToRecycleBin')`，可回收站恢复

---

## 八、Workflow 目录约定（已决 · 2026-07-25）

**唯一真源**：`F:\AI-Agent-Local\digital_human\workflows\`

- 项目目录保存经过筛选、可版本化、可复用的 workflow JSON。
- `E:\AI\ComfyUI_windows_portable\ComfyUI\user\default\workflows\` 是 **ComfyUI 运行时副本**，不作为长期真源。
- `E:\AI\comfyui_workflows\` 降级为历史/交换目录；新工作流不再只落这里。
- 从 ComfyUI UI 新建或修改的工作流，验证通过后必须回流项目 `workflows/`；项目版本部署到 ComfyUI 时由同步脚本执行。
- 同名冲突时先保留双方备份，再以项目目录中经用户确认的版本为准。

**后续开发任务（Claude Code）**：实现双向检查、默认单向发布（项目 → ComfyUI）的同步脚本和 UI 可见日志。脚本未完成前允许人工复制，但每次必须核对文件名、mtime 和哈希。

---

## 八、参考索引

- 规划目标: [`执行计划清单.md`](执行计划清单.md) v10 / [`执行计划清单.html`](执行计划清单.html)
- 项目入口: [`README.md`](README.md)
- 变更日志: [`CHANGELOG.md`](CHANGELOG.md)
- **迁移记录 / 排坑优先级**: 见本文件第七节
- 已沉淀跑通记录: `数字人多维度定型图生成器跑通记录.md` / `pose问题规律.md` / `TTS栈部署记录.md` / `M0-W1-D1-GPU自检报告.md`
- 调研与方案: `数字人计划调研更新版.md` / `自动化数字人新闻播报 — 本地部署方案.md` / `M0-GPU-checklist.md` / `计划收益测算.md`
- 需求池: [`docs/backlog.md`](docs/backlog.md)
- ComfyUI 规范: [`sop/ComfyUI出图规范.md`](sop/ComfyUI出图规范.md)
- 内容标准: `什么是优秀的短视频*.md`（母版 + 中/英/西/阿）

---

## 九、维护规则

- 本文件**反映真实**，不重复 v10 的"规划口径"
- v10 仍是规划目标的唯一来源；任何与本文件冲突时，**以本文件 + CHANGELOG 为准**判断"实际在做什么"
- 重大状态变更（技术栈 / 阶段 / 决策）必须同步更新本文件 + CHANGELOG
- 详细工作流 / Prompt 模板 / 模型清单等**不写在本文件**，保持精炼

---

## 十、核心设计原则：人类可观测性（2026-07-25 拍板）

**用户原话**："这个项目没开发完，还在完善，很多额外的开发其实都是因为，我是人类，我不知道后台在跑什么。"

**原则**：项目核心设计 = AI 跑 + **人类能看见后台在做什么**。

**执行硬规则**：
- **永不许绕过用户的 UI 直接调 API**（包括 Fish Speech / ComfyUI / DeepSeek / 任何外部服务）
- 所有交互从 UI 触发，让用户看见按钮、进度、结果、产物
- 如果某个功能 UI 里没有 = **未完成**，要补 UI，不能绕过
- 后台服务（54321 端口 web UI / Swagger / 数据库 / 输出目录 / 音色档案）都是为这条原则服务

**适用范围**：A 管线所有功能（角色生成 / TTS / 文案洗稿 / 视觉导演 / 视频合成 / 发布）/ 以及未来 B 管线解冻后同样适用。