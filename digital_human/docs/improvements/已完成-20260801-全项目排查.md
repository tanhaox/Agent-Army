# 进行中：digital_human 全项目排查（只排查不修复）

> 模式：审计模式 —— **只发现问题，不修复**，输出升级/修复报告，为后续工作指明方向。
> 创建：2026-08-01
> 范围：digital_human（FastAPI :54321）按 backlog ID-001~021 声明 vs 实际实现逐项核对 + 前端残余 bug + 后端健康 + DB 完整性 + 文档一致性。
>
> ⚠️ **2026-08-01 更新**：主线 P0-1（方案 1：失败不删盘 + 可续传）已实现并实测通过。用户在实现过程中要求："在修复过程中发现的新 bug 给予积极记录，在主线任务完成后，发现的 bug 需要继续修复。" → 主线已完成，P1-2 / P1-1 / P1-3 均已修复并浏览器实测通过，剩余 P0-2。
>
> ✅ **2026-08-01 晚间更新（全部收尾）**：P0-2 / P2-1~P2-7 全部处理完成。P0-2 + P2-2 + P2-5 清理已执行并经用户确认（阶段 4）：DB 删 66 行 + 磁盘 5 处回收站（4 个 zombie job 目录 + .test_videos/）。P2-1 HEAD 路由、P2-3 SSE onerror、P2-4 JSON.parse、P2-7 fetch r.ok 已修复。library.py 回收站改动已端到端实测通过。
>
> ✅ **2026-08-01 docs 全面更新同步完成**：本次全项目排查/修复已回写各文档 —— `backlog.md`（ID-002 状态 done、排期 #6 删除线、关联需求区补审计修复条目）、`CHANGELOG.md`（2026-08-01 审计修复条目）、`项目状态总览.md`（最新动作区 + ID-002 已完成 + 决策表追加）、ID-002 计划文档重命名 `已完成-20260727-ID002-视觉导演Agent2.md` 并标记全阶段完成。

## 任务目标
- 逐条核对 backlog.md ID-001~021 的"状态/修复记录"声明 vs 代码/DB 实际
- 审计 director.js / app.js / library.js 残余 bug（本次发现 `loadScripts()` 中 `sel` block-scoped 越界 ReferenceError，已修复——排查是否还有同类）
- 后端 router/service 健康 + 运行时错误日志
- DB 完整性：僵尸任务 / 孤儿行 / 测试残留数据
- 文档（backlog/README/CHANGELOG）与实际实现一致性
- 产出：《升级修复报告》，含问题清单 + 修复方向 + 优先级

## 已确认的事实
- 后端 PID 14404 运行中，54321 LISTENING，NoCacheWebMiddleware 生效（前端改无需重启）
- TTS 任务 9c32198e 正常推进（31/112，fish 合成中，~4min/段）
- 删除脚本 36cf6bd5 级联验证通过：scripts/segments/audio_jobs/director_jobs 全清，article 保留，同稿 7→6 版
- 发现并修复 `loadScripts()` 中 `sel` 未定义 bug（ReferenceError: sel is not defined）——此前 deleteScript 删除成功后刷新列表报错，DELETE 本身成功
- 已知 SSE 模型：内存队列广播（直播流非重放），新连接收不到历史事件
- 测试残留：`.test_videos/` 空目录仍在；video_assets 2 条测试记录已清
- 回收站实现：scripts.py 内置 PowerShell `Microsoft.VisualBasic.FileSystem::DeleteFile`（ID-021）

## 排查清单（核对 backlog ID）— 已全部核对
- [x] ID-001 URL 抓取 /api/articles/fetch-url
- [x] ID-002 视觉导演 Agent2（director_jobs/slots）
- [x] ID-003 Pexels resolve 服务
- [x] ID-008 前端模型选择开关
- [x] ID-009~014 导演台系列（SSE/心跳/停止/抽屉）
- [x] ID-015 ComfyUI 输入图质检
- [x] ID-016 TTS 句内截断
- [x] ID-017 library 视频播放
- [x] ID-018 SSE 心跳
- [x] ID-019 index 音频任务恢复
- [x] ID-020 僵尸任务清理 + 一键生成音频
- [x] ID-021 脚本删除（级联+回收站）

## 进度总览
- [x] 建 checkpoint
- [x] backlog 逐条核对
- [x] 前端残余 bug 审计
- [x] 后端健康排查
- [x] DB 完整性
- [x] 文档一致性
- [x] 升级修复报告（2026-08-01 交付）
- [x] 写入 checkpoint 报告 + P0-1 方案 1 实现计划（用户已确认）
- [x] **阶段 5 实现 P0-1 方案 1**（失败不删盘 + 可续传）✅ **2026-08-01 实测通过**
  - 后端：`tts_client.py` 失败不删盘 + 逐批跳过已存在段；`audio.py` 失败保留 completed_segments + 错误信息含续传提示；`tts_service.py`/`audio.py` 修复重复 AudioFile 行
  - 前端：`app.js` `restoreAudioJobs()` 对 failed job 显示「上次生成失败于 X/N 段 — 重新生成将从断点继续」
  - 单测：续传逻辑（scripts 临时脚本）+ `tests/test_tts_service_generate.py` 3 项全过
  - 实测：重启后端（PID 24976）→ 对 85 段全在盘脚本 POST 重新生成 → `completed 85/85`，fish 后端全程未拉起（85 段全走跳过路径），新 job audio_files=86 行（85 段+1 combined）无重复，旧 job 仍 171 行（修复生效对照）
- [x] **实测确认 P1-2 已在真实路径复现**：加载脚本时 `renderProjectDir` 在 `app.js:192` 崩溃（`.step:nth-of-type(2)` null）→ 任何脚本加载都被挡住。按用户指示转入修复
- [x] **修复 P1-2**（`app.js` `renderProjectDir`：锚点改用 `script-text` 父元素 + 缺失守卫）：浏览器实测 `fetchScript` 完整执行、project-dir 正确渲染、errors=[]、续传提示保留
- [x] **修复 P1-1**（`app.js` 新增 `setSaveDirectorEnabled` helper 替换 5 处 `btn-save-director` 直连）：浏览器实测 selectAll(true/false)+toggleSegment 3 次调用全静默成功 0 错误、112 段渲染正常
- [x] **修复 P1-3**（`library.js:63` `(a.tags || []).join(', ')`）：浏览器实测 tags:null 坏数据不再崩溃、网格正常渲染 2 卡
- [x] **P0-2 video_outputs 脏路径清洗**：5882f87b + 4 zombie visual job 行删除 + 4 残留目录回收站（见下方收尾记录）

---

# 2026-08-01 收尾记录（P0-2 / P2-2 / P2-5 清理执行）

> 用户已确认清理方案（阶段 4），执行结果：

## P0-2 video_outputs 脏路径 — ✅ 完成
- **根因**：`5882f87b` 行 `file_path` 存 `E:\机器人计划\...`（应为 `E:\数字人计划\`），实际根目录不存在 → 播放 404
- **处置（用户确认「清掉脏数据行」）**：
  - 删 `video_outputs` 5882f87b 行（mp4 已彻底丢失，正确前缀 `E:\数字人计划\composition\` 目录为空，无恢复可能）
  - 删 4 个 zombie visual job 行：6cf7c326 / 062be772（rendering 卡住）+ 9babfdbe / 1a58f97c（failed 空）
  - 4 个残留 job 父目录整体回收站：`E:\数字人计划\hf_visual\{id}\`（各 1.60/1.60/0.01/0.01 MB，含 avatar.b64/index.html/input.json/render.log）
  - 回收站核验：4 目录均在回收站，可恢复

## P2-2 失败任务堆积 — ✅ 完成（用户确认「全量清理」）
- **audio_jobs** 11 failed 删 9 条（0 段）→ 保留 9c32198e（43 段，P0-1 续传场景）+ a6f3301a（13 段），当前 failed=2
- **digital_human_videos** 6 failed 全删 → 当前 failed=0
- **visual_render_jobs** 48 failed 删 46 条 + zombie 4 条 → 当前 failed=0
- **删除安全性**：先核查 audio_jobs 待删行 audio_files 关联全 0、dh_videos 待删行 audio_jobs 关联全 0、FK 均 NO ACTION 无子表引用 → 无孤儿风险

## P2-5 .test_videos/ — ✅ 完成
- 目录含 15:34 生成的 test_b.mp4（246KB），整体回收站

## library.py delete_asset/delete_output 回收站 — ✅ 端到端实测通过
- 改动：`_recycle_file` helper（PowerShell `Microsoft.VisualBasic.FileSystem::DeleteFile`，与 scripts.py 同源）
- 实测：建临时 asset（id=331b8f74，含 2.6KB 测试文件）→ `DELETE /api/library/assets/{id}?remove_file=true` → HTTP 200，DB 行 0 残留、磁盘文件消失、`tmp_recycle_test.bin` 确认在回收站
- 注：video_assets 表生产为空（0 行），实际素材走 material_assets（206 行）

## 临时脚本
- 全部 `_tmp_*.py` + manifest json 走回收站清理，`scripts/` 已无 `_tmp_` 残留

---

# 升级/修复报告（2026-08-01）

> 排查范围：digital_human（FastAPI :54321）按 backlog ID-001~021 声明 vs 实际实现逐项核对 + 前端残余 bug + 后端健康 + DB 完整性 + 文档一致性。
> 排查结论：**P0×2、P1×3、P2×7、健康项×5**。除下述问题外，健康项全部正常。

## 🔴 P0（阻断级，必须优先修复）

### P0-1 失败 TTS 任务 DB/磁盘脱节（根因已锁定，方案已确认）
- **位置**：`scripts/tts_client.py:878-882`（`synthesize_lines` 的 except 分支）
- **根因机制**：`synthesize_lines` 通过 `_manifest_callback` 在生成过程中每段提交 AudioFile DB 行（file_path 指向 `output_dir/{idx:03d}.wav`）；失败时 except 分支把**已生成的 wav 与 manifest.json unlink**，但 DB 行保留 → DB 声称"有 43 段"，磁盘上全空 → 前端恢复/播放全部 404。
- **实测铁证（job 9c32198e，2026-07-31）**：
  - 43 条 audio_files 全部 `07:35:34~07:49:45` 一次写入（每分钟 5-7 条）
  - 07:49 第 44 段 fish 连接重置（10054）→ job failed
  - 备份库仍停在上一个 checkpoint（completed=17），主库 43 段已落 → 时间线唯一闭环：**生成→提交→失败→unlink wav**，与 DB 行数 43 完全吻合
- **已确认方案 1**（用户拍板，最贴合"重试"心智）：**失败不删盘 + 可续传** —— 生成失败时保留已生成的 wav 与 manifest；重试时跳过 `output_dir` 下已存在的段文件（`{idx:03d}.wav`），只重新合成缺失段。改动面较大，按计划推进。

### P0-2 video_outputs 脏路径（文件不存在 → 播放 404）
- **位置**：`digital_human_videos` 表 `5882f87b` 行 `file_path` 存 `E:\机器人计划\composition\...`（应为 `E:\数字人计划\`），文件不存在 → GET 404 / HEAD 405（已实测）
- **影响**：历史脏数据；视频库【成品】Tab 播放该行报错
- **修复方向**：数据清洗（改指正确目录或置空），或迁移脚本统一修正路径前缀

## 🟠 P1（高优先级，用户体验直接受损）

| ID | 位置 | 问题 | 影响 |
|----|------|------|------|
| P1-1 | `web/app.js` L324/338/303/350/365 | `document.getElementById('btn-save-director')` 空引用 5 处（index.html 无此元素） | 段落勾选/音色下拉/音频任务恢复不渲染 |
| P1-2 | `web/app.js` L191-192 | `renderProjectDir()` 中 `.step:nth-of-type(2)` 返回 null（index.html 只有 `.step-dot`） | 项目目录进度条渲染崩 |
| P1-3 | `web/library.js:63` | `const desc = a.description_zh \|\| a.description_en \|\| a.tags.join(', ') \|\| '—'` —— tags 未判空 | 单条坏数据让素材网格整体崩溃 |

## 🟡 P2（中/低优先级）

| ID | 位置 | 问题 |
|----|------|------|
| P2-1 | `app/routers/library.py` `_video_file_response` | 只有 GET 无 HEAD → HEAD 405 |
| P2-2 | DB 全库 | 失败任务堆积：`audio_jobs` 10 failed、`visual_render_jobs` 48 failed、`digital_human_videos` 6 failed |
| P2-3 | `web/director.js` L475/789 | SSE onerror 中 `readyState === CLOSED` 判断永不成立 → 无限重连 |
| P2-4 | `web/app.js` L96 | `JSON.parse` 无 try/catch |
| P2-5 | `.test_videos/` | 空目录残留 |
| P2-6 | `app/routers/audio.py` `background_tasks.add_task` | 进程被杀时 job 丢失竞态（15:51 新 job 从未落库实证） |
| P2-7 | `web/*.js` | fetch 普遍不检查 `r.ok` |

## ✅ 健康项（确认正常）
- 后端 :54321 LISTENING，14 个 router 全注册，NoCacheWebMiddleware 生效
- DB 孤儿行全 0；`material_assets` 206 行路径全部有效
- `_cleanup_zombie_audio_jobs` 启动时正常标记失效 job（error 信息完整）
- SSE 内存队列广播模型符合设计（新连接收不到历史事件属预期）
- ID-021 脚本删除（级联 + 回收站）已实现并实测通过

## 🧭 修复方向与优先级
1. **P0-1**（已确认方案 1）：`tts_client.py` 失败不删盘 + 重试跳过已存在段 + 前端续传提示 → 直接进入阶段 5
2. **P1-1/P1-2**：app.js 空引用清理（remove/guard 三态）+ renderProjectDir 兼容 `.step-dot` → 小改动
3. **P1-3**：`library.js:63` tags 判空 `(a.tags||[])` → 一行修复
4. **P0-2**：video_outputs 脏路径清洗脚本
5. **P2 收尾**：HEAD 路由、SSE 重连上限、JSON.parse try/catch、r.ok 检查、僵尸任务归档清理、.test_videos 回收站

## 后续工作建议（审计期结束后的长期方向）
- TTS 任务恢复建议改"磁盘实况为准"（DB 状态仅作提示），彻底消除 DB/磁盘脱节类问题
- 失败任务堆积 → 增加 retention 策略（自动归档/回收站清理）
- 前端统一 fetch 封装（自动检查 r.ok + 错误 toast），消灭 P2-7 类散弹问题

---

# P0-1 修复实现计划（方案 1：失败不删盘 + 可续传）— 阶段 5

> 用户已确认：方案 1 最贴合"重试"心智，改动面较大，按计划进行。

## 目标行为
- **失败不删盘**：`synthesize_lines` 失败时**不再 unlink** 已生成的 segment wav 与 manifest.json
- **可续传**：重试时跳过 `output_dir` 下已存在的 `{idx:03d}.wav`，只合成缺失段
- **DB 一致**：已提交的 AudioFile 行保留（与磁盘一致），失败 job 的 completed_segments 保留
- **前端提示**：重试时提示"将从第 N 段续传"

## 改动清单
| # | 文件 | 改动 |
|---|------|------|
| 1 | `scripts/tts_client.py` | except 分支移除/条件化 unlink；段循环前扫描已有 `{idx:03d}.wav` 跳过 |
| 2 | `app/routers/audio.py` `_do_tts` | 失败路径保留 completed_segments；错误信息含"可重试续传" |
| 3 | `web/app.js` restoreAudioJobs / 生成入口 | 已有 completed 段时提示续传 |

## 验证
- 单测：构造失败场景，确认 wav 保留、manifest 保留、重试跳过已存在段
- 实测：对 job 9c32198e 复现失败 → 重试 → 仅补缺失段 → completed
- 回归：正常任务不受影响；安全红线（无磁盘删除、专业日志、3 字段 TodoWrite）全遵守
