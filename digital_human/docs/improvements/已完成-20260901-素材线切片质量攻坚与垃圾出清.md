# 已完成-20260901-素材线切片质量攻坚与垃圾出清

> 背景: 用户对 RLR 批量入库素材逐条人工验收 (V20260901-5496 起连续报脏), 一天内定位并修复切片管线 8 项缺陷, 入库判据收敛为"只收摄像机实拍", 存量三轮出清。本篇为修复与决策记录; 批处理执行状态见文末。

## 一、切片管线 8 项修复

全部位于 `app/services/material_ingest_service.py` + `sandbox/yt_ingest.py`, 每项均由用户报脏的 asset_no 实锤定位:

| # | 缺陷 | 实锤样本 | 修复 |
|---|---|---|---|
| 1 | scene 阈值 0.20 对叠化/地图转场全漏 | 390s 仅 9 切点 (快剪频道应 80~150) | 0.05 + 相邻 <0.6s 切点并簇取簇首 → 88 切点/部 |
| 2 | `_split` 兜底"未覆盖才补"实为无条件追加 | clip_005≡clip_024 同窗双份 | 真·洞填补 (只补 final 未覆盖区间) |
| 3 | `//10` 均分让 16s 窗成一整块 | 112.5-128.5 十六秒单片 | ceil 均分, 片长 4~10s 封顶 |
| 4 | `-c copy` GOP 关键帧溢出 | 4876 尾帧串镜 | veryfast/crf21 重编码精确切 (2.1s/片实测) |
| 5 | 叠化尾巴 (切点在转场中段) | 同上 | 窗尾统一收缩 0.25s |
| 6 | `_ocr` 收窗外扩 0.5s 包进脏秒边缘 | 4787 窗首/4788 窗尾带字 | 方向反转: 内收 0.6s (顺带覆盖帧采样盲区) |
| 7 | fps=1 采样对 <1s 飞入即出闪字全盲 | 4417/4414: 同管道秒采样全空, 精确 seek 有 7% 大字 | fps=3 (盲区 0.33s), 帧号 k→秒 = k//3 |
| 8 | 输入 seek 音/视频流落点差 0.1~0.3s | 3973 音画不同步 (容器 0/0 但内容错位) | 输出 seek + faststart + **-an** (用户令: 素材只要画面) |

**连带修复**: `_probe` 改 merge 写 (re-probe 不抹 OCR 结果); `_backfill_dims` 停止 `json.dumps` 存 JSON 列 (6400+ 脏格式源头, library 翻页 500 的根因, 读侧 schema validator 止血双保险)。

## 二、入库判据收敛: is_real_footage (宁可错杀)

TAG_PROMPT 重构为单判据, **显式 true 才入库**:
- true = 摄像机实拍真实世界 (人物/街景/自然/建筑/器物/事件现场)
- false = 一切自制/合成: 地图/地形渲染/大面积国旗(特写/飘扬/纯色块)/国旗叠加/图表/CG/游戏/AI生成感/剪影+渐变/纯色背景示意图/商品棚拍/截图/文字卡/黑白老胶片/历史档案
- **空标注自动拒** (has_burned_text=None falsy 放行的 4409 漏洞随之堵死)
- 演进过程: is_map → +is_bw → 收敛为 is_real_footage (用户 4635~4642 连续报合成素材驱动)
- 黑白/商标豁免说明: 酒瓶商标类小面积单框 OCR 不判脏 + prompt 明确"产品小 logo 不算"; 误杀方向均为宁错杀

## 三、存量三轮出清 (库 8780→~5000 行)

| 轮 | 量 | 工具 | 备注 |
|---|---|---|---|
| 孤儿行 (文件已回收 DB 残留) | 625 | 手工 (精确 id, DB 先备份) | purge_bad_yt ① 整片回收分支漏删 DB 行的 bug 已修 |
| 黑白关键词 | 93 | scripts/purge_by_keywords.py | desc/kw 词表, 样本全正货 |
| 地图关键词 | 1034 | 同上 | dry-run 实证 **国旗词整批误杀政要演讲实拍** (冯德莱恩/澳议会样本) → 国旗词撤回仅统计, 留 VLM 区分"主体国旗"与"背景带旗" |

出清原则: 文件一律 send2trash 回收站; DB 行按精确 id 删 (带 WHERE); DB 先备份 (`pipeline.db.bak-20260901-orphan-purge`)。

## 四、批处理管线 (幂等三段)

```
rlr_reocr.py (fps=3 时间轴重扫, GPU 4进程 ~2h)
  → rlr_resplit.py (re-probe + 窗修正(按 dirty_sec_list 重建内收) + 重切 + 清旧行 + 重置 job)
  → rlr_stage2_parallel.py (is_real_footage 打标 + 双清入库)
  → recheck_real_footage.py (362 行非 RLR 存量 VLM 复判兜底)
```

单部验证记录: fUWTGTHrB1Y (16s 脏窗→真镜头边界拆分) / bZuxM4DEgLA (窗 132.5-176.5→133.6-175.4) / d8MFtI6oloc (fps=3 逮住 169 闪字秒, 4417 区域窗弃)。

## 五、架构决策 (下窗第一优先): CC 字幕轴

**用户提案采纳**: RLR 先稿后配, 烧录字幕=口播词 → YT 自动 CC (ASR) 时间轴 ≈ dirty 轴。
- `yt-dlp --skip-download --write-auto-subs` 只拉字幕 (~10min/277 部)
- CC 有词时段 = dirty 主轴 (**词级时间戳零盲区**); OCR 只扫 CC 空隙 (逮非口播标注字如 STEEL AND CONC)
- GPU 2h→~30min; 语义预判 (map/数字密集段跳过打标) 顺带成立
- 落地待下窗: 下载器拉 subs + 窗逻辑 CC 驱动 + 空隙 OCR 抽查

## 六、当日代码维护轮 (上午, 另见两篇 0901 文档)

siliconflow key 迁 .env / mktemp 竞态 / 弱哈希豁免; 回收站函数三合一 (safe_trash 永久删除红线违规修复); jy_draft_service / boost_service / orchestrator / ppt 产线四拆包; pexels 测试 2 例修复 (delenv 后 load_config 把 .env key setdefault 回来); library 翻页 500 (JSON 脏格式); pytest 41/41; 9 commits。

## 遗留

- [ ] 批处理执行: reocr→resplit→stage2→recheck 队列跑完 (进行中)
- [ ] CC 字幕轴方案落地 (§五)
- [ ] resplit v3 已切但将被 fps=3 新轴重切的视频: 中间产物, 以最终批为准
- [ ] stage2 完成后抽查 is_real_footage 实际拦截率 (误杀/漏杀校准)
