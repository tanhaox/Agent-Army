# 已完成-20260905-HF身份互动卡入产线+逐字闸门+模板八处接线定稿

> 会话: 2026-09-04~05, 分支 feat/evidence-image。06 身份句卡 / 07 收尾互动卡 从杂志风预览页面定稿到入产线, 途中把「新模板接线」定型为**八处清单**, 并立引用语**逐字闸门**。两次产线事故 (job 2375b001 / bdb6674b) 均复盘在册。

## 一、交付物

### 1. 两张新卡 (杂志风纸墨系补齐, 6 模板全集)

| 卡 | 模板 | workflow | 位置 | 时长 |
|---|---|---|---|---|
| 06 身份句 | `templates/hf_prep_v3/hf_identity_v1/` | `hf_identity` | 开场段 (前 3 slot 内), 全篇 ≤1 | 4~6s |
| 07 收尾互动 | `templates/hf_prep_v3/hf_follow_v1/` | `hf_follow` | 固定收尾 (最后一个画面), 全篇 ≤1 | 3~5s |

- **identity**: 自介句上屏 (tier 阶梯字号, 与 quote_v2 同款 JS 自动分档) + ABOUT 小标 + 240px 锈红印章 (2×3 格品牌字); `data-identity="{{identity_body}}"`。
- **follow**: 口号两句断行 (`，、` 处切, 后半 rust 色) + FOLLOW 大字 + 印章 + 底部品牌小标; `data-slogan="{{slogan}}"`。
- **均财经线 (tech) 专用**: `_require_tech_track` — geo 误规划 → RuntimeError → fallback 降级。track 取 `script.article.track` (缺省 tech)。地缘线模板另做一套 (用户令)。

### 2. 引用语逐字闸门 (`hf.py _gate_quote_text`)

05-A/B/C 档位 = 模板 JS 按去空白字数自动分档 (≤8 / 9~16 / 17~35, 标点计入), **导演零参与**; "逐字"靠执行层三层闸门:

1. quote 去标点空白 ⊆ text_context (零复写协议拼回的原句) → 放行原样;
2. 对不上 → difflib SequenceMatcher 遍历原句集合, **最相似原句覆盖** LLM 改写;
3. >35 字 → 最长句边界前缀截断; 单句 >35 且无句边界 → RuntimeError → fallback 链。

身份卡同闸门。旧 `[:80]` 硬截兜底撤销。净标点政策豁免键扩为 `quote_body/identity_body/slogan` (断行依赖标点)。

### 3. 提示词规则 (`config/visual_director_v2.txt`)

- 收尾金句分工: 口号句→hf_follow, 非口号点题句→hf_quote (name=老谭), 两卡中间隔"下期见"句画面不相邻。
- **金句禁选互动引导句** (2026-09-05 增, "评论区聊聊"被当选金句实锤): 评论区聊聊/关注我/点赞/下期见/期待你的看法 = 动作指令, 零信息量, 禁做 hf_quote; 归 broll/hf_follow 时段。
- `hf_* 不得相邻` 扩到全五个 hf 工作流。

### 4. 20s 间隔降级豁免 hf_follow

收尾 quote→"下期见"broll→follow 天然 <20s, 不豁免必被 `_HF_MIN_GAP_SEC` 误杀。豁免名单 = hf_opening + hf_follow (`_postprocess.py`); hf_identity 走常规规则。

## 二、新模板接线八处清单 (定型, 后续模板照抄)

① `templates/hf_prep_v3/<name>/index.html` — HyperFrames 协议: #root data-* + GSAP paused timeline 注册 `window.__timelines`, 入场 ≤2s + 尾部保持段
② `template_library.py` TEMPLATES 注册 — composition_id/source_dir/json_schema/required_input (全固定件卡 required_input 可空)
③ `hf.py` 分支 + executor — 品牌 `_merge_brand`; 赛道闸门 `_require_tech_track`
④ `slot_workflows/__init__.py` WORKFLOW_HANDLERS + `slot_executor.py` 分发元组 + `_EXECUTION_PHASES`
⑤ 解析白名单: `director_parser/_rows.py` _VALID_WORKFLOWS + `_rules.py` _HF_WORKFLOWS
⑥ `schemas/director.py` DirectorSlotPlan 两个 Literal (visual_type + workflow)
⑦ 枚举补挂: `_postprocess.py` _MIN_DUR / `jy_draft_service/common.py` _HF_TEXT_FAMILIES + `visual_director_v2.txt` 工作流表/参数节/映射表 + `scripts/sync_hf_templates.py` TEMPLATE_DIRS 跑一次部署 E盘
⑧ `template_filler.py` **_SAFE_KEYS 白名单 + _build_substitutions 注入新键** — 白名单外 `{{key}}` 按设计原样保留

漏挂症状速查:
- 漏 ⑤/⑥ → LLM 输出被洗成 hf_chart/broll 渲空卡, 或 job 整体 pydantic ValidationError 0 slot failed
- 漏 ⑧ → 成片渲染出**原始 `{{占位符}}`** (DB input_json 有值 ≠ 已上屏, 查残留看 filler 不看 executor)

## 三、两次产线事故复盘

### job 2375b001 — 两次 failed, 0 slot (ValidationError)
- 根因: `visual_director_v2.txt` **热载** (每次 build prompt 从盘读), pydantic schema **冷载** (进程启动定死)。提示词先广告 hf_identity, 旧进程 Literal 拒收 → 整 job failed。
- 规矩: **代码八处全落 → 重启后端 → 才跑 job**; 提示词 txt 与代码必须同批落+同批重启。

### job bdb6674b — 剪映草稿卡面印出 `{{identity_body}}` / `{{slogan}}` / `{{FOLLOW_WORD}}`
- 根因: 接线清单当时只有七处, ⑧ template_filler 白名单漏挂。brand_name/hot_word/duration_sec 老键在白名单 (印章/ABOUT 正常), 新键不在 → 原样上屏。
- 修复: `_SAFE_KEYS` + subs + 标点豁免分支三处补齐; 离线 fill_template 烟测 (占位符残留=无, 中文含标点上屏) PASS。

## 四、「杂志风预览页面」命名约定 (用户令 2026-09-05)

- 用户说「杂志风预览页面」= 调出 `file:///F:/AI-Agent-Local/digital_human/.tmp/style_gallery_h.html`。
- 页面三约定: 全模板锁横屏 16:9; 全页动效 ≤2s 播完**定格不循环** (录屏剪辑友好, 悬停手动重播); 新模板先在此页定稿才入产线 (风格门, 见 `design/模板入产线-风格页评审门.md`)。
- 06 短句/中句身份卡已撤 (单 >16字 档保留 — 易诱导截断, 用户裁决)。

## 五、验证

- 模板 playwright 占位符填充烟测: timeline 注册 / 长句三行断行含 rust 强调 / 印章品牌字 / slogan 两行断行 ✓
- 逐字闸门离线单测 9+ 分支 (verbatim 通过 / 改写覆盖 / >35 截句 / 单句无边界 RuntimeError / geo 拒绝 / 缺省通过) ✓
- template_filler 修复离线烟测 PASS; 后端重启加载新代码 (进程 03:23 > 修复 03:07)。
- 真机 npx 渲染 E2E: 随重跑 job 验收 (协议与已验证 v2/v3 同构)。
