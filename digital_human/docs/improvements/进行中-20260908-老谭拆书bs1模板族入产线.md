# 进行中-20260908-老谭拆书bs1模板族入产线

> 状态: **老谭线产线实战成片 (0908 白天)** — 第二次 render 全要素齐: 草稿 PPT_20260908_0625_094a4e9b (16页+双尾卡 B-Q1/B-08 深空蓝金+logo6处+老谭音色284s+ASR零错音+中文集号foot+新outro)。质量闸门/节奏修复/单页操作体系经多轮联调实战验证。静读书 bs1 化=老谭验证成功后再升级 (用户令 0908)。
> 流程定稿 (用户令): **稿件→①PPT生产(页单)→②确认(导演层·bs1_story工坊)→③音频合成→④视频生成→⑤剪映草稿箱(完成)** — 复用 ppt_pipeline job 骨架 (bs1 分支), 老谭线不进 audio 页不进导演台。
> 铁律: **老谭用 bs1 族, 静读书用纸墨系 (hf-*), 两线不互通** (用户令 0908)。

## B+C 落地清单 (0908 夜三)

1. **slide_gen + `replan_page`**: 单页重规划 (换页型 8 种正文互换 / 同型重产可带 hint); narration 铁律不动; fields 嵌套→平铺坑已修 (502 根因)
2. **新服务 `slide_render.py`**: build_context (品牌/logo 复用 `_book_brand`) / page_to_dict / fetch_page_image (Pexels 横图→workdir/imgs) / render_bs1_pages (fill_template→html→playwright 单浏览器批量截图, _shot 同法) / run_bs1_plan (①规划+配图+预览渲染); `_write_pages` 带 bs1_meta.json (重启恢复)
3. **router `ppt.py` bs1 端点组**: POST /bs1/plan (persona 闸门 400) / GET /bs1/state (**须声明在 /bs1/{job_id} 前**, FastAPI 顺序匹配; 内存→落库快照→workdir meta 三级恢复) / GET /bs1/{job_id} / PUT pages/{idx} (字段白名单) / POST pages/{idx}/replan (结构位拦截) / POST pages/{idx}/reimage (失败保原图) / POST /bs1/{job_id}/confirm (落库 episodes.bs1_pages_json={job_id,pages}) / GET pages/{idx}/png; render 复用 `/{job_id}/render` (未确认 400)
4. **pipeline bs1 分支** (`_run_ppt_pipeline`): mode=bs1 → slides=页单构造 (narration=notes) → TTS/对齐复用 → 渲染段 jy2 特判 (整幅 PNG 单层 base, 缺失现场补渲染) → 尾卡模板 id 按 `_book_brand` 分流 bs1-*/hf-* → jy2 草稿出口复用 (品牌/logo/免责已就绪); skin 不 apply (bs1 自带版式)
5. **DB**: book_episodes + `bs1_pages_json` (JSON) — database.py 手动迁移已加, 真机已应用
6. **前端新页 `web/bs1_story.html`** (页单工坊, 一站式): 状态流五步条 / 页卡流 (PNG缩略+页型徽章+时长+台词折叠+字段编辑+换页型▾/重新生成(带指令)/换图/保存重渲染) / SSE 日志 / 大图弹窗 / 草稿名展示
7. **入口分流**: books_story.js (BOOK_LINE gating: 老谭线隐藏 进产线/TTS/导演任务/音频页/重跑, 显示「📐页单工坊→」; produceEp 老谭线防御直跳工坊) / audio.js (`_IS_BS1_BOOK` 探测=bs1/state 放行: PPT 上传区替换为工坊入口+音频就绪不自动跳导演台+showDirectorHandoff 拦截)
8. **模板同步**: 仓库 templates/hf_prep_v3/bs1_*_v1 ×13 → 已 copy 到产线 E:/AI/digital_human/hf_prep/ (改模板须重新同步!)

## E2E 烟测记录 (HBO ep1, 后端 54321 已重启加载新码)

- [x] state 闸门: HBO(老谭) 200 / 蛤蟆(静读书) 400 拦截
- [x] plan: 16 页/覆盖100%/前15秒57字/渲染16/配图6 (一次 LLM 过, ~5min); 重跑 20 页/配图8
- [x] 目录=章题一一对应; quote×2 金句池句; compare 左灰右金3v3+VS; case 三行
- [x] PNG 视觉复核 (see-image): cover 版式完整/无裸占位符/字幕带留空; list 眉标页题条目金句条齐, 金句条 85%<字幕带 90% 无叠; E2E quote/compare 页无异常
- [x] 换页型 passage→list: 47.8s, fields 全产 (kicker/items/quote), narration 未动
- [x] 重新生成带 hint: 通 (chapter 页无金句条属正常)
- [x] 字段直改 PUT: title 改后单页重渲染 ✓
- [x] confirm: 落库 20 页 status=confirmed
- [x] 重启恢复: planned 态 workdir bs1_meta.json 三级恢复 ✓ (旧 job 补 meta 验证)
- [x] **render 全链 DONE** (草稿 PPT_20260908_0339_b473fb9e): TTS 20 段/整段337.8s (ASR 抓行7「转」错音自动拼音修复) → 对齐20段 → bs1 页1-20逐页消费 (audio 20/字幕63/系列角标2/免责1) → 尾卡 B-08 **深空蓝金系 (bs1-bookinfo-v1) persona 分流实锤** → jy2 草稿落剪映 Projects 目录
- [x] B-Q1 收尾: quotes 池正则两级标题兼容 (蛤蟆`###`/HBO`##`), HBO ep1-4 落 3句×4, 蛤蟆 6集18句回归无损
- [x] logo 落位: 用户提供 G:/下载/tanbooklogo.png (896×1184 圆形「老谭读书」深蓝金/白底) → 边缘 flood-fill 抠白透明 + 480px 缩版落 assets/watermark/laotanbook_logo.png (源保留 _source.png); B-08 尾卡右下+outro 居中偏上视觉复核透明无白块; 台标/页 logo 运行时读盘即时生效
- [ ] 静读书回归: 蛤蟆线 pptx 上传→尾卡仍纸墨系 (不互通验证)
- [ ] 前端页面人工过一眼 (bs1_story.html / books_story 分流按钮)

## 实战迭代 (0908 白天·用户联调, HBO ep1 多轮)

1. **流程修正 (用户令)**: ②确认后应进**音频加工页人工听审** (非直接 render) — 工坊加「🎵 音频加工」按钮 → `POST /bs1/{job_id}/bridge-audio` 建 Script (页=段, **老谭账号/音色**) → audio 页 (音色按 prompt_template 锁「老谭拆书号」) → 听审完金条引导「回工坊生成视频」; render 的 TTS cache_key=音色+段文本, 音频页成果 render 零重合成复用
2. **E2E 首跑音色 bug 修**: render 步骤1原用 ensure_book_account (静读书!) — mode=bs1 改 `ensure_laotan_book_account`
3. **单页操作全局一致性**: 换页型/重排后 `_renumber_chapters` (章号连续重编+contents items 重对齐+目录页连带重渲染); replan chapter 沿用原 no
4. **删空页**: `DELETE /bs1/{job_id}/pages/{idx}` 仅限零台词页; `POST /bs1/{job_id}/rerender` 全量重渲染 (刷 foot/品牌/logo 全局变更)
5. **质量闸门三连** (flash 输出不稳实证: 空壳 passage/0.5s 空页/40s 超长页三种事故): 正文页超 160 字弃 / 主字段空>30% 弃 / passage body 空计入 / 空 narration 正文页 sanitize 即丢; 两次不过走兜底 (passage/list 确定性轮换)
6. **页型节奏** (用户令: 抖音前段同型连排=跳出): 提示词交替铁律 + `_fix_monotony` (相邻同型后页 LLM 换型 compare/list 优先) + 兜底轮换; 实测多版零同型相邻
7. **恢复体系**: state 内存取**最新** job (重新规划不被旧 job 掩盖) + 快照/meta 统一比 workdir mtime; confirm 落库 {job_id, pages}
8. **模板改版 (用户逐条令)**: outro 主字位=固定品牌句两行 (同金句字体) / NEXT 卡居中上移 / 删互动问模块 / 删左下重复品牌位; foot=「第X集」中文集号 (build_context); **改模板须三处同步: 仓库+E盘产线+预览页快照(.tmp/bs1_preview, 含示例内容填充)**
9. **模板视觉复核坑**: 改模板 JS 用 regex 删块易留孤儿行 (askEl 未定义断整条时间轴) — 改完必渲染验证

## 已知小尾巴

- reimage 对无 img_query 页 (quote) 无意义 — 前端仅在有 query/图时显示换图按钮 (未改, 小事)
- B-Q1 quotes<3 静默跳过 — 可加一条 _evt 让用户在日志里看到跳过原因 (未改)
- B-08 简介 (bio) 空: HBO 书 author_bio 数据缺 — 内容层补数
- 单页操作约 15-48s (LLM+截图), 批量调页要有心理预期
- passage/case 页 LLM 偶留空 kicker/title — 模板空值优雅降级 (视觉复核未见异常)

## 历史完成 (0908 深夜一/二)

1. **模板族 13 页型** `templates/hf_prep_v3/bs1_*_v1/`: HyperFrames 协议 + preview-play + fit; 预览页 `.tmp/bs1_preview/`; 自检 `tools/_shot.py`
2. **②template_library** 13 个注册 / **⑧template_filler** 全键入白名单 (`book` 键漏挂已修)
3. **A. slide_gen v2 规划器**: `plan_episode_pages` 硬校验链 (序对齐/缺口补页/前15秒71字/目录=章题/金句对账/bars<4转data) + 确定性兜底 (覆盖100%); 金句池双格式 (新契约独立短行 + 老稿`金句：`前缀行); HBO ep1: LLM 20页/99.7%/57字/全页型; 兜底 23页/100%; flash 285s/次; 测试脚本 `.tmp/test_slide_gen.py` (脚本环境须 set_config(load_config()))
4. **HBO 内容线全链** (上轮): 6集×300s 路线/受众指纹/情绪/时长分流

## 关键文件索引
- 模板: templates/hf_prep_v3/bs1_*_v1/index.html (13个) → 产线 E:/AI/digital_human/hf_prep/ (须同步)
- 规划/单页重排: app/services/book_service/slide_gen.py (plan_episode_pages / replan_page)
- 渲染/页单 job: app/services/book_service/slide_render.py (run_bs1_plan / render_bs1_pages / fetch_page_image)
- 端点: app/routers/ppt.py (搜 bs1); 管线分支: app/services/ppt_pipeline.py (搜 bs1_pages)
- 前端: web/bs1_story.html (页单工坊) / books_story.js (BOOK_LINE gating) / audio.js (_IS_BS1_BOOK)
- 注册: app/services/template_library.py; 填充: app/services/template_filler.py
- 截图自检: tools/_shot.py; 视觉复核: .claude/skill/see-image
