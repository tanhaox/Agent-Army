# 已完成-20260901-安全密钥迁移与代码健康清理

> 背景: GPU 被视频打标占用, 本批为纯 CPU 代码整理 (用户令: 1 密钥/3 死代码/4 安全扫描先干, 2 大文件拆包后置)。

## 一、siliconflow 明文 key 迁移 (ID-047 待办清账)

- **改动**:
  - `config/app.yaml` siliconflow 段 `api_key` → `"${SILICONFLOW_API_KEY:}"` 占位符 (照抄 zhipu 模式)
  - `app/config.py` `SiliconFlowConfig.api_key` 加 `_resolve_env()` 包装
  - `.env` 新增 `SILICONFLOW_API_KEY=...` (51 字符)
  - `config/siliconflow.yaml` **死文件**回收站 (config.py 只读 app.yaml, 该文件无任何代码引用)
- **验证**: `load_config()` 冒烟 `cfg.siliconflow.api_key` 51 字符加载成功; app.yaml 未被 git 跟踪 (`.gitignore:88`), key 无历史泄露, 无需重置。

## 二、死代码清理 (vulture 90%+)

删除 3 处真死代码:
- `crawler/hot_news_detector.py` — unused import `Set`
- `tests/test_tts_client.py` — unused import `typing`
- `app/services/jy_draft_service.py` — `compute_block_timing` 死参数 `within_gap` (两处调用方均未传)
- `app/routers/scripts.py` — unused import `AudioFile` (清理合并时顺带发现)

**有意保留不删** (vulture 误报):
- `lit_video_builder.py` 9 个 `# noqa: ARG001` 接口参数 (workflow 固化参数, 设计决定)
- `book_service/orchestrator.py:717` `_douban_block` unreachable — 2026-08-24 暂停开关, docstring 注明"删 early-return 即恢复"
- `scripts/tts_lib/text.py` `keep_breaks` 兼容参数 (调用方 lines.py 仍传)

## 三、🔴 回收站函数三合一 + safe_trash 红线违规修复 (本批最重要发现)

- **问题**: 回收站删除存在**三份并行实现**, 漂移已造成真实违规:
  - `file_utils.safe_trash` — send2trash 未装时 fallback `shutil.rmtree` **永久删除** (违反 CLAUDE.md 红线 1), 而 **3 个活跃调用方** (`video_tagging_service/cleanup.py`、`routers/visual_render.py`、`routers/digital_human_video.py`) 删的都是目录 → 一直在静默永久删除
  - `routers/scripts.py::_recycle_file` 与 `services/library_service.py::recycle_file` — 逐字符相同的 PowerShell 文件版双副本
- **修复**: `app/services/file_utils.py::recycle_file()` 成为唯一权威实现 — PowerShell `Microsoft.VisualBasic.FileIO.FileSystem`, **支持文件+目录** (`DeleteFile`/`DeleteDirectory` 自动分派), 失败返回 False 且文件保留原地, **永不永久删除**; `safe_trash` 降为兼容别名 (行为变更: 不再 rmtree fallback)
- **调用方收敛**: `scripts.py` 删本地副本改 import; `library_service.py` 删本地实现改 re-export (`routers/library.py` 调用点零改动)
- **验证**: py_compile/import 链 OK; 真实回收站测试文件+目录双例 True+原地消失; `routers/library.py` 经 re-export 调用连通
- **⚠️ 后续重启后端生效** (`reload=False`)

## 四、安全扫描 (ruff S + bandit)

- **硬编码密钥: 0 条** (S105/S106/S107 全绿); SQL 注入 S608: 0 条
- **修复 mktemp 竞态 ×3** (S306): `routers/voices.py:85`、`scripts/tts_lib/audio.py:95/136` → `NamedTemporaryFile(delete=False)` 原子创建
- **弱哈希豁免 ×4** (S324): sha1/md5 均为缓存/去重指纹非密码学 → 加 `usedforsecurity=False` + `# noqa: S324` 标注
  - ⚠️ `articles.py` `_raw_sha` 与 `boost_service.py` `_raw_sha` 是同一缓存比对机制 (2026-08-25), **必须同算法**, 不可单边升级
- **豁免记录** (单机本地服务, 无暴露面):
  - S603/S606/S607 ×93 — subprocess 调 ffmpeg/PowerShell 是设计核心
  - B310 ×12 — 全部为 localhost 服务客户端 (ComfyUI/llama/TTS), URL 内部构造无 SSRF 面
  - S110/S112 ×63 — 清理路径吞异常, 风格问题非漏洞
- **工具**: venv 新装 vulture/pylint/bandit/ruff/pytest (dev 工具)

## 五、测试回归

- `pytest tests/`: **39 passed, 2 failed** — 失败为 `test_pexels_service.py` 两例 `Database not initialized`, **预存环境性失败**, 与本批改动无关 (git status 确认未触碰 pexels 相关文件)

## 六、遗留 (转 backlog/后续)

- pylint 重复代码 42 组: 大头是 `scripts/rlr_*` 系列批量脚本互抄 (素材线活跃, GPU 打标期间不动) 与 ffmpeg 工具块; 建议大文件拆包 (#2) 时顺带
- 大文件拆包排队: `jy_draft_service.py` 70KB / `boost_service.py` 69KB / `book_service/orchestrator.py` 57KB
- 2 个预存 pexels 测试失败待查 (DB 初始化顺序)
