# 已完成 20260914 · 受众两层 — L1 书定主线 + L2 两轴闲话池 (内容×人群双面大纲)

> 触发: 用户在目标读者以书定 (0913c) 基础上推进 — "第一层目标客户是书内容本身,
> 第二层是泛目标客户, 跟着书里的内容划分出来的; 成稿主线用第一层, 闲话层用第二层",
> 并定轴: "L2 池有两大类: ①上下游 ②组织中的上下层", "大纲一面是内容, 一边是说给谁听"。

## 一、设计定稿

```
受众地图 (书级, 锁定器 v2 一次产出, 存 input_json):
  L1     = 书定主线读者 (处境+决策场景, 全篇"你"不换人)
  上下游 = 价值链同构 ≤2 组 (给你定规则掏钱的/你产出交给的 — 观众世界版本)
  上下层 = 组织同构 ≤2 组 (你头顶批预算的/你手底下被摆布的 — 观众世界版本)
  每组带同构桥一句: 那群人自己的处境被同一机制摆布 (过"你不做这行也一样——"检验)

总纲双面: 内容面 (拆解三步) × 人群面 (闲话层: 每集从池中分配 1~2 组, 按本集概念落轴)
成稿: 主线论证/工具/诊断问=纯 L1; 迁移落地/共鸣处=闲话层点名+桥句; CTA 转发对象=L2 组
```

替代旧【层级配对铁律】(概念一/二/三各绑一类人) — 它是 ep2 钩子"受众大礼包/
'你'指代每句一跳"的病根: 宽不是靠主线换人, 是靠闲话搭桥。

## 二、落地 (四层)

1. **锁定器 v2** `_derive_audience_map` (creation_steps.py): L1+两轴池一次产出;
   反漂移铁律 — L2 组必须是**观众世界里的人** (用他们自己的身份+场景指称,
   "管投放预算的市场负责人"✓ / 书内 cast 平移"制片方/广告主"✗), 轴归位
   (平台/金主=上下游, "平台就是你的老板"也归上下游), 桥=处境同构非买卖关系;
   轴撑不起给空数组禁硬编。auto_fill 落 `input_json.受众地图`, L1 镜像进目标读者画像。
2. **导演层** (laotan_series_director.txt): 新增【闲话层调度】— 大纲每集两面,
   闲话层只从池选、按集概念落轴、相邻集禁同组连排; schema 加 `"闲话层"` 列;
   `_material` 喂受众地图。series_outline: `_norm_chatter` 归一 (dict/简写串/轴名
   漂移认轴) + 硬校验 (1~2 组、组禁重复) + 白名单落库。
3. **生成层** (laotan-book.txt): 层级配对铁律 →【受众两层铁律】(主线单受众+闲话
   按轴搭桥, 换人必带桥句, 无桥换人=大礼包); 钩子认领收窄改**纯 L1** (禁多类人
   排比, L2 不进钩子); 闲话即合同 (点名必落地); CTA 对象=闲话层组; 闲话层未分配
   (旧总纲) → 两轴各搭一桥兜底。episode_gen 注入【本集主受众】+ 本集闲话层(带池/桥)。
4. **顺手对齐**: creation_common 兜底基底同规则改写; 旧 `本书迁移视角` 链路保留
   (旧书兼容, 新链路受众地图优先)。

## 三、HBO 实跑 (三轮校准)

- 第一轮: L2 锁成书的 cast (制片方/广告主/平台规则方) — 漂移实锤, 补
  "观众世界指称/书内 cast 禁令/轴归位"三铁律;
- 第二轮: 平台误入上下层轴 → 轴归位规则补明;
- 第三轮定稿:

| 层 | 组 | 桥 (机制同构) |
|---|---|---|
| L1 | 握渠道靠外部供给、议价权被上游捏着, 纠结要不要砸钱自建原创的操盘手 | — |
| 上下游 | 定分发规则的平台运营 | 被自家算法KPI逼着判断谁配拿推荐位 |
| 上下游 | 管内容投放预算的市场负责人 | 预算买别人的内容, 留存不归自己, 买量vs自造 |
| 上下层 | 拍板内容预算的业务负责人 | 买流量vs养内容签字分钱, 短期vs长期两头拉扯 |
| 上下层 | 背着更新KPI的内容团队长 | 产量追着跑、质量压着头, 多而烂vs少而精 |

四组全过"你不做这行也一样"检验。已落 HBO input_json (受众地图+L1 镜像)。

## 四、级联提示 (等用户令)

HBO roadmap 尚无闲话层列 — 逐集生成走"两轴各搭一桥"兜底; **重跑总纲**后按池
正式分配 (总纲重排会级联 unit 重切+旧稿 stale, 时机用户定)。新书全链路自动。

单测 tests/test_book_audience_map.py 6/6; 全库回归 182 通过
(evidence 1 失败为分支既有, 无关)。

## 五、人设层防回流 (同日补)

用户追问: "老谭人设里有目标人群, 如何让老谭共享受众地图又不带偏? 否则人设层把
风格带回来。" — 实锤泄漏点: laotan-book.txt【受众画像·必须内化】烙死
"集团部门管理者/内容创作者操盘手/想升值的职场人"三层名单, 与受众地图双信号打架。

修: 该段降权为【听感底色·只管怎么讲, 不管讲给谁】(保留听感约束: 不爱术语/
怕认知掉队/要能拿去用的判断), 加优先级铁律 "讲给谁只认【本集主受众/闲话层】,
本节禁反推人群点名"; A/B类(观看深度)与 L1/L2(处境同构)两轴别混声明;
导演材料有受众地图时不再喂旧迁移视角清单 (旧书 legacy 保留)。
分工定稿: **人设=谁在讲+怎么讲 (声音), 受众地图=对谁讲 (随书)**;
persona.target_reader 降为书级锁定失败时的兜底+语感参考。
回归守卫: test_persona_layer_audience_demoted 锁死三层名单不回流。

## 六、ep1 大钩子豁免 (同日补, 用户令)

实锤: 新总纲 ep1 被规划成正文三步走 (A/B/C 型墙), 与生死集结构 (开局铁律八拍+
书三件套+片尾六集导览, 生成顺序 2-5→1→6 — ep1 要吃 2-5 实产全稿做大钩子) 打架。
根因: 拆解三步/闲话层硬校验全集一刀切, 未给 ep1 开例外。

修 (四处): 导演模板 ep1 例外条款 (三步填 []、闲话层可不填); _validate 豁免 ep1;
episode_gen ep1 不注入拆解三步 (含 ①②③ 兜底解析一并跳过); section_flow_issues
ep_index=1 跳过承接断层 (八拍结构段首设问合法), 工具名/系列句配额照查。
库里已清 ep1 旧正文式三步。守卫: test_ep1_big_hook_exempt。
