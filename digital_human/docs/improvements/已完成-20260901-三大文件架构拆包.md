# 已完成-20260901-三大文件架构拆包

> 承接同日《安全密钥迁移与代码健康清理》#2 遗留项。依据 [参考-20260807-Python大文件架构重构-规范](参考-20260807-Python大文件架构重构-规范.md), 沿用 slot_workflows/tts_lib/video_tagging_service 既有拆包模式: **文件级拆分 + 函数体原样搬运 + 旧路径转发**, 红线零行为变更。

## 拆包总表

| 原文件 | 原行数 | 拆后 | 最大新文件 | 转发方式 |
|---|---|---|---|---|
| `app/services/jy_draft_service.py` | 1594 | `jy_draft_service/` 包 ×11 | ppt_element.py 307 | `__init__` 转发 (旧 .py 删, 同名包即解析) |
| `app/services/boost_service.py` | 1231 | `boost_service/` 包 ×8 | pipeline.py 294 | `__init__` 转发 (同上) |
| `app/services/book_service/orchestrator.py` | 993 | 3 新模块 + 薄 orchestrator | episode_gen.py 447 | orchestrator.py 保留为转发层 (books.py `as orch` 属性引用不变) |

## 一、jy_draft_service/ (J 线剪映草稿)

- **布局**: common(时间轴/manifest/jy配置) / subtitle_text(洗涤纯文本) / subtitle_style(_StyledTextSegment 划重点) / sfx(音效) / auto_choreo(R9 编排) / job_draft(J1 主入口) / ppt_draft(整页) / ppt_layout(布局分析) / ppt_timing(element/cell/block/page 四策略) / ppt_element(元素级+免责/角标)
- **拆包关键坑 ①路径深度**: `Path(__file__).parents[2]` 在包内变 `parents[3]` — common.py `_PROJECT_ROOT` 与 sfx.py `_SOUNDS_DIR` 均校准并加注记 (config/data 路径)
- **拆包关键坑 ②相对导入**: `from .pinyin_fix import` / `from .jy_effect_library import` 改绝对导入
- **monkeypatch 修复**: `scripts/demo_jy_native_hf.py` 原写 `_jds._SUBTITLE_SIZE = 5.0` (改包命名空间不再生效) → 改为 `jy_draft_service.subtitle_style._SUBTITLE_SIZE` (定义模块)
- **验证**: 23 符号转发完整 / `_ANIM_PAIRS` 42 keys 证明 config 路径正确 / wash/split/highlight 纯函数断言 / 宫格 timing 假数据功能验证 / router 链 import OK

## 二、boost_service/ (爆品改造 + 事实 LLM 基础设施)

- **布局**: prompts(全部 Pass 提示词, 纯常量) / llm(`_call`/`_extract_json` 通用通道, 6+ 模块 lazy import) / splice(拼接清洗/锚点) / deconstruct(解构层) / emotions(P5) / tts_adapt / pipeline(run_boost+P7)
- **死 import 清除**: ThreadPoolExecutor / parse_script / 顶部冗余 load_config (拆包自然清除, vulture 此前漏报)
- **验证**: 27 符号转发完整 / clean_boosted_text 行为断言(剥层标题/模块标题/加粗) / 全部 8 个消费方 import 链 OK

## 三、book_service/orchestrator.py (拆书创作编排)

- **布局**: creation_common(人设常量/occult 锚定/弹性标签/LLM 工具) / creation_steps(Gate0+步骤1-4+L0 自动填充) / episode_gen(步骤5 逐集生成+确认+级联重跑) / orchestrator(36 行薄转发)
- **保留**: `_douban_block` early-return 暂停开关原样搬运 (2026-08-24 暂停, docstring 注明恢复方式)
- **验证**: 13 符号转发完整 / validate_script+elastic_labels 纯函数行为 / books.py/distiller/facing import OK

## 终验

- `import app.main` OK — 全路由注册, 无循环导入
- `pytest tests/`: **39 passed, 2 failed** — 失败仍为 pexels 预存环境性失败 (与拆包无关, 三轮回归结果恒定)
- vulture 三包 0 新增 (仅 2 条已知有意保留: douban 暂停开关 / facing.py 预存)
- 旧 `jy_draft_service.py` / `boost_service.py` 单文件已回收站; orchestrator.py 原地变薄 (转发层非删除)

## 遗留

- ppt_element 307 / episode_gen 447 超规范 250 软目标 — 受 `export_element_draft`(141行)/`generate_episode`(337行) 巨函数"原样搬运"约束, 函数级拆分待后续专项 (需逐段验证 prompt 拼装语义, 本轮不做)
- `creation_steps.py` 450 行同因 (auto_fill_from_l0 132行)
- 2 例 pexels 测试预存失败待查 (DB 初始化顺序, 转 backlog)
