# 已完成 · 20260910 — 全系统提示词审计清洗

> 触发: 用户令 — "重新审计一遍系统中的所有提示词, 分个性化/共性/废话/可精进四类"。原则对齐 [修模板不修稿](../../C:/Users/tanhaox/.claude/projects/f--AI-Agent-Local/memory/fix-template-not-script.md): 个案与工程注记不进提示词串。

## 一、审计范围与四类结论

**在产线 19 源**: config 9 模板 (laotan-book/jingshu-book/fandeng/laotan/laotan-geo/laotan-tech_7layer_v2/laotan_series_director/visual_director_v2/laochen_default*) + common 7 块 + 代码内 SYS 常量 (boost prompts/distiller/l0/series_outline/slide_gen/creation_common/episode_gen/material/shot_contract/evidence)。*laochen_default 清洗后停用。

| 类 | 典型 | 处置 |
|---|---|---|
| 个案污染 | series_director 全套 HBO/撞墙/红绿灯/钱源 (当前书 ep1-6 工具名写成结构铁律); laotan-book 翻账判身份/续费理由/绿色频道; distiller 莱文→CEO; dedup/interaction 的 328e0093 job-id + 加沙/油价实证数据 | 14+10 处→结构化占位/泛化 |
| 废话注记 | 串内 `(0908 用户令)` `(0909 Kimi 实锤)` `(328e0093 实证)` — LLM 不认识的工程史, 每链烧 600~900 token; 模板头部"v2合并说明/{{common:xxx}}示例"整段工程文档 | 9 模板+common+.py 全剥 (laotan-book 18346→13874 字符) |
| 共性合格 | fandeng 14 行 / persona / tts_rules / output_format / visual_director 主体骨架 / distiller 11 节 / P5 span 规则主体 | 不动 |
| 冗长 (缓后未做) | visual_director 603 行 (9维度说4遍); 口吻红线 3 处重复; book_core 自检 19 条与正文逐字重复 | backlog |

## 二、结构变更

1. **book_core 灵性分流**: 灵性/心理 13 条拆出 `config/common/book_spirit.txt`, **仅 jingshu-book 注入** (`{{common:book_spirit}}`); book_core 留通用 7 条 — 老谭读书线不再吃真我/胜肽/能量防御条款。后续若静线读商业书, 可改按 BookProject.book_type 分流 (字段现空)。
2. **默认模板换血**: `articles.py` 洗稿缺省 laochen_default (老陈人设+Fish Speech 标点情绪表, 双重过时) → **laotan-tech_7layer_v2**; laochen_default 等 6 个历史件 (laotan-tech/archived×2/backup×2) 头部打「已停用勿改勿用」标。
3. **DB**: persona「老谭拆书」→「老谭读书」(品牌令遗留补齐)。

## 三、事故记录 — stripper 误伤 () (重要教训)

批量正则剥离 .py 提示词串内日期注记时, 收尾的**全局 `replace('()', '')` 吃掉了代码合法括号** (def 调用/方法调用), 10 文件 100+ 处损伤。

**修复路径**: ① git diff -U0 hunk 对照 — `damage(old)==new` (三种机械替换可复现) 则还原老行, 102 行; ② difflib SequenceMatcher 二轮 (修 hunk 配对漏网, 35 行, 含 `time.monotonic()`); ③ 签名扫描手修 (def/`_flush`/default tuple 等 11 处); ④ py_compile + import + 功能烟测 (parse_json_block/_apply_delete/is_evidence_claim/_fallback_pages) 全绿。备份 `.tmp/damage_backup/`。

**铁律**: 批量改 .py 提示词 — ① 只动字符串内、逐字符状态机扫描; ② 禁任何全局裸替换 (尤其括号/空串); ③ 改完必 py_compile+import+受影响函数真调用。

## 四、验证

- 5 在产模板 `_render_common_blocks` 渲染: 0 残留占位 / 0 串内日期
- 10 .py 全部 compile+import 过; laotan-book 渲染 -25% 字符
- "连辞两家"稿重规划走清洗后模板出片正常 (33 slot 全完成)

## 五、遗留 (backlog)

- visual_director_v2 压缩 (603→~380 行) + 口吻红线三处统一出处 — 高风险缓后
- P5 三件 span prompt 参数化合并 (结构相同, 差异仅 3 段)
- 灵性块按 book_type 字段分流 (需先给 BookProject.book_type 落值)
