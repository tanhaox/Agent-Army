# 已完成-20260905-拆书线尾卡双卡入产线 (B-08 书籍卡 + B-Q1 金句卡)

> 会话: 2026-09-05, 分支 feat/evidence-image。拆书线尾页升级: 老书籍卡 (hf-source-v1 来源行版, 静姐读书) 退役, 换杂志纸墨系双卡 — B-08 书籍信息卡 + B-Q1 书摘金句卡。版式源 `.tmp/style_book_card.html` (08 来源卡衍生, 用户逐轮拍板)。

## 一、模板与接线

| 卡 | 模板 | 内容层 | 固定件 |
|---|---|---|---|
| B-08 书籍信息卡 | `hf-bookinfo-v1` | 书名(自适应字号)/作者/阅读指南(L0 全书核心主张)/简介(author_bio 按。；打包≤60字\|\|\|分段) | About the Book 小标 / 静读书尾注 / logo PNG |
| B-Q1 书摘金句卡 | `hf-bookquote-v1` | 三句书摘 (01/02/03 编号) + 书名(右上) + kN 锈红关键短语(须为 qN 子串) | 静读书·书摘刊头 / 尾注 / logo / paper-2 底纸 |

- **八处接线照册全落**: ①模板 ②template_library ③ppt_pipeline 渲染 (非 hf.py — 拆书线不走导演 executor) ④无 (拆书线无 slot executor) ⑤无 parser ⑥无 schema Literal (非导演工作流) ⑦sync_hf_templates 已部署 E盘 ⑧**template_filler._SAFE_KEYS** 新键 book_title/author/guide/bio/q1-3/k1-3/logo_b64 (logo_b64 跳过净标点—data URI 含 `;` 会被剥)。
- **logo 注入**: 原图 1004px/1.8MB → base64 2.5M 字符超 schema maxLength 实锤; `_logo_data_uri()` 降采样 240px (2x 显示) → 180K 字符通过。
- 强调规则确定性: 指南「」《》锈红、简介《》墨重 — 正则实现, 无 LLM。

## 二、金句规则与选句 (用户令)

- 每集 3 句 × 6 集 = **18 句全书不重复**; 句长 **≥12 字**; 逐字来自 L0 蒸馏「可引用原句」池 (调查: 池 95 句全为书内原文, 最短 9 字, ≥12 字 92 句)。
- 选句 = **确定性** (`book_service/quotes.py`): 池句与集脚本的字符二元组重合度打分 — 集口播引用/讨论过的句子天然高分 (ep1 选中的正是口播引用过的三句), 已被其他集用过则排除; 锈红关键短语 = 句内首个 5~14 字子句。零 LLM 依赖零幻觉。
- `Episode.quotes_json` 新列 (ALTER 已加), 已为《蛤蟆先生》6 集回填。
- **分工定谳**: 拆书线无导演 LLM — 选句 = 创作层 (ensure_episode_quotes 惰性回填), 摆位/渲染 = 剪映线 ppt_pipeline。

## 三、摆位与音效 (用户令)

```
PPT 全页 (原"下期见"尾页 = 原视觉 + 结尾口播, 不再被替换)
→ B-Q1 书摘金句卡  倒数第二 · 4.5s · 静音 · 入场 title_in whoosh
→ B-08 书籍信息卡  垫底 · 5s · 静音 · 入场 title_in whoosh
```

- 音效挂 `export_element_draft` (ppt_element): 页带 `sfx: "title_in"` → 复用财经线 `_TITLE_IN_SOUNDS` 池轮换 attach_sound 同帧。
- **品牌统一**: 静姐读书 → **静读书** (角标 badge_text / 卡片 brand_name / 尾注)。

## 四、验证

- fill_template 烟测: 两卡占位符残留=无, logo 注入 ✓
- playwright: timeline 注册 ✓ / 无溢出 / 三句上屏 / logo 加载 / 无 JS 错
- **真机 execute_visual_render_job**: 两卡 completed; 末帧 RapidOCR 验收全内容上屏 ✓
- 附带修复 (同会话): logo base64 超 schema maxLength (降采样解决)

## 五、相关

- 预览页: `.tmp/style_book_card.html` (设计定稿依据, 含规则脚注)
- 同会话其他批次: TTS bleed 独立扫描 / pptx 千问水印过滤 / 相邻页尾句去重 / 规划期赛道闸门 — 见 CHANGELOG
