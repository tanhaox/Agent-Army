# 剪映模板拆解 — 45-知识付费（音·字·稿协同范本）

> 状态：**深拆完成（2026-08-16）**
> 数据源：`data/jy_template_snapshot/45-知识付费/template.json.bak`（126.9s / 16 轨 / 147 文字 / 53 音效）
> 用户判定：**"这条绝了"——音频、文字如何配合、和口播稿如何配合，研究明白 = 系统大进步一块**
> 分析脚本：`scripts/_tmp_teardown_45.py`（时间轴 JOIN）
> 关联：[剪映模板套路分析-效果配方库](剪映模板套路分析-效果配方库.md)

---

## 1. 总体结构

```
126.9s = 同一内容跑两遍（0-63.3s / 63.35s-末）— 用户判读: 前后效果对照教学,
B 遍(后)为"讲课"正确演示版（无卖家讲解佐证, 但数据支持变奏关系, 见 §3.1）
16 轨: video×2 | text×10 (T1,T2,T4~T11) | audio×4 (旁白 + 音效混布)
旁白 = 原抖音视频音频直接铺（IV 抖音原版剪辑版）
```

## 2. 🔥 核心发现：协同三件套律

每句口播在画面上同时产生**三层联动**，且时间戳完全同帧：

```
口播句 "美女月入300000+"
   ├─ T5 字幕轨: 整句白字 10 号 (0.02s)
   ├─ T6 强调轨: 关键词大字 "300000+" (1.10s, 数字更醒目)
   └─ SFX: 收银机叮 (1.10s ← 与强调字同帧!)
```

| 层 | 轨道 | 字号/颜色 | 内容来源 |
|---|---|---|---|
| **字幕层** | T5（82 条，≈1.2s/句 高密度） | 白 10 号（63 条）；强调句 18 号；4 条金 24 号 | 口播逐句 |
| **强调层** | T6/T7/T8（引用+关键词大字） | **金 (1, 0.96, 0.54)** 10-21 号 | 字幕句内关键词**原词提取**（带引号""怎么追女生""） |
| **冲击层** | T9/T2（?/VS/负面词） | **红 (0.72, 0.11, 0.11)** 28-44 号 | 疑问/对比/负面冲击词 |
| 封面层 | T1/T11 | 70 号 | 标题 |

**三色板**（抖音知识区标准）：白=正文 / 金=强调 / 红=冲击。红与 16期 完全同值——**跨模板统一色板，可直接定为 J 线默认**。

## 3. 🔊 音效语义实践（53 个音效的落点规律）

音效对齐的**不是句子，是强调字的出现帧**（误差 <10ms）。落点：句首 23 / 句中 23 / 句尾 7——句中一半，因为关键词常在句中。

| 口播内容类型 | 音效 | 对应强调字形态 |
|---|---|---|
| 金额/数字（300000+、月入几十万） | **收银机叮**、水滴×2 | 金色大字数字 |
| 引用/设问/负面（"怎么追女生"、舔狗?） | **悬疑不对劲、沙锤长、紧张回响** | 引号引用 + 红 ? |
| 对比/反差（长得丑 VS 长得美） | **高音闪光、玻璃碎了** | VS/对比大字 |
| 概念强调（共产主义、白月光） | 水滴/空灵水滴、发条 | 金色概念词 |
| 打字机动画文字 | **打字声**（同步逐字） | 打字动画字幕 |
| 转场/段落切换 | 呼2/whoosh | 大标题/新段 |

→ 与用户标注的语义库（`config/jy_sound_semantics.json`）**完全互证**：money/suspense_hook/impact/punchline/typing 分类在这条模板里全部实战出现。

### 3.1 A/B 两遍对照的启示（2026-08-16 用户判读 + 数据验证）

两遍**音效骨架完全同构**（水滴开场→收银机→打字×2→悬疑×2→沙锤→呼→高音闪光→发条→空灵水滴，节拍一一对位），B 遍为**同语义变奏重混**：

- **音效族规则**：同一语义槽位绑定的是"音效族"而非固定单音——A 用 `玻璃碎了`，B 换 `玻璃破碎声1`；`发条旋钮转动齿轮`→`发条转动`。→ R9 实现时按**类别内轮换**选音（`jy_sound_semantics` 每类的 sounds 数组正好如此），避免长视频同一音效听腻。
- **B 版新增的注意力点缀**：`鼠标单击`（指认/点选时刻）、`木鱼`（节奏性强调）、`闪回`（回顾/揭示）、`魔法音效`。→ 正确演示版比基础版多的正是**交互感与节奏点**，可作为 R9 的进阶点缀层。

## 4. 对我们系统的映射（全部组件已建成，只差编排）

| 45期 的层 | 我们的组件 | 状态 |
|---|---|---|
| T5 逐句字幕 | `jy_draft_service` caption 轨 | ✅ J1 已上线 |
| 关键词提取（金/红升级） | `find_highlight_ranges`（数字+专名）+ 语义选词 | ✅ 已有（划重点）；**待升级：从句内样式升级为独立大字轨**（16期 的多文字轨轮换正好用上） |
| 同帧音效 | `attach_sound(script, "sfx", 名, at_sec)` | ✅ 已挂载 |
| 三色板 | `_StyledTextSegment` 样式 | 定白/金/红三常量即可 |
| 63s 循环结构 | — | 不需要（我们稿子全长） |

**结论：45期 的协同系统 = 我们已有三块组件（字幕/划重点/音效）+ 一跳编排**——"划重点命中的关键词 → 升级到强调轨大字（金）或冲击轨大字（红，疑问/对比类）→ 同帧挂语义音效"。这就是 **R9 协同配方**，J2 的最高优先级实现项。

## 5. R9 协同配方（从 45期 提炼）

```
对每句字幕:
  keywords = find_highlight_ranges(句) + 语义分类(数字/引用/对比/疑问/概念)
  if 数字/金额:   金色大字强调轨 + attach_sound("叮，收银机提示音1")  @同帧
  if 引用/负面:   引号引用轨(金) + attach_sound("综艺悬疑 不对劲")    @同帧
  if 疑问/对比:   红色大字轨(?/VS) + attach_sound("高音闪光")        @同帧
  if 打字动画句:  TextIntro 复古打字机 + attach_sound("打字声")      @句首
  转场/新段:      attach_sound("呼") @段首
密度参考: 45期 ≈ 每 2.4s 一个音效 / 每 1.2s 一句字幕
```
