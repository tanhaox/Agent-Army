## 2026-09-23(三)｜夜间大修 — 角色卡全书带入 / 违禁守门(文字+国旗) / SSE 实时 / 五连坑复盘

- **统一违禁守门 (用户双实锤: 伪文字+各国国旗)**: render_gated 双道 — ①RapidOCR(真文字) ②glm-4v-flash 一次查 文字/国旗/国徽/地图/政治人物 (K2 风格化乱字 OCR 假阴性, 正向 PROTECT_TAIL 对国旗同样压不住 — "正向提示词对K2压不住,必须事后守门"定论); 命中换种子重掷1次仍脏打标; 三发校准全中零误报; 覆盖产线每镜/选板/试渲/重调全出口
- **SSE 实时流**: 圣经/试渲/选板/重调全接 /api/jobs/{id}/events (job带id, 终态断流, 断流轮询兜底); 圣经页实时日志框
- **五连坑**: ①Edge卡死标签页一夜37万次 POST render-samples (幂等放行致GPU反复点火) → 杀PID+3分钟冷却 ②style_bible 页 renderSamples 渲染函数被同名POST动作函数覆盖 (声明后者胜) → 样张区永空+每次轮询偷发POST (幽灵拼图最后一块) → 动作改名 startSamples ③bible_view 样张清单写死 _1/_2 (两图时代遗产) 第三张永缺 → glob ④hidden 被 display:flex 特异性压死 → [hidden]!important ⑤轮次同名图浏览器缓存串档 → ?v=时间戳
- **配方密度词事故**: 补3D锚时误删"圆润笨拙造型"(黏土风画面密度主驱动, 试渲细节崩塌用户图对比实锤) — 恢复并立规: **加锚只许加不许换**; 调味改人物前置 (角色卡放开头主体权重最高, 原镜句不被稀释)
- **角色锁定卡**: 圣经 characters[{name,role,look}] (服饰+脸+道具关键词串), bible_block 注入每镜规划"keyframe_zh 原样携带 look 禁改名换装"; DIRECTOR_SYS 拆第三个赛璐璐残留; 露脸/服饰区分关系铁律三处规格

## 2026-09-22(二)｜角色锁定卡 + K2 文字守门 — 人物跨镜认得出来, 画面永不带字 (用户两条硬令)

- **角色锁定卡 (用户令: 服饰/脸/道具一起生成, 关键词锁形)**: 圣经新增 characters[{name,role,look}] (look=服饰+脸型发型+标志道具 25-50字关键词串); bible_block 注入"出场角色 keyframe_zh 必须原样携带 look, 禁改名换装" — 每镜规划都吃卡, 全书任何一张图继承; DIRECTOR_SYS 人物规范同步 + 拆第二个赛璐璐残留硬编码 ("1988赛璐璐卡通脸"→跟随圣经角色策略); 《定位》已种 4 卡 (领导者/挑战者/消费者/定位顾问, 服饰即身份)
- **K2 文字守门 (用户令: K2 出字即垃圾, 只准图形)**: k2.run_shot + 选板/重调/试渲全部出图口包 render_gated — 出图后 RapidOCR (material_ingest 双session工厂) 检字 (score≥0.55 且 ≥2% 画面尺寸), 命中换 seed+1 重掷 1 次, 仍脏 shot.text_suspect=True 交人检; OCR 不可用自动降级跳过; cfg=1 无负面通道只能正向压 (PROTECT_TAIL) + 事后确定性守门双保险
- **露脸/服饰/丰富度铁律三处落规格**: 试镜镜规划 (_SHOTS_SYS: 高元素密度+必须露脸带表情+服饰写明单品区分关系) / 圣经 character_policy / DIRECTOR_SYS 人物规范; 此前试渲无脸+服饰糙 = 旧规格没写这些, K2 自由心证
- **试渲调味确定性化 (两版 LLM 调味器都偷缩句, 密度比 0.5x 实锤)**: 正则抽圣经主色 + 角色卡轮穿上镜 + 纯字符串追加, 零改写; 调味句落盘 调味句.json 可查

## 2026-09-22(二)｜三步视觉流程闭环 — 大纲→风格选型→艺术圣经独立页 + 人物形态锚家规

- **流程定型 (用户令)**: 大纲跑完 ➜ 风格选型 ➜ 艺术圣经 (赋能本书统一视觉配方) ➜ 分镜开工; 圣经独立页 `style_bible.html` (六字段表单: 风格前缀/色板/质感/角色策略/大字卡/氛围)
- **圣经页三动作**: ✨生成 (灵魂三问+受众+选定配方 → Art Director LLM) / 🎲重掷 (`load_or_derive_bible` 加 force 参数) / 💾保存手调 (手调落盘=写缓存, 优先于重掷, 全系列生效); 未选风格页挡并指路选型
- **人物形态锚家规 (用户实锤 NeonDrip 同风格内人物 2D/3D 乱漂)**: head 只锚质感不锚人物形态 → K2 基模自由心证; 家规补 "head 必须锚定 2D/3D/Q版 三选一" (提案+重调双入口); NeonDrip 补锚 3D + 单列重渲; ClaymationBoardroom 选定于家规前, 补锚 3D卡通人物
- **排除语义翻转 (用户令)**: 排除=挡具体配方不封品类方向 — 同方向换执行再来合法 (2D版被排→Kimi 出 3D 锚定版是最佳状态), 仅同名禁
- **《定位》全流程首例**: 7×240s 总纲 ✓ → ClaymationBoardroom 黏土定格选定 ✓ → 圣经生成 ✓ (珊瑚橙/天空蓝/奶油黄色板, 指纹压痕质感, 抽象概念拟人化黏土角色) — plan 吃现成缓存开工

## 2026-09-22(二)｜风格选择板续建 — 留存/排除/重调三动作 + 卡通红线守门 + 三坑修

- **三动作正交化 (用户令)**: ①留存=配方入库备用且**仍在候选列随时可选定** (不绑本书/不入排除, 用户纠正首版文案误导) ②排除=该书永不再见, Kimi 按排除数补位提案 (1+N, 上限3), 提案方向避开被排除品类; ✕已排除可点击恢复 ③选定=落绑定开工。留存+排除可同列 (这本书不用但库里留着)
- **重调 (用户确认方案)**: "方向对但画面糙"单列回路 — Kimi 拿原配方+用户判词只修旋钮 (LoRA 换档/强度±0.1/质感词精修, 方向与名不变), 单列重渲 3 张 (~3-5min 后台 job), 板 created_at 刷新自动破缓存; kept 过的配方同步风格库
- **卡通红线三层 (用户令: 全链卡通, 禁真人)**: 镜头规划提示词 + 提案军规 + **确定性守门** (_BANNED_STYLE_WORDS: 真人/写实/照片/photoreal/realistic — 提示词字段命中即弃, 不靠 LLM 自觉)
- **三坑修**: ①图片缓存串档 — 轮次同名 style{i}_{j}.png 被浏览器喂旧图 (用户实锤"文字变了图没变"), URL 加 ?v=板时间戳 (发布卡同款药) ②板发现依赖内存 job, 后端重启失忆 → 改 board.json 盘读为事实源 ③hidden 属性被 display:flex 特异性压死 → 灯箱常驻关不掉, [hidden]{display:none!important} 一刀治
- **事故记录**: 加卡通红线时重启后端, 杀了用户正在跑的选型 job (9 张孤儿图, board.json 未落盘, 页面卡死) — 选型 job 无持久化, 重启=杀, 作业前须查 busy
- **《定位》板现状**: 排除 赛璐璐/塔罗/商务插画; NeonDrip 已留存入库; 新一轮板重出中

## 2026-09-22(二)｜风格选择板 — 动画风格选型操作方案落地 (真内容试镜 + 库轮询 + Kimi 新配方)

- **用户令**: 风格定夺无明确操作方案 — 补: 一段话规划 2-3 镜 → 库内风格轮流制分镜 → 选择哪种/找新风格; Kimi 出新风格提案 (提示词写好); 形成风格选择板。Kimi 只出配方 (样张≠产线能力: 云端画的本地 K2 复现不出), 样张全本地 K2; 讲书页按钮触发; 无绑定开工硬挡板
- **新服务** (`anim_pipeline/style_board.py`): ep1 总纲+灵魂三问 → LLM 切 3 试镜镜 (钩子/人物/概念, 禁文字元素) → 风格库全员 + Kimi 提案新配方 (军规: 卡通大类/题材适配/K2 可执行/LoRA 清单内实选或纯提示词) → 4 风格×3 镜=12 张 K2 渲染 (**同镜同种子**跨风格公平比, 串行占 GPU 锁与 k2/h3 批互斥) → board.json; 点选落绑定, 选中新配方自动入库 (json+参考图)
- **K2 覆写口**: `build_prompt/build_workflow` 加 style 参数 (显式配方覆写, 不走书级绑定) — 产线 run_shot 零改动
- **硬挡板**: `anim_service.start_phase` 无 风格绑定_{书名}.json 拒跑 (AnimStyleGateError 继承 Busy 走既有 409 通道, 路由零改动); 实测: 《定位》未绑定被拒 ✓ / HBO 有绑定放行 ✓ (误启的 plan 被产线幂等闸"已存在 shots.json"自拦, 零影响)
- **端点+页面**: POST/GET /books/{id}/style-board (+select +img 防穿越); `web/style_board.html` (进度日志+镜头×风格网格+点选绑定); 讲书页步骤3 工具栏加 [🎨 风格选型]
- **《定位》实弹**: 首板生成中 — Kimi 提案"复古漫画书_半调网点"(粗黑轮廓+印刷网点+波普, 实选 Krea2-漫画书Comic Book V2T2 LoRA@0.7, 与库内三维差异明确)

## 2026-09-22(二)｜单集 300→240s + 集数带 9→10 — 两硬指标驱动 (平均播放 1min 及格线 / 收藏最高权重)

- **用户令**: 单集别这么长、集数多一两集; 两硬指标 = 平均播放时长 (1min=及格线) + 收藏 (互动最高权重)
- **单集时长**: `_ep_target_duration` 老谭读书 300→240s (240s 集进度 25% 即过及格线; 摊薄单集=密度↑=收藏驱动; 3min 底线不动); `series_outline` 落库处硬编码 300 拆除改调 `_ep_target_duration` + 旧集行刷新 (6 老集 300→240 同步); 静读书 600s 不动
- **集数带**: 结构带 [5,9]→[5,10], K∈[3,7]→[3,8]; 导演提示词加时长锚 (单集 4min/工具 4 分钟内交付) + 宽域大工具按判定刻度可拆两件 (各有名字+刻度+动作, 禁同义凑数拆) + 概念两两不相交/切片段落独占硬校验预告; `facing._units_sys` 粒度同步 (5-8min→3.5-4.5min, 4-8 集→5-9 集)
- **总纲校验重修轮 (新增)**: 拆分约束加严后单发命中率降 (两连 422: 概念重叠/ep1 职能词/秘籍缺失) — `run_series_outline` 校验不过改带问题清单回喂 LLM 重修一轮再复检 (切片校验抽 `_check` 共用); 导演提示词补 ep1 秘籍=工具总清单 (不教工具但秘籍禁空) + 核心任务职能词
- **《定位》实弹重切**: rerun-outline 第 4 发过线 — 6×300s → **7×240s** (心智阶梯钩子/自测/关联/空位/重新定位/命名/行动册), units+hooks 联动重切 7, 旧总纲已备份 roadmap_backup_*.json; ep1/ep2 标题同挂"心智阶梯"可在讲书页手调
- **生效范围**: 新书全走新规; 《HBO》9×300s 在产线不动

## 2026-09-22(二)｜蒸馏断档根治两刀 — 云审计分块少喂 + 碎节合并装包 (《定位》3h17m 实锤)

- **病历 (《定位》epub 6.9M, 11802s 全链)**: 提取 133 块×75s=2h47m 占 84%; "提取完成"后静默 29min — ①Gate A 审计单包 90k (≈5万token) 进网关排队, kimi 超时换 qwen 再等 (实锤: 后端 CPU 0% + ESTABLISHED 指向 qwen 网关 + llama slot 空闲), 三级链最坏 ~30min 且旧版整包失败=全放行 ②编纂轮本地长生成本就零进度事件
- **刀① 云审计分块 (`audit_compliance` 重写)**: 池 JSON 切 30k/段 (90k→3段), 单段 180s 超时, 失败重试 1 次后**只跳过该段** (失败粒度远细于旧版全放行); 每段双播报 (`合规审计中 (云端 pro, 段 i/N)…`/`审计段 i/N ✓: 删X 改写Y 注意Z`); 段间按 orig 去重合并 (空 orig 孤儿保留), [:50] 帽不变; `distill_book`/`stage_gate_a` 两调用点同享
- **刀② 碎节合并 (`build_batches` 新增)**: 根因非书大 — 正文仅 13.8 万字, zhihailib 版切成 133 微节 (均 913 字), 逐节单调=每节付全价结构化生成; 相邻节合并 ~15k/包 (_BATCH_MAX), 包内 `〔第N节·章名〕` 逐节标记零语境丢失, 133 次→9 次; ≤40k 军规/超长章 _chunk_text 切分/微章<500 剔除全部不动; `distill_book`+`stage_parse` 同一刀 (chunks.json 结构不变, stage_extract 零改动)
- **编纂轮播报**: 入本地长生成前发一条 (10-30min 预告), 全链无静默段
- **验证**: py_compile 过; 《定位》实弹 133→9 包 (14-15k/包); 零丢失数学实锤 — 重组 121,648 vs 原文 121,400, 差 248=124 装包胶水×2 字符精确吻合; 合成边界 (36k 巨章独包/50k 切 40k+尾/尾片并微节) 全对; l0.py 已有自适应打包不受碎节坑
- **预期**: 提取段 2h47m→50-80min, 审计段 29min→2-4min; 后端已重启 (PID 22628) 双补丁生效; llama 全程驻留单权重设计不变 (用户令: 双 17G 实例=带宽白搭, --parallel 多 slot 才是正道, 本次未动)

## 2026-09-21(一)｜发布卡三件套去 LLM 化 — 大纲+讲稿程序调取 (用户令"完全不需要生成")

- **用户令**: 大纲 (books_story) 和讲稿 (books_content) 本就有现成内容, 调取即可; 即使要 LLM 也只能是这些内容的高度总结, 不是随便生成
- **修法** (`books.py` action=copy 重写, LLM 调用删除): 标题=黄金三秒钩子首句; 红字=主题破折号后段; 简介=讲稿首段直出 (剥【】标注/书名句/作者句/悬尾句, 行粒度贪心装≤122字+固定互动尾); 大字=页面输入框 (新增) > 存量, 不再 LLM 锻造; 标签=书名/拆书/商业思维/概念词/读书 上限 5。全部**子句粒度收口** (`_clause_fit`: 按，。；贪心装, 永不切词截半句 — 首版三处实锤: 标题截"是把能"/红字截"天花"/简介截"追流")
- **页面** (`publish_card.html`): 按钮改"一键调取文案" (秒回); 新增封面大字输入框 (1-2字, 留空不出海报); 红字/大字随 covers+copy 双向传递; 数据源标注"大纲/讲稿 (程序调取 · 零 LLM)"
- **验收**: ep1/2/4/5/6 五集实弹 — 标题 16-23 字完整子句 (ep1 "HBO 刚把信号铺满全美，增长就停了" 优于 LLM 版), 红字 7-19 字完整短语, 简介 102-118 字行边界干净, 标签 5 个含概念词 (分发优势/撞墙效应/发行窗口期/价值评估); 零 LLM 调用零延迟
- **红字保两行 (用户实锤 3 行, ep2/ep6)**: 双层修 — ①covers 兜底红字不再裸用全题 (主题含"——"整串上封面=三行, ep6 实锤), 改与三件套同源的破折号后段+子句收口 `_cover_sub_from`; 页面红字框无调取时置空走服务端兜底链 ②`render_covers` 浏览器内实测行数 (clientHeight/lineHeight), 超两行自动降号 3px/步到装下 (字号公式按理论字宽算, 真实字体略超即换行 — ep2 公式说装得下实际溢出的根修)。复检: ep1=2行(111px)/ep2=2行/ep6=1行
- **封面大字撤下 (用户令"没什么用")**: 入口 (页面大字输入框)、字段 (PublishCardBody.char / _poster_char 读写 / cover_p 返回)、模板 (`build_poster_html` 删除)、渲染 (poster job) 全链移除; 存量 cover_p.png 留盘
- **封面去逗号 (用户令: "同一份内容，凭什么卖五遍"→两行无逗号)**: `_hero_break` 重写 — 切点在**原文**找 (逗号即天然断行信号, 先剥逗号再找会抹掉切点 — 首版实锤"同一份内容凭⏎什么卖五遍"), 断点逗号丢弃/？！保留行尾, 行内残余逗号族 (，、；) 全清。六用例全中 (ep4 132px 双行无逗号/ep6 问号保留), 五集重渲染行数 2/2/2/2/1
- **全集补齐 (用户问"第三集为什么不生成")**: ep3 旧时代从未生成过封面, 此前批量圈集只含"已有封面"的 1/2/4/5+6 — 疏漏; ep3 页面直点已得新规封面 ("谁给你钱/决定你能做什么内容" 2行无逗号 ✓), ep7/8/9 补调取+渲染 (九集全齐, 红字 2/2/2/2/2/1/2/2/1)

## 2026-09-21(一)｜发布卡实测改版 — 封面层级翻转 (红字钩子=主视觉) + 标签上限5 (抖音主页截图实锤)


- **实测 (用户主页 1:1 网格截图)**: 0918 书卷封面模板在 404px 网格里层级倒挂 — 书名 118px→44px 可读, **红字钩子 52px→19px 糊** (卖点最先死), 品牌条 13px/角标 16px 死重量; 同格手搓黄大字封面 (钩子即主视觉) 一眼可读, 模板完败。另: 文案锻 8 标签, 抖音上限 **5** ("最多可添加5个标签"截图实锤)
- **封面 v2** (`cover_render.py`): 红字钩子升主视觉 — 新 `_hero_break` (≤7字单行/其余两行均衡断标点优先, 字号=min(132, 内容宽/最长行)); 竖版钩子 132px (网格 49px/字) / 横版 116px; 书名降次级 76px (网格 28px); 钩子加 1px 同色描边增重; 安全区不动 (1:1/3:4 双裁均安全, 预算 616<900px); `_manual_break` 删除
- **标签 5 化**: 生成提示词 6-8→**5** + 硬切 [:5]; GET 出卡对存量 6-8 标签历史稿同样裁 5 (DB 原文不动); 页面标签行注明"抖音上限 5 个"; 红字提示词升级为"封面主视觉大字, ≤12字, 网格缩略一眼可读"
- **重打样**: ep1/2/4/5 用各自存量钩子重渲染 (旧版留档 cover_v_v1_0918.png); VLM 缩略对比验证 — 新版钩子网格一眼可读/旧版不可辨/书名仍可读/新版更抓眼; 服务端已重启 (PID 3852)
- **v3 复审令 (用户: 第N集太小/书名太小, 要与红字同级)**: 书名与钩子同尺寸公式 (ep1 双双 121px, 仅黑/红色差), 角标 50→64px+厚垫, 品牌条 34→40px, 安全区左右收 130→110px 腾字宽; `_hero_break` 升两行贪大规则 (单行 ≥cap85% 才单行, 否则两行吃满 132); `_scale_px` 删除。溢出体检: 安全区外四带深色像素 0.00%; ep1/2/4/5 已重渲染, VLM 网格验证三元素全可读零挤压
- **同日缓存坑 (用户实锤"没看出来变化")**: 封面 URL 无版本参数 + FileResponse 无缓存头 → 重打样后浏览器一直吃旧 PNG (磁盘已新)。GET/POST 出卡封面 URL 均带 `?v=mtime` 破缓存
- **v4 复审令 (用户逐条: 去外围线框/书名占角标位/角标下底加大/老谭读书64/红字三行bug/横版去书图标)**: ①双层金线框删 (旧框位金色像素 0.000%) ②书名移左上原角标位 (116px 黑) ③第N集大角标移底部、老谭读书条之上、加大到书名同号 116px 深棕圆角章 ④老谭读书 64px (原角标号) ⑤**红字两行变三行根修** — 7字×121px+字距+描边=861>860 安全区宽贴线溢出换行, 定号统一乘 0.96 安全余量 (ep1 红字行带实测 2 行) ⑥横版虚线+书图标装饰删 (`_BOOK_ICON` 删除)。溢出体检四带 0.00%; ep1/2/4/5 已重渲染, VLM 网格验证布局符合逐条令
- **微调连发 (用户逐条)**: 品牌文案 "老谭 读书" → "老谭读书" (中间空格删); 横版全文字居中 (书名行质心 960=正中); 竖版书名居中+下挪一行 (y500→635, 质心 540 零偏差); 横版书名下挪半行 (y→186); 书名套《》 (模板层数据不动) 且**书名号按半宽计** (`《》` 负边距压至 ~0.46em + 书名行加宽出安全区 40px/侧, 线框已删无越界) — 按全宽算 7 字书名被挤到 93px (用户实锤"太小"), 半宽计回到 116px; 负 margin-left 会扰 flex 交叉轴居中 (实测偏 19px) → 只用 width 让 flex 对称外溢, 双向质心 539/959 复零; **叠字根修** — 对称负边距把正文拉进括号墨迹 (用户实锤"书名号和书名叠在一起"), 改只压括号外侧空半边 (`《`压左/`》`压右 -0.3em, 内侧零压缩永不叠字), 净省 0.6em + 行宽 860+120 → 书名 114px; 像素体检单墨带 (VLM"两行"系误读红钩子行)。每轮重渲染+像素体检+重启
- **口径**: 封面按 404px 网格缩略设计优先, 全尺寸次之 (主屏即考场); 用户已定时发布的旧封面不追改, 换封面走重新打样

## 2026-09-21(一)｜对齐落盘无限打回根治 — H3 封顶槽溢出被当漂移清视频 (ep6 s30b 用户实锤)


- **复现**: ep6 页点"对齐落盘" → 已成片的 s30b 视频被清回图 (anim_done→approved)。diff 实证槽/narration/beats 全程未动: 槽 10.44s / beats 10.0s 恒差 0.44s — 0916"状态型自愈"块 (`span > dur+0.3` 清视频) 每次对齐必中; 重生成视频仍是 10.0 (H3_MAX_SPAN 封顶) → 再点再清, **永不收敛**
- **根因**: 自愈阈值 0.3s 与两处设计打架 — ① H3_MAX_SPAN=10.0 注释明写"超出草稿定格吸收", 封顶溢出是设计内非漂移; ② 草稿端 0918 微降速拉满带宽 0.6s (s30b 1.03x 拉满, 不可感知), 0.44s 差额草稿本来就无感吸收
- **修法** (`anim_draft.align_episode` 自愈块): 清视频条件改 `_dur < CAP 且 _span > _dur + 0.6` — beats 已在 H3 上限的槽溢出豁免 (重生成不变长, 清了永不收敛); 未到上限却槽长出 >0.6s (超微降速带宽) 才真漂移打回; 品牌镜豁免照旧
- **数据修复**: s30b 从 bak_brandpass 备份回填 video_file/status/gen_narr_sha (mp4 一直留盘, 零重生成, 另留 .bak_restore 备份)
- **验证**: 后端重启后真弹重放同按钮 (POST /align) — s30b 保住 anim_done+视频, 63/63 镜全有视频, 时长核验 0 问题, 时间轴零变动 (幂等)

## 2026-09-21(一)｜品牌镜三连修 — 手标品牌哑标根治 (直通定稿卡换片态) + 仪式句漂字漏标容错 (ep6 s56 用户实锤)


- **实锤**: 用户手标 s56 品牌后发现"不能生成视频、也不能直接插入视频" — 两层根因: ①**漏标在先**: 剧本冻结句"多一个硬本事"被导演规划 LLM 转写成"涨一个硬本事", match_rules 全含匹配 miss → s56 被当普通镜生成了内容场景视频 (s15 同句未漂 → 正常直通定稿卡); ②**手标=哑标**: 已完成镜打品牌标后, 装配端"生成片优先"旧视频照用, brandPass 按钮又只对 status≠anim_done 显示 → 卡上零出路
- **修① match_rules 容错** (`anim_pipeline/config.py`): "多/涨一个硬本事"→"一个硬本事" (只松动词); 新增收尾 CTA 兜底规则 `["我是老谭","咱们下一集"]` — 全库扫描再抓两处历史漏标 (ep2 s33 "读一本，翻一座山咱们下一集" / ep4 s89 "点点关注咱们下一集"); 身份词"我是老谭"系统注入专属 (稿内禁写) 不怕误报。7 用例回归 PASS (含预告句无身份词不中)
- **修② 直通定稿卡换片态** (`anim_service.brand_pass_shot` + `web/anim/card.js` + `actions/shot.js`): anim_done 品牌镜若 video 是生成片 (非 `_资产` 定稿卡) → 按钮照显, 一键换片 生成片→定稿卡 (零 GPU, 原 mp4 留盘, shots.json 自动备份); 已是定稿卡才 noop; 前端按 `video_file 含 '_资产'` 判显, toast 区分 直通/换片/noop
- **落地**: s56 已换定稿卡 (`品牌卡_登山客_赛璐璐_v2.mp4`, 5.17s 入 5.69s 槽 → 微降速拉满同 s15), 幂等复点 noop ✓; 服务端已重启 (PID 8644)
- **口径**: 品牌镜设计 = 永不烧 GPU (0920 定稿), 想要生成视频走 🗑剥品牌标回普通队列; 挂任意自定义视频的入口暂无 (要可加)

## 2026-09-21(一)｜装配双 bug 根治 — overlay 大字轨弃渲 (零星孤岛字幕) + 字幕星光闪闪禁令 (ep6 用户实锤)


- **bug① 零星多生产字幕单独一轨** (ep6_0921_1637 实锤: 蒙对了/实/养/走不通): 全部 4 条 = 0920 浮字军规下沉的 overlay 条目, ep6 命中率 4/4 全垃圾 0 概念词 — ①s26 `实`: 计数竞争落选 (每镜至多1浮字, `虚`先占) 被下沉成孤岛大字 ②s30c `养`: 拆镜残片 0.12s 窗 (7.72→7.84) ③s37 `走不通`: 军规下沉分支只查 overlay kind 查重, 已登记 impact 又追加 overlay = 同字双条 (② keep 分支 0920 已修三 kind 查重, ① sink 分支漏修) ④s24 `蒙对了`: 3字动词短语下沉, LLM 浮字词汇实产全是叙述片段而非设计假设的概念词
- **修法 (装配层弃渲)**: `anim_draft.py` 删 overlay TrackSpec + `_render_overlays` + `_collect_text_events` overlay 桶 (返二元组) — 剪辑层不再渲染大字轨; `director2._gate_motion_text` ① 分支与"同句共沉兜底"补三 kind 查重 (hero_number/impact/overlay, s37 同字双条根修), 军规剥 motion 照旧; text_layer overlay 条目保留**数据层** (红色条目仍驱音效 cue `_sound_cues`、短字仍是 `_ensure_motion_text` 回填候选、留档), 音效照挂教义不变
- **bug② 字幕星光闪闪动效出戏** (实锤行 "一份难看的数据报表背后，藏着另外3本资产账"): 命中链 = 导演强调词 `3本` 锚进字幕行内 → 词型动效"数字→星光闪闪"; `_CAT_ANIM` money 类同配方。**修法**: `_render_captions` 动效决策后统一降级 `星光闪闪→放大` (0921 禁令, J线 money 配方在动画线不适用; J线 auto_choreo 本体不动); 卡拉OK首条/悬念上滑/金句放大/渐显基础入场全部保持
- **验证**: `.tmp/test_draft_fix_0921.py` 10/10 (overlay 不进渲染流/数字+money 双路无星光闪闪/首条卡拉OK保持/军规三kind查重/无登记沉字仍写数据条目/ep6 实数据零 overlay+强调锚保留); 沙箱 E2E 全装配 (LOCALAPPDATA 重定向, 真实剪映目录未动): 63/63 真材、captions 86/0 丢、`overlay_texts 0`、draft_content 全文零"星光闪闪"、独立文本段零孤岛词、动效块 88 (纯合法配方); 服务端已重启 (PID 20552)
- **用户动作**: ep6 重新装配即得净稿 (同名覆盖; 手改过的 _0921_1637 不受影响)

## 2026-09-21(一)｜动画工坊 — 重做购物篮(列队job) + H3 软超时判卡 + 0919-0921 工作树归档


- **重做购物篮 (用户令: 类购物网站多选)**: 卡头勾选框 (busy 隐藏与动作钮同闸) + 底部悬浮篮条 `♻️列队重做`; 勾选态存 localStorage 按书+集键控 (F5 不丢, 板重渲染回画+prune 死镜号); 新 `web/anim/cart.js` (sel 不进 store — 勾一格整板重渲染太重)
- **列队 job (后端)**: `POST /ep/{id}/{ep}/redo-queue {sids}` → 新 phase `redo` → `_phase_redo`: 按时间轴序逐镜复用 `replan_shot` (每镜自动备份/失败回滚), **单镜败×2 只跳过不炸整队** (用户定案), 成功集改道 `_phase_gpu` (only=成功集, chain_h3) 一条龙 K2→自动过审→H3; 时间纪律/政治禁区闸门照走; redo 入 GPU_PHASES 全程持锁与 k2/h3/brand 互斥。验收: mock 单测 3 用例 (排序/跳败/取消/全败不烧卡) + 假镜号实弹 job (镜不存在汇总, 零 GPU) 全 PASS
- **H3 软超时判卡 (用户令: 3min 没交货=卡住)**: ep6 s39 爬行模式实锤 (GPU 88% 在算/VRAM 48.5G, tqdm 步进不落盘假象冻结) — 旧机制两洞: ①240/s 超时公式 (~28min) 陪爬行干等 ②超时不 /interrupt, 弃 prompt 占队列头 → 后续镜连环超时=整批卡死。**修法**: `comfy.run_workflow` 加 `soft_timeout_sec=180` (旋钮 `comfy.soft_timeout_sec`) → 到点 POST /interrupt (步间生效, 爬行单步可达分钟级) → 宽限 `interrupt_grace_sec=180` 等中断入 history → 仍无果弃等, 该镜 anim_fail 降级静态图 **批队列继续** (⚡连带重试败镜可救); 硬超时公式保留兜底; 不传参=旧行为零扰动; K2 未接 (opt-in)。实测 /interrupt 对爬行有效 (ep6 救批实弹); mock 4 用例全 PASS (软线→中断→宽限→弃等 / 中断快返 / 旧路径 / 0=禁用)
- **杂项**: 任务面板 phase 中文标签; toast 让位篮条; 三元陷阱备案 (`soft_timeout_sec=0` 走 None 分支=禁用, 非即触发)
- **归档说明**: 本 commit 一并落盘 0919-0921 积压工作树 — anim_rf 模块化重构 (anim.html -837 行 → web/anim/ 16 模块 + CSP enforce; 旧页迁 web/anim_rf/), hook_zone 全片装包制, TTS atempo 三键旋钮, 品牌镜单镜直通 brand-pass, 卡片循环定位, 智谱引擎轮流, restart_backend 三连加固, 封面渲染 cover_render / publish_card, HEALTH_REPORT (0917 审计) — 详录见 CHANGELOG 0919-0920 条目与 docs/improvements/进行中-*

## 2026-09-20(日)｜智谱搜索切付费资源包 + 引擎自动轮流 — "一个用完用另一个"

- **背景**: 免费额度 09-12 到期后本地日期拦截全堵; 用户同账号充值 500次 Search-Std + 250次 Search-Pro + 250次 Search-Pro-Quark/Sogou (到期日控制台管理, 未提供具体日期)。同日用户令"轮流吧, 一个用完用另一个" → 引擎阶梯降级
- **配置** `config/app.yaml`: `zhipu.search_engine` 单值键 → `search_engines` 阶梯 (pro→quark→sogou→std, 旧单值键兼容); `free_quota_expires` 留空 = 关闭本地日期拦截, 填日期仍可拦截 (机制保留)
- **`app/services/zhipu_search.py`**: `_search_once` (原调用循环参数化引擎) + 主函数阶梯轮换 — 当前引擎**额度类报错** (`_QUOTA_KEYWORDS`) → 自动切下一引擎重试同查询; 活跃引擎落盘 `data/zhipu_engine_state.json` (重启不忘, 全灭落 None, **充值后自动恢复** — None 从阶梯头重扫, 成功即重新锚定); 非额度错误 (网络/业务码) 不轮换照旧直抛。`_quota_message` 兼容空日期
- **前端/文档**: `web/index.html` 素材面板提示改轮流口径; 项目状态总览 / README 同步
- **验收**: `.tmp/test_engine_rotation.py` mock 5 用例全 PASS (耗尽切换/直达活跃引擎/全灭报错/充值恢复/非额度不轮换); 实弹 search_pro 2 条 + search_std 2 条 (资源包生效, 状态文件锚定 search_pro); 后端重启后端到端通
- **同日修 bug (用户实锤 "搜索提示: 'InstrumentedList' object is not callable")**: `material_service.search_and_ingest` L478 `package.items()` 把 SQLAlchemy 关系属性当方法调 (0912 04c5e86e 引入, L481 同函数内写法本就正确) → 去 `()`. mock 冒烟: 去重+入库 1 条 PASS; 全仓扫无同类雷。**两轮"还是报错"实为僵尸后端**: 修复进程从未上线 — 见下条
- **`scripts/restart_backend.py` 三连加固 (0920 事故链: 假成功×2 + 连环误杀×3, 全因 54321 上僵尸进程)**: ①杀旧 = 命令行 CIM 匹配杀 + 端口归属补杀双保险 ②拉新前**等端口真空闲** (最多 15s, 7s 处再补一刀; 大应用退出 >2s, 抢跑=新进程 10048 绑定失败退出) ③健康后**端口归属核验**: 接管者 = Popen 进程**或其子进程** (`.venv\Scripts\python.exe` 是 trampoline 跳板, 真监听者是派生子进程 — 初版只认 Popen PID, 把每次成功误判假成功→连环误杀好后端)。绑定失败/归属不符 → 明确 exit 3/4 + 手动处理指引, 不再假成功



- **用户决策链** (讨论定案): ①LLM 规划图不管时长 ②影片总长=音频时长恒定 ③H3 本就受时长约束 (duration_s×24fps→17帧对齐→length, 0916"生成时长=语音长度"铁律已在) ④音频时长直入+LLM 零时长决策 → 特区 DP 装包推广全片。GPU 账修正: H3 总生成秒数≈恒定, 镜多只加 per-call 开销 ≈+8% (非 +40%)
- **装包层** `hook_zone.py`: ①`_absorb_short_units` 新增 — 短单元(<2.5s)并邻+超带(>ELASTIC_HI 6.3, 与 SPLIT_SENT 6.5 的阈值缝对齐)逗号再切+切后短片并回大侧, 两轮收敛; 三个坑全片化才暴露 (特区窗起=片头0从未踩): <2.5s 完整短句 12 顶格罚级联全并巨槽 (A3 曾 1 槽 65.4s / A2 曾 1 槽 29.6s 双实锤) ②`enforce_scene_shots` 新增 — 普通槽执法回填 (打 `packed` 标); zone 执法加槽号过滤防误收
- **plan_scene 换心** `director2.py`: 特区外余窗同 DP 装包 (`slot_mode`); 提示词加普通镜槽表 (slot 制输出禁时间); refs 协议退役为兜底 (sent_table 空时); 锚定/塌缩/碎镜合并段 slot_mode 短路 (槽位即时间, s08 65.5 洗槽同机理); `_build_doc` 透传 zone/packed/scene_tag; 整集 `_realign_narrations` 组/镜双层跳过 packed (防包边吸附洗槽位); 两处"句级失败回退包级"改硬错 (包级吸附=剔镜+组窗漂移破坏模式, ep4 实测剔 7 镜)
- **ep4 实弹验收** (79 镜 planned, 零 GPU): 均 4.43s/中位 4.1 (旧制 50 镜均 7.0, 四集漂 5.2-7.1 根治), 8s+=0 (旧 18), 碎镜=0 (旧 3), 缝仅开场卡, 末镜对账 355.17=tc+349.67 ✓, packed 67+zone 12 全系统槽; plan_warnings 仅 1988 风格词设计内提示
- **ep4 前置修复 (同日)**: ①plan_arcs 模块分支 `scene_list` 未定义 NameError (ep4 首踩, ep2 走旧路径) — 补窗列表构造 (口播全文不截断); ②pyflakes 全树扫 3 雷: shots.py reroll 台账 `_re` / planner `_v1_plan` 缺 director2 / _transcribe WhisperModel TYPE_CHECKING; ③S10 组界缝 2.07s 根修 — zone 让位段把首镜推到特区尾 65.5 + 死静音(包4 内 56.7-59.15 恰在缝区间)双重叠加, 重跑 align(句级)+守卫补跑(2 处 3.04s 裁 0.6)+manifest 重写+再 align, 时长核验 0 问题; ④plan_scene 拼回 `groups.module_id` 透传 (expand_refs_groups LLM 组重建丢字段, ep4 存量回填 8 组)
- **风格库**: `国风chibi_3D.json` 入库 (体外 img2img 测试定稿: K2 模型链+LoadImage/VAEEncide+start_at_step 表达 denoise; 2D→3D 风格翻转 denoise≥0.55 成立/0.15-0.3 仅润色; 参考图 2 张随库) + `DEFAULT_STYLE_NAME` 切换新书默认 (存量 HBO 锁赛璐璐不动) + 风格库 README (军规: 卡通大类内每书一皮肤逐书变) + 战略令记忆化
- **遗留观察**: 逗号切点使槽 narration 句读吊尾 (s13 "你的内容,") — 字幕半句观感若实锤, 下刀=槽切点优先吸附句末; ep4 79 镜待 K2 (~20min GPU)

## 2026-09-19(二)｜系统加固 — 死静音合成守卫 + 陈稿警示 + 孤儿回收扩容 (根因级, 用户令"加强系统能力")

- **触发链**: ep3 剪映时间轴乱象三源排查 → ①ep3 稿件前夜被重生 (新 script `24e8d0b3` + 音频全新随机重合成, 新 take 001 抽中 **3.98s 包内死静音** — 两连发实锤 IndexTTS 随机病) ②用户回听的音频页 URL 挂**旧稿** `7e2b3178` → 听的修过的旧 job, 产线吃带病新 job (页面/系统两套文件) ③装配时 IndexTTS2.5(7866) 未在线 → 开场人声链跳过 + `_as_video_path` 缓存键不含时长 → 人声版 reveal 窗 2.8s 命中无声稿 2.0s 缓存件 → 截取越界炸稿
- **A 死静音合成守卫** `lines.py::_trim_long_inner_pauses` (根因级): 合成链 loudnorm 后、气口引擎前 — 此刻包内所有静音皆模型产物 (有意垫的还没进, 零误伤); 50ms 帧 max<峰2% 判静音, 中段 >1.5s → 样本级压到 0.6s (切口 5ms 淡入淡出), 头尾不动 (归一/尾审计管); manifest duration 同步; 失败不阻断产线。`.tmp/test_silence_guard.py` 8 用例全 PASS
- **B 陈稿警示** `audio.js`: URL script_id ≠ 该集当前稿 (`/books/{id}` episodes.script_id) → 顶部红条"此稿已被新稿取代 + →去听当前稿"直达链接 — 页面/系统两套文件的洞从入口堵死 (检查失败不阻塞)
- **C 孤儿回收扩容** `anim_service._adopt_orphan_videos`: approved 之外也收 **anim_fail** (无 video_file 的) — H3 假死 (超时判死但 ComfyUI 实际跑完) 的产物自动回家, 不再手工领养
- **D 作品修复** (ep3): 001.wav 3.98s→0.6s (样本手术) + manifest/DB/combined 同步 (293.96s) + 特区 15 镜文本锚定重定时 (音频编辑后槽位跟轴, s65/s66 逗号切分件刚性切) + 全片对齐零缝 + 拉起 IndexTTS 补开场人声 (intro_1/2 落盘) + 草稿重装配 ✓ (`_as_video_path` 缓存键掺时长)
- **遗留观察**: 特区镜 4 枚视频超槽 (s67/s79/s82/s80, +0.86~1.75s, 草稿截前段吸收); 音频后编辑的特区重定时目前是手术流程, 未自动化 (低频路径不预造轮)
- **E 剥链误杀修复 (同日第四案: 页面视频"丢失")**: 音频平移后对齐重切 narration → `strip_media_on_narr_change` 规则①"文案变→媒体失效"把 **句挪边** 当内容变, s68/s18/s33 三枚成片视频被剥链接 (盘上文件完好, 57 文件 vs 52 链接对账现形)。修: 规则①加**相似度容忍** (归一文本 difflib ≥0.55 = 边界重切保媒体, 时长规则②接管; <0.55 = 真文案变照剥) — 回归验证 挪边 0.78 保/reroll 0.05 剥; 三镜已领回 anim_done (55/56, s09 品牌镜待定), 零缝
- **F GPU 跨族清场 + TTS 超时自适应 (第五案: ep4 合成 "timed out", 用户判"少清场程序")**: 根因 = K2 批 03:44 结束后**模型栈驻留 39G 不清** (free_memory 只在相位开始时卸上一相) → 03:48 ep4 TTS 叠在 ComfyUI 栈+llama-server 上跑 → 10.4s/步 (日志实锤, 单包 4分08秒) → 300s 硬超时整 job 炸。修: ①`GPUServiceManager.session` 拿卡后起服务前**跨族清场** `_clear_gpu_neighborhood` (TTS 起→POST ComfyUI /free 卸模型栈进程保温; ComfyUI 起→停托管 TTS; 失败静默) — 补上 `_ensure_running` 互斥的两个盲区 (健康快返回 + 外部拉起的 ComfyUI); ②`_fetch_indextts_audio` 超时按文本量伸缩 (300+2s/字) + 超时单次重试 (断点续传只救当前包)。**初版双 bug 用户实锤"没起作用"再修**: specs 值是 ServiceSpec 对象非 dict (`spec.get()` AttributeError 被 except 静默吞 = 清场从未执行) + TTS 族硬编码漏 indextts25 — 改属性访问 + 族从 specs 动态取; 实弹复验: indextts/indextts25 会话均打 ComfyUI /free 200, 4090 47.4G→9.3G。llama-server (9B Q8, 昨 01:22 起) 为用户自起, 提示未动

## 2026-09-19｜60s 特区 — 首分钟流量池镜槽预装 (所有视频的前60s) + anim 页特区横幅

- **用户令**: a1=每视频前1分钟; 口径定死 **"1分钟"=正文音频轴 0-60s (不含剪辑开头5.5s)**; 平台推荐机制锁定 → 冲平均在线时长, 不按正规视频节奏: 每镜 ≤5s (3s 底线/6s 上限), 组内场景禁重复 (连环镜头场景不一致), 画风仍锁圣经; 首帧复杂度下限 (简单首帧=简单动画=观众划走); 剪辑层特效单元 5.5s 为节奏锚
- **镜槽系统预装** `app/services/anim_pipeline/hook_zone.py` (新): 场窗∩正文[0,60s) → 句级单元 DP 装包, 目标带 [3,6] 偏好≤5, 弹性带 [2.8,6.3] (两集实测: 破界≈每集1镜, 全落弹性带); >6.5s 长句句内逗号切+静音吸附 (sent_table 增 `file/pack_t0`); **时间轴零 LLM** — 导演只填画面 (0917 refs 哲学贯彻到特区)
- **plan_scene 接入** (director2): 特区宪法注入 (覆盖宪法"场内共享场景"条款: 相邻镜换地点/环境/母题, 只换机位=废稿; 组 keyframe 写蒙太奇主题); slot 输出制 (特区镜带 `slot` 槽号, 禁 t/narration/refs); `enforce_zone_shots` 数对槽校验 (不符→重试网); 特区镜重组为场头独立组; refs 只展开槽后余窗
- **scene_tag 硬执法** (首轮实弹教训: 时序全对但 8 镜全同场景、s50/s52 逐字克隆 — 提示词无牙): 每特区镜强制外化 `"scene_tag"` (2-6字场景名), `zone_scene_issues` 确定性审计 (缺标/组内重复→拒稿重试); 宪法升"场景先行"(N槽=N场景, 旧版场景串禁沿用); 批评轮注入特区铁律; 页面 🔥特区 徽章带场景名, 同名重复肉眼可见
- **槽位神圣三级保护**: ①窗口对齐 needle/比例/钉头尾后原样恢复+邻镜让位防重叠 ②碎镜合并不吸收特区镜 ③`_realign_narrations` 特区镜旁路 (不按包边重切); `_finalize_doc` 盖 `hook_zone` 窗章 (全集 [tc, tc+60])
- **页面前端** (anim.html): 开场剪辑层下红边 **🔥60s 特区横幅** (窗/镜数/均长/最长/槽位预装数/未装槽警示) + 镜卡 🔥特区 徽章 + 动线行特区说明; status_view 增 `zone` 字段
- **实测** (ep3 A1: 5镜 7.5/9.9/3.5/5.8/6.0 → **8槽 3.1~5.7s**; ep2 A1: 10槽 2.9~5.9s; ep3 全域 14槽/60.6s 全带内; 6.5s 长句切走); `.tmp/test_hook_zone.py` 21 用例全 PASS, 五文件语法过, 页面 JS node --check 过
- **实弹二连**: ①ep3 A1/A2 重规划落地 — A1 首稿被 scene_tag 审计拒(3 处组内重复)重试后 15 镜 15 场景全窗零缝; ②K2 启动被 0916 时间纪律闸门拦 (逗号切点偏句边 3.35s/60s 硬界偏 1.09s) — `timeline_violations` 把 zone 镜槽位边界并入合法边集 (装包器产物=系统有意切点, 非 zone 镜仍严格句边), 修后 0 违规 K2 放行
- **H3 繁体隐形bug根治 (用户实锤: 中文文字出场爱漂繁体, 繁体笔画动画易错)**: 根因=风格锚 "**Traditional** 1988 cel animation style" 被模型读成 Traditional Chinese (且 `build_workflow` 调 `build_prompt` 未传 book_title → 全量镜都吃默认串, 全量中招)。修: ①`style_h3` 出口净化 traditional→hand-drawn ②`build_prompt` 整段出口净化 (旧镜 anim 字段规划时已烙 traditional, 连根拔) ③TEXT_POLICY 加简体铁令 (繁體 FORBIDDEN) ④config 默认+圣经缓存 style_prefix 同步去词。s77 实测: prompt 零 traditional+简体令在
- **字级钉死 (二连升级, s66 实锤: 净化+简体令后仍出「誰」— 泛指令劝不住)**: `build_prompt` 把 beats 将上屏的中文字符逐个圈出点名照抄 ("the ONLY glyphs permitted: 谁、付、的, stroke-for-stroke" + 讠旁正反例) — 模型对面前摆着的字形服从性最高。负提示词通道经 object_info 实证不存在 (MiniMaxH3 节点族无 negative 输入, turbo 蒸馏无 CFG), 正条件负句式=唯一等效通道
- **H3 长镜假死根治 (s11/s12/s21 两轮 600s 超时)**: ComfyUI history 实证三镜 **execution_success** — 全跑完了, 是 600s 定额太紧 (6.9-8.8s 长镜在低显存节点帧数放大超线性), 后端等不到判定死。孤儿 mp4 从 comfy output 手工领养回 anim/ (7.3/9.4/8.7s 对槽 ✓) → **56/56 anim_done**; 超时改按时长伸缩 `600 + 90×span`
- **特区磨合闭环确认 (用户认知纠偏)**: 🎲单场立意重掷不清任何产物 — 重做特区 = 🎲(弃意象 direction)+🔁重规划+🎨K2 ~5min/轮; ep3 A1 四轮迭代实证 (写字台同场景→scene_tag 执法→母题轮换→闸门隐喻换血, 用户逐轮验收 "可以")
- **遗留**: ep3 A1/A2 replan 实弹验收中; K2/H3 密画面败镜率观察 (特区镜数 +60%, 全局负词+人检门兜底)



- **入口**: 拆书讲书页每集行 📋发布卡 按钮 (替换"钩子三版"位) → `web/publish_card.html?book_id&ep` — 数据全对应本集, 非通用物料
- **三件套**: `POST {action:'copy'}` — LLM 只做文案 (**标题≤24字/封面红字 cover_sub≤14字单行悬念句/简介/标签6-8个**), 输入=本集真实底料禁编造; 封面红字参考"内容平台如何完成惊险一跃"质地 (ep1 实锻: "渠道铺到尽头，就是天花板")
- **封面**: `{action:'covers', sub?}` → `book_service/cover_render.py` **HTML 模板 + Playwright 截图** (模板化=可控, 不走 LLM 生图; 页面红字输入框可手改, 优先级 手改/cover_sub > 主题 > 钩子)
  - **样式正源=用户提供参考图原始提示词** (渲染图自身有漂移): 米黄宣纸纹 (双层 feTurbulence, 净底色 #F4E8CF 亮档 — 三版教训: 暗化层过量=土黄脏纸, 旧纸感来自颗粒非压暗) / 细浅棕双层框 / **左上圆角深棕标签白字+其余全居中** / 砖红副标 / **浅棕圆角实底品牌条深棕字** / 虚线+书图标
  - **安全区**: 按提示词"上下各留 25%、文字全放中间 48%" — 五元素坐标实测 y 524→1386 (27%~72%), 天然免疫抖音主页 3:4 裁切; 横版 1920×1080 同风格
  - 像素取证定案: 标签框实底深棕 (77% 内部深色, VLM"描边框"误读) / 主副标跨对称=居中 (非左对齐) / 渲染图品牌栏 90.7% 违反自身安全区规范
- **产物**: `outputs/动画/{书}/ep{N}/publish/cover_v.png|cover_h.png` 经 `/api/anim/ep/.../file/` 防穿越路由, GET 回填, 页面📥下载+点击灯箱放大
- **真 bug 根治 — "点了没反应"**: 页面 JS 用隐式 id 全局 (`btnCopy`), render() 重建 innerHTML 后命名属性未重注册 → ReferenceError 静默炸、零请求到站 (window.onerror 抓到实锤); 改显式 `getElementById` 根治, 两按钮真点全链复验通过
- **验证**: 版式四轮迭代 (居中→左对齐复刻→提示词正源) + 像素直方图对账 + 坐标实测; 浏览器真点 ✍️→cover_sub 自动填框→🎨 出图全闭环; 新增行 ruff 零新增

## 2026-09-17｜模块总线 — 六拍结构贯穿文档→TTS→切场→分镜 + refs 协议终结 LLM 自报时间 (A~D 代码完成, 待 ep3 验证)

- **触发**: ep2 动画"一句一镜/生成浪费/文字被截"两轮质问 → 数据核账推翻听感 (s01 语音实测 4.45s 非听感 2s; 本批 H3 浪费实测仅 4%=帧对齐零头; 文字截断=13:37 重置前旧批 beats 漂移, 当批已拉齐) → 真问题浮出: **四层结构断裂** (文档六拍标记被人工修稿整稿覆盖洗掉=ep1/2 裸稿元凶 / TTS 字数贪心切包 / LLM 自由切场 / 分镜自报时间被三层兜底覆写 — "放着现成对标不用")。用户定案: 六拍模块=全链唯一结构总线, 案卷 `docs/improvements/进行中-20260917-模块总线-*.md`
- **A 文档**: `module_map.py` (标记解析器, tail 锚=模块末行文本, 任意文本形态重定位免疫行号口径) + `Episode.module_json` 落库 + 生成侧 <6 段进打回链 + `edit_episode` 丢标防护 (元凶入口堵死: 保留旧表+warning) + **排雷** `_LABEL_RE` 空格变体 (`【0-28 秒｜钩子】` 旧口径剥不掉 → ep3-9 进产线会被 TTS 念出, ep3 真稿回归覆盖)
- **B TTS**: 模块墙强制断包 (复用情绪墙 walls 机制) + 模块尾气口 1.5s (取 max 不叠加) + manifest 每包带 `module_id`; 墙定位下沉 TTS 入口 tail 锚现场做 (tts_adapt **之前** — 拼音标注改文字致锚失配), 墙是派生值不落库 (物理行≠Segment 行口径坑)
- **C 规划**: `module_windows` 场界=模块界 (单模块>70s 按包界分窗) — plan_arcs 模块化分支 LLM 零时间输出只填隐喻, arcs/groups 系统定界; **refs 协议** — manifest 挂 `sent_table` (包→句静音吸附, 0916 归真设施升格公共), plan_scene 给场窗句级编号表, LLM 只出 `"refs":"c01-c03"` 禁 t_start/t_end/narration, `expand_refs_groups` 查表回填 + 确定性修复 (越界 clamp/漏句并邻镜/乱序排序) — 平移新闻线 segment_refs 已验证模式 (0916 token 降 ~70%), H3 侧"LLM 不发明结构"哲学终于贯彻到规划侧; refs 不可解析整场回退旧路径
- **D 执法**: 密度下限 4s 确定性并镜 (refs 展开内置, LLM 想碎也碎不了); 时长核验表场级加模块列
- **兼容**: 无 module_id 旧 manifest → TTS/切场全走原路径零影响; 老库自动迁移
- **验证**: 4 新测试文件 20 用例 (ep2/ep3 真稿回归), 全套 **212 passed**; 改动行 ruff 错误 **0** (12 文件存量 101 不受影响); **未 commit** — 等 E 验证
- **遗留 E (待用户配合)**: 重启后端(迁移) → ep2/ep3 重跑 TTS(manifest 带 module_id) → 🧭 验收四点 (切场=模块场/refs 零漂移/短镜并镜/核验表模块列); ep1 裸稿恢复总线=补标记轮(LLM+人闸)待令
- **0917 晚增量**: ①标签口径放宽 (用户 ep2 存档无时间数字形态【钩子】/【核心概念拆解（一）｜撞墙测试】实锤 → 时间可选+副题可选+`_canon_name` 别名归一[「总结+下集预告」ep5 变体收纳], 顺手修雷【双评论钩子】旧口径剥不掉会被念) ②**ep2 用户存档四段稿落库** (1398 字+module_json 带副题, `.tmp/ep2_archive_putback.py` 真实解析路径进程外直写+幂等迁移) — ep2 重做从此与 ep3 同权吃全总线; 全库快检 ep2=4 模块/ep3-9=6 模块零异常/ep1 待补; 新增 `test_ep2_archive_modules.py` 6 用例, 全套 **217 passed**

## 2026-09-17｜代码健康化重构第一轮 — T1~T5-2a（REFACTOR_PLAN 立项, 五 commit）

- **立项**: 全仓勘察（服务层 175 文件 42,379 行 / 路由层 31 文件 8,635 行）→ `HEALTH_REPORT.md`（P0×3 / P1×17 / P2×12, 全带 file:line）+ `REFACTOR_PLAN.md`（20 任务 6 批, 顶部冻结基线）。行为不变 · 小步快跑 · 每步四连验证 · 用户逐任务发令+独立复算+手写 commit
- **T1** `f2f4210b`: evidence_service.py:397 `items()`→`items` 真 bug（关系属性当函数调, 失败测试指认）, 测试套 186→187 全绿
- **T2** `f2f4210b`: audio.py 6 处关键路径 `except:pass`→`logger.exception`（终态落库/manifest 回写/.df.json/verify_report; 不改控制流）
- **T3** `28e304f6`: pyproject.toml 落地 — ruff 显式 8 规则 + B008 immutable-calls 豁免（Depends/File/Body, 171 处全为 FastAPI 惯用法）+ audio.py S110/BLE001 挂账 per-file-ignores; real_api mark 注册消 5 warning; 基线冻结（全仓 657→后续 655）
- **T4** `587d07fc`(计划修正: 前摇实证范围 3→6 处) + `a4c169f9`: `_publish/_job_channels` **逐字平移** services/job_events.py（diff exit=0）, jobs.py re-export 瘦身, 6 调用方 import 换线, service→router 依赖倒置消灭; SSE 五页眼验过; 双总线合并降级 T16b（director_events 跨线程+128 vs jobs put_nowait+64 语义不同）
- **T5-2a** `c7d5bc4b`: SSE 公共工厂 `app/routers/_sse.py`（7 参, 心跳 Callable/订阅在调用方/request 可 None/prefetch）+ books.py 首端点换线（26→21 行）, 眼验过; **余 4 端点 stream/jobs/ppt/tagging 待续**
- **附带只读排查**: "生成音频自动开 Chrome"—音频后端链路/四 TTS 引擎(F5 inbrowser=False 未传)/前端/根入口全净; 头号嫌疑 hf_client 系统 chrome 注入（HF 渲染时段重叠, 非音频路径）; 验证法=弹窗时 `wmic process where name='chrome.exe' get commandline` 看 `--remote-debugging-port` 指纹

## 2026-09-17｜拆书线 🔊 TTS音频 改人工闸门 — 去自动生成 + 勾选记录留存

- **问题** (用户报): 讲书页每集 🔊 TTS音频 点击即自动派发生成音频任务, 绕过人工审稿勾选; 且每次点击都当新任务, 之前勾掉的内容不带记忆
- **根因**: `books_story.js::ttsPreview` 在 produce(preview) 后直接调 `POST /api/audio/scripts/{sid}/generate-audio?selected_only=true` (0912 稿→音一键桥) — 生成权放错了按钮
- **修法 (二轮, 用户截图实锤后定稿)**: ttsPreview 只做 produce(preview+**force**) 桥接 + 跳转音频加工页 — **每次点击都按当前稿件全新重建**: 删旧 Script 级联清 segments/audio_jobs/audio_files (旧勾选/旧时长/重排空槽/回听记录全清, 磁盘 wav 留底), 勾选与「生成音频」全在 audio.html 人工完成 (第一轮误读为"勾选保留", 截图 69-71 旧尾段+空槽残留实证用户要的是全新)
- **边界**: 同文件 `ttsEp()` 查为零调用死代码, 未动
- **验证**: node --check 过; 前端真实点击待用户验

## 2026-09-17｜改字四处同步根治 — 页面原文永旧/segments错位丢行/稿件静默分叉 (033实锤)

- **复现** (script 16732ac0 ep2, 033包 7行→10行编辑): 音频是新文, 页面/✎编辑器/字幕层永显旧文。三层根因: ①reroll 从不回写 manifest `text` (页面显示 audio.py:597 读 manifest、剪映字幕层同源) — 只回写 duration; ②Segment 同步按位置 1:1 映射 — 行数变了就错位覆盖+**丢行** (033 后3行从 DB 消失, 多轮叠加连行40-43都残留旧轮重复内容); ③Script/Episode 逐行 splice, 稿件无该行时**静默 no-op** (导演层永远拿旧文), splice 还会把新旧行混成重复段落 (稿件实查出用户行×4+旧版本×7 连体)
- **修法 (四处同步, `_sync_chunk_edit`)**: ①manifest text/inference_text 回写 (页面/编辑基准/字幕层同源); ②稿件**整块替换** (块失配→行级兜底+留痕, 不再静默); ③Segment **无损重排** (`_redistribute_lines`: 段数不动 — line_index 全局序是 manifest line_indices/显示分组地基, 多行末槽吸收 `\n` 连, 少行尾槽置空; 下轮 produce 从稿件重建自然归位); ④编辑基准改读 manifest text (旧版从 Segment 重组, 与显示不同源)。附堵"line_indices 全失配回退全稿段"地雷
- **验证**: 18/18 (真函数+临时DB): 5→8→3→4 行多轮编辑零丢失、稿件块级替换、邻包不受扰、停顿标记 manifest 保档/inference 剥离、manifest 缺条目/稿件重生成双边缘不崩
- **数据修复 (033, 备份 .tmp/repair_bak)**: manifest text/segments 33-39/稿件伤区 (Script+Episode) 全部归位为用户权威 10 行文本; 页面 API 已验证回新文
- 服务端已重启 (PID 50688)

## 2026-09-17｜-Xs- 停顿标记句中失效根治 — "负一S"直念三层失守 (033包实锤)

- **复现** (script 16732ac0 ep2, 033包 ✎编辑插入 `-1s-` 于 `就是A。`/`B档` 之间): 旧语法只认**行尾**标记, 编辑按钮 `insertAtCursor` 是光标处裸插 → 句中标记三层全失守 — ①reroll 剥离块以 `_pause_after>0` 为闸 (audio.py), 非行尾不剥 → 标记直达引擎直念"负一S"; ②批路径 `_tts_text` 剥离正则带 `(?=\s|$)` 前瞻, 标记后紧跟汉字不剥; ③行尾垫静音 `_pause_after` 也只认行尾, 句中不垫。**批路径行尾标记其实也漏** (`||` 拼接让前瞻失配, `_tts_text('…-1s-||…')` 实测残留) — 整个 -Xs- 特性除"reroll+行尾"一格外全线失效
- **语义升级** (系统级, 非逐包手调): `-Xs-` = **合成硬边界** — 任意位置生效, 每片独立解码, 片间垫静音 (停顿永远落在解码边界, 0914 教义泛化)。前置守卫 `(?<![A-Za-z0-9])` 防连字词误伤 (`MS-1s-config` 不动, `。-1s-B档` 生效)
- **四处落点**: ①text.py — `_tts_text` 无条件剥 (引擎零标记安全网) + 新 `_split_by_pause_marks` 切片器 + `_split_text` 堵 segments 路径; ②tts_lib/audio.py — 新 `_concat_wavs_with_pauses` (按片尾停顿拼接, 纯零垫不加淡入淡出); ③lines.py `_run_batch` — chunk 模式切片独立解码+片间垫静音, **末片停顿清零归 `_apply_breath_pauses` 防双垫**, line 模式不拆片 (静音切分会误判行边界); ④audio.py reroll — 0914 行尾特例升级为通用切片拼接 (每片归一后垫, 静音不被 loudnorm 碰), whisper 回检基准 `clean` 同步剥标记 (防 "-","1","s" diff 误报)
- **验证** (29/29, 真实 033 文本+真 ffmpeg+stub 引擎): 切片/守卫/拼接时长 (3片+1.5s垫=3.25s) 全对; 033 全管线: 2 片解码、片1止于`就是A。`/片2始于`B档`、时长含 1s 停顿、引擎文本零标记、manifest text 保标记 (留档) inference_text 零标记; 行尾标记路径: 收包单片不双垫+尾垫 0.5s; reroll 序列: 归一后拼接含 1s
- 前端 audio.js: placeholder/提示改"任意位置插 (光标处)"; 行尾死约定文案废除
- 服务端已重启 (PID 40200) 全量生效

## 2026-09-17｜待令清单全闭环 — 立意人闸/批评注入/隐喻查重/patch重扫/前端P1×4

- **立意人闸**: 🧭 改两级闸模式 (concept_only) — 切场+立意确认轮即停, `立意.md` 落盘 (人读: 每场隐喻/运动线/撞车警告); 页面新 ▶️分镜续跑 按钮 (有场无镜态自动现身) → `POST /plan-scenes` → `resume_scenes` 从骨架续跑+收尾; 断点报错改指路 ▶️
- **隐喻查重** (确定性): 场间隐喻字符二元组 Jaccard ≥0.35 = 撞车报警 → 立意.md 警告区 + 终稿 plan_warnings (`_concept_warnings` 跨阶段携带); 单测: 相似对 0.79 报/不同对 0.00 放
- **单场批评注入**: plan_scene 有旧产出即转多轮 (assistant=旧场分镜清单, user=自动批评: 禁区命中/克隆家系/隐喻贯彻/黑名单自查 + 重出指令) — 重规划从"盲重掷"升级为"看着旧稿改"; 无旧产出 (两级化新场) 保持单轮
- **patch后重扫**: 巡检轮 patch 应用前留快照, 落刀后禁区重扫 (含 keyframe_en), 命中即整镜回滚
- **前端P1×4**: loadShots→load (标/剥品牌标+重排镜号后刷新修复)、reroll/🩹AI修 带 only:[sid] (连坐烧GPU根除)、🎞动画逐拍编辑复活 (status 回 anim.beats)、花字 kind 白名单七种保留 (hero_number 不再塌缩 caption)
- **ep2 端到端实测**: 立意闸 6 场零镜停+立意.md ✓ → 续跑 47 镜/禁区零/克隆零 (六场隐喻: 插旗圈地金币摊薄/金管道vs青蓝水流/大水池新管vs循环泵/三台打桩机/绳索捆绑/记分牌死胡同); 批评注入 A1 重规划多轮 PASS; ▶️负路径守卫报错正确
- 服务端已重启全量生效

## 2026-09-17｜【】标注符号专用化 — 展示层剥离 + 生成侧正文禁用 (用户令)


- **规则**: 【】为系统段标注专用符号 (【0-28 秒｜钩子】六段弹性标签); books_story 页集数卡片预览剥掉全部【】块 (stripMarks: 整行标注先删→行内残留再删→收空行); **修稿框保持原文** — 标注是 TTS 剥离/动画规划/换钩子的制作数据, 编辑存盘洗掉会断产线
- **生成侧立法** (episode_gen 附加指令第2条): 除 6 个标签行外正文任何位置禁用【】 (含引用/强调/批注/小标题), 强调用引号或破折号 → 剥离永不错伤; 全书通用 (注入点在附加指令, 不依赖模板)
- **实测**: ep2-ep9 每集 6-7 个标注块 (ep1 已被人工重写为零); node 真跑 stripMarks: 整行标注/行内块/空行全清, 正文含【渠道红利】的假想场景也被正确剥离 (未来生成不会再产这种正文)
- 服务端已重启 (生成禁令即时生效)

## 2026-09-17｜孤岛浮字"HBO"根治 — logo_note 漏网降维令 (用户实锤 20s 处孤岛字幕)


- **根因**: 0916 降维令 (hero_number/impact 砍层锚进字幕行内高亮) 漏了 logo_note — 仍走 _render_hero 独立浮层; 新宪法规划产出 logo_note 词条 (全片仅 s63 'HBO' @18.4s), 字幕行本已含"HBO" → 独立层只产出孤岛金字
- **修法**: logo_note 并入字幕行内锚定词表 (emph_by_sid), _render_hero 只剩 title (v2 不会出现); 未锚进字幕行的 logo_note 按 J 线教义丢弃 (音效照挂)
- **验证**: 0917_0724 — hero_texts 1→**0** (孤岛消失), hero_merged 34→**35** ("HBO"成为字幕行内金高亮), captions 63 零丢, short_holds 仍空 (品牌修复保持)
- 教训: 降维/砍层类教义要清点 kind 全集 (hero_number/year/impact/logo_note/title 五种), 漏一种就漏一层孤岛

## 2026-09-17｜品牌卡"差额卡补"复发根治 — 装配侧旧路由弃用生成片 (用户实锤"品牌卡前后bug没修好")


- **定案**: 建稿账本 kinds: s13/s44=brand_card + short_holds "缺2.9s/4.6s卡补" = 0916 "固定5.2s资产配8-9s槽差额裸奔" 同一病。根因在 `_shot_material` (anim_draft.py:894): 品牌镜**无条件**路由固定品牌片资产 — 0916 终版"品牌镜逐镜生成"只在规划/生成侧落实 (s13 8.71s/s44 10.13s 满槽生成片都在), 装配侧旧路由把生成成果扔掉, 差额退回填色卡
- **修法**: brand 分支改"生成片优先" — 有 video_file 且存在 → real:mp4 (走全局 ≤1.22x 微提速保全片, s13 1.08x/s44 1.04x 无感); 无生成片才回退固定资产 (主循环回退分支的仪式句锚定逻辑不动)
- **验证**: 旧稿 0917_0704 (s13/s44 卡补) → 新稿 0917_0713: **short_holds=[] 、46/46 real、s13/s44=real:mp4、零错配、63 字幕零丢**
- 教训: 教义切换要过"全链路由清单" — 生成侧改了, 装配/回退侧的同名旧分支会静默吃掉成果 (kinds/short_holds 账本是这类病的照妖镜)

## 2026-09-17｜ep2 全新集两级化实测通过 + 深度复查三修 — 规划正身定型


- **路由缺口修复**: 两级化原只挂在"已有doc+force+scorched"分支, 全新集掉进单窗老路 (ep2 首跑实锤: 581s 长集 25K 大包两连毙边界断裂) — 现两级化=规划正身, 全新集默认走它
- **深度复查三修 (自伤缺陷)**: ①巡检 patch 只改 keyframe 不重建 anim → h3 吃 opening_desc = 新图配旧视频锁 (已改 patch 带 motion_en + anim 整体重建, 与 plan_shot 同款) ②禁区扫描漏 keyframe_en 英文面 (词库补 13 条英文禁词共 41 词, 扫描面覆盖) ③arcs 场窗元数据 ±1s 永久漂移 (归真尾场窗回同步)
- **ep2 实测 (581.6s 音频 = 16 非空包 290.8s + 一个 290.8s 巨型空包垫尾 = 音频线半成品, 动画正确只铺有声部分)**: 全流程 16 分钟 — 切场 8 场 → 立意确认轮 ✓ → 逐场分镜 (碎镜合并实战 3 次) → 拆镜重设计 → 巡检修正 6 镜 → 时长核验; 复验 48/48 唯一首帧零克隆、禁区(含EN)零命中、场窗回同步后场级问题清零 (余 8 条镜级 ±1s 属对齐自愈类)
- **ep2 八场隐喻 (立意轮产出, 零重复零黑名单)**: 篱笆圈地金币水渠 / 泳池注水存量vs空间 / 双轨收割地图(收割机vs磨坊) / 管道网撞信号红巨墙+录像机黑盒 / 三岔路口三档 / 灯塔转型+漏水船不敢补洞 / 三层塔楼(塔顶空间·坡度努力·塔基数据真伪) / 巨账册划线找拐点
- 双线实录: 用户 ep1 H3 批 (46镜) 全程未断, 重启 watcher 空窗自动执行

## 2026-09-17｜对话形态按步择优 (调优令) — chat_turns 多轮入口 + 立意确认轮 + 全片巡检轮


- **用户裁决**: 调优不硬改 — 哪步哪种对话好处多用哪种, 禁一刀切。分派依据=今晚实证: 单场分镜单轮已证够 (A7 实锤, 不动); 多轮赢在两处 → 落地
- **`LLMService.chat_turns(messages)`** (llm_service.py): 多轮对话入口, 携 assistant 历史 (模型看着旧产出修正), 与 chat() 同兜底链; 单轮调用原样不动
- **立意确认轮** (plan_arcs 内): 切场初稿后多轮一轮 — "先答个明白再动笔": 逐场答隐喻映射/前后场重复/黑名单/口播承载, 再出修正版 arcs; 历史带原 user (DeepSeek KV 前缀缓存可命中); 失败沿用初稿
- **全片巡检轮** (`consistency_review` + scorched 收尾接线): 逐场渐进=局部视角 → assistant=全片分镜总览, 一轮只做跨场一致性 (人物双约/色板/相邻构图重复/隐喻贯彻/黑名单), 只出 ≤8 条 patches 确定性应用 (媒体重置回 planned), 零 patch=不落刀
- **真弹验证** (内存副本, 盘上零写): 巡检轮抓 4 真问题修 4 镜 — s17 美国地图平台风险(建议抽象网格)、s36b/c 与 s38/s40 相邻构图重复、尼克松跨场造型一致性 — 逐场视角看不到的接缝问题一网打; 词库只禁中国地图, 地图类风险由巡检轮语义层兜住
- 生效时机: 服务端下次重启加载 (用户任务在跑不强停)

## 2026-09-17｜国家指代手法 (央视同款) — 禁旗徽地图后的正向替代词汇


- **用户令**: 他国概念禁用国旗/国徽后, 用网友公认+央视同款指代物 — 美国=白头鹰、中东=骆驼
- **落地**: `config/compliance_visual.json` 新增 `substitutes` 指代表 (美国白头鹰/中东骆驼/俄罗斯北极熊/英国狮子/法国高卢雄鸡/印度大象/日本樱花/澳大利亚袋鼠/加拿大枫叶, 可扩展) + `compliance.visual_substitutes()` + **DIRECTOR_SYS 模块加载时动态拼装指代块** (改表即宪法同步, 双源永不漂移); 表外国家宪法指引"用其代表性动物/植物意象, 同禁旗徽图案"
- 验证: 指代表 9 条全注入宪法 ✓ 当前 47 镜重扫零命中 ✓
- 事故记录: 换代码重启时安全窗口检查只看了 60 字符截断输出, 误停一个进行中任务 — 评估损失为零 (无视频在产/盘态干净); 教训: 窗口检查必须读全

## 2026-09-17｜动画工坊全面审计 + 政治敏感画面禁区五层 (用户令: 中国地图/国旗/国徽/领导人画像禁入画, 卡通也不行)


- **五层防线**: 词库 config/compliance_visual.json (28词, 单一事实源) + compliance.check_visual 服务扩展 + DIRECTOR_SYS 宪法条款 (卡通/剪影/变形同禁, 变形不改性质) + collect_warnings 软警告 + K2/H3 生成闸门硬拒 + K2 引擎级政治防尾 (PROTECT_TAIL 物理注入, 不依赖 doc)
- **边界裁定 (可否决)**: 国旗/国徽=任何国家; 领导人=中国历任+现任外国元首; 书中非领导人历史人物 (尼克松/肯尼迪) 不禁 — 否则本书 A4/A5 无法视觉化; 扩禁只需词库加名
- **实战**: 上线即抓 3 真命中 (s23/s24/s25 尼克松/听证会场景的"美国国旗"背景) → 单镜🔁新宪法重设计 (电视屏幕/光柱替代) → 重扫零命中 → K2 only 重生成 ✓ (47/47 img_done)
- **前端全面审计** (探查代理, 全带行号): P1×4 — loadShots 未定义(:500/:509 标/剥品牌标与重排镜号后看板不刷新)、🎞动画编辑死 UI(:636 读 beats 但 /status 不回 anim)、reroll 连坐(:464/:482 不带 only → 连坐重跑全部 approved 镜)、花字 kind 塌缩(:584 hero_number 落盘变 caption); P2×4 — qc_sheet 死链(仅 CLI 生成)、僵尸 doFixText、全部过审无 running_job 门控、死参数 pos; P3×2 无害。前端 19 调用点全命中路由, 失配集中在数据形状
- **后端规则清单 14 项执法点成文归档** (docs/improvements/已完成-20260917-动画工坊全面审计与政治敏感画面禁区五层.md); P1 修复未动待令

## 2026-09-17｜K2 报错排障 → 两级化实战三连修 (塌缩镜/碎镜/九场断点) — 全集新宪法规划落成


- **定案**: 用户报"k2批量生图错误" ≠ K2 坏 — 是生成闸门拦截: 此前 🧭 两级化跑到一半崩, 盘上留下骨架+1.5场残局 (7镜39.4s), 9 处镜界脱句边 → K2 按纪律拒跑
- **修① 塌缩镜 (plan_scene 窗口对齐真 bug, 两连毙复现)**: 两遍对齐 b[k] 边界塌缩 (needle 撞重/比例挤压) 时 `if b2>a2` 不成立 → 该镜**保留 LLM 原始时间**混进对齐序列 = 乱序+重叠 (s09 44.6-50.2 压 s08 实锤); 修法: 塌缩镜零宽化剔除 (与归真空镜剔除同判)
- **修② 碎镜合并 (第二层, A5 两连毙)**: 边界挤压出 <1.5s 碎镜过不了硬地板; 修法: 确定性并入前镜 (窗口+口播续接, 首碎向后并), 与密度宪法 2-6s 同向
- **修③ _scorched_flow 宽捕获**: LLM 网络/瞬时异常纳入每场重试网 (原只捕 SystemExit/ValueError)
- **九场断点续实战**: 本次切场切出 9 场 (A8/A9 = 235.8-279.1s 尾巴), 崩在 A5 → 修复后 A5-A9 逐场续完 → 终对齐: **47镜/279.1s/覆盖差0.0/零轴隙零硬校验/闸门0违规**, 5 超长镜自动拆+分身新宪法重设计; 时长核验 2 条小残 (场界元数据 ±1s, 不挡)
- **全集新宪法规划落成**: 9 场隐喻全部立住 (A1 漏斗金币/A2 管道金币流/A3 烽火-管道-末端点火/A4 哈哈镜扭曲器/A5 雕刻刀+管道天花板/A6 透明天花板+金门/A7 九工位流水线/A8 三盆栽涨跌/A9 同屏拔河), 46/47 唯一首帧 (唯一重复对=冻结品牌卡); K2 批生图已健康跑起 (16s/镜, ComfyUI 单实例复用)
- 服务端管理教训: `(cmd &)` 脱管启动的进程是父子对, 杀父带崩子 (54321 两连掉线实锤) — 一律走后台任务方式拉起

## 2026-09-17｜导演宪法升级 + 🧭两级化 + 时长核验 — 提示词质量问题根治 (A1-A7 判定驱动)


- **判定复盘**: 整集版提示词 A1不行/A2还可以/A3-A6差/A7很一般 — 病根: 通用商务素材池(办公室/白板/咖啡厅/报表)字面配图 + 隐喻无结构槽(可选=预算紧时先砍) + 人物选角笔记化 + 对比不构图化; A2 及格恰因隐喻/构图叙事全齐 (执行率=判定分)
- **DIRECTOR_SYS 四升级 (用户拍板 1/2/3, 跳过人物格式与色板两条)**: ①金标准范本×3 内嵌 (隐喻型管道金币流/对比同屏型广播威严vs电视汗脸/构图叙事型365户小镇辽阔, 每条示范卡通大头+圣经色+灭字+视角锁死) ②场级 `visual_metaphor` 必填槽 (一句话 A=B 映射, 每镜都是隐喻的一个面; plan_scene 旧隐喻入上下文+透传) ③通用商务素材黑名单 (白板PPT/咖啡厅聊天/落地窗望城/报表特写默认禁, 叙事段例外) ④清单段铁律改旅程隐喻强制 (每镜推进一站)
- **🧭两级化 (用户拍板, 根治预算摊薄)**: scorched = `plan_arcs` 切场 (小输出, 每场隐喻必填, 场窗确定性归一零缝隙) → 逐场 `plan_scene` 分镜 (每场落盘断点续) → 收尾句级归真+拆镜分身重设计; 单窗整集大调用退役
- **时长核验 (用户新令)**: `duration_audit` 三方对账 — 场级 音频窗vs镜槽合计(+音频段重叠独立对账), 镜级 音频槽vs H3窗vs beats铺满; `时长核验.md` 人读表落盘; plan/replan/逐场流/对齐 四路接线; 首跑即抓 3 镜超 H3 上限 (s34/s38/s39 定格补差风险)
- **motion_zh 越界事件修复 (排查副产物)**: `_clamp_motion_events` — 拆镜继承/LLM新产的"N秒时"事件超镜长子句删除 (s21 5s镜留8s/12s事件实锤), 接线拆镜/重设计/单场拉伸三处 + 软警告
- **真弹验证**: A7 单场新宪法 — 隐喻"旧渠道公路vs登山线"(与品牌卡登山客闭环), 5镜零黑名单零越界, 逐镜卡通大头+圣经色+灭字+机位; `plan_arcs` 真弹 — 7场零缝隙, 每场强隐喻 (A3信号vs点灯/A4魔镜/A5管道爆裂vs深根树/A6登山九驿站); 均显著优于判定版
- 现场: 02:13-02:16 误启的逐场重规划已叫停 (A1/A3/A4/A5/A6 旧宪法重做落盘, 判定原版 bak_replan_20260917_021327); A7 已是新版; 全集 🧭 两级化重规划待用户令

## 2026-09-17｜ComfyUI 反复拉起根治 — 保温/孤儿收编/4060 取证 (GPU 服务管理器三修)


- **排查定案**: "批内反复拉起"不成立 (K2 整批一次 session, 实测 10min 零重启); churn 真源=跨操作: ①全局 idle 180s 看门狗在人审/重 roll 节奏下每次点击=82s 冷启 (日志实锤 6 连拉起每次只干 2-7 笔, 全是管理器无声 taskkill, 零 OOM); ②服务端强杀留 ComfyUI 孤儿, 僵死占 8188 + 旧红线"永不杀 8188" → 新实例 bind 失败死循环; ③48 次拉起中 4 次 Total VRAM 8188MB (落 4060) = CUDA_VISIBLE_DEVICES 未生效异常待定位
- **修① 保温**: ServiceSpec 新增 `idle_timeout_sec` (None=跟全局, 可被 app.yaml tts_services.<key> 覆盖); comfyui 内置 1800s (TTS 小栈维持全局 180s); 看门狗/session 收尾改走 `_effective_idle(key)` 按服务判定
- **修② 孤儿收编**: `_force_free_port` 红线分级 — 54321/7861 绝对红线不动; 8188 改"验明正身才杀" (新增 `_http.proc_cmdline` 按 PID 查命令行, 确认 ComfyUI main.py 才清, 陌生进程拒杀+ERROR 取证留痕); TTS 端口照旧清场
- **修③ 4060 取证锚**: `_launch` 日志追加 `CUDA_VISIBLE_DEVICES=<实际值>` — 再现 8188MB 横幅时: 值=0→CUDA 设备序翻转, 值=<unset>→有路径绕过 spec
- **验证**: 进程内真调用全绿 (comfyui 1800/fish 180 生效, 400s 空闲只杀 fish, launch 日志含 env, 五场景验明正身门 8188正身杀/8188陌生拒/7860照杀/54321/7861红线); 服务端已重启加载 (旧批结束后的又一次 180s churn 实况被顺手抓到=病根佐证)
- 后续观察: 下次 GPU 任务冷启一次后应连续保温 30min; 4060 拉起再现时对照新日志行定位

## 2026-09-17｜拆镜分身克隆根治 — "太多镜都一样" (🧭全清后 25/36 镜首帧逐字相同)

- **病根两层**: ①🧭全清=整集单窗 LLM 调用, 输出预算摊薄 → 只吐 20 个超长镜 (均 13.7s, 宪法要求 2-6s/镜数≈时长÷5); ②`_realign_narrations` 自动拆镜对超长镜 deepcopy 整镜 — keyframe/提示词/motion 逐字继承只 seed+1 = 同一张图换噪点, 16 分身落进 9 家系, 25/36 镜共享 9 个首帧 (0916 好版 38镜36唯一首帧 vs 本次 39镜20唯一实锤)
- **修法 (系统能力, 非逐镜手调)**: `redesign_split_clones` — 克隆家系一次 LLM 批量重设计 (家系=时间轴相邻+同组+keyframe 逐字同, `_clone_families` 确定性检测, 品牌镜 DNA 冻结豁免); 场景/年代/人物双约/风格与父镜一致, 景别/机位/时刻逐镜推进; 槽位 (时间/口播/镜号/字幕层) 钉死; 重置语义与 plan_shot 同款 (新 seed/planned/清媒体)
- **三路全接**: 整集规划 (`director2.plan` realign 后) / 对齐归真 (`align_episode` 拆出新分身当场重设计, 报告新增 `redesign_clones`; LLM 失败=保持克隆不炸确定性归真) / 软警告 (`collect_warnings` 克隆家系亮灯); 拆镜分身打 `split_parent` 溯源标记
- **ep1 盘上修复实测**: 16 分身 16/16 重设计, 槽位零漂移, 20→38 唯一首帧 (唯一重复对=两张冻结品牌卡, 设计如此); 顺跑对齐 s21(16.2s)/s23(11.9s) 拆 3 分身自动重设计, 39 镜全 ≤10s 零轴隙零硬校验问题; 备份 bak_clonefix_20260917_013804
- **待用户**: 24 个 planned 镜 (16 重设计+3 新拆+品牌2等) 待 K2/H3 重出; 遗留议题=整集规划密度摊薄 (单场>整集教义已录), 拆镜兜底已不再是复印机

## 2026-09-16｜开场链 v3 系统固化 (0410 验收 → 0412 定稿 → 0417 逐段口播) — 书墙滚动·人声解说·统一音量

- **voice 轨逐包口播 (0417, 用户令)**: 整段 voice_full → 逐包 AudioSegment (17 包+intro 2 段=19 段, 真实时长贴轴零重叠, 空文本包不入轨; -2ms 防毫秒取整; 失败兜底整段) — 字幕/镜头与口播逐段对齐可见可调

- **书墙滚动过场**: `covers.render_wall_scroll` 预渲染 (top10 静态墙 11 张=前8+本书+第9/10, 首帧 4 本/本书第9位身后剩2/三次方缓出 1.17s); 齿轮音 (发条旋钮转动齿轮) 截齐滚动
- **人声解说链 (老谭)**: `_intro_voice` 缺料自动合成 (IndexTTS2.5→双向裁静音→atempo 1.5× 保调变速=剪映同款): 拆解句 @3.00 与墙首帧同帧, 书名 @ 滴声半程 (一滴水滴声 1.63s), 念完+0.5s 气口 → tc 推轴 (build 层平移 shots 不动 shots.json, ep1 tc=7.17); reveal = epub 封面本尊折叠开幕 + 末帧虚化 14px 垫底, 无底字
- **统一音量 (Victory -6.5 LUFS 全库最热实锤)**: `sfx.sound_loudness` (ebur128 实测缓存, 取末值防 -70 占位坑) + 动画线 `_attach_sfx` 统一口 vol=0.9×10^((-18−L)/20) 只衰减; 36 段 sfx 响度归一落盘
- **素材双轨制**: top10 知名度墙静态固化 + 本书封面每书 epub OPF 提取; 源 zhihailib F 类 (投资/交易类筛除); 换书零调整
- 案卷: 已完成-20260916-开场链v3系统固化

## 2026-09-16｜前2秒打字卡固化 (0229 稿用户验收"可以了")

- **定稿形态**: 行1「如果你能坚持每晚睡前看一集，」静态直出 0→3s (y0.28) + 行2+3 常驻单元素「打字机 I」1.65s (0.25→1.9, y0.085 块中心) + 三承重金黄 (坚持每晚睡前/收益主体槽/超过90%) + 打字（字幕专用）音效@0.25s; 版式常量 size6.8/行距0.13/行槽基0.28(整体下移两行)/auto_wrapping
- **三轮被毙**: 逐字段重放(段限时清空+换行跳位) → 居中打字(中心展开非打字) → 整段原生动画(首行直显); 教训全数入打字卡教义记忆
- 切行吃冻结骨架红利: 切点「看一集，」+「，会超过」永在, 换书零调整

## 2026-09-16｜动画时间轴归真 — 句级细分 + 自动拆镜 (用户诊断"规划期时间轴没算明白")

- **四层病根**: ①8镜 duration_s 钉死 10s 上限 vs 口播槽 12-19s → 定格补差 3-9s; ②v1 切点包内字符插值=漂移源+劈句尾; ③真根因=ep1 音频是老式巨型多句包 (17包 10个>10s, 最大 23.6s, 包边≠句边); ④QC 拆镜阈值 25s 与 10s 上限脱节
- **修法**: `_sentence_segs` 句级细分 (句切+silencedetect 停顿吸附, J线同款平移); `_realign_narrations` v2 (时间锚为主+句包边吸附+严格递增+组按时间序+缝隙容差); **自动拆镜** (用户令"一个不够就两个": 整句包贪心分块≤10s, 块1继承原镜素材复用视频, 块2+新id回planned进队列, text_layer按绝对时间分家); align v2路径 只体检→确定性归真; 字幕链式游标 (J线铁律) caption_drops=0
- **实现坑实录**: groups 列表序≠时间序整组饿死 / 句包t值舍入发丝缝落缝兜底流尾 / ragged文本锚只配微调不配导航
- **ep1 实测**: 34镜全≤10s, 零轴隙零丢镜, 定格补差 7处3-9s→2处老视频短待重roll; 新拆9镜planned进生成队列; 备份shots.json.bak_0916
- 成果草稿 0916_0025 (caption_drops=0, 67/67) 待用户剪映验收

## 2026-09-16｜动画草稿字幕平移新闻线配方 — 字幕观感对齐 J线 + 丢段根治
- **字幕太大 → J线配方整体平移**: caption 档 size9 金粗 → 孤月体/字号5.0/奶油色#F9F3C4/居中/描边+阴影 (复用 `_StyledTextSegment`, 内联划重点 find_highlight_ranges)
- **"字幕不是完全体"真根因 ≠ 截断**: caption `t_end` 超镜尾 → 轨内绝对时间重叠 → SegmentOverlap 被容错吞掉整段丢 (ep1 实测 7/51); 伴随实锤 **hero 轨从未注册=强调字此前全灭**; 修法 = 文字层"先收集→同轨截尾去重叠→再装配" + `split_subtitle(30)` 断句分时 (J线三件套) + impact 单开文字轨 (同帧"VS"+hero_number 双层共存)
- **音效自动挂 (0916 二段, 用户令"研究明白再弄")**: 素材包查明 = `data/jy_sounds/` 223 mp3 本地库, 语义库 55 名缺 15 文件但动画线 7 类全健康; `_sound_cues` 事件流一份决策 (分类+轮换+实存过滤) 清单/自动挂共用; 密度闸门同 J线 R9 v3 (前30s全开/≥4s); ep1 43 点→自动挂 32 (密度跳10/同帧叠音跳1), sfx 音频轨零重叠
- **字幕动效 (0916 三段, 用户令①)**: J线 `_classify_chunk`+`_CAT_ANIM` 复用平移 — 首条字幕卡拉OK逐字点亮/悬念→上滑/金句→放大/金额→星光闪闪 (密度闸门≥4s); 本线微调 plain 块加渐显基础入场 (实测分类命中仅 7/58, 引 J线"渐显=专业稿标配"教义); ep1 58/58 字幕全带动效
- **两层浮字降维 (0916 四段, 2313 稿验收反馈)**: impact 层(？/VS/搬运工)+hero_number 层(错题本/回本难/40年) 整层砍掉 — J线 v2"砍强调轨划重点进字幕行"教义落地; 强调词三级锚定进字幕行内高亮 7.5 号金 (逐字/数字变体 40年↔四十/子序列 回本难→回本越难), 锚定块词型动效 (数字→星光闪闪/冲击→上滑/其余→放大); 38 词→29 锚定+9 未锚(VS/？类, 音效照挂); 音效系统与渲染解耦不受影响
- **打字卡 86万赞 1:1 (0916 五段, 用户令)**: 锚点逐帧取证 (.tmp/s6top 条图) — 逐字无光标 0.25→1.9s+2-3s 定格/**居中滑动式**(锚点本身即整行重居中, 二次取证实锤)/白字居中偏上/三承重词同字号金黄/打字音效同帧; `_StyledTextSegment` 参数化 hl_color/hl_size (J线行为零变化); 音效=打字（字幕专用）1416ms@0.25s; 修复 hl 漏传+长文案步长越定格窗+**出屏 (auto_wrapping=False→type:text 不换行 44字≈3400px; 开 True 转 subtitle 型按行宽换行, 字号 6.8 保两行)** 三个 bug
- **验证**: ep1 重建 2354 终版草稿 — captions 58/0, 浮字层灭, 打字卡 45 段全 subtitle 型两行三承重黄落盘, sfx 33 段; 2354 待用户剪映验收

## 2026-09-15 (夜·续)
- **选书特效装配化**: S00c 飞入(向上滑动)+S00d 收敛(放大)+动感扫光特效轨 (3.4-4.2s) — 86万赞 A族开场全自动, 特效失败不炸稿; 1419 中性视觉自验通过
- **剪映迁移丢件根治**: 真根因=段数组顺序≠时间顺序 (开场件后追加→被迁移器摘到片尾 3:45); 保存前全轨按 target start 排序; 伴随修复: 静图转 mp4 统一 VHS 档案 (bt709×3+tv+24fps); 1404 草稿中性视觉自验通过 (播放头@0 预览=标题底图本尊)
- 静图转换缓存 (still_mp4) 全清重生成; 排查方法论入案卷 (后台窗口假画面/GUI 亲验前置)
# 数字人项目 CHANGELOG

> 时间倒序记录“已完成里程碑 + 重大决策变更”。技术栈细节不在此，请看 [`项目状态总览.md`](项目状态总览.md)。

---

## 2026-09-15 收工（深夜·剪映草稿攻坚夜·下半场）
- **选书特效装配化**: S00c 飞入(向上滑动)+S00d 收敛(放大)+动感扫光特效轨 (3.4-4.2s) — 86万赞 A族开场全自动, 特效失败不炸稿; 1419 中性视觉自验通过
- **剪映迁移丢件根治**: 真根因=段数组顺序≠时间顺序 (开场件后追加→被迁移器摘到片尾 3:45); 保存前全轨按 target start 排序; 伴随修复: 静图转 mp4 统一 VHS 档案 (bt709×3+tv+24fps)
- **经验四层入库**: 段顺序铁律/后台窗口假画面/中性视觉提问/用户实勘优先 → 案卷+CHANGELOG+长期记忆
- **遗留** (见 进行中-20260916): ①字幕太大 → 平移新闻线 jy_draft_service 配方 (字号5/奶油色/孤月体/split_subtitle断句三件套/attach_sound音效); ②字幕有断句不是完全体
## 2026-09-15 收工｜0915 磨合之夜沉淀 — 案卷+索引+取代横幅

docs/improvements/已完成-20260915-动画产线三层画面模型定稿-磨合之夜.md 落档 (方法论/三层模型宪法/全部纪律/基建修复/事故留档/观察遗留/文件地图); INDEX 补行+篇数同步; 0914 两篇 v2 文档加"已被取代"横幅留档。记忆全夜同步维护。

---

## 2026-09-15z40｜剪映迁移丢图根治 — 静图全转 mp4 上轨 (用户报: 开场三秒底图没进去)

- **根因**: 我们生成的草稿开场完整在位 (0-3s 标题底图→书墙→封面), 但 PNG 以 VideoMaterial 挂视频轨 — 新版剪映打开迁移时把"视频轨上的图片"当非法素材**直接丢弃** (mp4 段全存活)。pyJianYingDraft 0.3.0 无 PhotoSegment
- **修法 (跨版本免疫)**: `_as_video_path` — 静图先 ffmpeg 转 mp4 (loop+余量+偶数化) 再上轨, 缓存 ep/still_mp4/; 五个上轨点全接 (开场三段/镜素材/定格补差)
- 排掉的路障: 帧量化截短 (-t 3.33→3.334s 定格源越界炸稿, +0.6s 余量); 中断残留半截烂卷被缓存复用 (产物 ffprobe 校验); S00d 1080×1543 奇数高 libx264 拒编 (scale 偶数化); z39 陈旧检测引用未赋值 audio (顺序修正)
- **验证**: 新草稿 1253 — 开场三段全 mp4 (0/3/4.2s), 材库零 PNG, 轨尾 video 230.37 = text 230.36, audio 231.56 (+1.2s 尾气口)

---

## 2026-09-15z39｜草稿三修 — 音画对齐根治 (用户报: 打不开/特效音画全不对齐)

- **音轨 452s 灾难**: 226s 空文本包混进整集口播 concat → 音轨比视频长 227s。修: concat_voice 过滤空包 + 草稿装配前 voice_full 陈旧检测 (ffprobe 时长 vs 非空预期 >2s 即重造)
- **打字卡爆窗**: 43 字书槽长句 × 0.2s/字下限 = 8.8s 压穿 5.5s 开场。修: 步进自适应 (窗口钉 2s, 下限 45ms 快速打字机) → 打字轨 3.00s ✓
- **重造验证**: 音轨 231.56 vs 视频 230.37 (差1.2s 尾气口正常) / 文字轨 230.36 / 打字 ≤5.5s ✓
- "剪映打开改格式" = 外来草稿正常单向迁移 (无害), 之前观感错乱来自上述内容 bug
- heredoc \n 陷阱第 6 次 (concat 行), 全部字符串编辑一律 Edit 工具铁律重申

---

## 2026-09-15z38｜对齐按钮体检化 + 老件审计 (用户令: 三个按钮是否吃到知识沉淀)

审计结论: 🎞草稿装配最全 (三色分档/品牌卡随书/仪式句后期/音效清单全在 ✓); 🎚两对齐按钮对 v3 文档空转 (安全但零价值)。
- **对齐 v3 分支升级为时间轴体检**: 硬校验问题数/轴隙扫描/尾部覆盖差 (vs 真实集长) + 落盘分支备音轨+刷新音效提示清单; UI 双模式文案 (v3 体检/v1 试算); 真跑: 25镜/0问题/0轴隙/覆盖差0.01s ✓
- 空文本包幻影在体检指标复发 (裸加 duration → 覆盖差 -226s) — 复用 z26 修法 (非空包终点), 教训档补: 同类指标一律过滤空包
- **安全重启守卫首次实战拦截**: 用户 H3 批 4/5 运行中拒绝重启 ✓ (等完成后重启成功)
- 修复过程教训: 生成脚本改码必须写盘收尾 (start_phase chain_h3 签名漏写盘 → 用户端 TypeError, 已入教训档)

---

## 2026-09-15z37｜重规划此镜一条龙 (用户实锤: 确认后停在图上没法下一步)

- **chain_h3**: K2 相位完成后同一 comfy session 内自动过审本批镜 → 续跑 H3 (一个 job 走完 图→审→视频, SSE 全程进度; GPU 锁不释放零冷启动)
- doShotReplan 改一条龙: LLM重设计(30-60s) → 生图(~15s) → 自动过审 → 生视频(~100s), 确认框写明全程~3分钟看任务面板
- 诊断留档: 上版链路其实成功 (s15 重规划✓备份✓) 但停在 img_done 等人工过审 — "当前没了"= 27s 静默 LLM + 无下一步的合成观感

---

## 2026-09-15z36｜单镜重规划 ♻️ (a5s5p3 实锤: 页面无法"只重做一镜")

用户场景: 某一镜设计坏了要重图+重规划 — 现有操作要么只换图不动设计 (打回), 要么整场陪葬 (重规划本场)。新增粒度:
- **director2.plan_shot**: 单镜窗版 — 时间槽/口播/shot_id 钉死 (原位替换), LLM 重设计 keyframe/motion/camera/text_layer (输入带场定调+邻镜画面衔接错开+旧版打磨不复读+圣经); 兄弟镜与已有图全不动; beats 单拍铺满槽; 校验网×2, 备份后落盘
- **UI**: 「♻️ 重规划此镜」(所有非品牌卡镜) → LLM 重设计 → 自动触发单镜 K2 生图
- 三粒度体系定稿: 场(🔁重规划本场) / 镜设计(♻️重规划此镜) / 镜动画(🩹AI修动画 + 🎲重roll + 🔁打回换图)

---

## 2026-09-15z35｜🩹 升级为 AI 动画医生 (用户令: 修字不实用, 要 AI 改动画提示词)

- **ai_fix_shot_motion**: 图/背景动画不动 — LLM (AI_FIX_SYS 内置全套文字纪律: 一镜一事/数字中文不共现/3字+走后期/无字拍原样保留/禁发明文字) 重写每拍 motion, 拍数强校验, 自动换 seed 回可重roll态; 输入带首帧/口播/已有后期花字 (已下沉的词绝不再入画)
- UI: 「🩹 AI修动画」按钮 (弹一句话问题可留空) → 医生改完自动重roll; 旧 fix-text 正则下沉保留为 API
- **s12 实跑验证** (用户实案: 1987好/定义权炸): 医生 3s 完成 — 定义权移出入画(后期已有)、1987 保留、警官环境动画原样, notes 一句话说明; 老修复路径 500 (json import 漏) 已修
- heredoc \n 陷阱第 5 次留档: 含换行的代码一律 Edit 写入

---

## 2026-09-15z34｜字体覆盖预检工具 (用户令: 字体缺字提前测)

研究定论: 艺术字体大多只做 GB2312 常用字 (6763), GBK 2.1万/GB18030 8.8万 — 缺字时剪映回退默认体 = "似繁非繁"错形来源之一。两路文字劈开: H3 入画字=模型画字无字体可测; 后期字幕/花字=字体渲染可预检。
- **scripts/font_coverage_check.py**: fontTools 扫 cmap — 语料自动取 shots.json 全部 text_layer+口播+品牌句 (--book/--ep) 或 --text; 目标字体默认系统五款中文 (--fonts 指目录)
- 实测: ep1 语料 428 去重中文 — 系统黑/楷/仿宋/雅黑/宋体全绿 (2.1万字级); 剪映自带 后现代体6555/悠然体7082/新青年体6555 字全绿 ✓ (常用叙事词安全, 生僻字才是雷区)
- 用法: 换花字字体前跑一遍, 缺字即换字体或该词回默认体

---

## 2026-09-15z33｜一键修字 🩹 (用户令: 字bug出来点一下就修) + 花字编辑器两列化

- **🩹 修字+重roll 按钮** (anim_done 镜): 入画字炸了一键修 — 自动检测 motion 里的文字事件 (CJK 连续段 + 引号内容; 或显式指定词) → 从 beats 抽除 (掏空拍回填安全环境动效+No text) → 下沉 text_layer 金色大字档 (拍窗口时机, 草稿/音效清单自动接手) → 换 h3_seed → 自动触发重roll。图不动、词不丢、视频变无字干净版
- **花字编辑器两列化** (用户设计): 去 kind 下拉 — 🔤 字幕列(底部白/口播对齐) + ✨ 花字列(金色大字) 各管各的; 修掉旧下拉 bug: hero_number/year/impact 打开时错显"字幕"、保存被压扁成 caption (分档渲染报废)
- 单元验证: a4s4p3 实案 (1987+定价权叠字) 双词全下沉、掏空拍回填 ✓

---

## 2026-09-15z32｜H3 文字"一镜一事"铁律 + 逐镜动画编辑器 (六样本定案)

**用户重roll实验定案**: 叠字=结构性触发点非随机 — 同结构重抽 N 次, 1987 每次好、"定价权"每次坏 (重叠即炸)。抽样修不了结构缺陷 → 五点纪律上系统:
1. 每镜最多 1 个 H3 入画文字事件 (数字/年份/拉丁 或 2字中文超短词, 二选一)
2. 数字与中文绝不共现同镜 (拆镜或错开整拍)
3. 3字及以上中文词 → 后期金字 (剪映零乱码)
4. 文字密集场景 (卡片阵/招牌墙/屏幕墙) → 场景全空白文字全后期
5. 反幻觉条款回归 (空白表面保持空白) — z29 回滚的防御重新装上 (数据已坐实)
- **逐镜动画编辑器**: 「🎞 动画」按钮 — 图不动只改 motion 拍描述 (POST /shot/{sid}/anim) → 🎲重roll 生效; 解决"图满意但文字结构要修"场景 (免整场重规划赔好图)
- 台账观察关闭 (6样本+实验定案), 转常规纪律执法

---

## 2026-09-15z31｜品牌卡随书生成 — 风格与书的动画一致 (a3s3p1 实锤)

用户报 a3 落款仪式句镜品牌卡风格与全集不一致。根因 = 品牌卡是全局固定资产 (登山客_赛璐璐_v2 所有书共用) — 而 品牌卡_配方.txt 0914 早写明"风格随书变, 每本书有自己的美术圣经" (记录未吸收又一例); 且旧资产把末集句"拆一本书翻一座山"烤进画面 (每集卡被末集句污染)。

- **brand.py 随书生成管线**: 骨架冻结 (登山客背影/山脊群山/三拍: 合书入包→起身望山→走向群山) + 书级风格 (k2.build_prompt 自动注配方 head/tail/LoRA, 单一事实源) + 圣经色板进画面; **视频无烤字** — 仪式句走草稿后期金字 (title 档自动合成); 冻结 seed 20250914 保复现
- **解析优先级**: 品牌卡_{书}.png/mp4 本书优先 → 全局固定资产回退 (k2/h3 直通 + 草稿装配三处改线)
- **页面**: 「🏔 品牌卡(本书)」按钮 (phase=brand, GPU 相位, ~2min 一次生成全系列复用)
- 干跑验证: 回退解析/提示词组装 (骨架+风格头+色板+灭字尾) 全过; JS/compile/路由 OK

---

## 2026-09-15z30｜效果知识库接入动画产线 (ID-061, 用户令) + 白闪禁令裁决吸收

- **DIRECTOR_SYS 三段**: ① R9 协同三件套律 (每句口播三层同帧: 字幕句白+关键词大字金+冲击红特大字; 三色板 白=正文/金=强调/红=冲击, 抖音知识区标准) ② 同帧音效点 (音效对齐大字出现帧非句首; 语义映射 money/suspense_hook/impact/punchline/typing/transition_soft; 同类轮换防腻) ③ 密度规则 (前30s高密度 ~每2.4s一个强调事件, 后降档留呼吸)
- **草稿渲染**: text_layer 新增 impact 档 (红 0.72/0.11/0.11 特大字居中)
- **音效提示.md**: 草稿构建时自动导出 R9 同帧音效图纸到 ep 目录 (大字出场时刻+语义类别+3候选轮换), 人剪映按图挂弹; 真数据干跑 9 音效点验证 (转场/hero/year/title 全对位, year 归 punchline 点题感)
- **白闪绝对禁用** (用户裁决"白闪绝对是个错误", 旧"放开"记录作废) 已入炫度纪律
- heredoc 
 陷阱第三次留档: 生成脚本写字符串字面量必须 \n

---

## 2026-09-15z29｜招牌幻觉 — 观察模式 (用户令: 样本太小不抓bug)

s02 (a1p2) 抽帧实锤 H3 自发在店面招牌位画乱码伪汉字 (注入链干净/旧禁令壳仍发生/首帧灭字合格 → 世界模型对"本该有字的表面"的补全先验)。**用户令: 疑似随机事件, 暂不修, 先采样复发率** — 曾上的双层防御 (H3 反幻觉条款+导演场景纪律) 已回滚入库待命 (CHANGELOG 留档), 观察 ledger 在 outputs/动画/_资产/观察_招牌幻觉.md; 假说三分: 场景型/随机型/种子型, 数据够再定。

---

## 2026-09-15z27｜全局一致性审计 — 局部修了没顾全局的五处补齐 (用户令)

用户令: "看看还有什么版本不统一, 有的局部修了没关注到全局"。审计发现并修复:

1. **arcs[] 双轴混用 (页面显示 5.5s 错位)**: _build_doc 原样存 LLM 音频轴时间 (无 tc), 组/镜却 +tc — 同页面 A1-A4 显示早 5.5s、A5/A6 (单场重规划修过) 正确; 曾误判为"A4→A5 轴隙"。修: _build_doc arcs 加 tc; 存量按镜重算 (A1 0.0→5.5 等 4 处); 时间轴现为 5.5→230.37 无缝
2. **text_layer kinds 断层**: v3 导演产出 hero_number/year/logo_note, 草稿装配却把所有文字一律 9 号底部小字幕 — "文字是画面主角"在成片层落空。修: 分档渲染 (hero_number/year=30号居中大字金/title=16号/logo_note=顶部小字/caption=原字幕)
3. **圣经 text_graphic 与字体纪律正面冲突**: 旧版"深青蓝无衬线粗体" vs 新纪律"黑体禁用" — 导演同时收到两个矛盾指令。手改为中文短词+艺术字体+暖琥珀金大字 C 位体系
4. **director2 模块 docstring 停在 v2** ("一镜一段 5-24s 长镜"): 更新为 v3 三层模型叙事
5. **UI 确认文案**: "锁定保留 (磨合成果)" → "保留 (省GPU不重生成)" — 磨合方法论=全局纪律提炼, 产物锁定只是经济账 (用户 0915 纠偏)

---

## 2026-09-15z26｜逐场流水重规划 + 窗口锁定对齐 (三代方案磨成)

**用户令**: "全部重新规划和单个ax规划不一样" → 整集重规划改逐场流水: 有产物 (img_done/approved/anim_done) 的场锁定保留 (磨合成果), 其余场逐个走 plan_scene 同管线; 每场落盘断点续, 全败不动盘; 全新集仍走整集首规划; 系列锁同步注入单场管线 (ep2+ 一致)。

**窗口锁定对齐 (plan_scene 重写, 三代方案)**: ① 整集 realign 微扰他场 → ② 锚式 (头尾锚) 被刚重规划的邻场毒化 (narration 不再音频精确) → ③ **定稿: 窗口内两遍对齐** — 口播头严格匹配 (窗内单调) + 未命中边界按口播字数比例在已知锚间分配; 单调由构造保证。修掉的配套 bug: 差一 (needle(ss[i]) 是起点不是终点, 每镜拿到前一镜的窗, s35 26.5s 实锤); 空文本包幻影 (manifest 末尾 226.1s 空包把 total_end 撑到 456 → 末场钉出窗外; 改从字符流真实末端推导); 零宽跳过保留 LLM 原始时间 (重叠根因)。

**事故坦白 (第三次同类)**: 临时副本测试复制 shots.json 未改 book_title 字段 → 测试存盘按字段写回生产 (A5 被写三版 s19→s25→s38, A6 s44)。资产零损失 (A5/A6 本为 planned), 生产现校验 CLEAN, 备份齐全。教训: 临时副本必须改写 book_title 字段再跑; 已入记忆。

**验证**: 逐场流水真测 (A5+A6, 真 LLM) 全过 — 替换断言/盘上校验/口播对齐逐镜核对; 生产 doc CLEAN。

---

## 2026-09-15z25｜系列风格锁 + 文字层中文优先 (0915 用户定稿三连)

- **z23 运动纪律** (a2p4 横移+边走边变身=抖废片): 运镜白名单只留 固定/极轻微推拉 (横移跟拍摇全禁, H3壳同禁); 特效时序铁律 先移动→静止→触发 (用户定式); 换装/物体变形=大面积重绘单镜禁 (光效+色彩反转表达, 几何大改靠切镜)
- **z23.5 首帧信息完备律** (a2p3): 首帧=动画起始状态 — 全部元素/道具/接触关系就位 (双手已扶墙); 动画=已有元素原位增强, 禁新元素/新接触 (墙从远处移到人=大重绘)
- **z24 文字层定稿** (a1 全英文实锤): 画面大字**中文优先** (关键词中文短词2-4字), 数字/年份/标志保持数字拉丁, 中文长句走 text_layer 后期; 字体纪律: 每镜一种字体禁混搭/艺术字体优先禁黑体/字色融场景/避让主体不压字 (正范本: 暖琥珀金艺术字HBO怯生生亮起; 反教材: LIVE#1平淡字体垮)
- **z25 系列风格锁** (用户令: 第1集生成后锁定, 一书一系列, 新书自由): director2._series_lock_block — ep2+ 整集规划注入第1集当前 doc 锚 (配方/画面策略/场叙事/场景语言/文字风格样例/角色跨集一致要求); 磨合 ep1 自动刷新锚 (跟最新版); 第1集未规划=无锁自由。真实 ep1 数据验证: 锁块 1242 字符含赛璐璐_1988+六场叙事+场景样例

---

## 2026-09-15z22｜三层画面心智模型 — 导演 v3 路线重写 (用户四轮复述确认) + LLM 供应链加固

**触发**: 用户 "导演规划出来的东西都不是我想要的" + 给出 365户/1972小镇 的完整示例, 经四轮复述确认 (用户令: 先复述确认再动手)。

**三层画面心智模型 (PPT 三层, 定稿)**:
- 第1层 背景图 (K2 首帧): 具象纪录片式 (真实年代/地点/场景, 弃抽象隐喻), **全灭字不变**, **视角生图时锁死**
- 第2层 动画层 (H3): 已有物品什么都能动 (车/人/烟/旗/光/水), **运镜轻微甚至不运镜**, 禁大面积重绘 (H3 重绘性能不行)
- 第3层 文字特效层 (内容主角): 关键数字/词随口播节奏浮出, 字体/花字/特效不限 (1972艺术字体/365大字/HBO近似标志弱小浮现)
- 切分: 场=叙事段落 (不按时长算术), 镜=画面单元 (句子是参考不是法律), 每场一组 (组退化容器)

**改动**: DIRECTOR_SYS 全文重写 (三层模型+具象+叙事切分+文字主角设计); h3.py 壳 AMBIENT_LIFE 改 "物品动效+纹理稳定" (旧无风条款是吉普力水彩补丁误伤赛璐璐), 新 TEXT_POLICY (首帧无字/动画中按拍出字/禁其他杂字), Physical lock 加视角锁定+轻微推拉+禁大面积重绘

**LLM 供应链事故与加固 (同夜)**: kimi 周配额尽 (403) + deepseek 推理队列饱和 — 官方实锤 HTTP 200 + 错误体 "900-second timeout limit" (无 choices, 旧代码 KeyError 崩)。修: 200+错误体→RuntimeError; deepseek 同 kimi 关思考模式 (官方文档: flash 默认思考开); ReadTimeout 不重试直接换厂商 (旧 bug 300s×3=15min 卡死)。DeepSeek 官方文档四页学习存档记忆 (定价忙闲双档/思考模式/KV缓存前缀匹配/多轮无状态)。A2 v3 真测通过 (qwen 兜底, 临时副本): 1972宾州小镇+365大字砸落悬浮/1980s客厅显像管金光爆发+90%冲击波/搬运工vs王者双人表情反差 (卡通脸双约跨镜一致)/首帧全灭字/花字随口播节奏 — 对照用户示例验收达标。链序终版 (用户令): kimi → qwen (token-plan 套餐优先用) → deepseek 末位; qwen enable_thinking=false 省配额; 附赠 MCP 权益清单存档记忆。

---

## 2026-09-15z21｜H3 花屏根治 — pinned-RAM 卸载超卖 → --fast-disk 磁盘背书

**触发**: 用户报 "H3 生成视频后是花屏" (宏块色度错乱帧)。

- **诊断链**: s37/s38 废片 10-12Mbps (健康档 1-2Mbps, 噪声压不掉) → ComfyUI 侧原件同垃圾 (排除传输) → s39 在客户端死后纯服务端生成仍废 (排除后端) → s36 硬错 `HostBuffer.read_file_slice failed` = **WinError 1450** (GetOverlappedResult 系统资源不足) → 根因: **H3 模型栈 CLIP 25.9GB + VAE 5GB + UNET 20GB = 51GB > 物理内存 47.6GB**, 默认 pinned-RAM 卸载超卖 — 硬错或静默脏权重 (= 噪声废片)。磁盘/文件直读健康 (0.2s/64MB), 排除坏道; 健康时段 (911 全天/当晚 00:57) 全是热身态 (模型已驻留不走流式)
- **修 ① `--fast-disk`**: ComfyUI 官方开关 "Prefer disk-backed dynamic loading over unpinned RAM" — 磁盘页缓存背书 (pageable 可回收), 正是为模型大于内存的机器设计的模式。改两处: gpu_service_manager/_specs.py (管理器拉起) + E:/ComfyUI_windows_portable/run_nvidia_gpu.bat (手动拉起)
- **修 ② 相位边界卫生**: comfy.free_memory() POST /free (unload_models+free_memory), anim_service 每个 GPU 批开跑前调用 — K2 批 (krea2 栈) 与 H3 批 (fl2va 栈) 不再叠内存
- **状态恢复**: s37/s38 废片 anim_done→approved 重生成, 噪声文件删 (备份 shots.json.bak_花屏修复_0215); ComfyUI 重启清 40GB 缓存堆栈
- **验证**: 真跑 s36 → **anim_done, 1.4MB/2.17Mbps/5.17s 健康档**; 帧统计 均值183/活性1.3 (大平涂赛璐璐签名, 噪声帧活性 30-80); ComfyUI 进程 cmdline 确认 --fast-disk 在位
- 连带: my 后端重启撞杀用户 H3 批事故留档 (教训已记忆)

---

## 2026-09-14z20｜卡通脸全放开 — 名人也露脸 (用户拍板)

**触发**: 用户问 "当前生成的人物都是无脸的, 是系统刻意压制吗?" — 实锤是: z16 人物解禁只解了"出场权", 画法被锁在剪影档 (圣经+导演纪律双层 "禁真实人脸/名人→气质剪影"), 35 镜 23 镜无脸。用户三选一拍板 C: 全放开。

- **DIRECTOR_SYS 人物规范重写**: 1988 赛璐璐卡通脸, 全部人物（含历史名人）都可露脸 — 强风格化变形 (大头比/简化五官/夸张表情), 名人靠标志特征识别 (发型/眼镜/胡型/体态/标志物) 不做写实肖像; 表情升为第一情绪武器 (大笑/皱眉/瞪眼/挑眉/冷脸); 新增 "不同人物外形要有区分度, 别同一张脸换衣服"; 剪影降为风格选项 (逆光登场/远景群像) 不再默认
- **圣经 HBO character_policy 手编** 同步 (系列锁定资产); 圣经生成器 prompt 也注入同教义 (新书不再自发回缩)
- 存量 23 剪影镜靠 🔁 重规划本场 逐场消化 (磨合循环本用途)
- **事故**: 重启后端时与用户在跑的 H3 批撞车 — job 被杀, 每镜落盘保住已完成 3 镜, 在飞 1 镜 GPU 工时损失; 教训: 重启前必查 running job
- 后端已重新拉起 (02:08), DIRECTOR_SYS 新纪律在位

---

## 2026-09-14z19｜单场重规划 — 磨合循环闭环 (规划→K2→H3 逐场反复测)

**触发**: 用户 "给 a1 一个重新生成规划的按钮, 这样 a1 就可以规划→生图→生视频反复测试"。

- **director2.plan_scene**: 单场重规划 — 同一 DIRECTOR_SYS 宪法/圣经的"单窗版"; 给 LLM 场窗 (真实音频原文+时间窗+邻场上下文+旧版画面摘要 "打磨不是复读"), 产出只含本场; 新组/镜用全局新 id (旧 id 退役防撞), `_build_doc` 加 sid/gid 偏移
- **钉窗拼接 (关键)**: 只对场内跑字符级 realign (shim doc + 邻场首组作尾锚, locate 精确落窗尾), 外边界钉死原窗 — 整集 realign 会微扰其余场边界 (实测 ±2 字/±0.5s, z17 毛刺重切), 禁用
- **planner.replan_scene**: 音频门 + 校验网内重试×2 (失败不动盘) + 落盘前 `shots.json.bak_replan_*` 备份; anim_service 加 `phase=replan` (LLM 零 GPU, ep 写锁), 路由 `POST /api/anim/ep/{b}/{e}/replan {arc_id}`
- **前端**: 场头按钮 `🔁 重规划本场` (确认框警示本场已生成产物将退役+有备份), job 面板显示 replan 场号
- **实测 (临时副本, 真实 LLM)**: A1+A2 连测 — 两场校验 CLEAN、其余场指纹零变化、窗口逐秒保真 (5.5-31.61 / 31.61-64.77)、前后缝严丝合缝 (31.61→31.61, 64.77→64.77)、连测链式稳定

---

## 2026-09-14z18｜K2 提示词范式对齐 — Krea2 官方自然语言 + 首帧运动潜力纪律

**触发**: 用户三段令 "1. 全网学习 K2 生图技巧 2. 首帧与动画的关系 3. 回头审我们的提示词"。学完 (Krea2 官方 prompting.md / Runway I2V 指南 / Luma 20 例 / Curious Refuge 首尾帧) 审出两个结构性缺陷:

- **缺陷① 头尾双风格块**: 旧组装 `[风格标签块]。[画面]。[防字]。[同款风格标签块]` — 同一风格词出现两次把画面夹中间, 违反官方"自然语言长句、风格嵌流不堆标签"范式, 主体权重被稀释
- **缺陷② 首帧无运动潜力**: keyframe 按"漂亮静帧"思维写, 没有 I2V 第一原则 — 首帧是视频起点, 姿态要取动作中途、构图要给运动方向留空间; 居中对称呆立图 H3 动不起来

**修 (三处)**:
- `k2.build_prompt` 重构: `[风格定调句(仅头一次)] + [画面自然语言] + [防字句] + [轻锚尾(16:9/平涂)]`; 修掉自引入的双句号
- 风格库 赛璐璐_1988.json head/tail 改写自然语言句 (head="这是一帧1988年代…电影定格画面：" 定调, tail 只留构图/质感锚) — 对所有书所有后续 K2 生效 (风格库单一事实源红利)
- `DIRECTOR_SYS` 三处新纪律: 铁律2 扩为"首帧=运动起点"(动作中途/留运动空间/居中呆立禁); 第五步 keyframe 要求自然语言完整句+镜头词+光线词; JSON 模板 keyframe_zh 说明同步

**验证**: 进程内真调用 build_prompt (真实书风格解析) — 组装为单句流水、风格单次、无双句号; DIRECTOR_SYS 三处纪律在位; py_compile 过

---

## 2026-09-14z17｜字符级对账 — 组/镜时间轴与 narration 由 manifest 确定性推导

**触发**: 用户对账质疑 "s2 这句 28 字, 你确认 8.8s?" — 实锤: 音频包是 0913 包化大包 (21.2s 三句), LLM 包内切组时间按包均分、narration 只摘首句 → 时间与文本两边对不齐 (s02 8.8s 槽装 6s 文本; s03 8.6s 槽塞 100 字).

- **职责重划**: LLM 只管切分意图 (场/组/镜边界 + 画面设计); 时间轴与 narration 由 `_realign_narrations` 按 manifest 字符流确定性重切 (组 narration 首句定位边界, 时间=字符→包内线性映射, narration 覆写为区间真实音频原文)
- **校验拆硬软两级**: 硬校验 (时间断裂/时长窗/缺motion) 才拦截重试; 软警告 (keyframe 混英文风格词 [白名单剔除圣经/配方词]/急推词) 记 plan_warnings 不挡规划 — 物理防护在 K2 灭字尾与 H3 壳, 人检兜底
- **重试捕获宽化**: ValueError (LLM JSON 坏) 也进重试循环
- 实测: s02 8.8s→6.5s (期望6.1 ✓); 时长vs文本量偏差>35% 从满盘→4/35 (边界毛刺级); 35 处误报不再挡规划

---

## 2026-09-14z16｜人物解禁 — 圣经角色策略重写 + 导演产线人物纪律

**触发**: 用户"电视辩论为什么不做两个人形象的对比？全片禁止出人物？看到 a5 一个人物都没出现"。

- **根因**: 第一次圣经 LLM 自定了"唯一剪影配角≤3次≤5秒, 其余全部物件图表光代言"的极端物件化策略 (flat vector 时代产物), v2 prompt 又未带 v1 人物军火库 → 导演把一切人物叙事物件化 (辩论拍成两台电视机)
- **圣经 character_policy 重写**: 人物=主要叙事载体之一; "人的对比/决策/困境"直接画人物永远强于物件隐喻; 双人同框对比=高级武器; 禁真实人脸名人肖像 — 名人用气质剪影演绎 (挺拔/垮塌/汗滴/手势落差)
- **DIRECTOR_SYS 人物规范入产线纪律** (所有书生效): 人物可出场可当主角; 露脸必双约跨镜一致; 光门显形/走入画面/群像剪影实证写法全迁
- **实测重规划**: 辩论段 s15 = "双人分屏: 挺拔发光暖金剪影(昂首) vs 垮塌青蓝剪影(含胸/双下巴/汗滴), 暖米演播网格" — 人物对比叙事到位且不碰肖像红线; 人物镜 0→8/30
- plan 反馈补强 (z15.5): LLM 静默期预告日志/导演调用前后日志/SSE 完成收尾/plan 进度文案/result 场组镜字段修正

---

## 2026-09-14z15｜产线宪法 — 双层定位注入规划系统 (文稿内训级 × 画面抖音级)

**触发**: 用户"暗黑风格不是手调一次的事, 是系统缺陷 — 规划 LLM 不知道我们面向抖音观众; 文稿已达内训级, 画面要达到抖音级, 这个规划不进去永远不及预期"。

- **受众与双层定位 (产线宪法, 优先级最高, 所有书所有集永久生效)**: 视频发布在抖音, 观众高速滑动 3 秒抓不住就划走; 内容可以深, 画面绝不许性冷淡
- **炫度纪律六条** (自 v1 62镜实测军火库回迁+强化): 每镜首拍1s内可见变化 / 构图饱满色块对比强禁大面积空旷禁一束光照暗房 / 动作幅度放开(H3安全域: 光束扫过/粒子迸发/数据跳动/速度线) / ending动感化 / 情绪特效层常规武器化(燃→能量光焰/喜→Q版桃心眼/尬→黑竖线汗滴) / 基调默认明亮鲜艳暗段≤1/3
- **圣经 Art Director prompt 同注入受众铁律** (防书级圣经再出 restrained 暗调)
- 分层定案: 产线宪法(代码)>书级圣经(按书,受宪法约束)>风格配方(画法) — 受众约束从此是系统属性不是某次手调
- 实测重规划 ep1: 6场20组32镜, 暗调镜 0/32, 光效词19处, 画面"金vs青蓝强对比/脉冲光/渠道线铺满变红打结"级饱满度
- 修 bug: director2 顶部缺 resolve_style 导入 (NameError)

---

## 2026-09-14z14｜风格皮肤架构 — 引擎冻结+皮肤数据化, 风格随书变贯穿生成层

**触发**: 用户"圣经(flat vector)与K2赛璐璐锚打架; 下一本书系统不会自己调 ComfyUI workflow, 脱离 agent 怎么办? 除非多个工作流让系统选?"

- **架构定案**: workflow 拆引擎(结构/模型/采样器/sigma — 实测调教冻结) + **皮肤(LoRA+风格锚 — 数据化书级资产)**。系统不需要智能调 workflow: 换风格 = 换一个绑定 JSON, 零代码
- **风格库** `outputs/动画/_资产/风格库/`: 每调教好一个风格存一份 JSON (style_prompt_head/tail + h3_style + lora + lora_strength); 一号成员=赛璐璐_1988 (0911 实测配方原样入库)
- **书级绑定** `风格绑定_{书}.json` → 指向库成员; `config.resolve_style(book)` 解析 (缺省全回退赛璐璐)
- **单一事实源执法**: k2.build_prompt = 配方风格头 + 纯画面 + 防字尾(引擎禁忌) + 配方风格尾; LoRA 节点从配方读; h3 style 锚从配方读 — 镜级 image_prompt_zh 只存画面, 不再拼圣经前缀 (两套风格指令打架根除)
- **正交分层**: 渲染画法=配方锚定; 画面内容/色彩语义=圣经/导演 (HBO: 赛璐璐画法 × 金=钱/深炭黑语义 共存)
- **圣经生成传风格约束**: user prompt 注入绑定风格家族描述, LLM 只按书定色彩/角色/氛围, 不得另起风格; doc 记录 style_recipe 留痕
- **存量清洗**: ep1 31 镜剥掉 flat vector 前缀 (备份 .prestyle); 实测: 配方解析/K2 提示词组装/LoRA 节点/H3 锚全链 ✓
- 将来扩展 (未做): 工坊页风格切换下拉; 圣经 LLM 在风格库语义标签内推荐新书绑定

---

## 2026-09-14z13｜工坊页重设计 — 布局根因修复 + 剪辑室工作台美学

**触发**: 用户"为什么都竖着挤在一起？把网页设计师能力调出来"。

- **根因**: 三层结构渲染进 #grid 容器, 而它带着 v1 卡片网格的 .grid 多列类 → 9 个场块被切成 4 列窄条 (每列 366px), 内部全竖排。修复: 主板改单列 max-width 容器
- **重设计 (剪辑室工作台方向)**: sticky 顶栏 (书名+计数徽标+相位按钮+动线一行); 场块=金色左条+等宽大写场标 a1+timecode 统计+折叠场说明; 组行=剧本文档版式 (左侧等宽 timecode 列 mm:ss+时长, 右侧组头/kf 灰字/琥珀运动线); 镜槽=三列横排, 空槽虚线大框 "🖼 图片槽 · K2 生成后填充"; 深蓝黑底+胶片颗粒纹理+金(场)/青(组)/琥珀(运动) 三色克制点缀; 卡片重写为字符串拼接 (消嵌套模板)
- 实测: viewport 1280 → 场块 1140px 全宽, 组行 flex 1125px, 槽 3 列 325px; 9场27组31槽; AI 视觉验收无挤压/错位/溢出

---

## 2026-09-14z12｜工坊页 qc_plan 文档版式 — 组=文档行, 镜=组尾图片槽

**触发**: 用户贴回 qc_plan 原文标注 "这里差1-n图片，图片审核过了变成视频" — UI 就是 qc_plan 版式, 组行下挂镜槽。

- **组块 = qc_plan 文档行**: 左时间列(起止+时长) + 组头(s1 [运镜] + 口播摘要) + 画面描述(keyframe 灰字) + 🏃 组运动线; 场块(A1 场头+弧运动线+虚线分割) 包住组行
- **镜槽 = 组尾 p1、p2 横排**: 组内重编号 pN (生产层 shot_id 不变, 悬停可见), 每槽图→审过→视频; 空槽显示"未生图"
- **修 bug**: cardHtml 嵌套模板字面量导致整页脚本语法失效 (node --check 定位) — 重写为字符串拼接, 消除嵌套反引号
- 实测: 27 组文档行渲染, 首组 s1[摇] 双镜 p1·p2 ✓

---

## 2026-09-14z11｜三层层级定稿 — 场→组→镜

**触发**: 用户定层级规则 "场(截图A1) → 组(截图S1) → 镜(页面里的s1)"。

- **层级语义**: 场(arc)=叙事意图+运动线; 组(group)=一段口播一个连贯画面逻辑(5-24s, 共享场景/主体/色调); 镜(shot)=生成单位(一图一视频, 4-10s H3 窗口)
- **director2 三层输出**: prompt 第四步切组/第五步组内切镜 (镜数=ceil(组时长/10), 组内运动推进禁跳场景); _build_doc 展开为 groups 元数据 + 全局镜数组 (生产层 k2/h3/approve/draft 只认镜, 零改动); 两层输出兼容降级
- **工坊页三层渲染**: 场块(A1·叙事+运动线) → 组头行(S01[运镜]+口播摘要+组运动+镜状态汇总) → 镜卡片(图/视频/QC操作); v1 旧数据降级 label 分组
- **修 bug**: director2 解析检查 groups/shots 双判; SystemExit 进重试循环; ep1 真跑 9场27组31镜 (4组双镜, 镜时长4.4-9.9s 全落 H3 窗口), 品牌卡镜 s10+s31 自动定位
- **事故记录**: 验证脚本还原 v1 后误覆盖 → v1 42组 shots.json 丢失 (产物文件全在; 唯一人检状态 s01 过审需重点, v2 镜号已变无对应) — ep1 现为 v2 三层结构在位

---

## 2026-09-14z10｜导演引擎 v2 正身接入 — 叙事弧结构 + 开场 5.5s 预留 + "镜头组"术语

**触发**: 用户指 qc_plan.html (scripts/anim_director.py 产出) "这个页面的逻辑就是我们讨论过的痕迹" — 工坊页 42 碎镜平铺"看起来像一句一稿", 弧运动线/圣经/运镜全缺; 且开场剪映特效段与生成动画抢时间。

- **director2 正身并入** `app/services/anim_pipeline/director2.py` (自 scripts/anim_director.py): 叙事弧+运动设计+一镜一段长镜 (5-24s); 时间轴直出 TTS manifest (音频先行原生, opening 5.5s 偏移内建, 不再需要事后 align); 美术圣经书级缓存 (_资产/圣经_{书}.json, 一次生成全系列锁定); prompt 加 keyframe_en/motion_en 英文字段供 K2/H3 直用; 每组补产线字段 (状态机/seed/单拍beats/text_layer/品牌卡补标); >10s 组 overrun_s 标记 (H3 上限, 草稿定格吸收)
- **planner.plan 切 v2** (老 v1 两步法留档 _v1_plan); 校验失败自动重试一次 (LLM 偶发急推词/时间漂); align 对 v2 doc 短路 (原生对齐只补 voice_full)
- **工坊页弧结构呈现**: arcs 分组 (弧头=叙事任务+运动线🏃+组数+时间) + 可折叠美术圣经块 + 每组运镜标签[缓推/移/跟/固定/缓拉] + overrun 徽标; v1 旧数据降级 label 分组; **术语全改"镜头组"** (用户令: 20s 的"一镜"是镜头组不是单镜头)
- **开场 5.5s 预留** (config brand_card.opening_sec): 0-2s 打字卡(逐字)+2-3s 定格 → 3-4.2s 书封墙+4.2-5.5s 主书封 (剪映特效位, 素材拼贴非生成, 与生成动画不抢时间); 草稿装配自动备料: 书封←书库 epub (拆书线 prep_book_wall 同源, 实测主书封+5本拆书史拼墙), 提不出占位卡; 飞入/扫描/收敛动画与音效=剪映后期位
- **时间轴毛刺防御**: 对齐兜底推进 cursor + 草稿装配段重叠顺延 (SegmentOverlap 不再炸整稿)
- 实测: v2 规划真跑两轮 (校验器抓真问题: 开场偏移差/急推词→重试过); anim_director 真产出灌入工坊页 7弧17组渲染全通 (圣经/弧头/运动线/运镜); 开场套件+书封备料真链路通; ep1 已还原 v1 (v2 切换留给用户点"重新规划")

---

## 2026-09-14z8｜QC 定向打回 + 逐镜花字编辑器

**触发**: 用户工坊页走查两问 — "图不通过不叫修改(只想换seed重抽卡不够)怎么办" + "规划没给花字, 想加怎么办"。

- **打回升级为定向修改**: 打回弹窗 = 理由(必填留痕) + 画面描述编辑(预填 image_prompt_zh); 留空=纯换seed抽卡, 改了=定向重生成(新描述+新seed) — K2 文生图的正道: 换seed是抽卡, 改prompt是定向
- **逐镜花字编辑器** 「✍️ 花字」(所有镜可用): 加/改/删 text_layer (类型字幕/大字标题+文本+镜内起止秒), 存 shots.json, 草稿装配自动上暖金色字幕轨; 按钮带现有条数计数; 描边/弹跳等花字特效模板在剪映草稿里手动套 (pyJianYingDraft 本版不支持花字模板)
- 新端点 `POST /api/anim/ep/{b}/{ep}/shot/{sid}/text-layer`; status 镜表补回 text_layer 全量+image_prompt_zh (编辑预填)
- 实测: 花字存取/空行过滤 ✓; 定向打回 s01 (描述更新+换seed+回planned) ✓; 纯抽卡 s05 (描述不动+换seed) ✓; 浏览器双编辑器渲染+保存链路 ✓

---

## 2026-09-14z7｜音频先行执法 + 动线定位 — 动画线与页单工坊二选一

**触发**: 用户"应该是先生成音频再做规划，是不是流程乱了？" + "动画线和单页工坊线是二选一"。

- **动线定位**: 页单工坊=文字→工坊制作 (PPT 替代品族); 动画线=文字→**音频**→分镜→动画 (新动线), 同一集二选一; 工坊页/讲书页按钮/文档全部按此表述
- **音频先行硬门** (0912 用户架构令"先语音→实测真实时长→动画按具体秒数"执法): 无已完成口播 → 规划按钮禁用 + plan job 立即拒绝 ("先在音频页生成"), 零 LLM 花费; 实测蛤蟆ep1 拒绝 ✓ HBOep1/ep2 放行 ✓
- **规划吃真实音频**: plan job 完成后自动对齐归真 + 未生成镜 anim.beats 按真实音频秒数重排 (stretch_beats 公共函数, planner auto_fix 同源) — H3 首次生成即正确时长, 段超8s 在规划时(零GPU)暴露并标 overrun_s; anim_done 镜 beats 不动 (视频已在, 失配交给人决策)
- **align 按钮职责收窄**: 仅音频重渲染 (reroll 改字) 后的重新归真
- ep1 实测 (备份还原): s02 估算6.74→真实6.54s 重排 ✓; s21 真实14.86s→beats铺8s+overrun 6.86s ✓; s07/s11 anim_done 一字未动 ✓
- ep2 稿件无【】段落标签 → plan 报错清晰 (既有数据问题, 待后续稿格式统一)

---

## 2026-09-14z6｜动画产线系统化 — 管线平移入系统 + 工坊页全流程

**触发**: 用户"不要生成了，生成不是你要做的事情，你要做的是根据成功经验开发系统了"。

- **管线整体平移**: `sandbox/anim_pipeline` → `app/services/anim_pipeline` (包自述设计"接入时整体平移"; 零 app 依赖保持, CLI 入口 `python -m app.services.anim_pipeline.run` 语义不变)
- **编排服务** `app/services/anim_service.py`: 内存 job + GPU 占位锁(并发409) + 每集写锁 + k2/h3 包 gpu_service_manager.session("comfyui") 自动拉起 + 镜间协作取消 + 管线日志桥接 SSE (复用 /api/jobs 通道); shots.json 即持久层, 重启自动恢复
- **API** `/api/anim` (`app/routers/anim.py`): status/plan/k2/approve/h3/align/draft/file(防穿越, query 模式支持 ../../_资产)/job+cancel
- **品牌卡零 GPU 直通**: 冻结文案匹配 (落款仪式句+收官品牌句) 自动定位品牌卡镜 (ep1 实测 s11+s42 双命中) → k2/h3 跳过生成直插定稿卡资产
- **对齐归真** `anim_draft.align_episode`: 音轨真理源=主产线口播 (P1 实证, 非重新合成), manifest 包文本×shots narration 规范化字符流对齐 → t_start/t_end 归真+漂移三分类审计 (dry_run 只出报告); ep1 实测: 口播451s→全集成片≈453s, 39/42 精确匹配, 暴露 s09/s10/s21 音频超 H3 8s 上限
- **剪映草稿装配** `anim_draft.build_draft`: 42镜时间轴+品牌卡(5s播完+首帧定格续槽)+text_layer暖金字幕+打字卡标题(roadmap.打字卡 逐字)+整集口播 voice_full.wav; 缺片镜占位卡降级 (拼图彩排); ep1 已装配 (6真材+36占位) 
- **工坊页** `web/anim.html` (bs1_story 范式): 相位按钮门控 + 镜卡片网格(视频内联/行内✅过审🔁打回🎲重roll) + SSE 实时日志 + 进度条; 讲书页每集行加「🎬 动画 ↗」入口
- **验证**: 全链真数据 (42镜 status/approve备份还原测/align dry-run/占位草稿落剪映目录/浏览器走查工坊页含交互); 未跑任何 K2/H3/TTS 生成 (用户令: 生成是人的事)

---

## 2026-09-14z5｜收工 — 开发就绪文档 + 长期记忆更新

- 开发就绪文档: `docs/improvements/进行中-20260914-动画导演引擎-开发就绪文档.md` (新窗口读此文件直接开工)
- 当前状态: P1段5镜全链验证通过(赛璐璐风格), 品牌卡v2定稿, 剩余37镜待批量
- 下次开发: ① 批量k2+h3 ② 音画合成入剪映 ③ 系统化接入(讲书页按钮)

---

## 2026-09-14z4｜美术风格定稿赛璐璐 + 风格随书变原则

**触发**: 用户"1988赛璐璐手绘动画风格可以选定; 以后和核心目标人群一样, 要随着书变化一下"。

- **风格定稿 (HBO 书)**: 1988 赛璐璐手绘动画 — 线条干净/色彩鲜艳/明暗分明/12帧呼吸感/赛璐珞胶片质感; LoRA=白鲸记NSW@0.8
- **风格随书变**: 美术圣经 = 书级资产 (同受众地图), 每本书可能不同; 导演引擎圣经生成器按书的内容/类型/文化背景出风格
- k2.py / h3.py / director_prompt.txt / 品牌卡配方 全部从吉普力切到赛璐璐

---

## 2026-09-14z3｜品牌卡定妆 — 登山客读书→走向群山, 全系列复用

**触发**: 用户"弄好看点复杂点, 复用不用每集都生成" + "s60 有女人/下坡/形象不行, 要冲锋衣+徒步背包驴友造型"。用户确认"可以"。

- **角色**: 登山客 (烧橙冲锋衣+橄榄绿徒步包+棕色登山靴, 背影), 吉普力水彩 1920×1080
- **三拍**: 合书入包→起身眺望→走向群山 + "拆一本书翻一座山"楷体书法金色淡入
- **资产**: outputs/动画/_资产/品牌卡_登山客读书_v1.{png,mp4} + 配方.txt
- **复用**: 每集身份句直接插此 mp4, 不重新生成

---

## 2026-09-14z2｜动画管线 sandbox 全量沉淀 — 14 文件知识蒸馏

**触发**: 用户令"把这个文件夹下的所有内容学习一下, 省得忘了"。完整记录: `docs/improvements/已完成-20260914-动画管线sandbox全量沉淀.md`。

- **全量沉淀**: sandbox/anim_pipeline/ 14 文件 (1942 行) 的架构/配方/提示词/禁忌/纪律全部蒸馏入档
- **关键发现**: director_prompt.txt = 62 镜实测调教的导演手册 (安全词/禁令/微叙事范式/情绪曲线/隐喻轮换); H3 可渲染中文文字且可控字体 (三实证); K2 2ST 配方来源=用户最爱图逆向工程
- **对接要点**: 新导演引擎 (anim_director.py) 应以本管线为底座 (导演/转译/K2/H3/QC/状态机全复用), 不是从零写

---

## 2026-09-14z｜动画导演垂直切片沉淀 — K2/H3 产线技术定稿

**触发**: 用户令"把图片、视频、生成的、约束的、管线的，该沉淀的沉淀"。完整记录: `docs/improvements/已完成-20260914-动画导演垂直切片-K2H3产线沉淀.md`。

- **K2 定稿**: krea2_turbo + 柔和水彩 LoRA@0.7 + 1920×1080 + 吉普力提示词模板 (正面+负面全量); VAE 名必须带 000_ 前缀 (400 报错实锤)
- **H3 双模式定稿**: Mode A 定焦 (locked camera + 6步sigma, 禁 ambient life) / Mode B 首尾帧 (2张图插值, H3 稳定运镜唯一解); 4步→6步 sigma 治抖
- **禁忌表**: K2 文字/H3 自由运镜/H3 ambient/4步sigma/快推快拉/VAE 前缀 — 全部实锤
- **导演引擎**: 7弧17镜校验全过, S02 试产 Mode A+B 均出片

---

## 2026-09-14y｜动画导演垂直切片 — S02 试产定稿吉普力 16:9

**触发**: 用户"不满意太丑了, 按吉普力美术走; 牌匾 TV 字压下去; 改横屏"。

- **风格定稿**: Studio Ghibli hand-painted watercolor + LoRA `krea2_装饰艺术柔和水彩softwatercolor` (0.7); 负面: flat vector/corporate/photorealistic/3D/文字牌匾; 圣经生成默认吉普力
- **分辨率**: 1920×1080 (Node 6 EmptyLatentImage)
- **K2 文字压制**: 正面 "completely blank signboard" + 负面 "TV text/writing on signs/readable content" — 牌匾留空给 text_layer 叠字
- **产出**: S02_keyframe_ghibli_16x9.png ✓ (用户确认"可以")
- **下一步**: H3 从 keyframe 生运动视频 → 全弧试产 → 接系统

---

## 2026-09-14x｜reroll 包 GPU 服务托管 — 后端不在线自动拉起

**触发**: 用户重启后端后直接点编辑器重合成 → 连接拒绝 (TTS 后端未启动)。根因: 批量生成有 `manager.session()` 自动拉起 TTS 后端, 单行 reroll 裸调 `_synthesize_single`, 服务不在线直接 WinError 10061。修: reroll 合成包进 `manager.session(_backend)` — 排队→VPN查杀→确保在线→执行→空闲关停, 与批量同路径。

---

## 2026-09-14w｜reroll 路径停顿标记剥离 — 修"负0.5秒"念出

**触发**: 用户编辑器加 `-0.5s-` 后重合成, TTS 把标记念成"负0.5秒"。根因: `_synthesize_single` 不走 `_tts_text` 清洗链 (批合成管线有, 单行重合成没有), 标记直达引擎。修: reroll 端点合成前 `_pause_after` 剥标记 + 合成后 (归一后) apad 垫静音。

---

## 2026-09-14v｜改字同步修复 — 剥离 ASR 判决门

**触发**: 用户编辑器改字+重合成后, 音频已用新字但稿面/段文字不变(字幕全错)。根因: 三处同步被 `and ok` 卡住 — ASR 回听有杂音(语气词/插字)就不同步, 用户改的字丢失。改字是用户明确意图, 合成成功(wav 已产出)就该持久化; ok 只管音频要不要再 roll, 不该管文字要不要存。一行修复 `if _text_changed and ok:` → `if _text_changed:`。

---

## 2026-09-14u｜音频页统一段编辑器 — 改字+注音+停顿一个文本框

**触发**: 用户令 "把改字、注音、停顿（气口）三个功能合并为一个按钮, 用同一个文字编辑框解决"。

- **✎编辑** 按钮替代 ✎注音+✏改字: 弹出 modal (textarea + 气口快插按钮 + 注音行), 一个界面改完一处重合成
- **textarea**: 直接改字 (→段/稿/书三处同步); 行尾 `-0.5s-`/`-1s-` 垫停顿 (TTS 清洗层剥离, 合成后 apad 垫)
- **气口快插**: `-0.5s-` / `-1s-` 按钮 cursor 位插入; 提示"问句自动 1.2s · 大段落末自动 1s (不用手动)"
- **注音行**: `字=拼音` 格式 (恰=QIA4 份=FEN4), 解析为 annotate dict → reroll API `{text, annotate}` 一发双效
- toast 汇总: 时长+疑点+气口数+注音数

---

## 2026-09-14t｜气口引擎三规则 — 问句1.2s/大段落1s/标记叠加

**触发**: 用户令 "问句在系统停止外再加-0.5s-; 大段落加-1s-; 歧义句上下句加-0.5s-; 气口=留观众反应时间"。

- **三规则** (`_apply_breath_pauses`): 问句=系统停顿0.7+再加0.5 合计 **1.2s**; 大段落边界 (空行分段末行, 整篇末行除外) **1.0s** (`_paragraph_final_indices` 从原文算段结构 — 切行时空白被剥的补账); `-Xs-` 行尾标记改**叠加**语义 (歧义句手控, 无确定性判据不自动化)
- 问句+段末取 max 不叠乘; 上限 3s 兜底; manifest breath_pause 留痕; e2e: 问句1.0→2.2s/段末→2.0s/标记→1.5s; 守卫测试升级, 回归 192 通过

---

## 2026-09-14s｜设问停顿 — TTS 拼装层垫静音 + -Xs- 手控标记

**触发**: 用户令 "所有设问句停顿 0.5-1 秒, 不要连贯读稿, 留观众思考空隙; 在音频合成或剪映层实现" + 手控语法 "-0.5s-" 可行性确认。落音频层 (停顿进音频, 字幕分页/分镜校时/剪映时间轴全下游自然继承)。

- **混合式**: 设问句自动垫默认 0.7s (`question_pause_sec` 可调); 行尾 `-0.5s-`/`-1s-` 标记逐处覆盖 (手控粒度)
- **包器**: 问句/标记行强制收包 (停顿永远落 wav 边界, 不切已解码音频 — bleed 病源不碰); 修溢出回退把标记行再切出去的边界 bug; 全陈述旧行为零变化
- **垫静音**: `_apply_question_pauses` — apad 在响度归一之后 (静音地板不被归一碰), manifest duration 同步 + question_pause 留痕; 标记在 _tts_text 清洗层剥离, 后端绝不喂 (|| 读"炸"教训同源)
- e2e 实测: 1.0s wav 问句行 → 1.7s ✓; 单测 +1 (判定/标记/包组/垫静音四段); 回归 191 通过。回听 ASR 对尾部静音天然兼容

---

## 2026-09-14r｜里程碑 — HBO ep1 文字版收官, 转入音频与产线推进

**用户验收**: "第一集文字版完美收官; TTS 基本可用, bug 随发现随修; 下一步音频生成后梳理后续步骤。" 完整路线: `docs/improvements/进行中-20260914-HBO产线-ep1收官与后续路线.md`。

- ep1 现稿 1523 字: 冻结卡→六拍→P1-P3 逐字钩子→导览钩子行→闲话桥, 确定性执法全 CLEAN; 手裁两处人设锚 (三声部教义: 观众处境/书的事实/工具承诺)
- 后续路线落档: 音频(TTS2+回听)→分镜(书揭示件备料)→素材替换→合成→ep2-9 验收→发布数据回流
- 系统待办: 服务重启上执法闸门; 0914 全天改动待 commit

---

## 2026-09-14q｜ep1 老谭第一人称经历零出场 — 用户令三声部定稿

**触发**: 用户删掉回顾段"这个坑我踩过"三句后问"确保以后都不会自动塞进来?" — 且执法器反扫现稿又抓出拆解三段第二处同病灶 ("我当年带销售…那一年我才懂"), 同病同删 (保"渠道的尽头是自己铺的"金句)。

- **教义**: ep1 大钩子集只有三个声音 — 观众处境/书的事实/工具承诺; 老谭第一人称职业回忆零出场, 血肉留给工具集
- **三层落**: 模板过来人佐证加 ep1 豁免 (禁"我踩过/我当年/我管过"式); ep1 双 hint 注入禁令; `persona_anchor_issues` 确定性执法 (标记命中打回, 守卫测试含"我把这书读薄了"消化感第一人称放行)
- 现稿两处已删 (1625→1523字); 回归 190 通过

---

## 2026-09-14p｜ep1 开场秩序与打字卡骨架确定性执法 — 模型违规打回

**触发**: 重启后实测 044822 稿 — 提示正确(六拍/固定卡全在)但模型违规: 自造旧式打字卡(无骨架)、六拍全跳、P1 直接开头 (deepseek 兜底服从性不足); 旧回填只查长度险些把不合规卡冻进书级。教训: 结构性指令必须有确定性闸门, 不赌模型自觉。

- **typing_card_issues**: 冻结卡→逐字一致; 自产卡→骨架三件套 (每晚睡前/超过90%/15-60字/禁内部词); 缺卡行=违规
- **ep1_opening_order_issues**: 报书名(书名首次出现)必须早于逐字段第1段开场; 钩子段首句不得是逐字段开场 — 六拍先行/钩子后移的机器执法
- **回填守门**: 自产卡骨架合规才冻结书级 (旧版只查长度)
- HBO 重生成实测 (亲跑): 六拍全落地 (报书名→宣言作者→地位×2→自谦→承诺无集数→L1认领+收益路标), P1-P3 逐字全保留数字原样, 冻结卡逐字照抄; 遗留软伤 1417>1380 字+一处金句回声 (耳朵验收定夺)。守卫测试 +2; 回归 190 通过

---

## 2026-09-14o｜书揭示件落地 — 固定开场第二拍, 封面书库自给

**触发**: 用户令 "86万那个 2s 后的剪辑效果, 同款, 直接落; 书的图片看电子书里能不能拿到"。

- **固定开场套件 0-3s→0-5.5s** (storyboard_draft): 打字卡(0-2)+定格(2-3)+**书揭示件** — S00c 书封墙飞入+扫描(3-4.2s, 动画:向上滑动/放大+音效:翻页/扫光)、S00d 收敛主书封定格(4.2-5.5s, 渐渐放大+whoosh+嘭盖章); 新制作位"书封墙"(剪辑·书揭示); 动画/音效=剪映后期位标注在卡上 (pyJianYingDraft 无原生枚举, 沿打字机前缀段模拟哲学)
- **封面书库自给**: `_epub_cover` (OPF cover 引用优先→前6图最大竖图, HBO 首图=站标 332²、真封面 Image00001 2013×2770 实测识别); `prep_book_wall` --book 触发 — 主书封→S00d, 墙=书库其他 epub 封面 5 联张→S00c (账号拆书史语义, 优于扒热榜陌生书); zhihailib 首页=书库不足时的补充源 (未实现, 备档)
- 实测: HBO 墙=乌合之众/亲密关系/人类简史/修补世界/写作这回事 5 本, 全自动; build_history 留痕 book_wall; 无封面时色卡占位兜底

---

## 2026-09-14n｜价值承诺拍禁报集数 — 30秒不给用户压力

**触发**: 用户令 "'我全揉进这九集'就这一句暂时不能透 — 视频30秒不给用户压力, '我全揉进拆书里'就够了"。⑥价值承诺拍措辞改"全揉进这次拆书里", 加禁令: 开场六拍禁报集数/系列长度 (九集/一个系列✗), 集数留到系列导览拍再亮。模板+拼装 hint 同步, 回归过。

---

## 2026-09-14m｜ep1 开场六拍化 — 86万赞模式整段照搬, 系统钩子后移

**触发**: 用户令 "它30多秒都有用, 直接用它这个模式, 把系统钩子往后放" — s6 逐字稿 2.9-31.5s 六拍逐拍验证全有效, 整段采用, 不再自创缝拍。

- **权威梯八拍→六拍** (laotan-book 开局铁律): ②报书名(与书封揭示同步, 禁讲内容)→③硬核宣言+作者→④地位拍×2→⑤"说实话"式自谦反转→⑥价值承诺→⑦受众认领+收益(L1); 旧代入/痛点悬念/缺口/共情/独立书地位/作者拍废 — 痛点悬念由后移的钩子扛, 缺口/共情压成一两句缝, 认领拍已代入禁重复
- **钩子后移**: 六拍完(~32s)才接【开局逐字段】第1段 (故事钩子) — ep1 拼装式 hint 重排, 逐字段摆放注记同步; 元问题深挖仍=第2-3段
- 依据: .tmp/clean_s6_top.txt 时间戳逐拍 (2.9/5.9/11.1/14.5/17.3/20.9/26.1/31.5s); 回归 188 通过

---

## 2026-09-14l｜打字卡书级冻结 — 全系列每集同一句, 骨架冻结两槽随书

**触发**: 用户令 "今后每一集开头前 2s 都是这句话…写活就可以落" — 86万赞真读书号的固定开场是自我收敛+跨高赞反复验证的用法 (重复=系列认别+行为训练, '每晚睡前看一集'本身是行为指令); 结尾品牌句机制的另一半。

- **骨架冻结两槽随书**: "如果你能坚持每晚睡前看一集，一个月后，{收益主体}，会超过90%的{对比人群}。" — 收益主体=书让观众变好的能力翻零上下文大白话 (财商式); 对比人群=和谁比/在哪脱颖而出 (同行/同事…); 其余一字不改
- **落位**: 总纲落库时 `_derive_series_card` 产卡冻结 input_json.系列打字卡 (flash, 骨架不合格拒); episode_gen 全集型 (大钩子/工具/行动册) 首行照抄注入, roadmap_json.打字卡 镜像 (视频层零改动); 无卡时 ep1 自产同铁律并**回填冻结** (旧书兼容闭环)
- 守卫: test_series_card_frozen_formula + test_typing_card_doctrine 合并重写; 回归 188 通过

---

## 2026-09-14k｜打字卡零上下文铁律 — 系列内部词汇禁入卡

**触发**: 用户裁定示例卡 ("九集/判定工具/墙") 莫名其妙 — 卡是零上下文位, 观众刚刷进来脑中什么都没有, 卡上任何需要本系列才能解码的词都是自嗨; s6 真锚每个词冷启动秒懂。

- **零上下文铁律**: 收益主体翻成人人想要的大白话 ("财商"式, 非"工具/判定"式); **禁系列内部词汇** (九集/系列/工具/判定/墙/行动册/秘籍 — 第60秒后的事; "一集"是行为单位可用); 模板+hint+守卫同步
- HBO 卡正确形态示例: "如果你能坚持每晚睡前看一集，一个月后，你看内容生意的眼光，会超过90%的同行。"

---

## 2026-09-14j｜打字卡料源纠偏 — 观众收益承诺三承重 (86万赞真锚), 书主角反差不进卡

**触发**: 用户裁定 0914i 的"数字反差优先"不是痛点 ("365户起家干到90%是HBO的事") → 调档 s6 (86万赞拆书) 真锚卡原文: "如果你能坚持每晚睡前看一集/一个月后，你的财商将会超过90%的人" — 三承重词标黄 (行为频率/收益主体/量化对比), 痛点收益全落在观众本人。

- **打字卡定稿**: 双职能 (给刚刷进来的人一个反应 + 定场) 不变; 文案=对"你"的收益承诺三承重 (86万赞原式), **禁书主角反差当卡** (书的悬念留给口播开场 — s6 口播同样是定场式, 卡管观众收益+口播管书的悬念=真锚分工, 顺带护住 P1 逐字); 禁空泛口号
- 依据: .tmp/benchmarks/tech/hits/20260911_样本6_前90s双轨对照.md (0-2s 深拆) + 跨样本共性 (A族文字钩子卡 s6/s9 双真锚)
- 模板+双 hint+守卫测试同步; 回归 187 通过

---

## 2026-09-14i｜打字卡定场铁律 — 双职能+数字反差优先

**触发**: 豆包 86 分评审讨论吸收 (用户令定稿): "开篇打字卡是给上一个视频的缓冲, 也是定场的内容, 必须确定下来"。3 秒反差的前置载体=打字卡 (豆包看不见的层), 口播 P1 保持逐字。

- **打字机承诺卡→打字机定场卡** (laotan-book 开局铁律①+ep1 双 hint): 双职能 = 上视频切换缓冲 (静音打字不抢口播) + 定场 (3秒砸全片最硬一击); 料源优先级 = 开局逐字段数字反差 ("365户起家，干到90%市场——不是规划出来的"式) > 收益承诺三承重; 空泛口号禁 ("早一步拿回定价权"裸句✗)
- 守卫: test_typing_card_doctrine; 回归 187 通过; 重启+重生成 ep1 生效 (打字卡存 roadmap_json.打字卡, 视频层取)

---

## 2026-09-14h｜ep1 逐字校验两处精化 — 数字形态从严/引号形态等价 + 500 修复

**触发**: ep1 重生成实测 — 三段逐字全挂。逐处定位: ①数字被汉字化 (365户→三百六十五户/1928年→一九二八年); ②引号被弯化 (源稿直引号"美剧之王"→成稿弯引号, 后者是模板字幕/反讽规则要求的形态)。

- **NameError 500 修复**: 0914g 闲话解禁改 `_chatter_ep` 时误删 `_chat_rows` 赋值行 → 全集生成 500 (py_compile 查不出 NameError); 已补回并进程内复跑验证
- **数字形态锁定**: 拼装式开局 hint + 逐字段块加铁律 "数字形态也是字 (365户禁写三百六十五户, TTS 自己会念)"; 重生成后 P2 已原样
- **校验器引号等价**: 直引号"≈弯引号“” (产线规则本来就要求弯引号, 源稿直引号是源文件工件) — 引号形态放行, 数字形态保持从严; 守卫测试双向锁死
- ep1 当前稿 1607 字三段全保留 ✓, 导览钩子行 ✓, 闲话两轴桥 ✓ (回归 186 通过)

---

## 2026-09-14g｜ep1 闲话解禁 — 大钩子集也沿两轴搭桥

**触发**: 9集实测用户验收 — ep1 受众地图料齐 (L1+两轴池带桥) 但稿里一句闲话都没有。根因: 闲话层注入/调度做成了工具集专属, ep1 被头尾例外一刀切豁免 — 而 ep1 恰是系列受众最宽的一集, 池子不用=白炼。

- **生成层**: 闲话注入/池注入/兜底桥 全开到 ep1 (`_chatter_ep = 工具集 or ep1`, 末集行动册纯干货保持豁免); ep1 拼装式开局提示加节拍 — 缝里或导览总起前沿两轴各搭一桥 ("你不做这行也一样…"式拉人)
- **导演层+校验**: 闲话层调度/硬校验改"大钩子集+工具集必填, 仅末集豁免"; 拆解三步的头尾例外不动 (ep1 无正文三步是结构属性)
- 当前 HBO 书 ep1 闲话层为空 → 走池兜底桥 (无需重跑总纲); 新书由导演正式分配。回归 186 通过

---

## 2026-09-14f｜ep1 导览钩子化 — 灵魂三问❸收益链接电 + 禁目录腔

**触发**: 9集全流程实测, 用户验收 ep1 收尾 — 导览每行裸报名 ("第X集，撞墙测试。" 无为什么要听), 且比导演已提炼的灵魂三问❸收益链弱。

- **导览拍铁律升级** (laotan-book ⑩): 钩子清单式 — 总起=【系列收益承诺】收益链一句 ("N件工具串成从X到Y的链条"), 每工具集**一行一钩** (差异化动词+工具名+半句为什么要听), 末行=行动册装订悬念; **禁目录腔裸报名**
- **两股料首次接电** (episode_gen ep1 料区): 【系列收益承诺】=灵魂三问❸ (导演提炼的收益链, 导览总起/收尾承诺必用); 【各集钩子料】=各工具集黄金三秒钩子 (每行一钩的改写源) — HBO 实跑 7 条钩子料全部现成可用
- ep1 两版开局提示 (拼装式/八拍) 同步; 回归 186 通过; 重启+重生成 ep1 生效

---

## 2026-09-14e｜建书链误章修复 — 启动迁移不再盖书级读者画像

**触发**: HBO 全流程重建实测 — 建书后 auto_fill 在服务端异常中断 (state 停 created, 错误被吞), 随后服务重启触发 2026-08-20 启动迁移, 给书盖上 **jingshu-book** 人设受众 (无视书自己的 persona_id, 老谭书挂女性受众章)。手动同路径复跑全通, 确认代码无恙、病在迁移越权。

- **database.py 迁移收权**: 书级 目标读者画像 启动回填整段摘除 (persona 缺省补齐保留) — 书读者只能书内容锁定 (auto_fill 流程内), persona 仅带书兜底; 顺手修摘除后遗留的 fixed_b 引用
- **确定性修复新建书**: author/core_claim 从 L0 meta 回填; 受众地图重锁定 (L1=平台内容操盘手); facing 三面按书定读者重跑
- 教训: 启动迁移写业务语义字段=越权 (它看不见书的上下文); 建书异常被 except 吞进无形 console — 复现要靠服务端日志落盘

---

## 2026-09-14d｜ep1 拼装式开局 — 樊登前三段一字不改

**触发**: 用户想法"樊登稿质量非常高, 前三段一字不改, 大钩子从稿里提, 加大纲+蒸馏质量能保证"。完整记录: `docs/improvements/已完成-20260914-系列结构参数化-集数由书定.md` §五。

- **逐字段注入**: ep1 取樊登稿 P1-P3 ⟦⟧原话保护 (P1=开场宣言+书地位锚, P2-P3=元问题深挖); P4+ 降级背景参考(禁剧透); 常规"重讲"切片拐棍让位 — 成品文不再过传话游戏
- **拼装式开局**: 打字卡→P1逐字→代入/痛点/缺口/共情(生成缝,L1)→作者拍(蒸馏)→P2-P3逐字+一口预演→工具导览→收尾; HBO 实测 3段672字≈146s
- **确定性校验** fandeng_opening_issues: 逐段子串校验(标签行只许插句号处), 换词/缺段打回, 三验收点全接; 无稿书走原路径零改动。单测+回归 186 通过

---

## 2026-09-14c｜拆书线提示词二轮清扫 — 无上下文备注出模板

**触发**: 用户令"影响 LLM 的无上下文干扰信息从提示词模板移除; 括号内容逐审: 备删提留"。完整记录: `docs/improvements/已完成-20260914-拆书线提示词二轮清扫-无上下文备注出模板.md`。

- **判定标准**: 工程注记 (日期/用户令/锚/已废) 整剥; 实锤例剥标签留例 (HBO 个案泛化); 机制说明 (公共块注入/{{common}}渲染/文件交叉引用) 剥; 式示例/料源标签 (逐一对 user_p 核实注入)/反例教学/输出契约全留; .py 注释不喂 LLM 不动
- **42 处清洗**: laotan-book 32 / director 5 / jingshu 头部 4 行 / interaction 1 / episode_gen 提示词串 7 (含 2-5集/六集导览/第6集 三处 0914b 漏网硬编码); 渲染终扫三模板零残留; 回归 185 通过
- **新纪律**: 溯源注记只写代码注释/案卷, 不进提示词串 — 本轮 31/42 是 0910 后新欠的债, 写规则时就得拦

---

## 2026-09-14b｜系列结构参数化 — 集数由书定, 系统只守不变量

**触发**: 用户令"不能一本书一改系统, 让系统自由掌控规划能力"。完整记录: `docs/improvements/已完成-20260914-系列结构参数化-集数由书定.md`。

- **结构不变量+书定参数**: 第1集=大钩子集(不教工具)/末集=行动册集(纯干货装订)/中间=工具集(一集一工具); 工具件数K从蒸馏材料数出(3~7), 总集数=K+2∈[5,9] — 旧六集写死废, 头尾不再干双份活
- **四层落地**: 导演模板数K排集+灵魂三问❷只写L1; 校验 结构带+职能检查+头尾豁免; 生成层集型角色化(行动册预告动态K步X集/行动册集提示重写纯干货装订/大钩子料只吃工具集); 模板与前端六集措辞清扫+批量生成顺序角色驱动+系列句正则泛化
- 单测 9/9 (6/8集合法/带外拒绝/职能缺失打回), 回归 185 通过; HBO 需重启+重跑总纲拿新结构

---

## 2026-09-14｜受众两层 — L1 书定主线 + L2 两轴闲话池 (内容×人群双面大纲)

**触发**: 用户令"第一层=书内容本身, 第二层=泛目标客户跟着书的内容划; 主线用L1, 闲话层用L2", 池定两轴 (上下游/组织上下层), "大纲一面内容一面人群"。完整记录: `docs/improvements/已完成-20260914-受众两层-L1书定主线与L2两轴闲话池.md`。

- **锁定器 v2** `_derive_audience_map`: L1+上下游池+上下层池一次产出, 每组带同构桥; 反漂移三铁律 (观众世界指称/书内cast禁令/轴归位 — HBO 第一轮实锤漂成制片方广告主后补的)
- **总纲双面**: 导演层新增闲话层调度 (只从池选/按集落轴/相邻集禁同组连排) + `_norm_chatter` 归一硬校验落库; 大纲 = 内容面(拆解三步) × 人群面(闲话层)
- **生成层**: 层级配对铁律(每块换一类人=ep2大礼包病根)废 → 受众两层铁律: 主线"你"全篇=L1不换, 闲话按轴搭桥(换人必带桥句), 钩子认领纯L1, CTA对象=闲话层组; episode_gen 注入主受众+闲话层(带池/桥)
- **HBO 三轮实跑定稿** (平台运营/市场负责人/业务负责人/团队长, 四桥全过"你不做这行也一样"检验), 已落 input_json; 重跑总纲后闲话层正式进 roadmap (级联等用户令)。单测 6/6, 回归 182 通过
- **ep1 大钩子豁免 (用户令)**: ep1=生死集 (开局八拍+书三件套+片尾导览, 生成顺序 2-5→1→6), 不走拆解三步 — 导演层 ep1 行填[]/闲话层可不填, 校验豁免 ep1, 生成层 ep1 不注入三步, 段间承接检测 ep1 跳过路标检查 (八拍段首设问合法, 配额照查); 库里已清 ep1 旧正文式三步
- **人设层受众段降权 (防风格回流)**: laotan-book【受众画像=三类人名单】→【听感底色·只管怎么讲】+ 优先级铁律 (讲给谁只认受众地图注入, 本节禁反推人群点名); A/B类=L1/L2 轴别混声明; 导演材料有受众地图时不再喂旧迁移视角双清单; 回归守卫单测锁死名单不回流

---

## 2026-09-13c｜目标读者以书定 — 输入补全受众来源翻转

**触发**: 用户令"目标读者来自账号设定, 不对, 应以书的内容精确锁定"。完整记录: `docs/improvements/已完成-20260913-目标读者以书定-输入补全受众来源翻转.md`。

- **病理**: 目标读者画像 0907 起直接继承 persona (账号人口学), facing 三面 (选题/钩子/读者痛点) 受众基准也是 persona — 书的读者信号 (kernel.why_read "内容创作者急需摆脱流量陷阱") 在蒸馏料里没人用; 下游代价=ep2 钩子受众大礼包
- **【书目读者锁定器】** `_derive_book_reader`: kernel+L0 概念+blurb → 处境+决策场景画像; 三铁律: 禁人口学套话/同构处境 (拆HBO≠只给电视人看)/锁不住 eligible=false; HBO 实跑两轮校准 (第一轮锁成"平台负责人"→补同构铁律), 终版零账号人口学
- **四点翻转**: 字段填充/受众指纹校验/评论层 (顺手修漏传 book 拿错人设的旧bug)/总纲重切 — 书定读者优先, persona 只兜底; persona 继承值视为未定, 补全自动重锁; UI 徽章「来自书内容/来自人设·兜底」
- HBO 库字段已更新 (tier=L0); facing 三面重跑级联**未触发**, 等用户令。单测 4/4, 回归 176 通过

---

## 2026-09-13b｜拆书线段落割裂根治 — 拆解三步调度 + 路标句铁律 + 段间承接检测

**触发**: 用户验收《HBO的内容战略》ep2 —"三五句挺好, 段落间割裂"。完整记录: `docs/improvements/已完成-20260913-拆书段落割裂根治-拆解三步调度与段间承接检测.md`。

- **病理**: 拆解三块内容分配全靠模型即兴 — ep2 拆解一把三档总览完/拆解二新设问重启/拆解三换轨+表名系列句回环; 句级校验管不到语篇层
- **总纲层**: `拆解三步`调度列 (导演切三步, 步间禁重叠禁跳步, 生成器只执行禁自选) + `_norm_steps` 归一/硬校验/落库; 旧总纲无列时 `derive_breakdown_steps` 从秘籍①②③确定性兜底; **顺手修死bug**: 构件调度落库白名单漏掉 (0910 起从未生效) + UI 总纲保存覆写空串
- **生成层**: 模板 0b 路标句铁律 (每块首句=步骤路标, 接住上块尾+报出本步, 禁设问重启) + 0c 表名配额 (全称≤2处, 点名用回指) + 000 回指豁免 (半句指路≠车轱辘 — 段间胶水)
- **校验层**: `section_flow_issues` 段间承接确定性检测 (路标词/bigram咬合/工具名配额/系列句配额), 主稿/重试/压缩pass三处验收点全接; 实库校准: ep2 命中3处, ep1/3/5/6 零误报; 单测 9/9, 回归 172 通过

---

## 2026-09-13｜拆书线音频引擎切回 TTS2 (双参考) + 回听闭环强化 + CTA 集间差异化 + 分镜拼图产线

**触发**: ① 2.5 拖腔方差/截断/胡话三病齐发, 用户"太费心"; ② 系列每集转发话术逐字同句; ③ EP1 要占位彩排试产。完整记录: `docs/improvements/已完成-20260913-TTS25调参弧线与TTS2回摆-回听闭环强化.md`。

- **TTS2 回摆 (老谭线定稿)**: Voice→`老谭·说书(TTS2)`, 音色参考 untitled#8 + 情感参考 untitled#4; orchestrator 补 TTS2 emo 透传 + 带参考时 P5 向量让位; GitHub 上游零更新 + #801 社区盲测 2.5 中文质量回归佐证。验收: 嗯消失/节奏可接受/多音字未见
- **包模式包缝语义化 (深夜补刀)**: 旧分组字数满即切 (行≠句, 033 行逗号开头实锤) → `_group_lines_semantic` 只在句末收包 + 超限回退包内最后句末行, 情绪墙强制断包不跨; EP1/EP2 真实边界逐包一致 = 续传零作废; 单行>140 独包无上限在案; 行模式零改动, 回归 61/61
- **2.5 调参弧线 (封存)**: 台阶三区→斜坡化(zone_duration_factor 边界差0.0017)→全篇5.3地板+1.00+0.6 超调(气口没了说的硬)→**1.16+0.8 回摆定稿**; 5.3 地板改首分钟窗口制(150字), 正文自然方差豁免; ⏩改累加制(df×0.9/行级sidecar锚)
- **回听闭环五强化**: 错音修复预算 6→动态 max(12,n/8); ASR 百分号归一("90%"→百分之九十, 三行假吃字冤案); bleed 匹配同音容差(_bleed_hit 纯同音滑窗, 头→投漏检案 9/9 回归); EP2 实战 bleed 3行+吞字 7行全清; 单行重合成=bleed 结构性根治
- **块生成验证 (待产品化)**: 客户端句边界分包≤160字+独立完整解码+自拼, 351字块 98%完整零语气词; 服务端自拼接判死(接缝劣化胡话); mtok 安全线≤200 token(600+长文=静默截断)
- **CTA 集间差异化**: 固定转发话术撤出系统结尾→按本集秘籍现写收尾段末(模板CTA规格五条+validate_script require_cta兜底+评审四件同步); 豆包分镜框架借鉴五条入待铸
- **分镜拼图草稿产线**: `scripts/storyboard_draft.py` 通用化—占位纯色卡(动画粉/剪辑绿)→SXX素材落地即换→voice.mp3挂口播→每版新草稿+build_history.jsonl留痕; 蓝图0-3s静默开场(逐字打字机前缀段模拟)默认注入

---

## 2026-09-12｜四视图角色定妆照定稿 (老谭读书男女配对) + Krea2EncodeRebalance 考据

**触发**: 用户要做老谭读书虚拟形象。完整记录: `docs/improvements/已完成-20260912-四视图角色定妆照与Krea2EncodeRebalance考据.md`。

- **Krea2EncodeRebalance 考据** (本机节点源码级): text+clip+image1-4, imageN_tokens=low256/normal512/high1024/max1280; `Picture N: <vision>` 节点自动拼正文前 (编号=已连线槽位序, 非槽位号); 正文 `Picture N` 寻址; 人物锚=high 风格锚=low
- **四视图配方** (三视图工坊模板改造): 左35特写/右65四身A-pose + 身份锁定段(GROUND TRUTH/位置固定唯一标识/侧脸90°镜像反向条款/禁漂移五连); 跑 K2 2ST 生产档
- **角色定稿 (配对)**: 女=25岁干练御姐 (狐狸眼+右眼下泪痣+银灰白短发+8头身微丰满沙漏肉感+白衬衫卷袖黑超短裙低腰线); 男=35岁沉稳睿智 (剑眉+左眉尾旧疤+银灰白短发+8头身精瘦倒三角+白衬衫卷袖黑西裤)
- **定妆照入库** `outputs/动画/_资产/`: 四视图母本×2 (原图整张不裁切, 用户令) + 提示词txt×2 (seed 在档, 改妆即改即跑); 特写裁片留 .tmp 实验用
- **路线分叉实证**: 身体规格纯文字灌 >> 参考图 (vision token 整场景渗漏压不住); 参考图原始提示词 (8头身微丰满肉感) 文字化后一次全绿
- **教训在册**: 头身比=抽卡项 (6-8波动, 批roll择优); 侧脸同向=四视图高频翻车点; glm-4v-flash 结构QC可用/bbox不可信; RMBG-2.0 节点缺模型+InspyrenetRembg CPU卡死 (损坏在册)

---

## 2026-09-10｜情绪坡度三修 + 证据图出处闸 + 提示词审计清洗 + HF 字幕配色

**触发**: ① 用户实锤 — 008/009 音色突变 (P5 强度 3→5 硬跳变); ② "连辞两家"稿成片零证据图 (截图全被池规则拦); ③ 用户令 — 全系统提示词审计 (个案/废话/冗长分类清洗); ④ HF 卡底与奶油字幕同色 + hf_title 空配置卡面垃圾。

- **情绪坡度三修**: ① P5 波浪铁律 (≥5档区间≤8句 / ≥5档总量≤1/3 / 峰顶≤3句后回落) + 段边界上跳≥2档逐句爬升平滑器 (α 0.05/档, 台阶→坡度); ② TTS 断点续传情绪指纹闸 (`.emo_*.json` vector+alpha) — 情绪重标后旧 wav 不再被复用; ③ kimi 思考开关翻译 `thinking.type=disabled` (enable_thinking 被无视, 72句标注烧 7964 reasoning tokens 后截断空, 三厂商链全灭→P5 整篇同档兜底实锤)。真机 72/72 重合成: 上跳≥2档=0, 波形 4→2→3→4→5→6→4…有潮汐
- **证据图管线②③**: 新闻线放行 kind=screenshot 入池 + `_NEWS_KW` 事实来源闸门 (声明/推文/公开信/辞职… 过闸无须数字) + screenshot kind 信号对齐; **has_source 出处闸** (VLM 新字段: 平台界面元素/机构标识才算出处, 文档自带标题页/署名/日期不算 — 裸截图信服力=自制HF卡, 不配占位); `_has_scan_product` 升级强制旧标签补扫迁移。"连辞两家"稿 4 张 36氪原文推文截图入片实证, 两张裸封面出局
- **证据图动效修复**: zoompan 铺满全时长→文字截图全文一闪而过 + 低分辨率取整抖动; 改动画窗口 ~1.2s 后定格收尾态 (全文可读≥88%) + 2x 上采样抗抖 (evidence_image.py `_build_vf`)
- **提示词审计清洗** (完整记录: `docs/improvements/已完成-20260910-提示词审计清洗.md`): 5 在产模板+4 common 块+.py SYS 串全部剥离日期/出处/job-id 注记 (laotan-book -25% token); series_director HBO 全套→结构占位; laotan-book/distiller/dedup/interaction 个案泛化; book_core 灵性 13 条拆 `book_spirit.txt` 仅静读书线注入; articles.py 默认模板 laochen_default→laotan-tech_7layer_v2 (老陈/Fish 遗产清场); persona 老谭拆书→老谭读书
- **HF 卡字幕配色**: HF 文字卡窗内字幕切 #465773 深蓝灰 (奶油 #F9F3C4 与纸墨卡底同色实锤) 且窗内去金色高亮单色化; P 线字幕奶油+金高亮不动 (subtitle_style `_SUBTITLE_COLOR_HF` + job_draft 窗口判交)
- **hf_title 大小字硬闸**: LLM 4/7 漏给 render_config 时口播自动提取把整句塞卡面 (title=句子碎片/kicker=27字) → hf.py 确定性闸 (title 4~12字 ∧ kicker≤16字, 违规 RuntimeError→降级 broll) + 导演提示词后果条款
- 事故与修复: 批量正则清注记时全局 replace('()','') 误删代码括号 — git diff hunk 对照还原+difflib 二轮修复 (共 137 行) + py_compile/import/功能烟测全绿; 教训在册 (批量改 .py 提示词串禁全局括号替换)

---

## 2026-09-09｜LLM 主力切 kimi (deepseek 降级系统兜底) + hf_chart 空数据卡巨 0 修复

**触发**: ① 用户令 — 新闻线/拆书线等所有 deepseek 调用点换 kimi (k3=pro 对标 / kimi-for-coding-highspeed=flash 对标), deepseek 保留为系统兜底; ② 产线实锤 — job 91fab4fb slot #32 (收尾口播零数字) broll_pexels 失败后 fallback 到 hf_chart, 模板空兜底造 value:0 → 470px 巨 "0" 形似字母 o, 整卡无内容。

- **kimi 接入实测**: base 必带 `/v1` (缺则全 404); temperature 仅收 1 (传 0.2~0.7 全 400) → 适配层发送前摘除; json_object / stream delta (reasoning 走 delta.reasoning_content 不污染 content) / enable_thinking 全兼容; key 走 `.env KIMI_API_KEY` 不入库
- **主通道**: `LLMService(cfg, fallback=)` 双厂商请求层 `_post_providers` — 连接失败/401/403/404/408/429/5xx 单次请求内换兜底并记日志, 400 不换 (自家 payload 问题换端同炸); stream 成功返回后才读 body, 兜底不重复内容; 模型别名按厂商各自解析; 统一工厂 `get_llm_service()` (kimi 无 key 自动退化 deepseek 单 provider)
- **boost 通道** (素材/实体/镜头契约第二基础设施): 厂商链 `kimi → deepseek → 硅基流动`, `_call` 厂商级循环, 单厂商内核 `_call_cfg` 原重试/空响应 thinking 兜底逻辑零变更
- **调用点全换工厂**: 新闻线 scripts/articles、导演工序单 _llm、拆书 creation_common/creation_steps/distiller 六处; 复查无遗漏直连
- **hf 空卡三层修**: ① 渲染闸 `hf.py` — chart 归一化前置, hf_chart 无 items 且无 growth (`_chart_has_data`, hf_chart.py 共享闸门) → 降级 hf-title-v3 复用已提取 title/subtitle, 判定在模板/时长选择之前保 clamp 对齐; ② fallback 路由三分 `_fallback.py _h_fallback_workflow` — quote→hf_quote / 有数据→hf_chart / 无数据→hf_title (旧逻辑无数据段也进 hf_chart 白跑空卡); ③ 模板双保险 hf_chart_v3 JS `noData` — 无 items 又无 growth 不造 value:0 巨数, stage 留空 (sync_hf_templates 已落 E 盘)
- 验证: 连通性四组合 + json/stream/参数容忍实测; 闸门单测四态 + fallback 路由四态; slot #32 真机重试出标题卡 (manifest template_id=hf-title-v3, 巨 0 消失); 空/有数据 hf-chart-v3 烟测双绿 (空卡纯纸面兜底 / 三柱柱群正常); kimi flash+pro chat、假 key 401→deepseek 兜底实锤、boost 链走 kimi、流式洗稿经 kimi 出稿; 后端重启 health 200
- 代价在册: kimi 上 temperature 恒 1 (审计/判定类 0.2 稳定性诉求让位于可用性, 仅兜底 deepseek 路径保留温度语义); reasoning 占 max_tokens 预算 (极小上限会空 content, boost 空响应重试已兜, 系统现最小 600 可控)

---

## 2026-09-05｜拆书线尾卡双卡入产线 (B-08/B-Q1) + 金句选句 + 品牌改静读书

**触发**: 尾页升级拍板 (版式源 `.tmp/style_gallery` 系 style_book_card.html)。完整记录: `docs/improvements/已完成-20260905-拆书线尾卡双卡入产线.md`。

- **双卡**: `hf-bookinfo-v1` (书名/作者/指南/简介) + `hf-bookquote-v1` (三句书摘 01/02/03 + 锈红关键短语) — 老书籍卡 hf-source-v1 退役; 摆位 = PPT 全页照旧 → B-Q1 (4.5s 静音, title_in whoosh) → B-08 (5s 静音, whoosh) 垫底
- **金句规则**: 每集 3 句 × 6 集 18 句不重复 / ≥12 字 / 逐字出自 L0「可引用原句」池 (95 句调查在册); 选句 = 确定性二元组重合打分 (`book_service/quotes.py`, 集口播引用过的句子天然高分), Episode.quotes_json 新列已回填 6 集
- **logo**: 原图 base64 2.5M 字符超 schema maxLength 实锤 → `_logo_data_uri` 降采样 240px (180K) 注入
- **品牌**: 静姐读书 → **静读书** (角标/卡片/尾注); template_filler 新键 11 个 (logo_b64 跳净标点 — data URI 含 `;`)
- 验证: fill/playwright 烟测 + 真机渲染两卡 completed + RapidOCR 末帧全内容上屏

---

## 2026-09-05｜HF 身份句/收尾互动卡入产线 + 引用逐字闸门 + 模板接线八处定稿

**触发**: 杂志风纸墨系补齐 06/07 两卡; 途中两次产线事故把「新模板接线」定型为**八处清单**。完整记录: `docs/improvements/已完成-20260905-HF身份互动卡入产线与逐字闸门.md`。

- **两卡**: `hf_identity_v1` (开场自介, 全篇 ≤1, 4~6s) + `hf_follow_v1` (固定收尾, 口号+FOLLOW+印章, 3~5s); 均财经线专用 (`_require_tech_track`, geo→降级)
- **逐字闸门** `_gate_quote_text`: quote ⊆ text_context 放行 / 改写→difflib 最相似原句覆盖 / >35 字截句边界 / 单句无边界→RuntimeError 降级; 档位=模板 JS 按字数自动分档 (导演零参与); 旧 `[:80]` 硬截撤销; 净标点豁免键=quote_body/identity_body/slogan
- **20s 间隔降级豁免 hf_follow** (收尾 quote→下期见→follow 天然 <20s, 豁免名单 hf_opening+hf_follow)
- **提示词**: 收尾金句分工 (口号→follow / 点题→quote) + 金句禁选互动引导句 ("评论区聊聊"被当选金句实锤)
- **八处接线清单定型**: ①模板 ②template_library ③hf.py ④slot_workflows/slot_executor ⑤parser 白名单 ⑥schema Literal ⑦枚举+提示词+sync E盘 ⑧**template_filler._SAFE_KEYS** — 漏⑧=成片渲染原始 `{{占位符}}` (job bdb6674b 实锤, brand_name 老键在白名单印章正常而新键漏填); 漏⑤⑥=job 整体 ValidationError 0 slot (job 2375b001 实锤, txt 热载广告新工作流而旧进程 schema 冷载拒收 → **规矩: 八处全落→重启→才跑 job**)
- **「杂志风预览页面」命名约定**: = `.tmp/style_gallery_h.html`; 三约定 16:9 锁 / 动效 ≤2s 定格不循环 / 新模板先过风格门
- 验证: playwright 模板填充烟测 (timeline/断行/印章/slogan 两行) + 闸门离线单测 9+ 分支 + filler 修复烟测全过

---

## 2026-09-04｜Pexels 慢滴流挂死 + LLM 漏 keywords 双修（job 33b2b922 实证）

**触发**: Pexels 网络劣化日, slot 02 下载慢滴流挂死 15min+（requests `(10,300)` 只卡字节间隔永不触发）+ LLM 规划 8/23 pexels 槽漏 `keywords`（下游拿整段口播原文当 query 必然 no usable material）。

- **下载 wall-clock 总上限**: `_http.py` chunk 循环内 `time.monotonic()` 对总时长硬卡（默认 180s, `config.defaults.pexels_download_total_timeout_sec` 可调, lifespan 未跑回退 180）— 超限抛 `PexelsResolveError` 清半成品走 fallback 链; force-stop docstring 同步（最坏 180s 自行超时, 不再无限占用）
- **keywords 兜底（双层防线第二层）**: 提示词第一层（visual_director_v2.txt ⛔ 漏词禁令）+ `_postprocess.py` `_backfill_pexels_keywords` 确定性补词 — 主体 = 同 plan 多数 `keywords[0]` 继承（全片主题一致, 降维搜索主词永不被丢）, 场景词 = 口播/`shot_contract.visual_goal` 命中概念词典（12 组, 蒸馏自具象化铁律视觉符号映射表, 禁自造隐喻）, 全未命中保底 `person using computer`; 在 evidence 闸门**后**跑（降级来的 broll 槽一并覆盖）; 补词记 `params.fallback_keywords` + trace `pexels_keywords_backfill` 可观测
- 验证: 9 单测（假时钟慢滴流截断/半成品清理/脚本环境回退默认/主体继承/概念命中/空数组/visual_goal/cap3+去重/非 pexels 不动）+ 全量 138 过（排除 real_api 真网）

---

## 2026-09-04｜HF 卡黑金 v2 → 编辑纸墨风 v3 入产线（title/chart/quote/source 四模板）

**触发**: 黑金风与科技/时局双赛道调性不符; 设计源 = `.tmp/style_gallery_h.html` (H 系编辑纸墨: 纸色 #F2EEE6 底 + 墨 #1C1613 + 锈红 #9E4A2F, Noto Serif SC + Playfair Display)。**图卡数据规则用户裁决**: 单点→巨数卡 / 恰 2 点→对比卡 / 占比结构(合计≈100%)3~5 段→饼 / 其余数量对比→柱状优先于饼(人眼判长度远准于角度); 饼 >5 段不可读转柱; 柱离散度≥1.8×。

- **模板源**: `templates/hf_prep_v3/` 四模板 (F 仓唯一事实源) + `scripts/sync_hf_templates.py` 增量部署 E盘; 旧黑金模板原样保留, **回滚 = git revert**。chart 四布局合一 (pie→环形/1→巨数/2→对比/3~5→柱群), JS 从数据形状分派 — LLM 契约零新增参数
- **接线**: hf.py 路由 v3 系; hf_chart.py 数据规则闸门 (pie <3 或 >5 → bar, 判定在 [:5] 截断前防 6 段截 5 破坏合计≈100%); 删旧 pie 单扇区自动补"其他"; source 容量 5→8 (条目收集+模板+schema 三处)
- **净标点政策唯一豁免**: `quote_body` 键保留原文 ，。、 (引用卡断行依赖) 仅 HTML 转义; 旧键 `quote_text` 仍净标点 → v1 模板不读新键, 回退安全
- **烟测真机三修** (npx hyperframes 6 例渲染 + RapidOCR 逐帧验证): ① title 日期 `new Date()` 在渲染沙箱冻结为 epoch 出 **1970.01** → 改 `{{issue_date}}` 填充端注入; ② source 8 行 ×26px padding 总高 1036px 溢出 900px 内容区, disclaimer 被挤出画幅 → 行距按行数自适应 (7+ 行降 13px); ③ 对比卡单价场景贵 10 倍被标"领先 10×" → label/unit 命中 单价/价格/成本/price/cost 时低价更好 (chip/verdict 出 更低/便宜 X×), 柱群 hero 同步受益
- **quote v1 协议 bug 修复**: v1 漏 `__timelines` 注册, v2 修复
- 06 身份卡 / 07 收尾互动卡 → 二期 (需新 workflow)
- 验证: 20 单测 (路由闸门/填充豁免/注册接线守卫/来源容量) + 全量回归 (排除 real_api 真网用例 — Pexels API 本机当日不可达挂死, 环境问题非回归)

---

**触发**: Gemini 3.8 稿测试/对比段全是 Pexels 空镜，说服力低。**用户硬条件: 只有测试/比较段才配图**；总闸 = index.html「🌐 抓取 URL」同行「📷 搜图」开关。

- **① 抓图层**: `extract_images`（懒加载/装饰图黑名单/宽度≥400/cap8, 零额外请求）→ `articles.images_json`（非空=总闸开）；建包后台审计后自动扫图 + `POST /packages/{id}/scan-images` 手动补扫
- **② 扫图/选图**: `evidence_service.py` — 条目页扫图（条目≤20/每页≤4/整包≤24）→ 下载缓存（Pillow 校验）→ 9B VLM 打标（is_chart/kind/numbers/quality/watermark, fail-closed）→ 池（is_chart∧quality≥4∧非heavy水印）→ 段级选图（**数字中文读法匹配 ×10**（复用 `_num_to_reading`, "73.7"↔"七十三点七"）+ 关键词×2 + kind×2）
- **③ 执行器**: `evidence_image.py` — 等比+黑边（截图数字不变形）→ zoompan Ken Burns → fade → **drawtext 图源角标**（右下半透明, 避开字幕区）→ render_scale_pad 归一化；缓存丢失误下；params 写回 image_url（同 job 去重）；失败走 fallback 链 broll_pexels
- **④ 导演接线**: visual_director_v2.txt 菜单/params{claim 含数字}/规则/约束≤10/示例 + **三道代码闸门**（可用性: 池空全降级 / 语义: 数字+测试比较词双条件（"一样"词素剥离防误过） / 数量: >10 降级），降级写 fallback_reason + trace；12 处注册点（Literal/白名单/PHASES/PIPELINE/fallback 链/WF_TIER/MIN_DUR/前端标签）全落
- **⑤ 前端**: 搜图开关（默认开）+ 素材条目缩略图行（is_chart 优先, 点击开原图）+ 扫图 SSE + 素材包「🔍 扫图」按钮
- **验证**: 30 单测（Gemini 稿真实段落正反例）+ 真机 ffmpeg（角标像素实证）+ 回归 78 用例全过；设计方案 `docs/design/证据图管线-设计方案.md`

---

## 2026-09-01｜素材线切片质量攻坚（8 修）+ 垃圾三轮出清 + 只收实拍 + 大文件拆包

### 切片管线 8 项修复（material_ingest_service / yt_ingest，用户逐条验收驱动）
- **切点漏检**：scene 0.20→**0.05+叠化簇合并 0.6s**（390s 实测 9→88 切点；RLR 式叠化/地图转场边界 score 仅 0.05~0.08）
- **兜底逻辑**：_split "无条件均匀重切追加"→真洞填补（同素材双份 clip_005≡clip_024 根因）；`//10`→ceil 均分（16s 窗不再一整块）
- **尾帧串镜**：-c copy→veryfast/crf21 重编码精确切（GOP 关键帧对齐溢出）+ 窗尾收缩 0.25s（叠化尾巴）
- **窗容差方向反**：干净窗外扩 0.5s→**内收 0.6s**（外扩把脏秒边缘带字半秒包进窗，4787/4788 实锤）
- **闪字盲区**：OCR 采样 fps=1→**3**（<1s 飞入即出的动效闪字两采样帧全错过，4417/4414 实锤：同管道秒采样全空、精确 seek 有 7% 大字）
- **音画不同步**：输入 seek→**输出 seek**（流落点差 0.1~0.3s）+ faststart（moov 前置）+ **-an 不带音轨**（用户令：素材只要画面，草稿挂载本就 volume=0）
- **JSON 脏格式根治**：_backfill_dims 停止 json.dumps 存列（6400+ 行 `'["工业"]'` 源头；library 翻页 500 已修）
- **probe merge 写**：re-probe 不再抹 OCR 结果；_ocr 收窗内收 0.6s

### 入库判据收敛：is_real_footage 单判据（宁可错杀）
- 只收**摄像机实拍**；地图/地形渲染/大面积国旗/图表/CG/AI 生成感/剪影渐变/商品棚拍/截图/黑白老胶片全拒；空标注自动拒（4409 漏洞：has_burned_text=None falsy 放行）
- VLM 复判存量兜底 recheck_real_footage.py（362 行非 RLR）

### 垃圾三轮出清（库 8780→~5000 行）
- 孤儿行 625（purge_bad_yt ① 整片回收分支漏删 DB 行 bug 修复）；黑白关键词 93；地图关键词 1034（国旗词 dry-run 实证误杀政要演讲实拍→撤回留给 VLM 区分主体）

### 批处理管线（幂等三段，全部 scripts/）
- rlr_reocr（fps=3 时间轴重扫）→ rlr_resplit（重切+清行+重置 job）→ rlr_stage2_parallel（is_real_footage 打标入库）；DB 出清前均备份

### 架构决策（下窗第一优先）：CC 字幕轴
- 用户提案采纳：RLR 烧录字幕=口播词，YT 自动 CC 时间轴≈dirty 轴——**CC 轴为主 + OCR 只扫空隙**，GPU 2h→~30min 且词级时间戳零盲区；语义预判（map/数字段跳过）顺带成立

### 代码维护轮（上午）
- siliconflow key 迁 .env / mktemp 竞态 / 弱哈希豁免；回收站函数三合一（**safe_trash 永久删除红线违规修复**）；jy_draft_service(1594行)/boost_service(1231行)/orchestrator(993行)/ppt 产线四拆包（函数原样搬运零行为变更）；pexels 测试 2 例修复（\_.env 再注入根因）；pytest 41/41；9 commits。详见 improvements 两篇 0901 文档

---

## 2026-08-27~28｜素材层 2.0 实体管线贯通 + 方向裁决（画面层主攻）+ 视觉双层 + 情绪惊讶打底

### 方向裁决（用户令）
- 双主线拆书+新闻（财经/地缘）；稿件层 80-90 分只做维护；**画面层 40-50 分唯一主攻（素材+剪辑）**；ComfyUI 停止投入；实验未成功不并入产线；跑 GPU 前强制退出 VPN（互斥守卫系统级固化）

### 素材层 2.0（设计 docs/design/素材层2.0-实体驱动管线.md）
- 实体抽取六类路由（person/military/event/org/place/concept）+ 显著性过滤（仅公众人物/剔泛化国家）；油管入库管线 yt_ingest（batch/split/tag/register）四段贯通
- 首批 471 片段入库（NVIDIA/台积电/小米官频+半导体纪录片），库容 2129→2600，**烧录字幕检测**无字幕 253 片优先调取；实体靶心通道实测命中（休眠未接产线，ENTITY_LAYER_ENABLED=False）
- 视觉双层定稿：9B 粗标（87% 可信）+ Qwen3-VL-30B-A3B-Q5 精判（21.7G 就位；20 片交叉验证：30B 描述/OCR/品牌全面高一档，Thinking 空响应 25% 待修）
- VPN：变色龙弃用（GUI/admin杀不掉/虚拟网卡冲突）；mihomo(Clash Meta) 装好待订阅；tools/vpn v2 命令行托管

### J 线深化（ComfyUI 部分随停止令封存）
- J2 效果目录 v2：992 草稿全扫 277 条目 + 悬案 7416258989467374091 对号（智能调色·基线画质预设）+ 查询封装 jy_effect_library
- slot 镜头契约层（Hell Grind 12 段裁剪版 7 段）+ effect_recipe 接线导草稿挂特效；i2v 迭代纪律工具（10 失败码+QC+两批停抽）；意象生成收编 tools.i2v（GPU 托管拉起，SaveVideo images 键坑修复）
- 迭代实测教训：F-PROMPT-OVERDO（烟雾过度人审改判）；亮度链（23→77/255）；风格链（卡通手办美学，放弃写实较劲）

### 稿件层维护级
- 口播适配端点（手改稿重跑数字禁令：50%→百分之五十/**加粗去星号**/连字符转中文；漂移守卫；结果只回编辑区人工目检）
- 情绪标注**全程惊讶打底**（全赛道 surprised 主基调起伏靠强度档；撤 geo=serious；兜底全惊讶）——推翻 2026-08-25 geo 规则
- P4 画面感公式 + 拆书线具象化写法（参照物证据/物理行为二选一，禁自夸形容词）

### 知识资产
- 全项目 97 个 md 盘点归类（design/guides/teardown/status 四新分类）+ docs/INDEX.md 总索引与存放规则
- Hell Grind 官方开源参考库入库（higgsfield-prompt-skill 34 子技 + 中文七层架构版）；参考片拆解（Higgsfield 教程片/R edknot 先进封装动画反推）
- 参考仓实验产物补入库（拆解工具×3+报告×6）；GPU-VPN 互斥守卫（admin 进程 Path 隐藏漏判坑修复）

---

## 2026-08-26｜TTS 产线回退 IndexTTS2 + 情绪 span 协议 + 导演层 reasoning 事故根治 + A/B 优化

### 语音产线回退 IndexTTS2（2.5 转实验位）
- **用户决策：2.5 未调清不可投产** — _specs.py 托管回 index-tts-windows；batch_max_chars 恢复 150 / 切分预算 120；ppt 缓存因子 indextts2；duration_factor=0.75 保留（旧版忽略，回升即用）
- **回升 2.5 待办四条已记 memory**（语速重校/serious 听感/呼吸感参考音/句级批 A/B）
- **情绪基调规则随引擎回退恢复**：撤 2.5 时代的 geo 严厉限制（惊讶≤2段≤4），爆点/转折惊讶恢复至强度 6；保留身份段低调、强度坡度、"全篇一个情绪到底=平"等普适规则
- 2.5 接入期的引擎无关修复全部保留生效（中文情绪映射/现场标注/拼音纠音/段快照/缓存引擎因子）

### 情绪标注 span 协议（P5 v2，全链实测）
- 旧协议（LLM 复写全文 3000+ 字贴标签）触发大输出空响应，需 thinking 压制约 48s；重试链最坏 5 分钟（用户实测"情绪标注卡住"）
- **新协议：代码用与 TTS 完全相同的分行逻辑编号 → LLM 只输出 `{"spans":[[起,止,情绪,强度]]}`（几百 token 零复写）→ 代码逐句填充（漏句继承前段）+ 连续同情绪合并** → flash 直跑 ~15s，token 降 90%，段文本与 TTS 行天然逐行对齐（零漂移）

### 导演层 v4-pro reasoning 失控事故（连环三日排查根治）
- **症状**：规划连败 `JSONDecodeError: char 0` — content 空手而归
- **实测定位**：deepseek-v4-pro 对 31KB 规划 prompt 的 reasoning 失控（13.5K~24.4K 波动），8000/16000 max_tokens 均被思考吃光；`enable_thinking=False` 被 pro 忽略（纯 reasoning 模型关不掉）；排除 json_object 嫌疑（去格式一样失控）；旧版能跑是模型服务端行为近期变化
- **修复**：max_tokens=32000（reasoning 24K + content 7K finish=stop 实测通过）+ 空响应可读防御

### 导演层 A+B 优化（快+省）
- **A 前缀缓存**：实测 cached_tokens=0 — 旧版三处占位符替换（标题/口播/素材清单都在静态模板中部）令前缀从第一字符就变；改为**静态模板 21KB 原样作前缀 + 动态全移尾部【动态输入】区** → 实测 cached **16640**，输入费用降 ~60%
- **B 分段先行**：句贪心预聚 12~25s 候选段（83 句→28 段），LLM 决策从"83 句逐句"变"28 段组合"；输入3 改段视图（段号|起止|S 范围|预览）+ 紧凑句级时间表；实测 **28 slot 全部 segment_refs、零 text_context 复写**
- **诚实发现**：reasoning 仅降 25%（18K）— 思考大头是逐 slot 的 9 维选词 + 44 条禁令校验，非切分；C（规则瘦身）/D（降 flash）留待后续
- 净效果：单次规划成本约省一半（缓存 60% 输入 + 零复写输出 + reasoning -25%）

### Pexels 中英文搜索实测（规则定案）
- 中文 query 存在**南京偏置**（港口→玄武湖、北京街头→南京×3）+ 宽匹配跑题（航母甲板→无人机）；英文在中国题材不弱（广州街头/深圳高铁/自动化车厂）
- **定案：搜索词纯英文，不加双语分轨**；实测数据归档备查

---

## 2026-08-25｜IndexTTS2.5 全链收口 + 拼音纠音体系 + 情绪链路重构 + geo 内容引擎（豆包框架复刻）

### IndexTTS2.5 全链收口（切换暴露的四连问题逐一根治）
- **托管切换修复**：删除 app.yaml 里 2026-08-07 的 `tts_services.indextts.cwd=旧目录` 覆盖块（8-24 迁移时漏删，`build_specs` 合并规则把 `_specs.py` 的 2.5 路径压回旧版 → 托管实际一直拉起 IndexTTS2 旧版）；7862 不变，重启后自动 2.5
- **语速校准**：2.5 基线比 2 慢 ~26%（实测中位 4.8 vs 6.5 字/s）→ `indextts_duration_factor: 0.75` 全链贯通（app.yaml→config→tts_service→orchestrator→engines→api_server），音色 params 可覆盖
- **句级批合成**（根除逗号切断+怪停顿）：旧 150 字批 > 服务端 120 token 切分预算 → 服务端按逗号再切+拼接静音（006.wav 实测两处 ~0.5s 停顿）；改 `batch_max_chars=1`（一句一调，wav=系统分句）+ 切分预算 300 → 2.5 整句推理，句内韵律/呼吸由模型自然生成
- **缓存混引擎**：ppt 线 cache_key 掺 `indextts2.5` 引擎因子（防 2 时代缓存音频混拼）
- 2.5 影响面审计：接口向后兼容（超集）/采样率无回归/GPU 充裕/emo_vector 路径不依赖 qwen_emo

### 拼音纠音体系（替代"昇腾→生疼"同音换字补丁）
- **两类机制**：恒定读错（铟/昇）进永久词表 `config/tts_pinyin_map.json`；多音字（行/着/卡）稿内手写 `<行|HANG2>` 临时标注，当次生效不沉淀（防偶发错误固化成系统性错误）
- `pinyin_fix.py`：TTS 入口注入（含防重入/词表损坏静默降级）+ `strip_pinyin_marks` 三处剥离（J线字幕 `wash_subtitle_text`/HF 卡提取/文稿导出）— 观众可见文本永远干净
- 拼音逐条对 `pinyin.vocab` 校验；洗稿 5 个模板清除摆设情绪标签指令（`[calm]` 等无下游消费，纯耗 token）

### 情绪链路重构（P5 拆离 + 三连 bug 根治）
- **P5 从爆改拆离 → 生成音频时现场标注**（audio.py `_do_tts` 入口）：与 TTS 输入同源文本现场标 → "改稿后旧标注错配"窗口物理消灭（此前删句场景匹配指针卡死、后续整段拿错情绪向量，实测复现）；耗时藏进 TTS 引擎冷启动
- **中文情绪名映射**：`resolve_emotion` 只认英文 key 而 P5 输出中文（惊讶）→ KeyError 静默吞掉、**全部情绪段被丢弃恒为 calm**（长期未发现）；`normalize_emotion_key` 中→英 + 别名表，未知名降级 calm
- **基调重写**（修复"逗逼语音包"）：旧 P5 指令"整篇以惊讶为主基调（身份段都用惊讶）、禁止平静"在 2.5+情绪接通后放大成大惊小怪腔 → 按内容气质二分（严肃分析类=serious 主基调，惊讶仅真爆点 ≤2 段 ≤4 档；轻快叙事类=惊讶可用低档）+ geo 赛道硬规则注入（`annotate_emotions(track=)`）+ 身份段恒主基调低档

### HF 卡大小字规范 + hf_quote 接通
- **大小字角色定义**（visual_director_v2.txt）：大字=定性锚点（名词性 4~10 字，"美军航母困局"）/小字=张力调味（动词对偶 3~8 字两拍，"不敢靠 赌不起"）+ 反例 + **语义锚定硬约束**（必须取自本 slot 口播，禁外部金句 — 多卡累积"奇怪"=跳出）；fallback 提取锚点启发式（`_pick_title_and_sub`）
- **hf_quote 全链接通**：解析白名单/Pydantic Literal/fallback 数据感知路由（此前 LLM 输出被洗掉 → quote 数据无消费者 → broll 整句搜索必败 → 降级 hf_chart 渲染空卡）；Slot #8 实测修复（黑金引用卡正确渲染）

### geo 内容引擎（豆包评测框架复刻 + 调优稿技法沉淀）
- **P7 流量评审器**（boost 自动跑）：五维预估（停留/完播/点赞/评论/收藏，赛道基准内置）+ 优势/短板/微调建议/overall 评级；检查点含钩子冲突度、30秒换维度、四层情绪、官方内讧、锚点物件贯穿、人物线回扣、三连排比、关注锚点；只评审不改稿，前端完成态常驻"📊 流量评审 A-"
- **geo 模板 14 条新技法**（豆包调优稿逐段拆解沉淀）：锚点物件一物三用（隐形眼镜级）/人物线闭环（中后段回扣）/官方内讧素材/认知校准句/三连排比/数字反直觉解读/金句锚点（每 200 字 1 个）/密度红线（纯科普 ≤60 字必穿解读）/具体人物钩子/收尾金句/双向思辨收口/定位收口（四拍：金句→思辨→定位→固定结尾）
- **P-L 反问目录层**（独立 P 层，两段式）：LLM 只产 3~5 行递进反问（通读全文、对应后文模块、尾问落观众利益）→ 定稿后**代码确定性插入**（"剥开看"锚点定位+幂等）— 三层保险：LLM 不复述全文（零截断）、不经过 P4（免疫精修删除）、插入纯代码（必然执行）；tech 线跳过（对照组）
- **P4 加固**：首句保序护栏（精修稿开头 12 字 ≠ 原稿 → 回退，防身份段挪到第一句毁首屏标题感）+ 金句去重（收尾金句禁与中段重复）
- **赛道-模板校验**：persona.prompt_template 覆盖时 track 匹配检查（geo 稿被通用模板静默接管 → 强制切 laotan-geo + SSE 提示）；前端选人设时模板下拉自动联动（所见即所得）

### LLM 调用协议升级（全产线普查后分批落地, "LLM 只产标签/引用/增量, 代码负责拼装"）
- **批次1 防崩护栏**：director 规划调用补 `max_tokens=8000 + response_format=json_object`（此前不设限, 长稿输出截断=JSON解析失败=job报废）；P4 上限 4000→8000；解构产物嵌 `_raw_sha` — 洗稿前原文未变直接复用（每次洗稿免一次解构 LLM）；P1/P2/P3 死代码 ARCHIVED 标注
- **批次2 零复写协议**：① director 工序单 — 逐句 S 编号表替代 timings 全量 JSON, slot 用 `segment_refs` 引用编号（禁复写口播）, 代码三级兜底拼装 text_context（refs→旧文本→时间夹逼）, 输出 token 降 ~70%；② 素材增量审计 — 已审素材单行化（补搜轮 prompt 40K→<8K）, `item_numbers` 持久映射跨轮续号, item_tags 只收新编号+旧轮合并, layer_tags 回填改精确映射（修位置反推错位 bug）；③ 洗稿默认 max_tokens 8192
- **批次3 修正+评审**：correct 改 patch 协议（行编号+只输出改动行, 输出降 ~85%, 未改行代码保证原样, 打字机按行模拟, 失败回退旧全稿协议）；P7 同稿 sha 缓存复用
- 前置同款先例：P5 情绪标注 span 协议（复写全文 3000 字→句编号区间标签, flash 直跑 48s→15s）

### 工程修复包
- **J 线导出闭环**：导出剪映草稿成功 → job reviewing→completed + `jy_draft_name` 落库常驻显示（幂等迁移 ALTER TABLE）
- **TTS 段快照**（ObjectDeletedError 根治）：ORM Segment 对象不跨长任务持有，`_do_tts` 在最后一次 commit 前快照 `(id, text)` 纯元组
- **library P 线在线搜索**：悬停视频预览（video_files 最小 mp4，pointer-events:none 屏蔽迅雷类扩展悬浮条）/ 单页 24→40 + 翻页 / 默认横屏
- hf 时长 clamp 跟随模板 `duration_sec_range`（14.9s slot 撞 hf-title-v2 max 10 校验炸）；materials `item_tags` 缺键容错；七层 geo 前端层名双轨（track 驱动）；拆书线女性目标人群语感法则（2 总纲+5 法则，真实修改样本作教材）
- **迁移备份**：digital_human_2 纯代码备份（412 文件 9.5MB，git 清单+运行必需配置），全程同步

---

## 2026-08-23｜口播稿两层机制 + relations 理解面向 + 豆瓣爬虫 + 源头书库体系

### 口播稿质量体系：创作/编辑分离（两层契约，`orchestrator.py`）
- **创作层**：质量标准 8 条入 sys_p — 口语化（6 组反例→正例示范）/ 忠实原文（**原话用 ⟦⟧ 包裹**，禁编造）/ 角色连续性 / 生活常识 / 反讽引号（"挺热闹"=假热闹，字幕保留引号）/ 禁写写作指令 / 段落去重 / 比喻贴语感
- **编辑层 `editorial_pass`**：身份=「抖音口播稿审核编辑」（激活真人口播语感），专职口语化；⟦⟧ 内原书原话一字不改（删标记保留原话）；原书术语概念名（人生坐标/儿童自我状态等）不得替换；输出清 ⟦⟧ 残留；失败回退初稿
- **两层契约**：创作层 ⟦⟧ 标记原话 → 编辑层见标记不改 → 只改标记外表达（替代"单 prompt 干所有活"：大气海报 ≠ 报纸）
- **人设自称「静姐」→「静」**（拉大受众群；`_PERSONA_IDENTITY/_ENDING` + jingshu-book.txt 4 处；prompt 本就禁自称，是后处理注入层写死）
- 字幕链路核实：`wash_subtitle_text` 不剥引号 → 口播稿引号直通字幕
- 验证：E3/E4 重跑 — 獾身份直接给出、高赞金句引用、槽点「康复太快」被回应（"偶尔退回老路，很正常"）、口语化达标

### facing relations 理解面向（L0「提取≠理解」的修复）
- **根因**：L0 是提取层（entities 只有名字无身份、概念只有定义无论证位置、各章割裂）；獾是谁下游只能猜 → 缺的是面向不是 L0
- **build_facing_input 补原料**：【作者序/导读】正文（此前只给名称！）+【全书人物/实体】跨章聚合
- **relations 面向**（第 8 个面向）：characters 跨章人物图谱（identity/relation_to_hero/role_in_book — 老獾=对立者/压力源/代表父母状态）+ concept_roles（核心框架 vs 支撑概念）+ author_intent（core_question/framework/reader_journey）
- 逐集注入 `_relations_block`：人物首次出现直接用图谱身份介绍

### 豆瓣爬虫（`book_service/douban.py`）— 真实读者反应素材
- `search_book`（suggest 接口 → subject id）+ `fetch_comments`（高赞短评=情绪/共识，8128赞「荣格觉醒痛苦」级）+ `fetch_reviews`（书评正文=笔记/深入分析，top5 全文 2500 字）
- **真人槽点金矿**：「蛤蟆康复太快」「结尾投资房地产」「读过的觉得浅显」→ 做稿规避或回应
- 建书自动 `_attach_douban` → `input_json.douban`；逐集注入 `_douban_block`（真实共鸣/槽点/深度）；搜不到优雅降级

### 建书流程重构（防「建书空数据」）
- **create_book 双分支**：L0+facing 就绪 → 同步 auto_fill；未就绪 → 后台线程补跑 L0→facing→auto_fill，前端轮询 `GET /books/{id}/prep`（弃同步阻塞 — 实测 25 分钟建书请求 curl 都超时）
- **【新书】下拉只列全流程蒸馏完成的书**（`/book-sources/ready`：蒸馏txt + L0 + 全 facing 齐）
- 弹窗简化：去作者/书页链接（作者从 L0 meta 自动带）；只留书名 + 可选商品/卖点
- auto_fill 评论层回退：facing-readers 缺失时 flash LLM 生成（防部分 facing 跑过 → 评论层 0 条）
- facing 空 JSON 重试一次（防 kernel 等 `{"error":"empty"}` 静默空）

### 源头书库体系（L0 references/entities/backmatter 孤岛消除）
- **reference_library.py**：`build_library` 聚合全库 L0 references → `data/l0/reference_library.json`；`selection_pool` 选题池（已拆书引用但书库缺失 → 下一批拆书候选）；`leaderboard` 跨书引用榜（被反复引用=有分量）
- 端点：`/book-sources/ready|pool|leaderboard` + `/books/{id}/source-list`（书单体系：本书引用的源头书，粉丝书单/内容关联）
- 前端：books.html 选题池 + 引用榜 panel、books_content.html 源头书单 panel
- 逐集注入源头书/理论 + 相关人物实体（orchestrator）；backmatter 参考文献《》书名并入
- **决策**：源头书素材补搜不走 zhipu — 电子书线解决（选题池候选 = 缺书源清单，补 epub 即拆）

### 成本测算（deepseek v4-pro，08-17 新价）
- v4-pro：输入空闲 4.5/高峰 9 元/百万，输出 13.5/27 元/百万（峰谷定价）
- 一本书**全走 pro 约 3~6.5 元**；现状（L0/facing 本地 Gemma）约 **1.5~3 元/本**

---

## 2026-08-22｜拆书 L0 两级蒸馏 + 产线自动化（UI 去分析按钮）+ 总纲三段结构

### 两级蒸馏（`l0.py` + `facing.py`，设计见 `docs/蒸馏产物schema设计.md`）
- **第一层 L0**：reader 章节块（元信息 OPF/toc/排版强调 §B§/注释锚点/序言附录）→ 自适应打包（小章合并/大章拆 part，≤12K 字符）→ Gemma 逐单元提取（概念/案例/金句/引用源头）→ `data/l0/{书名}/l0-chapter-v1.json`
- **第二层 facing**：kernel（一句话内核）/ units（三段结构）/ quotes（格言/情境分级）/ cases（era_sensitive+当代桥接）/ compliance（tier+safe_angles）/ readers（痛点）→ `facing/*.json`
- **reader 分章精进**：epub 文件内按正文"第X章"标题分节（含 §B§ 粗体 + 目录节过滤）→ L0 章节粒度 = 真实章

### 拆书产线自动化（UI 去分析按钮）
- **评论层精修**：接 facing-readers（痛点+单元关联 unit_id）
- **总纲三段结构**：E1 导读（含系列预告：共 N 集+各集主题，引导追更）/ 中间拆解 / 末集落地；每集带**卖点**（挂车钩子）；集数按 units 自适应
- **逐集按单元消费**：从 roadmap unit_id 取单元素材（core_claim/概念/共鸣/敏感）+ L0 匹配章节金句/案例 + 过滤评论；E1 注入系列预告 prompt
- **auto_fill_from_l0**：创建书后自动填充补全/合规/评论层/总纲（幂等）；前端【预评估/补全/确认1/生成总纲】按钮去掉 → 🧠 L0 面板 + 🔄 同步蒸馏
- **UI 融合**：books_content L0 面板（内核/集数/金句分级/合规）、books.html 书库 L0 标 + 🔄 重蒸馏（已蒸馏书）、books_story 总纲卖点列 + 系列预告

### 其他
- 素材包书外搜索待接入（zhipu 搜书评/访谈，非分析类，后续单独接）
- compliance facing 可选云端 DeepSeek（安全判断容错低，待用户定）

---

## 2026-08-21｜PPT 元素级剪映出片（jy2）+ 拆书 6 集系列上线 + 安全评级

### PPT 出片（`/api/ppt`，08-20~21）
- **元素级拆解 jy2（默认）**：每页 = 1 base 层（背景+自选图形）+ 逐元素透明 PNG（文字/前景图），剪映多轨草稿逐级渐显错峰 —— 替代旧逐帧捕获（真实稿 3h → 秒级截图 + 剪映 GPU 渲染）
- 编排 `compute_page_timing` 纯确定性几何（宫格/块级/口播匹配），不依赖 VLM；字幕轨复用 R9 v3 动态字幕
- **系列皮肤包**（SeriesSkinPack）：母本集抽 color_tokens/背景/字号 → 拆书 5-6 集视觉统一；08-22 改：保留原 PPT 文字色/背景，皮肤不再强改配色
- TTS 缓存（台词哈希 key，稿没变免重跑）、剪映商用字体（孤月体/思源黑体）、首帧免责独立轨 + 系列角标
- 修复链：白底字幕描边阴影、全幅图片 shape 升背景、custGeom 图标 `lnTo` 直线段、voice 音频段重叠

### 拆书系列（`/api/books`，08-18~21）
- 6 集系列编排：输入补全→评论层(读者反应)→素材包(书化)→多集总纲→逐集生成（级联重跑 N 作废 N..6）
- **蒸馏**：本地 Gemma 蒸馏全书→精华稿（`/distill`，batch + 单本，结束全杀腾卡）
- **安全评级（预评估 Gate 0）**：LLM 风险分级 `green/yellow/red` + 雷区 + 安全操作，前端 🟢/🟡/🔴 图标
- 口播稿生成特性 + 豆包 6 集合规规则入库 + 剪映草稿免责/角标

### 合规审查（08-16~19）
- 豆包规则榨取入库（`docs/compliance/` 规则库 JSON + 提示词补丁）：财经科技 / AI 内容专项 / 时政赛道 CR-0 红线
- 拆书线 6 发规则（灵性检测下沉代码扫描：境由心转/21天 等关键词）
- R20 参考资料尾卡（AI 溯源格式）

---

## 2026-08-17~18｜赛道勾选 + 素材质检治理 + R9 自动编排

### 建稿赛道勾选（08-16）
- 文章建稿选赛道：**科技/商业 vs 地缘/国际** → 驱动评论层、七层审计（地缘 L6=横向对照与花边）、洗稿模板分支（laotan-tech / laotan-geo）
- 地缘合规红线入模板（CR-0）：官方表态/领土主权/带节奏敏感词汇，与科技版分道

### 素材质检治理（烂素材治理①②③，08-16）
- 本地碰撞策略开关（`p_line_local_collision`：normal/strict/off）
- VLM 内容质量分（`asset_quality` 深度验收 rubric）+ matcher 硬底线
- Pexels 下载质检三档（sync 同步换候选 / **async 默认** / off），下载即质检闭环

### J 线 R9 自动编排（08-17）
- 字幕块语义分类（金额/爆点/悬疑/金句）→ 内联划重点（金/红）+ 同帧语义音效 + 动效（卡拉OK/放大/星光/上滑）+ 密度闸门
- R9 双缺陷修复：P5 情绪标签泄漏、强调内容从裸关键词升级为语义短语
- 评论分流钩子四范式：2:30 流失高峰位导流评论区
- 来源声明尾卡 + 赛道品牌注入（老谭科技观/老谭观时局）+ v3 动效字幕 + HF 净标点
- 洗稿卡死修复：赛道默认模板引用未定义 article 致 NameError 杀线程；补搜 422（queries 上限 6→10）

---

## 2026-08-16｜J 线（剪映草稿产线）上线 + 效果知识库大丰收

### J1 上线（代码 → 剪映草稿全链路）
- **调查闭环（四连实验）**：改加密草稿三条路全堵（明文镜像被无视/剥离加密不被注册/Timelines 只写不读）；**实验 A 成功**——pyJianYingDraft 生成明文草稿，新版剪映直接打开（打开接受明文、保存才加密），**无需旧版剪映**
- **`jy_draft_service`**：导演 job → 剪映草稿（slot→video 轨，素材短于分配窗口时微降速 ≤15% 拉满修 1.9s 漂移；manifest 逐段 wav→audio 轨；字幕洗 TTS 读法+30 字断句+**自动划重点**——数字/专名大一号黄色，schema 逆自剪映实测 styles 多段 range）；字号 8→5；`attach_sound()`+sfx 轨已挂载
- 导演台「🎬 导出剪映草稿」按钮；`POST /jobs/{id}/export-jy-draft`；`jianying_drafts_dir` 配置
- **模板研究工具链**：`jy_snapshot.py`（通用收割器，打开草稿→一跑即收明文）/`jy_draft_diff.py`（手修学习循环：用户剪映精修=编辑即标注，diff 基线 vs 手修版=结构化学习数据）/`_inspect_jy_template.py`

### 效果知识库（研究循环大丰收，用户指认→AI 解码入档）
- **配方库 R1~R19**：字幕浮现/强调/人像美颜/冲击/讲述/标注/开场钩子/错峰收尾/**R9 协同三件套**（45期：字幕句+关键词大字+同帧音效<10ms）/排版流（16期逐词多轨并行）/R10·R13 引用双方案（商务打字机 vs 文艺连发）/R11 列举鱼贯/R12 引号夹观点/R14 否定标记（红块+撕胶布声）/R15 证据展示（正片叠底标记层）/R16 金句递进（字号阶梯 12→34.6）/R17 评论区证据（我们 deconstruct_json 可直接生成评论卡，反超模板作者）/R18 回忆跑马灯/R19 新闻回顾人物卡
- **音效三层知识**：语义 13 类（用户听音标注两批+16-疗愈全片验证全命中）→ 动画搭配 42 族（教学科 2-1 官方表 83 组）→ 叙事功能 12 类（教学科 2-2，含**情绪音效表 12 条直接对接 P5**）；音效库收割 49 个语义命名 mp3（10 个待剪映打开 17期 落盘）
- **BGM 光谱 6 条**：Wallis（泛提问钩子）/Judgment Day（争议钩子+论证底）/Veil of Darkness（层层推进）/Dismantle（成长历程）/Push Up（事情严重）/Bridge over Troubled Water（温情回忆）
- **三色板跨模板定标**：白正文 / 金 (1,0.96,0.54) 引文 / 划重点黄 (1,0.87,0)/(1,0.75,0.09) 指令 / 冲击红 (0.72,0.11,0.11) / 金句深红 (1,0.09,0.22)；错峰三档 0.1/0.17/0.5s；**密度规则：前 30s 高密度 2~2.5s/个，之后降档 4~6s 只留核心三类**
- **悬案破案**：`7416258989467374091`（×180 最高频）= 纸张做旧质感特效（49期 11 次实证）；`7078971008769593887` = 知识区主字体（16/28期 两现，名字待剪映对号）
- **素材资产**：73 行业工程+教学科入库（明文藏 template.json.bak，v1 检查器盲区已修）；快照 118 模板效果目录重聚合；横版参照 21 个已识别

### 其他
- **P 线关键词双层防线**（ID-050）：导演提示词加具象化铁律+概念→具象映射表（AI→circuit board macro 等）；broll_pexels 代码兜底 `_strip_abstract_words`
- backlog 新增 ID-051 flaticon 图标/logo 素材源 / ID-052 canva 图表（均标合规待确认）
- 效果演示校准共识：供给能力≠成片水平，J 线价值=混合模式（自动 80 分+人工 10 分钟精修到 90 分）；16期排版流模仿验证"意思对，需要调教"

---

## 2026-08-15｜素材聚合（七层喂饱）+ 流水线三页拆分 + 智谱补搜

### 背景
7 层洗稿模板要求 1370~1550 字、每层特定信息类型（背景/参数/实测/商业），单篇原文喂不饱 L3~L6（占字数 75%），叠加真实性红线 → 模型只能少写，字数塌（实测无包 827 字）。用户拍板：洗稿前加「素材聚合层」——多源抓取 → 素材包 → 七层覆盖审计 → 缺口定向补搜（智谱 web-search）→ 整包注入洗稿（实测带包 **1843 字**，钩子/身份段/锚点全对）。同时把流水线页拆成三页：**新闻线索（文章+素材包）→ 文字加工中心（洗稿/修正/爆改）→ 音频加工中心（段落/TTS）**。

### 后端（6 新 8 改）
- **模型**：`MaterialPackage`（audit_json 七层审计产物）+ `MaterialItem`（url/manual/search 三来源，layer_tags 回填）；`Script.material_package_id` 迁移加列
- **`app/services/zhipu_search.py`**：智谱独立 `/api/paas/v4/web_search` 端点（非旧 tools 形态）；query 70 字符硬截断；**`ZhipuUnavailableError`（key 未配置/免费额度 2026-09-12 到期/耗尽）→ material_error 明确中文提示**（用户特别要求：到期不能静默）；调用前比对 `zhipu.free_quota_expires` 日期拦截；双形态解析防御
- **`app/services/material_service.py`**：七层清单常量 + 审计 prompt（LLM JSON 输出 per-layer {covered, evidence, gaps, search_queries}）+ `collect_gap_queries` + `build_material_context_block`（按层分桶 L3~L6，cap 12K，尾部审计缺口提示「未覆盖的层不要编造」）+ `batch_fetch_urls`（ThreadPool×4）+ `search_and_ingest`（link 去重）
- **`app/routers/materials.py`**：8 端点（packages CRUD / items / audit / supplement-search），daemon-thread job 照抄 boost 模式；SSE：`material_fetch_progress / search_start / audit_start / done / error`（jobs.py close-list 已加）
- **rewrite 集成**：`RewriteRequest.material_package_id` → `_do_rewrite` 注入素材块（原文→伪评论→素材包），无包路径 byte-identical；`ScriptOut` 加 `material_package_id` + `prompt_template`
- **顺手修死代码**：articles.py deconstruct 层 `_publish` 引用未定义 `script.id` → NameError 被吞 → 解构层从未生效（已修）
- **配置**：`config/app.yaml` zhipu 节（search_pro/high/count 5/到期日）；`.env` 加 `ZHIPU_API_KEY=`（**待用户填 key**）

### 前端（4 新 9 改 1 删）
- **index.html → 新闻线索**：文章输入（DOM 不动）+ 素材包面板（多 URL 批量抓取/手动贴素材/单条 URL 快加/素材清单/七层红绿审计 chip+证据+缺口/补搜词 checkbox 默认勾选/补搜缺口/去洗稿带参跳转）+ **常显「智谱免费搜索额度至 2026-09-12」提示**
- **writing.html + writing.js**（新）：洗稿全流程 + 素材包徽章（N 条素材 · 覆盖 x/7）+ **字数实时统计**（目标 1400~1550，不足时提示回新闻线索补素材）+ URL 参数 init（article_id/package_id）+ 去生成音频
- **audio.html + audio.js**（新）：段落全家（勾选/拖拽/即时落库）+ 音色 + C/P/H 管线开关 + 一键成片 + 导演台 handoff（不变）+ **persona 音色锁按 script.prompt_template**（拆页后模板/音色分居两页的解法）+ 本页脚本下拉（回调 `selectScriptForAudio` 与 writing 不同名）
- **app.js v6→v7**：只留共享层（api/setStatus/toggle/toast/管线开关/fetchScript 硬化/loadScriptList/loadVoices 守卫）；单页函数全部迁出；`?v=7`
- **nav**：全站 9 页「流水线」→「新闻线索/文字加工/音频加工」三链接；删 index.js（回收站）

### 验证
- 后端 E2E（python requests 实跑）：建文章→建包→手动素材→审计 SSE（material_done 5/7）→补搜（key 未配置 → material_error 明确提示 ✓）→ 7 层模板+包洗稿 **1843 字**（目标 1370~1550）+ material_package_id 回填 + 锚点/情绪标签正确；坏包 ID → rewrite_error；无包回归正常
- 前端 Playwright：三页 0 console 错误；news 面板/7 chip/到期提示 ✓；writing 徽章「2 条素材 · 覆盖 5/7 层」✓；audio 57 段落+5 音色+音色锁 ✓；跳转链 news→writing（带参）→audio（带 script_id）✓
- 待办：用户填 `ZHIPU_API_KEY` 后实测真补搜；到期日 2026-09-12 后客户端会拦截提示

---

## 2026-08-14｜boost 7层适配 + 情绪链路全线打通 + 提速 (enable_thinking)

### 背景
洗稿提示词升级到 7 层（`config/laotan-tech_7layer.txt`），但爆款改造 boost 的 P1-P5 仍为旧六模块设计，跑 7 层稿全面破坏结构；P5 情绪标注链路多处断裂（IndexTTS 永走 calm 兜底）；reasoning 模型失控思考导致 boost 单步 110s+。

### 改动（5 块）

**① boost 提速**（`app/services/boost_service.py` `_call`）
- payload 加 `"enable_thinking": False`（siliconflow DeepSeek-V4-Flash）。实测复杂 prompt 单步 **110s+ → 15s**——根因是 reasoning 思考链失控，`max_tokens` 限不住思考（只限 content）。
- 降 max_tokens：P2 8k→3k / P3 10k→3.5k / P4 10k→4k / P1 4k→1.5k / P5 12k→4k。

**② boost 7层适配**（核心）
- 实证 A/B 对照（`_test_7layer_ab.py`）：7层+P5 vs 锚点版+P1-P5。结论 **P1-P4 对7层纯破坏**（覆盖钩子/身份段/结尾、清单残留、重复句），A 完胜。
- **砍 P1/P2/P3**（电击开场/预埋/呼吸点——7层已有等价结构，它们只覆盖）。
- **P4_PROMPT 重写**：1:1 锁定 7层结构/身份段(`我是XX专盯`)/结尾(三件套)/钩子，只精修表达；去硬编码旧身份段(`大家好我是老谭…不确定的时代`)/旧结尾(`听懂逻辑…下期见`)/行内`[calm]`注入。
- `run_boost` 编排简化：`原稿 → P4 → clean → P5`。
- `clean_boosted_text` 加剥 markdown 标题（`## 第X层` / `# 精修清单`，防残留进 boosted_text）。

**③ P5 情绪链路修复**（3 处连环 bug，修通后 IndexTTS 才拿到情绪）
- `app/routers/scripts.py:321`：`boost.get("emotion_annotations")` → `"p5_annotated"`（run_boost 返回 key 不匹配，**P5 情绪标注恒 None，IndexTTS 永走 calm**）。
- `app/services/emotion_dict.py`：加 `confident`（P4/P5 白名单含 confident 但字典无 → KeyError 被吞）。
- `app/services/tts_service.py` generate 入口：emotion_annotations **str → list[dict] 解析**（DB 存 P5 产的 `"[情绪/强度]文本"` 字符串，TTS 当 list[dict] 遍历 → `'str' object has no attribute 'get'`）。

**④ TTS `||` 停顿修复**（`scripts/tts_lib/text.py` `_tts_text`）
- indextts 也 `||`→逗号。原 `keep_breaks` 保留 `||`（误以为 IndexTTS 认），实测 **IndexTTS 不认 `||` 读成"炸"**（本地源码 `_split_test.py:15` PUNCT = `，。！？；…`，无 `||`）。

**⑤ 前端**（`web/app.js` `fetchScript`）
- 加 `toggle('btn-audio', true)`——调取已有脚本后生成音频按钮恒灰（漏 toggle）。

### 新文件
- `config/laotan-tech_7layer_anchored.txt`（锚点恢复版7层，对照实验B用，阶段5未采用——保留备查）
- `_test_7layer_ab.py`（A/B 对照实验脚本）

### 涉及模块
`app/services/boost_service.py` / `app/routers/scripts.py` / `app/services/emotion_dict.py` / `app/services/tts_service.py` / `scripts/tts_lib/text.py` / `web/app.js`

### 验证
- boost 实测 **21s 跑完**（原 5-15 分钟），身份段 7层格式（`我是老谭‖专盯…`，无旧版），emotion_annotations 8400字落库（P5 标 8 段：surprised×5 + serious×2 + happy×1）。
- IndexTTS 日志铁证情绪传导：`Use the specified emotion vector` ×1204 + `scaled emotion vectors to 0.4x [..0.2799(surprised),0.04]`（P5 `[surprised/3]` → resolve_emotion vector[0.7]+alpha0.4 → IndexTTS 消费）。
- 完整链路：`P5标注 → emotion_annotations落库 → tts_service解析 → resolve_emotion转vector → synthesize_lines按段分组 → _synth_single(emo_vector) → IndexTTS`，第一次全程跑通。

---

## 2026-08-08｜Slot「重试」按钮无反应修复 — 僵尸 running 放行 + retry 事件流打通

### 背景
用户：Director 页面 Slot #2，「重试同类」点击后执行日志有明确反应，但「重试」点击后执行日志毫无反应。

### 根因
job `79fc44b6` 处于 `reviewing`，但 slot_index 2（`hf_title`）是 **`running`** 且无 output —— 服务重启/执行中断遗留的僵尸状态。此状态卡死两条链路：
1. **前端** `showSlotDetail` 的 `canRegenerate` 只认 `completed/failed/skipped` → 僵尸 running 的「重试」按钮被 `disabled` → 点击物理无反应。
2. **后端** `retry_slot` 对 `status=="running"` 一律 409 拒绝，即使 job 已不在后台执行。
「重试同类」只看同类其它已完成 slot，故不受影响 —— 这正是两按钮行为差异的来源。
另：`retry_slot` 正常路径原本无日志、不发 SSE 事件，即使重试成功执行日志也空白。

### 改动
1. **`app/routers/director_routes/retry.py`**：
   - 新增 `_job_truly_executing(job)` 僵尸检测（`job.status=="executing" and job.id in _executing_jobs`）；`running` 分支改为：真在执行 → 409；僵尸 → `logger.warning` + 重置 queued 放行。
   - `retry_slot` 补 `logger.info/warning` + `director_events.publish` 的 `slot_start/slot_done/slot_fail` 事件（与 `/execute` 事件流一致）。
2. **`web/director.js`**：
   - `showSlotDetail` 的 `canRegenerate` 增加 `zombieRunning = status=='running' && !_jobExecuting`。
   - `retrySlot` 点击后先 `showExecLog()` + `connectSSE(currentJobId)`，与「重试同类」一致，让重试进度实时显示在执行日志抽屉。

### 涉及模块
`app/routers/director_routes/retry.py` / `web/director.js`

### 验证
- ✅ 僵尸 running slot（`ab9b278d`，slot_index 2）POST retry：HTTP **200** `completed` + 产出 `visual_segment.mp4`（此前 409 拒绝）
- ✅ SSE 订阅实测捕获 `slot_start` → `slot_done` 两条事件（msg「Slot #2 重试开始/重试完成」）
- ✅ 服务器日志出现 `treat as zombie and allow retry`
- ✅ `py_compile` + 模块导入 + `node --check` 全过

---

### 背景
用户：合成后语音和画面没对齐，视频长一截（4:24 vs 音频 4:09 就结束）。量化 job 79fc44b6（58 slots）：`sum(alloc)=245.89s` vs `sum(actual)=264.19s`，视频超长 **+18.30s**。

### 根因
`hf.py` 渲染 HF 卡时 `input_data["duration_sec"] = max(5, min(30, round(duration)))` — 模板 schema 硬约束 `duration_sec ∈ [5,30]` 整数秒，短卡（如分配 1.77s）被夹取渲染为 **5.00s**。concat 用 `-c copy` 不裁剪，累积偏差让视频轨比按分配时长精确切片的 TTS 轨（`atrim`/`aevalsrc`）长一截，尾部画面无配音。broll 精确（`-t` 重编码）、host 直接传精确时长，唯独 hf 被夹取。模板约束无法源头修复，须在合成阶段归一化。

### 改动
1. **`app/services/composition_service/slots.py`**：Step 1 `_collect_slot_sources` 循环后对每个 clip 做时长归一化 `_normalize_clip_duration`：
   - 实际时长 vs 分配时长（`slot.end_sec - slot.start_sec`）偏差 ≤ `_NORMALIZE_TOLERANCE`（0.2s）→ 复用原文件，保持 `-c copy` 零重编码
   - 超长 → `-vf tpad + -af apad + -t <alloc>` 重编码裁剪（`tpad=stop_mode=clone` 冻结尾帧、`apad` 补静音，`-t` 钉死精确时长）；下溢 → 冻结尾帧 + 静音补齐
   - 输出落点 = **job root** `normalized_<slot_index>.mp4`（非 `src_paths[0].parent`），确保 cleanup 能回收
   - 编码参数（h264 / 48kHz aac / 30fps / yuv420p）与 `render_scale_pad` 一致，可与 `-c copy` concat 混拼
2. **`app/services/composition_service/pipeline.py`**：`_collect_slot_sources(db, completed, evt, root=root)` 传入 job root。
3. **`app/services/composition_service/common.py`**：`_cleanup_intermediates` patterns 加 `normalized_*.mp4`。

### 涉及模块
`app/services/composition_service/slots.py` / `app/services/composition_service/pipeline.py` / `app/services/composition_service/common.py`

### 验证
- ✅ 真实素材离线三阶段（normalize→concat→mix）：drift **+18.30s → +0.34s**；真实 `compose_director_job` 重跑成片 **246.25s**，`video=246.25s audio=246.25s delta=+0.00s`（目标 ≤0.2s），归一化 13/58 clips（slot 2/7/14/17/21/26/31/32/41/51/52/54/57）
- ✅ 音量无回归：成片 mean −29.7 dB / max −8.9 dB，与修复前成片（−30.0/−8.9）一致
- ✅ cleanup 闭环：82.8 MB 中间件全部回收，成片 + manifest + 洗稿.txt 保留，slots/ 零残留
- ⚠️ 已知（修复前已存在，非本次引入）：loudnorm `input_i=-inf` 走 copy fallback，成片音频流实际有声音

---

## 2026-08-08｜合成视频后写入洗稿文本 — 成片同目录产出 `洗稿.txt`（对照复核）

### 背景
用户：洗稿后的 txt 长期只存 DB（`scripts.script_text`），本地找不到文本文件，人工对照成片复核不便。要求**合并到【合成视频】流程**：合成成功后把洗稿 txt 放进视频同一文件夹。

### 改动
1. **`app/services/composition_service/output.py`**：`_finalize_output` 成功路径（manifest 落盘后、`job.status="completed"` 前）新增 `_write_script_txt(job, final_normalized.parent)` — 取 `job.script.script_text`（洗稿后手动编辑以最新编辑为准），UTF-8 写入 `composition_root/洗稿.txt`，与 `director_<job_id>.mp4` 同目录。空文本/无 script 跳过（非致命，异常不阻断合成）。
2. **`app/main.py`** retention sweep 孤儿清理白名单加入 `洗稿.txt` — 过期 job 扫描不再误删洗稿文本（与成片 + manifest 同保留）。

### 涉及模块
`app/services/composition_service/output.py` / `app/main.py`

### 验证
- ✅ 单测 `_write_script_txt`：正常写入（内容一致）x2 / 空文本跳过 x1 / 无 script 跳过 x1
- ✅ 回归 `_cleanup_intermediates`：仅删 concat_list/concat_raw/with_audio/normalized/silence_fallback，成片 + manifest + 洗稿.txt 全保留
- ✅ `main.py` retention sweep 保留集含 `洗稿.txt`
- ⚠️ **必须重启后端**（`reload=False`）生效

---

## 2026-08-08｜人物即账号 — Persona 吸收品牌/开结尾/host_id，成片去「老陈」硬编码（最终方案）

### 背景
成片上仍出现「老陈」字样。已排查确认模板已完全占位符化，字样来自注入数据链路：`main.py` seed 硬编码唯一 host「老陈聊财经」（persona_key=`laochen`）+ `hf.py _merge_brand` 从 `host.name` 注入全名、猜前 2 字注入印章。用户设计指令：**如果需要注入，页面上应该有选择、可编辑、可流水线改造，或在人物关联中绑定**（同一人的洗稿 txt 是每个数字人的开始，不会乱）。经三次方向纠正，定稿为「人物即账号」：人物（Persona）与账号（Host）合并为一个人物页，人物页是唯一管理入口。

### 改动
1. **数据模型（人物即账号）**：`Persona` 吸收品牌三字段 `brand_name`(128)/`stamp_name`(16)/`brand_tag`(64) + 口播开结尾 `fixed_opening`/`fixed_ending` + 显式 `host_id` FK（1:1 绑定 Host）。`Host` 保留三 brand 字段（迁移遗留，兼容旧数据回退）。`database.py _apply_manual_migrations` 幂等 `ALTER TABLE ADD COLUMN`，personas 数据绑定修复仅在 `len(hosts)==1` 时自动补绑。
2. **API 层**：`personas.py` CRUD 支持 brand 三字段 + 开结尾 + host_id；`GET /api/personas/by-template/{tpl}` 供模板联动。洗稿 `RewriteRequest` 带 `persona_id`，`articles.py _do_rewrite` 解析链路 `persona_id → persona 的 host → request.host_id → config 默认 → 首个 host`；persona 未绑 host 时明确报错「Persona has no bound host」，不让老陈兜底冒名顶替。
3. **流水线取数闭环**：品牌取 `persona.brand_name/stamp_name/brand_tag` > `host` 同名字段 > 中性兜底（财经频道/财经/数据解读）；开结尾取 `persona.fixed_opening/fixed_ending` > `host` 同名字段；音色取 `persona.voice_id` > `host.default_voice_id` > host 下第一条 voice。`hf.py _merge_brand` 经 host.id 反查 persona 取品牌，显式字段优先。
4. **前端**：人物页 `web/personas.html` 为唯一管理入口（人物 = 模板 + 音色 + 形象 + 账号/品牌 + 开结尾）。「账号管理」页 `web/hosts.html` 已弃用入回收站。洗稿页「数字人账号」下拉改从 `/api/personas` 读取（选项值 = persona.id），`rewriteArticle` POST body 带 `persona_id`，模板选择联动自动锁定音色/形象/账号。
5. **去硬编码**：`main.py _seed_defaults()` 默认 host 品牌三字段置 None（name=「数字人频道」，persona_key=`laochen` 保留为 config 默认键，纯回退不再上屏）。

### 涉及模块
`app/models/content.py` / `app/models/persona.py` / `app/database.py` / `app/schemas/articles.py` / `app/routers/personas.py` / `app/routers/articles.py` / `app/services/slot_workflows/hf.py` / `app/main.py` / `web/personas.html` / `web/index.html` / `web/app.js`

### 验证
- ✅ **E2E 35 PASS / 0 FAIL**（`e2e_persona_account.py`，临时库隔离）：迁移幂等 x2 / seed 中性无老陈 / persona CRUD 品牌字段 / 洗稿 `persona_id→host` 解析 / 首段 opening + 末段 ending 取 persona 开结尾 / 洗稿文本与注入数据均无「老陈」/ `_merge_brand` 注入谭聊财经·谭聊·数据锐评
- ✅ 「老陈」全仓残留审计：无真正影响成片的硬编码，残留均为注释、LLM 示例（已被 `_strip_host.py` 去出镜化）、测试占位符或清洗规则
- ⚠️ **必须重启后端**（`reload=False`）迁移才应用到生产库（personas 表新增列、seed 中性化生效）

---

## 2026-08-08｜HF 渲染 300s 超时修复 — `--low-memory-mode` 绕开 calibration 卡死 + 横屏模板 letterSpacing lint error 清理

### 背景
job 79fc44b6 slot#2（hf_title，横屏）渲染报 `HyperFrames timed out after 300s`。render.log 显示 compile 41.9s 后 **capture_calibration 阶段卡死**：auto-worker calibration 两次 `Runtime.evaluate timed out`，150 帧 0 完成。

### 改动
1. **后端 `app/services/hf_client.py`**：render 命令追加 `--low-memory-mode` — 固定 1 worker + 强制 screenshot 捕获 + 跳过 auto-worker calibration。本机 32 cores + Intel UHD 集显下 `--workers auto` 高并发起多个 Chrome → 集显资源耗尽 → calibration 超时；`--low-memory-mode` 实测 39s 稳定出片（此前 300s 超时）。产物规格（1920×1080 / 30fps / h264）与成功 render 完全一致。
2. **横屏模板 `news_magazine_v1_ls\index.html`（仓库外）L415-418**：移除 `tl.fromTo(headline, { letterSpacing: "0.12em" }, { letterSpacing: "0.02em" })` — 触发 `gsap_non_transform_motion` lint error（文本重排属性，seek-by-frame 捕获引擎下 stutter）。字距展开感已由逐字 stagger 弹入承担，无需额外字距动画。

### 涉及模块
`app/services/hf_client.py` / `E:\AI\digital_human\hf_prep\news_magazine_v1_ls\index.html`（仓库外）

### 验证
- ✅ 后端链路（fill_template → render_visual）完整渲染：44.6s exit=0，mp4 333632 bytes，1920×1080 / 30fps / h264
- ✅ render.log 无 `gsap_non_transform` / `letterSpacing` lint error（此前存在）
- ✅ slot#2 产物已回填（`hf_title_002.mp4`，DB status=completed，供合成继续）
- ⚠️ **必须重启后端**（`reload=False`）新渲染才用上 `--low-memory-mode`
- ⚠️ job 79fc44b6 另 6 个 CANCELLED slot（#26/31/41/51/54/57，用户主动取消）未重跑，待确认

---

## 2026-08-08｜HF 模板三件套 — 圆饼图缺角修复 + 横屏模板 + 品牌泛化

### 背景
近三块 HF 模板相关工作此前未入账，本次统一归档：① 用户反馈「圆饼图有缺角」；② 横屏画幅此前硬编码竖屏模板；③ 模板被多账号共享但品牌栏/印章硬编码账号名。

### 改动
1. **圆饼图缺角修复（模板层）**：竖屏/横屏新闻杂志模板 `buildDonut` 三处守恒重写 — `avail = C - n*SEG_GAP`（缺口按比例预留）、`stroke-dashoffset = -used` + `used += len + SEG_GAP`（offset 守恒）、去主段 `scale:1.1`/`rotation:14°` 分离动画改纯 opacity 错峰淡入。根因三重叠：主段分离动画 + 小段 clamp 失配 + offset 不守恒。
2. **HF 横屏模板**：新增 `news-magazine-v1-ls`（1920×1080，composition_id `news_main_ls`），`_pick_hf_template` 按 `job.video_format` 宽高比自动选横/竖模板（代码引入 commit `4997ca1c`，2026-08-07）。
3. **品牌泛化**：`_SAFE_KEYS` 27 个 key 新增 `brand_name`/`stamp_name`/`brand_tag`；`_merge_brand` 从 `slot.director_job.script.host` 注入账号名/印章/标语（lazy=selectin 零额外查询，缺省中性回退）。

### 涉及模块
`app/services/template_filler.py` / `app/services/slot_workflows/hf.py` / `app/services/template_library.py` / `app/services/slot_workflows/common.py` / `E:\AI\digital_human\hf_prep\news_magazine_v1\index.html`（仓库外） / `news_magazine_v1_ls\index.html`（仓库外）

### 验证
- ✅ 双模板各重渲染一条含 pie 成片（`items:[58,22,15,4.9,0.1]`，含 0.1% 极小段，8s），抽 2.4s/7.8s 关键帧，PIL 沿环 0.5° 扫描：竖屏 719/720、横屏 720/720 整环闭合，**无缺角**（缺失约 0.5° 为 1px SEG_GAP 设计内细缝）
- ✅ 几何模拟（同压测数据）：旧算法段对重叠 2 次 → 新算法 0 次，段间 gap 均匀 0.3-0.4°
- ✅ 品牌泛化/横屏选择逻辑 `python -m py_compile` 通过
- ⚠️ 品牌泛化待重启后端后 E2E 验证（`reload=False` 不会自动加载新代码）

### 决策
- 模板实体位于 `E:\AI\digital_human\hf_prep\`（`hf_template_root` 默认路径），**不在 git 仓库内、无版本控制**，改动需手动备份
- 首版「固定弧长 gap」方案经几何模拟证明仍有 wrap 重叠，收敛为 offset 守恒方案

---

## 2026-08-07｜素材生命周期策略升级 — 保留 slot 素材防返工（保留 7 天 + 出口闭环）

### 背景
原策略（v3 引入）在合成成功后无条件 `shutil.rmtree(slots/)` 删除全部 slot 产出，而 retry 只重置**部分** slot 的 DB 记录。两者不一致 → 用户「重试同类 / 换素材」局部重制后，其余 completed slot 的 `output_path` 指向已删除文件 → REPAIR 被迫用新素材+新搜索补齐 → 画面与用户当初选定不一致 = **返工**；REPAIR 失败还直接触发合成线程崩溃。

### 改动
1. **删除合成后自清理（Step 7）**：composition_service.py 移除 `shutil.rmtree(slots/)`，slot 素材默认保留至 job 删除或过期保留扫描
2. **新增保留扫描**：main.py `_auto_cleanup_stale_jobs()` 增加分支 — completed 且 `completed_at` 超 `slot_retention_days` 的 job → 清 `slots/` + `hf_visual/<job_id>/` + 孤儿中间件，**保留成片** `director_<job_id>.mp4` + `composition_manifest.json`
3. **清理补充**：`_cleanup_intermediates` patterns 追加 `silence_fallback.wav`（此前遗漏，0f1f7ed3 有 84KB 残留）
4. **purge 连带删目录**：`purge_replaced_slots` 删 DB 行基础上连带删 `composition_output_root/<job_id>/slots/<replaced_id>/`
5. **新配置项**：`slot_retention_days: 7`（defaults 节，0 = 关闭保留，回退到旧「出片即删」行为）

### 涉及模块
`app/services/composition_service.py` / `app/main.py` / `app/routers/director.py` / `app/config.py` / `config/app.yaml` / `scripts/verify_retention.py`（新建隔离验证脚本）

### 验证
- ✅ 隔离验证脚本 `scripts/verify_retention.py`（临时 DB + 临时 composition 目录，不触碰真实数据）**19/19 通过**：核心链路 slots/ 保留 / retry 单条其余 slot mtime 不变 / 幂等返回 / 保留扫描清 slots 留成片+manifest / silence_fallback.wav 清理
- ✅ 现状磁盘清理：85f51962 / b64aeb28 孤儿中间件（manifest + with_audio.mp4 + concat_list.txt 残留）已清；0f1f7ed3 的 silence_fallback.wav 已清
- ⚠️ 待重启后端后 E2E 验证（`reload=False` 不会自动加载新代码）

### 决策
- 拒绝「最小改动」，整体升级素材生命周期策略：保留 slot 素材使「重试同类 / 换素材重合成」不返工
- 删 slots/ 的**唯二出口**：① 用户删 job（`_cleanup_job_files` rmtree）② 保留扫描（completed 超 7 天）
- REPAIR 补齐逻辑（缺失 slot 自动补）保留为安全网：仅当素材确实丢失时触发，不再承担「正常返工」职责
- 幂等守卫语义更新：job completed + 成片存在 + 无 `slots_changed_since_compose` → 幂等返回，不再触碰磁盘
- 素材库 GC 本次不纳入（用户确认）

---

## 2026-08-03｜导演生产路径优化 v3 — 零拷贝 + 48kHz 统一 + 合成后自清理（8 阶段）

### 背景
导演生产管线存在三重浪费：① ComfyUI 输出 576×1024@24fps 需后归一化缩放至 1080×1920@30fps、② poll 阶段 copy2 搬运文件、③ concat 前 `_prepare_inputs()` 二次 re-encode 所有 slot。用户要求从 ComfyUI workflow 和 TTS 切片两个源头解决问题。

### 8 阶段改动
1. **ComfyUI 双 workflow 模板**：新建 `ltx23_video_portrait.json`（9:16, 1080×1920@30fps）和 `ltx23_video_landscape.json`（16:9, 1920×1080@30fps），插入 ImageScale node 440 在模型输出后放大到目标分辨率，无需后归一化
2. **poll_comfyui 零搬运**：`slot_workflows.py` 直接返回 ComfyUI 输出路径，不再 copy2 搬运到 director 目录
3. **音频链路统一 48kHz**：`extract_audio_slice_wav` -ar 44100→48000；3 个非 host workflow（broll_pexels/broll_local/black_placeholder）从 `-an` 改为 `anullsrc=r=48000:cl=stereo` 静音轨；`mixed_host_broll` 加 `-ar 48000`
4. **删除 `_prepare_inputs()`**：composition_service.py 删除 ~80 行归一化函数，改为存在性校验 + 直接 `-c copy` concat（所有 slot 输出已同一分辨率/帧率/编码/48kHz 音频）
5. **目录统一 + HF 归一化**：`slot_root()` 从 `director_output_root` 改为 `composition_output_root/job_id/slots/`；HF 产物自动加 48kHz 静音轨 wrapper
6. **copy2 → os.replace**：5 处改为原子 replace（跨盘 fallback copy2+unlink）— composition_service.py ×4、comfyui.py `_persist_outputs`、digital_human_video.py Step E
7. **合成后自清理 + stale job 磁盘删除**：compose 成功后 `shutil.rmtree(slots/)`；`main.py` `_auto_cleanup_stale_jobs()` 删 DB 行时同步删磁盘目录
8. **config deprecation**：`app.yaml` 中 `director_output_root` 和 `hf_visual_root` 保留但标注 `# @deprecated`

### 涉及模块
`slot_workflows.py` / `composition_service.py` / `lit_video_builder.py` / `visual_render_service.py` / `comfyui.py` / `digital_human_video.py` / `main.py` / `data/workflows/ltx23_video_*.json`（新建）/ `config/app.yaml`

### 验证
- 所有模块 `python -m py_compile` 通过
- 待重启后端后 E2E 验证（`reload=False` 不会自动加载）
- 计划文件：`C:\Users\tanhaox\.claude\plans\glowing-spinning-crayon.md`

---

## 2026-08-01｜全项目审计修复收尾（P0×2 + P1×3 + P2×7）+ docs 全面更新同步

### 改动
- **P0-1 失败 TTS 任务 DB/磁盘脱节（方案 1：失败不删盘 + 可续传）**：`scripts/tts_client.py` 失败时不再 unlink 已生成 wav/manifest，重试跳过 `output_dir` 下已存在的 `{idx:03d}.wav` 只补缺失段；`audio.py` 失败路径保留 completed_segments、错误信息含续传提示；`tts_service.py`/`audio.py` 修复重复 AudioFile 行
- **P0-2 video_outputs 脏路径清洗**：删 `5882f87b` 脏行（file_path 前缀错为 `E:\机器人计划\`）+ 4 个 zombie visual job 行（rendering 卡住 / failed 空），4 个残留 job 父目录整体回收站
- **P1-1 app.js 空引用修复**：新增 `setSaveDirectorEnabled` helper 替换 5 处 `btn-save-director` 直连
- **P1-2 app.js `renderProjectDir` 崩溃修复**：锚点改 `script-text` 父元素 + 缺失守卫，`.step:nth-of-type(2)` null 不再阻断脚本加载
- **P1-3 library.js 崩溃修复**：`(a.tags || []).join(', ')` tags 判空
- **P2 系列收尾**：P2-1 library HEAD 路由、P2-2 失败任务全量清理（audio_jobs 删 9、dh_videos 删 6、visual_render_jobs 删 50）、P2-3 SSE onerror 重连上限、P2-4 JSON.parse try/catch、P2-5 `.test_videos/` 回收站、P2-7 fetch r.ok 检查（P2-6 后台线程 job 丢失竞态仅记录，见报告）
- **library.py 删除走回收站**：`_recycle_file` helper（PowerShell `Microsoft.VisualBasic.FileSystem::DeleteFile`，与 scripts.py 同源），delete_asset/delete_output 支持移入回收站
- **docs 全面更新同步**：`backlog.md`（ID-002 状态 done、排期 #6 删除线、关联需求区补审计修复条目）、`CHANGELOG.md`、`项目状态总览.md`（最新动作区 + ID-002 已完成 + 决策表追加）、ID-002 计划文档重命名 `已完成-20260727-ID002-视觉导演Agent2.md` 并标记全阶段完成

### 验证
- ✅ P0-1：对 85 段全在盘脚本 POST 重新生成 → `completed 85/85`，fish 后端全程未拉起（全走跳过路径），新 job audio_files=86 行无重复；`tests/test_tts_service_generate.py` 3 项全过
- ✅ P0-2/P2-2/P2-5 清理：DB 删 66 行 + 磁盘 5 处回收站（4 个 zombie job 目录 + .test_videos/），回收站核验可恢复
- ✅ P1-1/P1-2：浏览器实测 selectAll/toggleSegment 静默成功 0 错误、112 段渲染正常、fetchScript 完整执行
- ✅ P1-3：tags:null 坏数据不再崩溃，网格正常渲染 2 卡
- ✅ library.py 回收站：临时 asset DELETE → HTTP 200、DB 行 0 残留、磁盘文件消失、`tmp_recycle_test.bin` 确认在回收站

### 决策
- ✅ P0-1 采用方案 1（失败不删盘 + 可续传），最贴合"重试"心智；长期方向改"磁盘实况为准"（DB 状态仅提示）
- ✅ P0-2/P2-2 数据清理经用户确认（清掉脏数据行 / 全量清理）
- ✅ 删除磁盘文件一律走 Windows 回收站（`file_utils.safe_trash` 因 send2trash 未装会永久删除，不可复用）

---

## 2026-08-02｜项目盘符迁移 C: → F: + 硬编码路径清理

### 背景
项目整体从 `C:\AI-Agent-Local\digital_human` 迁移到 `F:\AI-Agent-Local\digital_human`。迁移后若保留旧 C 盘绝对路径，会导致数据库/脚本/启动入口找不到目标，调用出错。

### 改动
- **核心配置**: [config/app.yaml](config/app.yaml) `data_dir` 从 `C:/AI-Agent-Local/digital_human/data` 改为 `"data"`，由 `app/config.py` 自动解析为项目根目录下 `data/`
- **启动脚本**: 所有 `.bat`/`.sh` 开头统一改为 `cd /d "%~dp0"` / `cd "$(dirname "$0")"`，以脚本所在目录为项目根，不再依赖 C 盘绝对路径
  - `launch_system.bat`、`launch_system_fixed.bat`、`launch_system_final.bat`、`launch_news_system.bat`、`simple_launch.bat`、`run_4090_gpu.bat`、`start_dashboard_simple.bat`
  - `_run_fix.bat`、`_apply_vocab_fix.bat`、`_run_tokenizer_fix.bat`
  - `_apply_vocab_fix.sh`
- **Python 脚本**: 将硬编码 `C:\AI-Agent-Local\digital_human` 改为动态 `Path(__file__).resolve().parent/parents[1]`
  - `scripts/audit_llm_slot_drift.py`、`scripts/verify_timeline.py`、`scripts/rewrite_setnode.py`
  - `_concat_audio.py`、`_list_scripts.py`、`_apply_vocab_fix.py`、`_trigger_fix.py`
  - `smoke_test.py`

### 验证
- ✅ 配置加载：`data_dir = F:\AI-Agent-Local\digital_human\data`
- ✅ 数据库 URL：`sqlite:///F:\AI-Agent-Local\digital_human\data\pipeline.db`
- ✅ `python -m uvicorn app.main:app --host 127.0.0.1 --port 54323` 正常启动
- ✅ `GET /web/index.html` 200
- ✅ `GET /openapi.json` 200
- ✅ `GET /api/articles/prompt-templates` 200 并返回数据

### 决策 / 排查优先级
- 项目此前已**全链路跑通**；后续若出现「找不到文件/数据库/目录」类错误，优先排查**盘符/绝对路径是否又发生漂移**
- 所有启动入口已改为脚本自定位，再次迁移目录时只需复制文件夹，无需再改 `.bat`/`.sh`
- 外部资源目录（`E:/数字人计划/*`、`E:/AI/ComfyUI_windows_portable/*`）不属于本次盘符变更范围；若这些资源也迁移，需同步更新 `config/app.yaml` 中对应配置项

### 孤儿文件清理
- 清除项目根 `git ls-files --others --exclude-standard` 中历史开发残留的无引用文件 33 个，全部经 Windows 回收站安全移除
- 包括：根目录临时脚本/配置/测试页 28 个、`data/pipeline.db.bak-*` 数据库备份 4 个、`tests/__init__.py` 1 个
- 清理命令：`Microsoft.VisualBasic.FileIO.FileSystem::DeleteFile(path, 'OnlyErrorDialogs', 'SendToRecycleBin')`，可回收站恢复

---

### 改动
- **合成视频异步化 + SSE 进度透传**：`director.py` compose 端点从同步阻塞改为后台线程执行，立即返回；`composition_service.py` 添加 `evt` 回调，每步 emit SSE 事件（`compose_start` → `compose_step` ×N → `compose_done`）；前端 `composeJob()` 点击后自动展开日志面板 + 连接 SSE，实时显示标准化/拼接/字幕/响度/校验每步进度
- **执行日志面板 DOM 修复**：`showExecLog()` 和 `clearExecLog()` 从 `innerHTML = ''` 改为 `pre.textContent = ''`，避免销毁 `<pre id="exec-log-pre">` DOM 元素导致 SSE 事件全部静默丢弃
- **合成去重质量优先级**：`compose_director_job` 去重逻辑从按 `updated_at` 最新者取胜改为按 workflow 质量 tier 优先级（host=0 > broll/hf=1 > black_subtitle=9），同 tier 再按 updated_at；解决 black_subtitle 重试时间戳更新覆盖所有 host slot 的 bug
- **按钮 spinner 转圈恢复**：`appendExecEvent` 的 `exec_done` / `exec_cancelled` 事件恢复执行按钮；`compose_done` / `compose_error` 恢复合成按钮；SSE `onerror` 兜底 `_resetSpinnerBtns()`；`selectJob` 选择 executing 状态任务时自动连接 SSE

### 验证
- ✅ compose 端点异步化：POST 立即返回 `{"status": "composing"}`，SSE 实时推送 6 步进度
- ✅ 按钮转圈恢复：exec_done / exec_cancelled / compose_done / compose_error / SSE 断连 5 条路径均恢复按钮
- ✅ selectJob 自动 SSE：刷新页面选中 executing 任务时自动连接 SSE 接收进度
- ✅ 合成去重修复：26 host + 35 broll + 5 hf_chart + 13 hf_title + 6 black_subtitle（之前是 0 host + 32 black_subtitle）

### 决策
- ✅ 合成从同步 POST 改为异步线程 + SSE 透传，与执行 Slots 同一套进度反馈模式
- ✅ 合成去重按 workflow 质量 tier 优先（不是 updated_at），host 内容永远不被 black_subtitle 覆盖
- ✅ SSE 断连时 `_resetSpinnerBtns()` 兜底恢复所有按钮，防止永远转圈

---

## 2026-07-31｜导演台 Slot 重试去重 + 执行日志抽屉 + black_subtitle 字体路径修复

### 改动
- **Slot 重试/去重修复**：fallback 链 `replace_failed_slot` 为同一 slot_index 创建多个 DB 行（原始标记 replaced + 新行），前端 timeline 原样展示所有行导致重复
  - 前端 `director.js` `renderJobDetail` 按 `slot_index` 去重，同一索引只保留最新活跃条目（replaced 被新条目覆盖）
  - 后端 `director.py` `retry_slot` 端点拒绝 `replaced` 状态（仅 `failed` 可重试），避免重试已替换的 slot
  - 新增 `POST /jobs/{id}/purge-replaced` 端点 + 前端「🧹 清理已替换」按钮，批量删除所有 `replaced` 状态的 slot 记录
- **执行日志固定底部抽屉**：`#exec-log` 面板从 grid 右列内部移到 `<body>` 末尾
  - CSS 改为 `position: fixed; bottom: 0; z-index: 200; height: 260px` + 毛玻璃背景 + 蓝色顶边发光 + `transform` 滑入动画
  - JS 定时器管理重构：引入 `_sseCollapseTimer` 独立变量，`disconnectSSE` 不再自动排收起定时器，仅 `plan_error`/`plan_done`/`exec_done` 等 terminal 事件后由 `_scheduleCollapse()` 3s 自动收起
  - `body.log-open` class 适配 toast 位置（`bottom: 280px`）
- **black_subtitle ffmpeg fontfile 路径修复**：`slot_workflows.py` 中 `execute_black_subtitle_slot` 的字体路径从 `r"C\\:/Windows/Fonts/msyh.ttc"`（raw 字符串产生双反斜杠 `C\\:`）改为 `"C\\:/Windows/Fonts/msyh.ttc"`（单反斜杠 `C\:`），ffmpeg 在单引号内 `\:` 正确转义驱动器号冒号

### 验证
- ✅ hf_chart 单 slot 执行 29.1s 成功（333KB MP4 输出）
- ✅ hf_title 单 slot 执行 20.9s 成功（415KB MP4 输出）
- ✅ black_subtitle ffmpeg 命令 Python subprocess 直测通过（单反斜杠）
- ✅ 最新任务 41 个 failed slot 全部为 black_subtitle（fontfile bug），HF 渲染管线无 bug

### 决策
- ✅ 前端按 slot_index 去重显示（不修改后端 fallback 链逻辑），后端仅新增 purge 端点供手动清理
- ✅ 执行日志从 grid 内部提升为 body 级固定抽屉，不受页面布局影响
- ✅ `replaced` 为终态，禁止重试（只能重试 `failed` 状态的 slot）

---

## 2026-07-31｜视频库：素材库 + 成品库 全链路落地

### 改动
- **DB 模型**：`app/models.py` 新增 `VideoAsset`（素材库 16 字段：source/pexels_id/file_path/orientation/width/height/duration_sec/description_en/description_zh/photographer/source_url/category/tags/liked/used_count）+ `VideoOutput`（成品库 8 字段：job_id/title/file_path/orientation/duration_sec/video_format/description）
- **素材库 API**：`app/routers/library.py` — `GET /assets`（关键词搜索+画幅/大类/赞筛选）+ `GET /assets/{id}` + `GET /assets/{id}/file`（视频流）+ `PUT /assets/{id}`（编辑描述/大类/标签）+ `POST /assets/{id}/like`（切换赞）+ `DELETE /assets/{id}` + `GET /categories` + `GET /tags`
- **成品库 API**：同上 — `GET /outputs`（搜索+筛选）+ `GET /outputs/{id}/file` + `DELETE /outputs/{id}`
- **Pexels 自动入库**：`pexels_service.py` 新增 `_register_video_asset()`，在 `_process_candidates` 下载成功后调用，按 pexels_id 去重写入 VideoAsset，带画幅/时长/摄影师/标签
- **Compose 自动登记**：`director.py` compose 端点在合成成功后自动创建 VideoOutput 行（标题取脚本前 30 字，关联 job_id + 画幅 + 时长）
- **前端页面**：`web/library.html` + `library.js` + `library.css` — 双 Tab（📦 素材库 / 🎬 成品库）+ 搜索防抖 + 画幅/大类筛选 + 仅赞过滤 + 视频预览 + 编辑弹窗（中文描述/大类/标签）+ 赞切换 + 删除 + 深色主题
- **全站导航**：9 个页面（index/director/library/templates/visual_render/voices/roles/personas/digital_human_video）统一加「视频库」链接

### 验证
- ✅ 素材库 API 端点可访问（list/search/like/edit/delete/categories/tags）
- ✅ 成品库 API 端点可访问（list/file/delete）
- ✅ Pexels 下载 → VideoAsset 自动入库（pexels_id 去重）
- ✅ Compose 成功 → VideoOutput 自动登记
- ✅ 9 页导航均包含「视频库」链接

### 决策
- ✅ 素材库与成品库共用同一 router（`/api/library`），前端用 Tab 切换，不拆两个页面
- ✅ Pexels 下载自动入库不增加额外接口，在现有 resolve 流程中透明写入
- ✅ Compose 自动登记失败不阻塞主流程（try/except + warning log）

---

## 2026-07-30｜导演台执行引擎健壮性大修 + 前端交互完善

### 改动
- **大文件上传修复**：`app/main.py` 将 `BaseHTTPMiddleware`（会缓冲请求体导致大文件上传失败）替换为纯 ASGI 中间件，仅修改响应头不触碰请求体
- **HF 渲染死锁修复**：`app/services/hf_client.py` 将 `subprocess.run(timeout=)` 改为 `Popen` + `communicate(timeout=)` + Windows `taskkill /F /T` 杀整棵进程树（npx→node→chromium），解决管道继承导致 communicate 死锁
- **GPU 服务失败优雅降级**：`slot_executor.py` 中 GPU session 启动失败不再炸掉整个任务，标记该阶段 slot 为 failed 后继续后续 Phase；`_handle_fallbacks` 中 ComfyUI session 同样加 try/except
- **ComfyUI 便携版 numpy 修复**：numpy 2.3.5 → 2.2.6，解决 scipy.ndimage 循环导入崩溃
- **Host–Persona–Role 关联链修复**：`slot_workflows.py` 中 `execute_host_slot` 通过 `Host.persona_key` 前缀匹配 `Persona.prompt_template` → `Persona.role.view_groups` 获取机位图，解决 Host 对象无 view_groups 属性的 AttributeError
- **replaced 状态不再被重复执行**：`execute_all_slots` 收集 pending 时只取 `queued` 状态，`replaced` 为终态不再参与执行
- **僵尸 executing 状态检测**：`retry-workflow` 端点检测 job_id 不在 `_executing_jobs` 集合时自动修正为 reviewing
- **Slot 重试不再创建重复 slot**：`retry_slot` 端点改为在原 slot 上重置为 queued 并重新执行，不走 fallback 链
- **black_subtitle ffmpeg 转义修复**：去除情绪标签 `[calm]`、`||` 分隔符，完整转义 `\ ' : [ ] | %`
- **Slot 详情面板一闪而过修复**：用 `_selectedSlotIdx` 记住展开状态，轮询时保持展开而非强制关闭
- **Workflow 类型中文化**：`WF_LABELS` 映射表（host→数字人、broll_pexels→下载素材、hf_chart→HF图表 等）
- **停止/取消按钮**：后端 `POST /jobs/{id}/cancel` + 前端 `■ 停止` 按钮（仅执行中显示）；后台线程在每个 phase 边界检查取消标志
- **重试按钮策略**：执行中隐藏重试和批量重试按钮，非执行时仅 failed 状态可点
- **按 workflow 批量重试**：后端 `POST /jobs/{id}/retry-workflow?workflow=host` 重置所有该类型 failed slot 为 queued；前端「重试同类」按钮
- **执行日志收起后无法重新打开**：在操作按钮组加始终可见的「📡 日志」切换按钮

### 验证
- ✅ `retry-workflow(host)` → 200，成功重置 failed slot
- ✅ `cancel` → 409（正确拒绝非 executing 状态）
- ✅ ComfyUI 拉起后 host slot 正确取到 view_groups 机位图并提交 prompt
- ✅ black_subtitle 含特殊字符文本不再崩溃
- ✅ 重复 slot 清理脚本删除 273 条历史记录

### 决策
- ✅ 手动重试 = 原 slot 重置重跑，不走 fallback 链（fallback 是自动降级逻辑）
- ✅ 执行中禁止重试操作，避免 GPU 抢占冲突
- ✅ `replaced` 为终态，不再参与执行循环

---

## 2026-07-29｜导演台执行引擎重构 + 人物关联 + 全站导航统一

### 改动
- **PEXELS_API_KEY 加载修复**：`app/config.py` 新增 `_load_dotenv()` 轻量 .env 加载器（无第三方依赖，`os.environ.setdefault` 不覆盖已有变量），解决 .env 有 key 但进程从未读取的问题
- **ComfyUI 纳入 GPU 托管**：`gpu_service_manager.py` `_BUILTIN_SPECS` 新增 `comfyui` 条目（`E:/AI/ComfyUI_windows_portable`，`CUDA_VISIBLE_DEVICES=0`），`execute_all_slots` 用 `session("comfyui")` 包裹 host/mixed 批次，导演台 retry 同理 → ComfyUI 按需拉起、空闲自动关停
- **执行模型重构为 Phase 调度**（`slot_executor.py` 核心重写）：
  - 原逻辑按时间序逐个执行 → 新逻辑按 workflow 类型分 4 阶段批量执行：Phase 1 ComfyUI(host/mixed) → Phase 2 Pexels → Phase 3 HF → Phase 4 本地
  - 收益：30 段音频中若仅 4-5 段需 ComfyUI，只拉起一次 session 跑完，而非 30 次
  - Fallback 集中兜底：`_handle_fallbacks` 非 GPU 步骤立即重试，GPU 步骤攒一批一个 session
- **相邻约束 + 参考来源段处理**：
  - `director_service.py` 新增 `_enforce_adjacency_rules`：host 不能挨 host、HF 不能挨 HF（交换策略）
  - `script_parser.py` 新增 references 段检测（`_REFERENCE_HEADER_RE` + `_NUMBERED_URL_RE`），标记 `selected_for_host=False`
  - 参考来源自动追加 `hf_title` slot（`no_voiceover=True`，轻音乐带过）
  - `visual_director_v2.txt` prompt 规则同步更新
- **人物关联（Persona）功能**：
  - `app/models.py` 新增 `Persona` 表（`prompt_template` unique + `voice_id` FK + `role_id` FK）
  - `app/routers/personas.py`：CRUD + `GET /api/personas/by-template/{name}` 流水线联动查询
  - `web/personas.html`：人物管理页面（暗色主题 CRUD UI）
  - `web/app.js`：流水线选模板后自动查询 Persona → 锁定音色/形象下拉框（disabled + 锁定提示条）
- **全站导航 UI 统一**：8 个页面（index/director/templates/visual_render/voices/roles/personas/digital_human_video）顶部导航与 director.html 看齐 — sticky 毛玻璃 56px 导航条 + 7 链接 + active 高亮 + API Docs

### 验证
- ✅ Pexels API 返回 200（key 生效）
- ✅ `POST /api/personas` 创建成功，voice/role 关联正确返回
- ✅ `GET /api/personas/by-template/laochen_v3` 精确命中
- ✅ 8 个页面导航结构一致（7 链接 + 正确 active）
- ✅ 所有页面 HTTP 200 可访问

### 决策
- ✅ 执行模型从"逐段顺序"改为"按 workflow 类型分 Phase 批量"，ComfyUI 单次 session 跑完所有 host/mixed slot
- ✅ 提示词模板 + 音色 + 形象三维绑定为"一套人物"（Persona），流水线选模板后音色/形象自动锁定不可更改
- ✅ 全站导航以 director.html 为基准统一（sticky 毛玻璃 + 蓝紫渐变 logo + 7 链接）

### 已知问题（未修，仅记录）
- ⚠️ 导演台任务跑到“执行 Slots”阶段后服务死掉（Phase 调度重构后首次实跑命中）；另 director.html 在调不存在的 `GET /api/scripts` 接口（404）。详见 `docs/backlog.md` **ID-009**（P0）。

---

## 2026-07-29｜音频质量三连修（残音 / 极短句 / alignment 500）+ 流水线→导演台衔接

### 改动
- **`scripts/tts_client.py` 修残音与切分错位**（问题①）
  - `_tts_text` 新增两条清理：批量拼接产生的 `句。||下一句`→`句。，下一句` 中句号后逗号会让 TTS 念出可听残音，现予剔除；行首多余逗号同步去除
  - `_split_wav_by_silence` 切分算法重写：原“取前 N-1 个静音点”会把句内逗号停顿误当句界（实证：11 字句配 22.23s、大段文字仅 0.37s）。新算法按清洗后字数比例算预期边界，在容差 `max(0.9s, 0.35×平均句长)` 内吸附最近静音区间中点，超容差则直接用预期点
- **`app/services/script_parser.py` 修极短句**（问题②）：新增 `_effective_len`（去标签/停顿符/标点后字数）+ `_merge_short_sentences`，有效字数 <10 的句子（约 <2s）合并进相邻句，消除“您好”“哎呦”类碎音频
- **服务器解释器切换 `.venv`**（问题③）：导演台 `alignment failed: type_error.302` 根因是 54321 服务器跑在系统 Python（ctranslate2 4.6.0 nlohmann json bug），代码相同下 .venv（ctranslate2 4.8.1）完全正常。已用 `.venv\Scripts\python.exe` 重启
- **流水线→导演台衔接**：`app.js` 音频完成（tts_done）后弹出 8s 倒计时横幅自动跳转 `director.html?script_id=..&audio_id=..&auto=1`（可取消/立即进入）；`director.html` 解析 URL 参数自动预填并建任务，`history.replaceState` 防刷新重复建
- **`gpu_service_manager.py` 强制释放兜底**：`_force_free_port` 在杀进程树后端口仍 LISTENING 时按端口反查 PID 逐个 `taskkill /T /F`（最多 3 轮）——venv launcher 派生的工作进程不在 Popen 句柄树内，此兜底实测真实触发
- **桌面启动 bat 升级**：`findstr` 加 `LISTENING` 过滤（防误杀浏览器连接方）、新增 7860/7861/7862 TTS 孤儿进程清理、解释器改用 `.venv`

### 验证
- ✅ `tmp/test_fixes.py`：短句合并 + 标点清理全 PASS
- ✅ alignment 复现脚本：同一音频系统 Python 复现 type_error.302，.venv 下 ok=True 返回 29 条 timings
- ✅ 重启后进程模块检查：54321 监听进程加载 92 个 `.venv` 模块、0 个系统 site-packages 模块（venv launcher 派生的 `Python311\python.exe` 子进程属正常机制）
- ✅ GPU 服务闭环实测：IndexTTS2 拉起→释放→Fish 拉起→释放→强杀兜底成功
- ✅ 流水线→导演台衔接浏览器实测 4 项全过

### 决策
- ✅ 服务器一律用 `.venv` 解释器启动（bat 与手动均是）；系统 Python 的 ctranslate2 4.6.0 有序列化 bug 不再使用
- ✅ 分句最短限制定为有效 10 字（中文口播 4-5 字/秒 ≈ 2s 底线）

---

## 2026-07-29｜TTS GPU 服务托管（按需启动 / 排队 / 空闲自动关停）

### 改动
- **新建 `app/services/gpu_service_manager.py`**：单卡 4090 的 TTS 服务生命周期管理器
  - 生成语音/试听/抽卡时自动健康检查，服务不在线则用对应 venv 自动拉起（fish 7860 / f5 7861 / indextts 7862，启动命令内置，可在 `app.yaml tts_services` 覆盖）
  - 全局 GPU 锁串行排队；拉起新服务前先停掉托管的其他 TTS 服务腾显存
  - 看门狗线程：空闲超过 `idle_timeout_sec`（默认 180s，0=立即）自动 taskkill 进程树释放显存，给 ComfyUI/LTX 让路
  - 外部手动启动的服务只用不杀；应用退出时杀掉所有托管进程不留孤儿
- **接入点**：`audio.py _do_tts`（生成语音）、`voices.py` 试听/抽卡/锚点三入口
- **新 API**：`GET /api/tts-services/status`（健康/托管/排队状态）、`POST /api/tts-services/stop-all`（手动腾显存）
- **前端**：生成音频 SSE 新增 `tts_service` 事件，展示“正在启动 IndexTTS2 服务…”等状态
- **附带修复**：音色下拉框页面初始化即加载（原先只在洗稿完成后加载，造成“无法使用”）+ 默认音色占位项

### 验证
- ✅ E2E 实测：7862 离线 → 试听请求 → 自动拉起 IndexTTS2（模型加载 ~2min）→ HTTP 200 出 wav（cuda）→ 空闲 180s 后看门狗自动杀进程，端口释放

### 决策
- ✅ TTS 服务不常驻：单 4090 必须在 TTS 与 ComfyUI/LTX 之间腾换显存
- ✅ `auto_manage: false` 可一键回退到手动启动服务的旧行为

---

## 2026-07-28｜前端 UI 全面升级 + 提示词模板管理

### 改动
- **前端设计系统升级**：统一深色主题 + 玻璃拟态 + 径向渐变光晕 + 蓝紫渐变文字
  - 所有页面共享同一套 CSS 变量系统（`--bg-primary` / `--bg-card` / `--accent` / `--violet` 等）
  - 统一导航栏（6 页互链：流水线 / 导演控制台 / 模板管理 / 视觉渲染 / 音色管理 / 角色管理）
- **新建 `web/director.html` 导演控制台**（ID-002 前端）
  - 4 步流程指示器（创建→执行→合成→下载）+ IntersectionObserver 滚动自动高亮
  - 左右两栏布局（任务列表 340px + 详情面板）
  - Slot 时间轴可视化（横向彩色条，颜色区分 queued/running/completed/failed）
  - 4 格统计面板（总/完成/执行中/失败）+ 自动轮询（3s）+ Toast 通知
- **重写 `web/index.html` 流水线页面**
  - 3 步流程（输入文章 → AI 洗稿 → 生成音频），移除冗余的"分段/导演选择"步骤
  - AI 洗稿步骤新增"数字人/提示词模板"下拉选择器（动态加载 `config/*.txt`）
  - 保留所有 app.js 所需 element ID，完全兼容
- **新建 `web/templates.html` 提示词模板管理页面**
  - 上传新模板：输入名称 + 拖拽/点击上传 .txt → 保存到 `config/` 目录
  - 模板列表展示（大小、内置标识）
  - 改名功能：弹窗输入新名称 → `PUT /api/articles/prompt-templates/{id}`
  - 删除功能：非内置模板可删除（`laochen_default` 受保护）
- **后端新增 4 个 API 端点**（`app/routers/articles.py`）
  - `GET /api/articles/prompt-templates` — 列出可用模板
  - `POST /api/articles/prompt-templates/upload` — 上传新模板
  - `PUT /api/articles/prompt-templates/{id}` — 重命名模板
  - `DELETE /api/articles/prompt-templates/{id}` — 删除模板

### 决策
- ✅ 段落选择功能保留在流水线"生成音频"步骤内（`selected_for_host` 决定 TTS 范围）
- ✅ 移除"保存导演选择"按钮和拖拽排序功能（由导演控制台负责）
- ✅ 扩展方式：新增数字人只需在 `config/` 下放 `{name}.txt`，前端自动识别

---

## 2026-07-27｜视觉导演 Agent 2.0 后端 E2E 跑通（ID-002 续）

### 改动
- **`config/visual_director_v2.txt`**：prompt 改为输出 slot 工序单(`slot_index` / `start_sec` / `end_sec` / `workflow` / `params`),不再走旧 line_id + material_source 包裹结构
- **`app/services/director_service.py::_parse_llm_plan`**：自动检测新/旧 schema,新 schema 直接解析 slot 字段,旧 schema 走 `_parse_legacy_row` 兼容分支
- **`_enforce_host_rules`**：first/last slot 强制 host;无中间 host 时强制中间 pivot 为 host
- **`replace_failed_slot`**：默认 fallback chain = broll_pexels → broll_local → host → hf_chart/hf_title → black_subtitle

### 验证(离线 4 项断言全通过)
- ✅ 新 prompt 7-slot JSON → 7 slot 落库,first/last host,mixed_host_broll 保留
- ✅ 3-slot 全 broll 边界 → first/last/middle 全部被强制为 host
- ✅ 7 个 workflow 在 `_WORKFLOW_HANDLERS` 全部注册(host / broll_local / broll_pexels / hf_chart / hf_title / mixed_host_broll / black_subtitle)
- ✅ `replace_failed_slot(broll_pexels → broll_local)` 推进 fallback chain,新 slot status=queued

### 决策
- ✅ schema 双兼容:新 prompt 是默认路径,旧 prompt(若有残留脚本)走 legacy 分支不再报错
- ✅ backend E2E 离线验证完成;前端 UI 待补
- ⏸  端到端通过真实 uvicorn + ComfyUI 联调,需要 GPU 服务在线;本次仅做算法路径验证

---

## 2026-07-27｜视觉导演 Agent 2.0 需求确认 + 开发计划启动（ID-002）

### 改动
- **ID-002：视觉导演 Agent 2.0 方案确认并启动开发**
  - 导演角色重新定位：从"每句分配画面"升级为"音频时间轴上的大 Agent"，输出按 slot 组织的工序单
  - 新增 `docs/improvements/进行中-20260727-ID002-视觉导演Agent2.md` 作为阶段 checkpoints
  - 新增数据模型规划：`director_jobs` / `director_slots`
  - 新增服务层规划：`alignment_service.py`(Whisper 对齐) / `director_service.py`(大导演) / `slot_executor.py`(slot 执行+替换) / `composition_service.py`(合成)
  - 新增路由规划：`app/routers/director.py`

### 决策
- ✅ 废弃原 ID-007 "选句子 + 拖拽排序" UI 作为导演强依赖，保留字段兼容旧数据
- ✅ 导演触发时机后移到**整段 TTS 音频生成后**，用 Whisper forced alignment 获取真实时间轴
- ✅ IndexTTS2(端口 7862) 作为 A 管线财经主线主力 TTS
- ✅ slot workflow 类型：`host` / `broll_pexels` / `broll_local` / `hf_chart` / `hf_title` / `mixed_host_broll`
- ✅ 主持人出场策略：最后一句 CTA 必须 host；中间 0-1 次 host
- ✅ 失败替换优先级：broll_pexels → broll_local → host → hf_chart/hf_title → 黑场+字幕保底
- ✅ 开发顺序：先 ID-002 后端接口跑通；ID-003 Pexels resolve 用 mock/最小清单延后实现
- ✅ 前后端顺序：先后端 E2E 再补前端 UI

---

## 2026-07-27｜Web UI 导演选择体验优化（ID-007）+ URL 抓取/模型开关（ID-001/008）

### 改动
- **ID-007：分段导演选择 UI 优化**
  - `web/index.html` 步③ 增加"全选/取消全选"按钮、拖拽提示、已选句数/预估总时长
  - `web/app.js` 重写 `renderSegments()`：
    - 显示 `segment_type` 中文标签（开场/钩子/正文/行动/结尾）与 `estimated_duration`
    - 已选段左侧显示 ☰ 拖拽手柄，支持 HTML5 drag-and-drop 调整 `host_order`
    - 未选段按 `line_index` 排序，不参与拖拽
    - 保存时先调用 `POST /api/scripts/{id}/segments/reorder` 批量更新顺序，再 `PUT /api/segments/{id}` 更新选择状态
- **ID-001：URL 自动抓取标题和正文**
  - 新增 `app/services/url_fetcher.py`：分层解析（Toutiao 站点规则 → JSON-LD articleBody → meta/paragraph 回退）+ 真实浏览器 UA/Referer + 15s 超时
  - `app/routers/articles.py` 新增 `POST /api/articles/fetch-url`
  - `web/index.html` 增加 "🌐 抓取 URL" 按钮；`web/app.js` 新增 `fetchUrl()` 自动填入标题和正文
- **ID-008：洗稿模型选择开关**
  - `web/index.html` 步② 在"开始洗稿"旁增加 `<select id="rewrite-model">`（flash / pro）
  - `web/app.js` 的 `rewriteArticle()` 将选中 `model` 透传至 `/api/articles/{id}/rewrite`

### 决策
- ✅ 导演选择保存顺序改为先 `reorder` 再批量 `selected_for_host`，减少 PUT 次数
- ✅ 拖拽仅作用于已选段，避免未选段混入排序
- ✅ URL 抓取失败不阻塞用户手动粘贴，前端明确提示

---

## 2026-07-26｜HF 视觉渲染模块接入 54321 Web

### 改动
- **新能力：HF (HyperFrames) 视觉渲染**(纯静态模板 + GSAP 时间轴 → MP4 + 关键帧 + manifest) — **独立管线**,不接 ComfyUI、不占 GPU、不与数字人视频状态机耦合
  - 单一模板 `news-data-v1` v1.0.0(composition_id=`news_main`,来源 `E:/AI/digital_human/hf_prep/test_demo/`,5-30s)
  - 后端：`app/routers/visual_render.py` 8 端点(`templates` / `jobs` CRUD / `generate` 同步 / `download` mp4 / `frames/{first,middle,final}` / `delete`)
  - 服务层(4 文件,4 层职责):
    - `app/services/hf_client.py` — `npx hyperframes render . -o visual_segment.mp4` 的 subprocess 封装,300s 硬超时 + stdout/stderr 落 `render.log`
    - `app/services/template_library.py` — `TEMPLATES` 字典 + `list/get/resolve/validate_input`,附 jsonschema `title/subtitle/metrics[1-4]/chart/caption/source/duration_sec/assets`
    - `app/services/template_filler.py` — 复制模板源到 `<hf_visual_root>/<job_id>/` + `input.json` 落盘 + `{{KEY}}` 白名单占位符替换(26 个安全 key)
    - `app/services/visual_render_service.py` — 6 步编排(preparing → rendering → validating → completed/failed),ffprobe 校验 h264/1080×1920/fps/duration,ffmpeg 抽 first/middle/final 三帧,`render_manifest.json` 落盘
  - 数据模型：`app/models.py` 新增 `VisualRenderJob` 表(19 字段,独立 `visual_render_jobs` 表),状态机 `queued → preparing → rendering → validating → completed/failed/cancelled`
  - 配置：`config/app.yaml` Defaults 加 4 个 key(`hyperframes_bin=npx` / `hf_render_timeout_sec=300` / `hf_visual_root=E:/数字人计划/hf_visual` / `hf_template_root=E:/AI/digital_human/hf_prep`)
  - Pydantic：`app/schemas.py` 加 `VisualRenderJobCreate/Out` / `TemplateInfo` / `GenerateVisualResponse`
  - 前端：`web/visual_render.html` 4 步 SPA(选模板 → 动态表单填入参 → 提交 + 2s 轮询 → 历史 + 视频预览 + 3 帧缩略图 + 下载);`web/index.html` 顶部导航新增 "🎨 视觉渲染"
- **持久化**：产物落 `E:/数字人计划/hf_visual/<job_id>/{input.json, index.html, avatar.b64, render.log, render_manifest.json, rendered/visual_segment.mp4, rendered/frames/{first,middle,final}.png}`
- **模板抽象约定**：模板 `index.html` 须满足 — 唯一 `data-composition-id` + `data-duration` + `.clip` 类 + GSAP 时间轴 paused + 无 `Date.now()/performance.now()/未种子化 Math.random()` + 无 `file:///` 图片 + 无 mouse/scroll/hover 依赖

### 决策
- ✅ HF **不**接 ComfyUI / **不**占 4090 引用计数(CPU/Chrome 渲染,独立进程)
- ✅ HF **不**与 `DigitalHumanVideo` 状态机耦合 — 独立表独立服务独立 status 字段
- ✅ **不**批量创建 30~60 个模板;只 1 个 `news-data-v1`,只为未来扩展(comparison-chart-v1 / title-card-v1 / quote-card-v1 / timeline-v1)**预留接口位**而不实现
- ✅ **不**让模板内部调用 DeepSeek / Fish Speech / ComfyUI(模板是纯静态 + GSAP)
- ✅ **不**每次请求从零生成 HTML — 模板是项目侧固定文件,只做占位符替换
- ✅ **不**把 `template_id`/`composition_id`/`renderer_version` 硬编码进生产逻辑,均走 schema
- ✅ 子进程硬超时 300s,超时 → `status=failed` + `error_code="TIMEOUT"`
- ✅ 占位符替换走 26-key 白名单,防 LLM 注入
- ✅ 渲染成功判定必须 ffprobe(h264 + 1080×1920 + 30fps + duration 与 `data-duration` 一致 ±2s 软警告),**不**仅凭 HTTP 200
- ✅ 删除走 send2trash(CLAUDE.md 铁律),shutil.rmtree 仅作兜底
- ⏳ 同步执行 + 2s 轮询(MVP);异步队列 + SSE + 取消端点留 backlog
- ⏳ DeepSeek 自动选模板 / `config/visual_director_v2.txt` 的 `render_config` schema 接入留 ID-002 后续

---

## 2026-07-26｜LTX23 音频→视频 接入数字人视频环节

### 改动
- **新能力：数字人说话视频**（音频 + 分镜图 → MP4）— 完全在 54321 Web 闭环
  - 工作流 SSOT：`workflows/digital_human_video_ltx23.json`（由 `manifest.yaml` 自动同步到 ComfyUI 运行时副本）
  - 后端：`app/routers/digital_human_video.py` 8 端点（`eligible-audio` / `videos` CRUD / `upload-storyboard` / `generate` / `download` / `delete`）
  - 服务层：
    - `app/services/audio_aggregator.py` — ffmpeg concat demuxer，≥5s 硬阈值，0.3s 静音填充 `||`
    - `app/services/lit_video_builder.py` — LTX23 workflow dict 构造（`LTXVAddGuide` ×N + `LTXVSamplerCustomAdvanced` + `LTXVAudioVAEEncode/Decode` + `VHS_VideoCombine`）
    - `app/services/video_validator.py` — ffprobe 7 项硬校验（video stream / audio stream / fps / `8n+1` frame count / duration match ±1s / duration ≥ min）
  - 数据模型：`app/models.py` 新增 `DigitalHumanVideo` 表（16 字段含 status / prompt_id / output_video_path / validation meta）
  - Pydantic：`app/schemas.py` 加 `DigitalHumanVideoCreate/Out` / `EligibleAudioItem/Response` / `GenerateVideoResponse` / `StoryboardUploadResponse`
  - 前端：`web/digital_human_video.html`（4 步 SPA：① 选音频 ≥ 5s → ② 上传分镜图 + 参数 → ③ 提交生成 + 状态轮询 → ④ 历史 + 下载）；`web/index.html` 顶部导航新增 "🎬 数字人视频"
- **同步路径**：`lifespan` 启动期同步；与已有 `character_three_view` workflow 共用 `manifest.yaml`
- **存档保护**：原始工作流在 `G:\下载\LTX23-...` 保持不动；运行时副本由 SSOT 双向同步保 护

### E2E 验收（2026-07-26 切片 6）
- `POST /api/dhv/videos` 创建任务 → `multipart/form-data upload-storyboard` 上传 2 张分镜 → `POST /api/dhv/videos/{id}/generate` 同步跑通
- 音频聚合：`032.wav (5.27s) + 031.wav (3.44s)` → `aggregated_duration_sec=8.71s`
- ComfyUI `/prompt` 返回 HTTP 400（workflow JSON 中 `loop_count` 等输入待补）→ 路由捕获异常，DB 落 `status=failed` + `error_message` 完整记录
- 删除 `DELETE /api/dhv/videos/{id}` 走 `send2trash` 思路清理产物目录（实测 HTTP 204）
- 路由 200：8 端点全部 `openapi.json` 已注册且 `eligible-audio` 实时返回 `total_duration_sec=16.38s`

### 决策
- ✅ **不**覆盖原始工作流 `G:\下载\LTX23-...`
- ✅ **不**把临时实验 payload 当 SSOT；`workflows/digital_human_video_ltx23.json` 是项目侧占位 + 注释
- ✅ **不**仅凭 ComfyUI HTTP 200 判定视频成功；必须 ffprobe 校验 audio stream / fps / frame count
- ✅ **不**因 SageAttention DLL 等单次失败阻塞主流程；WARN + 继续
- ✅ 音段聚合阈值 **≥ 5s**（用户原话："先作测试用，5 秒以上的音频"）
- ✅ 帧数公式 `round(duration * fps) + 1` 对齐 `8n+1`
- ✅ 与 IndexTTS2 入口并列：复用 `article → audio → 数字人视频` 自动接续链路
- ⏳ 前置节奏改造（每段 5-10s）留 backlog（本轮不展开）

---

## 2026-07-25｜ComfyUI 工作流 SSOT 入项目目录,54321 Web 自动同步

### 改动
- **角色管理接入 ComfyUI 多视图定型** — `web/roles.html` + `app/routers/{comfyui,roles}.py` + `app/services/workflow_sync.py` + `app/models.py` 新增 `Role` / `WorkflowSync` 表
- 工作流 SSOT：`workflows/character_three_view.json` + `workflows/manifest.yaml`
- 启动期同步：lifespan 钩子比对 sha256 → 覆盖 / 记录 `WorkflowSync` 表 + `E:\数字人计划\logs\workflow-sync-*.log`
- 角色 CRUD：参考图落 `E:\数字人计划\roles\<uuid>\ref.<ext>`；3 视图落 `E:\数字人计划\roles\<uuid>\{front,side,full}.<ext>`
- `apply` 端点只产 `mainstream_input` JSON，**不**接 A 管线分镜（留 ID-007 backlog）
- `web/index.html` 顶部导航新增 "🎭 角色管理" 入口

### 决策
- ✅ **不**绕开 54321 Web 直调 ComfyUI API
- ✅ **不**新增端口（仅 54321 + 复用 ComfyUI 8188）
- ✅ **不**同步到 `E:\AI\comfyui_workflows\`（已降级历史目录）
- ✅ ComfyUI 内部参数（checkpoint / LoRA / prompt 模板）对用户不可见

---

## 2026-07-25｜记忆与 SSOT 整理

### 决策
- **文档分层统一**：`项目状态总览.md` = 当前现实；`执行计划清单.md` = 规划 SSOT；`执行计划清单.html` = 可视化视图；`CHANGELOG.md` = 历史。
- **Workflow SSOT 锁定**：项目 `workflows/` 为唯一真源；ComfyUI `user/default/workflows/` 为运行时副本，后续由 Claude Code 实现可观测同步。
- README 移除已过期的 M0 “1 分钟 3 条 30 秒”当前门禁表述。
- 清理动作不再描述为“待权限授权”，而描述为待用户做保留/归档/删除的内容决策。

---

## 2026-07-25｜文档整合与决策固化

### 决策（用户拍板）
- **MuseTalk 完全废**：技术栈移除；`E:\tools\MuseTalk-main\` + 隔离 venv + wrapper 后续清理
- **M0 门禁过期**：原"1 分钟 3 条 30 秒"硬门禁作废；M0 已跑通，后续只关心精进
- **38m 项目归档**：作为 M0 实验田已完成，不维护；`C:\Users\tanha\Desktop\38m\` 回头可清
- **多维度定型图定位**：M0+ 全阶段的**角色资产层**，不是独立阶段
- **v10 不改**：作为规划目标保留，通过新"项目状态总览"反映真实差异

### 新增文档
- [`项目状态总览.md`](项目状态总览.md)：反映真实技术栈、真实进度、整合决策、待决项、清理待办
- `CHANGELOG.md`（本文件）：里程碑 + 决策时间线

### 文档结构变化
- `README.md` 项目入口表新增 2 行：本文件 + CHANGELOG（其他内容不动）
- v10 / 调研 / SOP / 跑通记录 / pose 规律 等已沉淀文档**全部未改**

---

## 2026-07-24｜Pose 库分类沉淀

- 1700+ 张预览测试（Flux2 Klein 9B + 参考图方案）
- **高风险姿势**（不稳定，少用）：
  - 横版贴墙姿势（climbing 系，双人重影 / 穿模）
  - 近距离特写（被识别成脸部特写）
  - rest 系列静止待机（全类别穿模）
- **稳姿**（推荐）：
  - 标准全身直立、站立插兜/抱臂、行走/奔跑
- **性别差异**：女性赤脚失败率 > 男性穿鞋
- 沉淀文档：[`pose问题规律.md`](pose问题规律.md)

---

## 2026-07-23｜多维度定型图方案定型（V4）

- **问题**：V1-V3 在领口/头饰/服装细节上仍出现版本不一致
- **解法（V4，纯 prompt 方案）**：
  1. 角色名 identity anchor（如 `character named Xiao Fengxian`）
  2. 固定 CHARACTER CORE（脸、眼、发型、发簪、发饰，两张图一字不差）
  3. 结构化视角词：正脸特写 `front view eye-level shot close-up` / 全身 `front view eye-level shot wide shot`
  4. 针对身份漂移的 negative（`different face, changed face, altered face, different hairstyle, ...`）
  5. `<sks>` 触发词 + 一致性 LoRA + 固定 Seed
- **结果**：古装女侠 V4 完全通过；男性宗师 V1/V2/V3 三套全通过（扫地僧 / 江湖老剑客 / 隐世老人）
- 沉淀文档：[`数字人多维度定型图生成器跑通记录.md`](数字人多维度定型图生成器跑通记录.md)
- **同步验证规则**：使用 `wearing a simple black strapless swimsuit to ensure bare neck and shoulders` 做无领口强约束（替换为 `white sleeveless undergarment` 后模型又生成领口）

---

## 2026-07-23｜男性宗师角色生成（38m 实验田）

- 目标：为《一代宗师》38 秒台词视频生成老年男性宗师（扫地僧气质）
- 三套候选：V1 扫地僧 / V2 江湖老剑客 / V3 隐世老人，全部通过用户验收
- 沉淀：**正脸特写无领口技巧**（swimsuit 强约束）+ **CHARACTER CORE 复用**
- 产物位置：`C:\Users\tanha\Desktop\38m\master_v{1,2,3}_face_v2_00001_.png`（38m 内，但方法回流到 digital_human 跑通记录）

---

## 2026-07-22｜v10 执行计划发布（规划目标）

- 12 个月路线图 / 8 阶段（M0-M7）/ M12 月收入硬截止
- TTS 栈换为 Fish Speech + F5-TTS + ElevenLabs（替换 Edge TTS）
- ComfyUI 作为唯一主链（替换 MuseTalk / LivePortrait / LatentSync 主链）
- 国内平台优先（抖音 / 视频号 P0；YouTube 降级为素材复用池）
- 删除资金 / 借款 / 朋友豁免条款；只留"月收入"作为 M12 硬截止
- 决策记录：见 `执行计划清单.md` v10 §十
- **注**：v10 发布时 M0 尚未跑通，发布后陆续被现实覆盖（详见 2026-07-25 整合决策）

---

## 2026-07-22｜基础设施就位

- **GPU 自检 A 级**（4090 eGPU 49GB + 4060 8GB）：详见 [`M0-W1-D1-GPU自检报告.md`](M0-W1-D1-GPU自检报告.md)
- **ComfyUI 便携版 v0.28.0**：装在 `E:\AI\ComfyUI_windows_portable\`
- **TTS 本地部署**：
  - Fish Speech（s2-pro 4B，中文主力）— 8.6s 中文 wav 44.1kHz 冒烟过
  - F5-TTS（v1 Base，轻量备选）— 6.8s 中文 wav 24kHz 冒烟过
  - ElevenLabs（API）— 待用户注册 key
- **踩坑记录**（详见 `TTS栈部署记录.md`）：
  - torch 必须钉 2.5.1+cu121（避免 PyPI CPU 版覆盖）
  - huggingface-hub < 1.0 + tokenizers 0.15.2 互锁
  - uv.lock override protobuf <6 + tensorboard==2.19.0 配合
  - F5 vocoder 手工下载 + HF_HUB_OFFLINE=1
- **ComfyUI 出图全局约定**：见 [`sop/ComfyUI出图规范.md`](sop/ComfyUI出图规范.md)

---

## 2026-07-21｜M0 跑通（MuseTalk 方案，**已废**，仅历史）

- 端到端 demo：`demo_yongen_eng_v15.mp4`（60s 中文脸 + 英文音频，跨语种）+ `demo_yongen_yongen_v15.mp4`（8s 中文）
- 工具：MuseTalk v1.5 + Edge TTS `zh-CN-YunyangNeural`
- 沉淀：见 `E:\数字人计划\m0-day1\W1-MuseTalk跑通.md` + `W1总结.md`
- **踩坑 5 个**：Hermes PYTHONPATH 污染 / numpy ABI 死循环 / tokenizers 互锁 / torch CPU 默认 / mmcv 2.0.1 编译失败（最后一条未修，被 MuseTalk 跑通过程绕过）
- **当前定位**：**已被 v10 + 2026-07-25 决策替代**，不入生产工作流；产物可作历史归档

---

## 2026-07-20 及更早｜项目启动

- 数字人短视频项目立项
- v1-v9 计划迭代（v9.1 → v10 是当前规划基线）
- 早期调研：MuseTalk / LivePortrait / LatentSync / CosyVoice / GPT-SoVITS 等候选工具
- 沉淀：`数字人计划调研更新版.md`（已对齐 v10）

---

## 维护规则

- 每次"完成里程碑"或"重大决策变更"追加一条，**时间倒序**
- 每条包含：日期、做了什么、沉淀在哪个文件
- 技术栈细节不放这里（看"项目状态总览.md"）
- 不修改历史条目（错了加 erratum 条目，不动原文）