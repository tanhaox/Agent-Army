# digital_human 环境变量模板 — 复制为 .env 并填入真实 key
# .env 已在 .gitignore (第 2 行), 永不进 git; 启动时由 app/config.py::_load_dotenv 注入.
# 缺 key 时的行为: 该服务通道静默降级或明确报错 (见各注释).

# DeepSeek (主 LLM 通道: 洗稿/解构/导演/boost) — https://platform.deepseek.com/api_keys
# 缺失时: 回退硅基流动; 两者全缺则 LLM 功能不可用.
DEEPSEEK_API_KEY=

# Pexels 视频素材 (P 线 B-Roll 在线下载) — https://www.pexels.com/api/
# 免费额度: 200 req/hour + 20000 req/month. 缺失时: P 线降级 degraded 仅返元数据.
PEXELS_API_KEY=

# 智谱 Web Search (素材聚合补搜/拆书素材包) — https://open.bigmodel.cn/usercenter/apikeys
# ⚠️ 免费搜索额度 2026-09-12 到期 (用户拍板: 到期续费, 前端常显到期提示+服务端拦截).
ZHIPU_API_KEY=

# 硅基流动 SiliconFlow (DeepSeek 备选通道) — https://cloud.siliconflow.cn/account/ak
# 缺失时: DeepSeek 主通道正常即不受影响.
SILICONFLOW_API_KEY=
