# OpenClaw 部署经验对比

> **DigitalOcean (经验)** vs **阿里云 (当前)**

---

## 📊 配置对比

| 配置项 | DigitalOcean (157.245.195.58) | 阿里云 (112.126.61.223) | 状态 |
|--------|-------------------------------|------------------------|------|
| **Gateway 绑定** | localhost + nginx 反向代理 | **0.0.0.0:18789** (直连) | ✅ 当前更简单 |
| **访问协议** | HTTPS (nginx) | HTTP (直连) | ⚠️ 可升级 |
| **Control UI** | - | **allowedOrigins: ["*"]** | ✅ 已配置 |
| **Token** | openclaw123 | `b65090...b04f65359` | ✅ 已配置 |
| **模型** | GLM-4.7 | **GLM-4.7** | ✅ 相同 |
| **内存** | 1GB + 2GB swap | 3.4GB | ✅ 当前更好 |
| **公网访问** | ✅ 通过 VPN/公网 | ✅ 直接公网 | ✅ 都可以 |

---

## 🎯 关键借鉴经验

### ✅ 已应用的经验

1. **直连模式** - 当前阿里云直接绑定 0.0.0.0:18789，比 DigitalOcean 的 nginx 反向代理更简单
2. **allowedOrigins** - 已配置允许公网访问 Control UI
3. **GLM-4.7 模型** - 使用相同的智谱 AI 模型

### 💡 可以借鉴的改进

#### 1. **添加 HTTPS 支持（可选）**

如果你需要更安全的访问，可以添加 nginx 反向代理：

```bash
# 在阿里云服务器上安装 nginx
apt update && apt install -y nginx

# 创建自签名证书
mkdir -p /etc/nginx/ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/openclaw.key \
  -out /etc/nginx/ssl/openclaw.crt

# nginx 配置
cat > /etc/nginx/sites-available/openclaw << 'EOF'
server {
    listen 443 ssl;
    server_name 112.126.61.223;

    ssl_certificate /etc/nginx/ssl/openclaw.crt;
    ssl_certificate_key /etc/nginx/ssl/openclaw.key;

    location / {
        proxy_pass http://127.0.0.1:18789;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF

# 启用配置
ln -s /etc/nginx/sites-available/openclaw /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

然后访问：`https://112.126.61.223/#token=...`

#### 2. **配置 trustedProxies（可选）**

如果使用反向代理，需要配置：

```json
{
  "gateway": {
    "trustedProxies": ["127.0.0.1", "::1"]
  }
}
```

#### 3. **多 Agent 配置（高级用法）**

如果需要多用户隔离，可以参考文档中的 Telegram Bot 方案。

---

## 📝 当前阿里云配置总结

### ✅ 已完成

- [x] 安装 OpenClaw 原版 (v2026.3.8)
- [x] 配置 zai/glm-4.7 模型
- [x] Gateway 绑定 0.0.0.0:18789 (公网可访问)
- [x] 配置 Token 认证
- [x] 配置 allowedOrigins 允许公网访问 Control UI
- [x] 阿里云安全组开放 18789 端口

### 🎯 访问方式

**HTTP (当前)**:
```
http://112.126.61.223:18789/#token=b65090c779a6fb8d11b4f39db2028fb8ec893d5b04f65359
```

**本地 Token**:
```
b65090c779a6fb8d11b4f39db2028fb8ec893d5b04f65359
```

### 📂 配置文件位置

```
/root/.openclaw/openclaw.json    # 主配置
/root/.openclaw/workspace/         # 工作空间
/tmp/openclaw/openclaw-*.log        # 日志
```

---

## 🔧 管理命令

```bash
# SSH 连接
ssh root@112.126.61.223

# 查看状态
openclaw gateway status

# 重启 Gateway
openclaw gateway restart

# 诊断问题
openclaw doctor

# 修改配置
openclaw configure

# 查看日志
tail -f /tmp/openclaw/openclaw-*.log
```

---

## 💭 结论

当前阿里云配置比 DigitalOcean 更简单直接：
- ✅ 无需 VPN
- ✅ 无需 nginx 反向代理
- ✅ 公网直接访问
- ✅ 内存更大 (3.4GB vs 1GB)

唯一可能改进的是添加 HTTPS 支持，但对于个人使用来说，当前的 HTTP + Token 认证已经足够安全了。

---

**创建时间**: 2026-03-10
**服务器**: 阿里云 112.126.61.223
